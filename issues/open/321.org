# priority: medium
# category: features
** WIP #321 Forbid NULL: no-default ref fields, Option(ref T), then remove NULL

Depends on #320 (ownership qualifiers in template type args and enum
variant payloads -- needed for ~Option(ref T)~).

Goal: remove ~NULL~ from the language entirely, ala Rust. A ref that
exists always points at something; "maybe absent" is spelled
~Option(ref T)~ and the absence is handled explicitly by the type
system instead of by runtime null-deref panics (the generated C
~DEREF~ macro's "null deref" exit).

*** Current state (verified 2026-07-21)

- #242 (fixed) already made struct fields without a default MANDATORY
  at instantiation ("missing required field" type error). So the old
  premise "every field has a default, hence every ref field defaults
  to NULL" is gone; the required-field mechanism exists and ref fields
  can already be declared without a default.
- But in practice ~ref x : T = NULL~ is still the dominant pattern
  (context.til, interpreter.til, garbager.til, file.til, ...), and
  NULL is the one default a ref field may carry:
  check_ref_field_owned_default (src/self/typer.til:618) forbids
  owned-RHS defaults on ref fields but explicitly allows NULL as a
  "forced exception". It is even the documented canonical form:
  doc/qualifiers.org's struct-field table spells the ref rows as
  ~ref field : Type = NULL~, and doc/ownership.org's Fields table
  does the same.
- ~NULL~ is a keyword (KwNull, src/self/lexer.til:164) producing
  ~Literal.Null~, and ~is_null()~ checks plus ~return NULL~ from
  ref-returning lookups are everywhere: ~840 NULL/is_null occurrences
  across src/, examples/, test/.

*** Steps

1. Migrate ref FIELDS off NULL defaults: each ~ref x : T = NULL~
   becomes either a no-default mandatory field (when every
   constructor site really has a value -- the common case) or
   ~Option(ref T)~ (when absence is a real state: e.g.
   TypeScope.parent at the root scope, File.handle after close).
   Needs #320 for the Option cases. Then forbid ~= NULL~ as a struct
   field default (drop the forced exception in
   check_ref_field_owned_default).
2. Migrate ref LOCALS and ref RETURNS off NULL. The big one:
   ref-returning lookup functions that ~return NULL~ for "not found"
   (scope/struct-def/method lookups in typer/context/initer, and the
   ~returns ref~ container accessors doc/ownership.org calls out --
   ~Vec.get~, ~Map.val_ptr~, ~scope_get~ and friends) become
   ~returns Option(ref T)~, and the ~if x.is_null()~ call sites
   become switch/match on the Option. Also the
   ~ref mut acc : T = NULL~ late-init locals (see #147's
   dangerous-refs list -- partially migrated already). Note the
   lifetime angle: til has no origin checker, ~returns ref~ is "the
   documented dangerous case" (doc/ownership.org), and a NULL ref
   borrows nothing -- once refs always point at something, every ref
   that escapes (including inside an Option) must be covered by the
   garbager's lifetime extension, so this migration also shrinks the
   dangling-ref surface #147 tracks.
3. Remove ~is_null()~ on refs once nothing legitimately produces a
   null ref.
4. Remove ~NULL~/~Literal.Null~ from the language: lexer keyword,
   parser literal, typer, interpreter value, builder emission of
   til-level null (the C backend keeps using C's NULL internally,
   that is invisible at the language level). Update the
   doc/qualifiers.org and doc/ownership.org field tables and any
   other docs showing ~= NULL~. The ~DEREF~ null panic stays during
   the migration as the safety net and can only be dropped from
   generated code at the very end, if ever.

*** Open questions / what NULL does today that needs an answer

- C interop boundary: ext functions and ext structs traffic in raw
  possibly-null pointers (File.handle ~ref handle : U8 = NULL~,
  malloc'd buffers like Vec.data ~own data : U8 = 0~, vendor
  bindings). Rust solves this with two mechanisms, and til should
  copy both:
  1. The GUARANTEED niche ("null pointer optimization"): Rust ~&T~ /
     ~Box<T>~ / ~fn~ / ~NonNull<T>~ are non-null by construction, so
     the compiler guarantees (documented layout, not merely an
     optimization) that ~Option<&T>~ uses the null bit pattern as
     ~None~ -- one pointer wide, ABI-identical to a nullable C
     pointer; ~extern "C" fn f(cb: Option<extern "C" fn()>)~ is
     idiomatic FFI. til's ~Option(ref T)~ should get the same
     guaranteed None == null layout: no tag, ext signatures can take
     and return it directly, and step-2 lookup returns stay one word.
  2. Quarantined raw pointers: Rust never removed nullable pointers
     -- ~*const T~ / ~*mut T~ can be null, but cannot be dereferenced
     outside ~unsafe~ and never convert implicitly to references; the
     boundary converts explicitly (~ptr.as_ref()~ ->
     ~Option<&T>~, ~NonNull::new(ptr)~ -> ~Option<NonNull<T>>~).
     til's analog: ~Dynamic~ / raw ext pointers may keep the null
     bit pattern (they already sit outside the garbager's
     guarantees, like Rust's unsafe layer), but the moment a value
     enters typed til it is an ~Option(ref T)~ (or a proven non-null
     ref), never a bare null ref.
  So "remove NULL entirely" means what it means in Rust: the keyword
  and null REFS disappear from the language; nullability survives
  only as a bit pattern inside the Option niche and inside the
  untyped ext layer.
- Interpreter Value representation of Option(ref T) and the
  scavenger/garbager treatment of a None ref. (Partially answered by
  the niche ruling below: the interpreter may keep its tagged Value
  internally; only FFI marshaling must encode None as a null
  pointer.)
- Sequencing: this touches the self-hosted compiler's own hottest
  files, so it has to land as a series of small migrations (module by
  module, or pattern by pattern), each keeping
  ~make clean && make test~ green, with the language-level removals
  (steps 3-4) last. Note the soundness ordering: the niche layout is
  sound BECAUSE ~ref T~ can never be null, which is exactly what this
  issue establishes -- Option(ref T) with the niche and the NULL-ref
  removal are one coherent transition, sequenced by the #320 -> #321
  dependency.

*** Progress

**** Step-1 survey of all ~ref ... = NULL~ struct fields (master a1ad866)

Classification of every NULL-defaulted ref field outside vendor/ and
test fixtures, by whether NULL is ever a real runtime state:

DEFAULT DROPPED (first migration commit; field now mandatory at
instantiation via #242 -- every ctor site already passed it):

- src/std/file.til File.handle -- both ctors pass a handle (cfile_open
  exit(1)s on failure). Also removed close's ~self.handle = NULL~
  poison write: close is only called from delete, nothing reads after.
- src/self/builder.til CollectionInfo.type_name, DynCallInfo.method --
  single ctor site each.
- src/self/garbager.til LocalInfo.name -- bare ctor + immediate
  assign; the assign moved into the ctor call.
- src/self/interpreter.til HeapBinding.ptr + HeapBinding.ctx -- the
  one ctor site passes both. ptr is still NULLed at move-out/teardown
  (real runtime state, step-2 material); the dead
  "heap binding without ctx" panic guards went with the ctx default.
- src/self/interpreter.til ExprPtrBox.ptr, FFITypePtrBox.ptr --
  single ctor site each; the defensive ~box.ptr.is_null()~ teardown
  check went too (every cache entry is built non-null).
- src/self/interpreter.til InterpCallableBox.func_def/env and
  ExtStr.data -- ctor NEVER runs (raw EvalHeap/marshal structs), the
  defaults were dead code.
- src/self/interpreter.til Scope.parent -- scope_new always passes
  parent; the ROOT scopes pass a NULL value (scope_new_boxed(NULL)),
  so the nullability itself stays until Option.

NEEDS OPTION (NULL is a real expected state; blocked on #320):

- src/self/context.til TypeScope.parent -- root scopes are bare
  ~TypeScope()~ (context.til:433/626); parent-chain walk to root.
- src/self/context.til Context lazy/per-phase transients:
  constfolder_known, return_value ("takes and clears"),
  interp_ret_dest, interp_ret_type, ns_fields, current_fdef, and the
  six #177 caches (cached_str/array/vec_def/name).
- src/self/interpreter.til InterpSession.global -- late-init, freed
  and re-NULLed at teardown.
- src/self/interpreter.til FFIEntry.* -- built incrementally from a
  bare ctor; return_type is legitimately absent (procs).
- src/self/garbager.til GcCfgBlock.body -- "NULL only for the
  synthetic exit block", checked as real control flow.

C BOUNDARY (generated bindings / marshal mirrors; handled by the
binder + niche'd Option decisions above): vendor/bindings/libffi.til,
raylib.til, nng.til pointer fields. Test fixtures exercising NULL
semantics on purpose (test/ref.til RefNode.next linked list,
test/aliases.til, test/typer_errors.til, test/priv_error.til) stay
until the language-level removal.

**** Step-1 Option migration, batch 1 (post-#320, Option in core)

- garbager GcCfgBlock.body -> Option(ref Expr) (None only for the
  synthetic exit block; consumers switched, root-block reader fails
  loud on None).
- Context cached_* block: cached_str_def / cached_array_def/_name /
  cached_vec_def/_name were WRITE-ONLY (dead) and are REMOVED;
  cached_str_name became an OWNED Str clone ("" = not registered),
  which also dissolves this field's #299 teardown-ordering hazard
  (no more borrow into the imported ASTs).
- Context.current_fdef -> Option(ref Expr) (builder read sites
  switched; save/restore of the Option value at closure emission).

**** Step-1 Option migration, batch 2

- TypeScope.parent -> Option(ref TypeScope) (the compiler's hottest
  scope chain: get_binding / get_struct / struct_def_of / lookup_func
  / lookup_func_symbol / the target-width walkers, plus the typer and
  garbager root-scope tests and all 11 TypeScope(parent = ...) ctor
  sites). This is the first SELF-REFERENTIAL Option(ref T) field, and
  it flushed out two places that still assumed an inline payload --
  fixed in the preparation commit (builder topo sort, interpreter
  dispatch_enum_size); see issue #320.

Remaining ref-NULL fields for the next batch:
Context.constfolder_known / ns_fields, Context.return_value /
interp_ret_dest (reclassify as plain Dynamic per the quarantine
decision) / interp_ret_type, InterpSession.global, FFIEntry.* (mixed
Option/Dynamic), interpreter Scope.parent, test fixtures
(RefNode.next, aliases), THEN the
forbid: drop check_ref_field_owned_default's NULL exception for
non-ext structs (ext_structs keep NULL defaults until the niche
layout gives Option(ref T) the C pointer ABI), plus doc table
updates (qualifiers.org / ownership.org / enums.org).

**** Step-1 Option migration, batch 3

- Batch 2's blocker is fixed: #320's ref-payload ctor check now uses
  the typer's own ~expr_is_ref_decl_source~, so a REF-RETURNING call
  is accepted (the same borrow-source rule a ~ref~ DECL already
  requires); temporaries stay rejected. Test:
  test_option_ref_payload_from_ref_returning_call (test/option.til).

- FINDING that stops the three Scope-typed fields here
  (Context.constfolder_known, Context.ns_fields,
  InterpSession.global): their problem is OWNERSHIP, not nullability.
  Each is a manually-managed box (~scope_new_boxed~ ... ~scope_free~),
  and the pre-#321 ~ref~ label was already a lie about ownership --
  the field said "borrow" while the code freed it by hand. Wrapping
  them as ~Option(ref Scope)~ makes the lie explicit and the typer
  correctly refuses the teardown: "cannot pass ref variable to 'own'
  parameter" at ~scope_free(own nsf)~, because ownership cannot be
  transferred out of a BORROWED payload. Attempted and backed out.
  What they need first is an ownership answer, not an Option:
  either an ~own~ field (auto-deleted by the parent's generated
  delete -- but teardown ORDER matters here, see the #299 note above),
  or an owning Option payload shape, or the Dynamic quarantine. Same
  question already flagged for return_value / interp_ret_dest.
  Worth its own issue before #321 step 1 can close.
  Bonus observed while attempting it: ~ns_fields~ carries a redundant
  ~ns_inited~ Bool that an Option would subsume (the tag IS "is it
  built?"), so whichever shape wins should drop that second source of
  truth.

**** Step-1 Option migration, batch 4

- The ownership hole the three Scope-typed fields hit is now filed as
  issue #324 (a ~ref~ FIELD passed to an ~own~ parameter type-checks,
  while a ~ref~ local correctly does not). #321 step 1 cannot close
  for constfolder_known / ns_fields / InterpSession.global until #324
  settles their ownership.
- Context.interp_ret_type -> Option(ref Str): a genuine borrow of the
  callee FuncDef's ~return_type~, saved/restored around each call.
  Its two readers run while evaluating a ~return~ INSIDE a call, so
  None there is a broken invariant -- read through an accessor that
  panics rather than substituting a default.

**** Step-1 Option migration, batch 5

- interpreter Scope.parent -> Option(ref Scope), plus the ~parent~
  params of scope_new / scope_new_boxed. The four walk loops keep
  their iterative single-probe shape (the hot path: one frame per
  lookup); only the "step outward" line becomes a switch.
- LESSON worth keeping: those loops' ~while cur.is_null().not()~ was
  doing TWO jobs -- terminating the parent walk AND absorbing a NULL
  scope ARGUMENT (callers pass tables that may not be built yet, e.g.
  ~ctx.ns_fields~ before interpreter_init_ns). Migrating only the
  first silently dropped the second and segfaulted in ~ns_get~. The
  argument guards are restored with a REM: they are about a nullable
  ref PARAM (step 2), not about the chain. Every ~is_null~ removed in
  this migration needs that same "which of the two nulls is this?"
  question asked.

**** Step-1 close-out findings (batch 6)

- The quarantine class does NOT migrate, and "reclassify as plain
  Dynamic" (written above) is WRONG -- tried and reverted. A
  ~ref x : Dynamic = NULL~ field is a non-owning raw pointer slot, and
  the ~ref~ is load-bearing: it is what makes the auto-generated
  ~delete~ SKIP the field. Dropping it makes the field Shallow, and
  the generated delete then emits ~Dynamic.delete(field, false)~
  against a signature taking ~own self~ ("argument for 'own' parameter
  'self' must be marked 'own'"). There is no plain ~Dynamic~ field
  anywhere in the tree, which is why nobody had hit this. So
  Context.return_value / interp_ret_dest and the FFIEntry pointers
  KEEP their shape, and the eventual forbid must EXEMPT Dynamic-typed
  ref fields -- exactly the Rust split this issue already chose:
  ~&T~/~Option<&T>~ are non-null, raw ~*mut T~ is not.
- Test fixtures (test/ref.til RefNode.next, test/aliases.til) exist to
  COVER ref fields with NULL defaults, so they convert as PART of
  landing the forbid, not before it.

**** Step 2 starter (batch 7): ref RETURNS -> Option(ref T)

First three lookups migrated, chosen for small call-site counts so the
pattern is proven before the hot, high-fanout ones:

- ~mode_resolve~ (src/self/modes.til, 2 sites)
- ~resolve_local_fn_sig~ (src/self/builder.til, 2 sites)
- ~find_ns_decl~ (src/self/typer.til, 3 sites)

Pattern: the function returns ~Option(ref T)~ and each caller's
~if x.is_null()~ becomes a ~switch~ whose None arm holds what the null
branch used to do (panic, fall through to the next fallback, or emit
the diagnostic). Note the two nulls again (see batch 5): where the
lookup also takes a nullable ref ARG (find_ns_decl's ~sdef~), that
guard stays -- a null def simply has no members -- and is step 2's
separate param job.

**** Step 2, batch 8: three more lookups, and one null that was a LIE

- ~resolve_fn_sig~ (src/self/typer.til, 3 sites): 16 separate
  ~return NULL~ miss paths, all "no funcsig for this callee" -> one
  Option return; callers switch and fall through to their next
  fallback exactly as before.
- ~enum_payload_type_ref~ (src/self/interpreter.til, 2 sites):
  "this variant carries no payload" is a real state the deep
  free/clone walkers branch on -> Option.
- ~scope_get_binding_scope~ (src/self/interpreter.til, 5 sites): NOT
  migrated to Option -- deleted the null instead. Every caller looks
  the binding up FIRST and then dereferences the result unchecked, so
  "not found" was never a reachable state; it was a null that could
  only ever produce a segfault at the caller. It now panics at the
  source with the name. Worth watching for more of these: a
  ~return NULL~ whose callers never test it is not an Option
  candidate, it is a missing invariant check.

**** Step 2, batch 9: the desugarer's finder family

~find_bang_fcall~, ~find_variadic_fcall~, ~find_kwargs_fcall~ and
~find_array_vec_fcall~ (src/self/desugarer.til) all share one shape --
recursive "return the first matching FCall, or nothing" -- so they
migrate together. "Nothing found" is exactly the state their callers
branch on. Two things fall out nicely:

- the recursive arms just pass the Option through unchanged, so the
  bodies get SHORTER, not longer;
- the three presence-only call sites become ~.is_some()~ instead of
  ~.is_null().not()~ -- Option answers that question directly.

**** Step 2, batch 10: find_namespace_func (18 sites) + Option.unwrap

~find_namespace_func~ (src/self/typer.til) migrated across all 18 call
sites in builder / desugarer / typer.

This batch needed a new core method first. Several sites are
guard-then-use inside long loop bodies (~if x.is_null() { continue }~
followed by 30 lines using ~x~); a ~switch~ would have re-indented
those whole bodies into the Some arm -- noisy and easy to get wrong.
So ~Option.unwrap~ landed (panics on None, ala Rust), enabling the
idiom the code was already written in:

: o := find_namespace_func(...)
: if o.is_none() { continue }
: ref x : Expr = o.unwrap()

That is the "until usage demands more" trigger we set when Option went
in with only is_some / is_none / unwrap_or. Its doc comment says when
NOT to reach for it: prefer ~switch~ when both arms do real work, and
~unwrap_or~ when a default is meaningful -- ~unwrap~ is for when None
is unreachable by construction, so it fails loudly if that changes.

Two sites shrank instead of growing: one only ever tested the result
for null (the binding was dead -- now just ~.is_none()~), and the
"does this type have a delete?" check collapsed to a single
~.is_none()~ expression.

**** #320's niche layout has LANDED (unblocks the hot migrations)

Option(ref T) is now ONE pointer with None == null, so the two things
this issue was sequencing behind it are unblocked: the hot
ref-returning lookups (TypeScope.get_struct and friends) can migrate
without doubling the value they return, and the C-boundary story in
"Open questions" above is now real rather than planned -- an
Option(ref T) IS a nullable C pointer.

Perf note RESOLVED: an enum return used to be heap-boxed (~returns E~
is Own), which is why the mass migration waited. With the #320 niche
plus auto-shallow for niche enums, an ~Option(ref T)~ return is now a
single pointer passed in a register -- the emitted C went from
~Option__ref_Mode *mode_resolve(...)~ to
~Option__ref_Mode mode_resolve(...)~, no malloc per lookup. The hot
lookups (TypeScope.get_struct / get_binding and friends) are free to
migrate now.

**** Step 2, batch 11: the TypeScope func/struct-def lookup family

The first HOT batch, unblocked by the niche + auto-shallow landing: the
scope-walking name lookups themselves, plus the two leaf probes they
share and the builder's signature-resolution family that forwards them.

- context.til: ~func_defs_lookup_one~, ~bindings_lookup_one~ (leaves),
  ~lookup_func~, ~lookup_func_symbol~, ~struct_def_of~.
- builder.til: ~find_callee_fdef~ (a one-line forwarder over
  lookup_func_symbol, so it MUST migrate with it), ~fcall_fn_sig~ and
  ~resolve_decl_fn_sig~ -- the three cascade off it across 30 sites.
- ~90 call sites across typer / desugarer / builder / loader / initer /
  garbager / context.

Two notes worth keeping:

- Two guards died as a consequence, not as a cleanup:
  ~builder_is_type_gen_macro_def~'s ~fdef.is_null()~ (its only nullable
  caller was the ~lookup_func("Array"/"Vec")~ pair; every other caller
  hands it a node it has already dereferenced), and the implicit null
  ~sig~ reaching ~emit_fcall_closure_call~ from the Ident/funcptr emit
  path -- that one is now ~.unwrap()~, since the sig is dereferenced
  unconditionally downstream (~emit_fcall_funcptr_cast~ UNREACHABLEs on
  a non-FuncDef). Another instance of batch 8's rule: a null nobody
  tests is a missing invariant check.
- The predicate-only sites read BETTER than before:
  ~scope.lookup_func(n).is_null().not()~ -> ~.is_some()~, and the
  alias-rebroadcast loops in typer.til lost their
  ~NULL.ptr_eq(x)~-is-false trick for an explicit ~is_none~ skip.

**** Step 2, batch 12: get_struct and get_binding (the last two hot lookups)

~TypeScope.get_struct~ (115 sites) and ~TypeScope.get_binding~ (89
sites) both return ~Option(ref T)~ now, which finishes the scope-lookup
family. Cascades they dragged with them:

- ~find_struct_def~ (builder, a one-line forwarder over get_struct).
- get_binding's four in-namespace consumers -- ~find~, ~get_type~,
  ~get_func_type~, ~is_mut~ -- which all read better as
  ~if bo.is_some() { ... bo.unwrap() ... }~.
- ~is_pod_enum_clone_of~ takes ~Option(ref Expr)~: its doc comment
  already said "caller resolves the enum def their own way", and both
  callers hand it a lookup result, so the miss belongs in the type.
- The FCall trio ~infer_and_validate_fcall_args~ /
  ~validate_fcall_own_args~ / ~resolve_fcall_return_type~ take
  ~Option(ref TypeBinding)~: each already opened with
  ~if callee_bind.is_null() { return }~, so "this callee has no
  binding" was always a state, never a guard.

Five more null guards died as a consequence (each had exactly one
nullable caller, which now unwraps): ~check_priv_access~,
~type_binding_is_type_token~, ~binding_func_type~,
~collect_missing_paths~ and ~infer_func_ptr_field_call~. And the sites
that write a freshly-registered binding (~scope.set(name, ...)~ then
~scope.get_binding(name)~ on the next line -- 18 of them across typer /
desugarer / loader / initer) now say ~.unwrap()~: a miss there is a
broken invariant, and unwrap fails loudly at the source instead of
null-dereferencing one line later.

**** FOUND while testing batch 12: auto-shallow can dangle an
Option(ref T) return

A function that ~returns Option(ref T)~ and borrows from one of its own
PARAMS hands back a dangling pointer when that param is auto-shallow
(#223) eligible:

: Cell := struct { n := 0 }
: pick := func(c: Cell) returns Option(ref Cell) {
:     return Option(ref Cell).Some(c)
: }

emits ~Option__ref_Cell pick(Cell c)~ -- the param is a by-VALUE copy --
and then ~Option__ref_Cell_Some(&c)~ takes the address of that dead
stack copy. Writes through the returned borrow are lost and reads are a
use-after-return. The equivalent ~returns ref Cell~ function is correct:
it emits ~Cell *pick(Cell *c)~.

Root cause: the param-demotion gate in infer_func_def_expr
(src/self/typer.til, three sites) is spelled
~fdef.return_own_type.is(OwnType.Ref).not()~, and its own comment
states the rule -- "A ref-returning function may hand a param back by
reference ... so its params must stay borrowed pointers". A function
returning an ENUM WITH A REF PAYLOAD hands a param back by reference in
exactly the same way, and ~return_own_type~ is Own/Shallow there, so the
gate misses it. Same question applies to ~returns_ref_val~ (which drives
the borrow-escape checks over the body).

Latent, not active, in the compiler itself: every Option-returning
function in src/self takes pointer params (Expr/Context/TypeScope are
all too big for auto-shallow), which is why the suite is green. It bites
user code the moment a small POD type is the borrow source.

Fix sketch: a ~fdef_return_carries_borrow(fdef, scope)~ predicate =
~return_own_type.is(OwnType.Ref)~ OR "return type resolves to an enum
def with any ref payload" (enum_variant_payload_ref already exists), used
at the three demotion gates and at the returns_ref_val stamp.

**** PERF: a niche enum's constructor was still allocating

Measured after batch 11 landed: self_ms went 166084 (f7ad2ea, the
auto-shallow commit) -> 180184 (d8b82e6, first totals row containing
batch 11), +8.5%. ~--prof~ on the self-translate found the cause was
NOT the ~is_some~ / ~unwrap~ calls (they do not appear in the CPU top
20 at all) but the allocation profile:

: Option__ref_Expr_Some   13,069,816 allocs   102 MB   13,069,816 calls

The niche layout made the VALUE one pointer and auto-shallow made
params and returns pass it by value, but the auto-generated variant
CONSTRUCTOR still followed the tagged-enum convention: malloc a box,
fill it, return the pointer -- and every caller then DEREFed the box
and freed it. So each Option construction was a malloc + free.

Fixed by giving niche ctors ~return_own_type = OwnType.Shallow~ in
generate_enum_variant_constructors, which is all the generic machinery
needs (func_return_ctype, fcall_is_shallow_return and the call-site
hoist all key off it); the hand-written ctor BODY in emit_enum_def and
the four hand-written prototype emitters now spell the value return
through enum_ctor_ret_c. Emitted C went from

: Option__ref_Expr *h = Option__ref_Expr_Some(fd);
: { Option__ref_Expr _ret = DEREF(h); free(h); return _ret; }

to a plain ~Option__ref_Expr h = Option__ref_Expr_Some(fd);~.

Measured: self-translate 50.7s -> 49.1s (~3%). That recovers only part
of the 8.5%; the rest is NOT the accessor calls either. The remaining
suspect from the same profile is the garbager --
~gc_flow_solve~ 16.6s self over 8374 calls, ~expr_collect_uses~ 101s
total -- since every migrated site added one extra local (the ~_o~
Option temporary) to its function's dataflow. That needs its own
before/after profile rather than a guess.

**** Step 2 COMPLETE (batch 13): every ref-returning lookup is an Option

Swept the whole tree for `returns ref T` functions containing
~return NULL~ -- 39 of them -- and resolved each by the rule batch 8
set: an Option when the miss is a state the callers branch on, a loud
failure when it is not. There are no ref LOCALS left initialised to
NULL (the earlier survey's hits were all FIELDS, step 1 material), and
no namespace method returns NULL.

Migrated to ~Option(ref T)~ (28): member_get / def_ns_get (context),
expr_type_name_ref / structdef_of / enumdef_of (parser),
get_stack_local_ctype (builder), resolve_variadic_elem_type
(desugarer), generics_lookup_one / init_macro_pick_return /
init_lookup_type_gen_macro (initer), extract_trivial_literal_return
(constfolder), find_ns_decl_fdef_imported and the seven-function
lazy_recursive_* / lazy_stmt_* family (loader), and in the interpreter
field_nested_def, lookup_interp_type_def, field_access_decl,
find_field_decl, resolve_field_assign_sdef, scope_lookup_heap_binding,
lookup_type_def_helper, known_ffi_type and ffi_call_ret_struct_def.

Deleted the null instead (the callers never tested it, so it was a
missing invariant check): the trailing ~return NULL~ after ~panic~ in
require_mode / interp_ret_type_name / scope_get_binding_scope -- a
panic in tail position already counts as diverging, so the dead return
was pure NULL-keeping ballast -- and the ~UNREACHABLE(); return NULL~
arms of def_ns_decls, def_ns_name_at and build_struct_ffi_type, which
are now a single loud panic.

Two decisions worth recording:

- ~ref Dynamic~ RETURNS are NOT a quarantine case, and calling them one
  here was wrong. Reviewed all four -- ns_get, ns_lookup_flat,
  field_access_base, resolve_field_assign_base -- and every NULL they
  return is a lookup or resolution MISS that every caller branches on
  (`no namespace field`, `field assign on non-struct`, fall through to
  the next fallback). Not one caller treats the null as a value. The
  ~Dynamic~ is only what the slot points AT; it has nothing to do with
  why the answer is absent. All four now return ~Option(ref Dynamic)~.

  The real constraint, which is a different one: the niche cannot spell
  ~Some(null)~, because None IS the null bit pattern. So a slot whose
  legitimate VALUE can be a null pointer cannot round-trip through
  ~Option(ref Dynamic)~. That is reachable in exactly one place today --
  field_access_base's ~deref(slot)~ for a non-shallow field declared
  ~ref x : T = NULL~ -- and it changes nothing, because the callers
  already conflate "absent" with "null" and error out on both. Step 1's
  forbid removes the case entirely, so the ordering is the opposite of
  what the quarantine note implied: these returns wanted the FIELD
  forbid first, not an exemption.

  Where NULL genuinely survives is narrower: C's own null inside the
  generated code, and null written INTO slots (~write_ptr(slot, NULL)~,
  deref of an uninitialised pointer) -- values, not return conventions.
- ~src/core/ast.til~'s member_name_of: the ordering blocker is FIXED as
  of the seed_noreturn_chain preparation commit (loader.til), which
  settles core/exit.til's noreturn chain to a fixpoint BEFORE any body is
  type-checked, so a unit typed before exit.til still sees panic as
  diverging. The migration itself is the USE half of the pair and lands
  in the next commit -- proven to need the split, not assumed: with the
  pre-pass in the tree, til_boot (built from the previous commit's boot/)
  still rejects the migrated ast.til, while the freshly built bin/til
  accepts it. Evidence the pre-pass is purely an ordering fix: the
  regenerated boot/til.c changes by the two new functions and nothing
  else -- zero ~__attribute__((noreturn))~ differences on existing
  functions, i.e. the same answers, reached earlier.
- (superseded, kept for the record) The original claim was that
  Option there pulls in Option.unwrap, whose ~case: panic~ arm only
  counts as diverging once panic is known noreturn -- and core.til
  imports ast BEFORE exit, so when that unit is typed panic carries no
  noreturn stamp and the instantiated unwrap fails
  check_all_paths_return. Making the #303 seed order-independent is now
  a prerequisite for step 4.

Caught by the suite, not by the compiler: lookup_type_def_helper's
miss IS a state -- its own comment says type_name is empty for a
primitive switch scrutinee -- so unwrapping it there panicked on
`switch` over an I64 (test/flow.til, test/regression.til, interpreted
mode only). Restored as an Option return. That is the third time this
issue that the "which of the two nulls is this?" question decided the
answer.

**** Step 2 CLOSED (batch 14): member_name_of, and the bug it uncovered

member_name_of -> Option(ref Str) lands as the USE half of the pair
whose preparation was seed_noreturn_chain. That was the last
ref-returning NULL in the tree: src/ now has ZERO
~returns ref T~ functions containing ~return NULL~.

It did not land clean. With the seed fixed the migration COMPILED, but
test/first_class.til then corrupted the heap in INTERPRETED mode
(test_struct_def_of, which calls def.field_name_at). Isolated to 20
lines:

: pick := func(h: Holder) returns Option(ref Str) {
:     for i in 0..h.keys.len() {
:         return Option(ref Str).Some(h.keys.unsafe_get(i))
:     }
:     return Option(ref Str).None
: }
: name_at := func(h: Holder) returns ref Str { return pick(h).unwrap() }

Compiled: prints the key. Interpreted: heap corruption. With a STRUCT
payload instead of Str, both modes are fine -- so it is Str-specific,
reproduces with an explicit ~switch~ as well as ~unwrap~, and is a #320
defect that predates this migration. member_name_of is simply the first
~Option(ref Str)~ that interpreted code calls.

Root cause: ~call_copies_ref_str~ (interpreter.til) classified ANY
ext-callee call whose result is a ref Str as an owned COPY -- the rule
exists for a C-owned buffer crossing the boundary. But it keys on the
call node's STAMPED type, and a ref-payload case binding is
~deref(get_payload(..))~ with the deref call pre-typed to the payload
type by make_payload_ref_decl. ~deref~ is an ext_func, so the borrowed
Str payload was treated as owned and freed. Fixed by keying on what the
callee DECLARES: an ext function hands back a C buffer only when it
declares ~returns Str~; one declaring ~returns ref Dynamic~ (deref,
to_ptr) is laundering a pointer into memory the program already owns.

Test gap that hid it: every case in test/option.til used a struct
payload, and Str is the payload that matters because the interpreter
manages its buffer. test_option_ref_str_payload covers unwrap, the
switch form and None, and was verified to FAIL against the unfixed
interpreter ("expected '', found 'alpha'" -- the borrow had been freed).

**** Step 1, batch 15: FFIEntry, and #324's real reach

FFIEntry.fn / .cif / .arg_types: the NULL defaults were DEAD -- the one
construction site assigns all three immediately after building, so they
move into the ctor call and #242 makes them mandatory.
FFIEntry.return_type -> Option(ref Str): a void ext function genuinely
has none, and it is read-only (never freed), so it migrates cleanly.

Surveying the rest turned up that #324 reaches further than the three
Scope-typed fields this issue already blocked on it. Two more classes,
each PROVEN by the compiler rather than assumed:

- FFIEntry.param_shallows / .param_dynamics are only malloc'd when the
  signature has a shallow / dynamic param, so absence is real -- but
  ffi_cleanup releases them with ~free(own fe.param_shallows)~, a ref
  FIELD passed to an ~own~ parameter, which is exactly the hole #324
  tracks. Ownership cannot be transferred out of a borrowed payload, so
  the Option form cannot free them.
- Context.return_value: context_return_take MOVES the pointer out to a
  caller that owns it. With the field as Option(ref Dynamic) the typer
  rejects the take with "cannot return ref variable from non-ref
  function" -- the same transfer-out-of-a-borrow wall.

So the step-1 remainder is now uniformly one question, not several:
constfolder_known, ns_fields, InterpSession.global, FFIEntry's two
buffers and Context.return_value are all "a ref field the code owns by
hand". #324 is the gate for every one of them. Context.interp_ret_dest
is separate: it threads through the interpreter's ~destination~
parameter chain, so it is a nullable-ref-PARAM job (step 2's leftover),
not an ownership one.

**** Step 2 leftover, batch 16: the nullable ref PARAMS

The batches kept flagging "is this a nullable ref PARAM?" as a separate
job from the returns. Surveyed every function whose first statements
guard one of its own parameters with ~is_null()~: 29 of them.

Nine were DEAD -- every caller already hands them a node it has
dereferenced or unwrapped -- and the test corpus confirmed it (the rule
from CLAUDE.md: remove the guard and run the suite; 189/189):
func_def_type, builder_ns_has_func, builder_ns_func_returns_shallow,
builder_ns_func_param_shallow, init_ns_has_func, init_lift_in_ns_decls,
init_coverage_for_enum, and find_namespace_func / find_ns_decl's ~sdef~
-- the last two only became dead once batch 12 made every get_struct
caller unwrap, which is why they survived this long.

20 remain, and they split cleanly by param type:

- 6 x ~Dynamic~ (heap_drop_enum, heap_drop_struct, heap_drop_by_type,
  scope_set_heap_owned, callable_box_is, raw_result_finish's
  ~destination~) -- the raw layer, same class as the ~ref Dynamic~
  FIELDS.
- 8 x ~Expr~ + 2 x ~Scope~ + Str/Mode/LoadedProgram -- genuine
  Option(ref T) candidates, each needing its callers checked the same
  way (real absent state vs missing invariant check).

The two ~delete(self)~ guards are their own thing: an auto-generated
delete may run on a null self, so they are not user-facing at all.

**** Batch 17: five more dead guards, and interp_ret_dest is blocked after all

Five more of the 20 were dead, again settled by the suite rather than by
reading (193/193): fcall_fn_sig, resolve_fn_sig, sink_nontransfer_paths,
lazy_stmt_inline_walk, find_alias_rhs_in_ast. Their callers all pass a
live node -- the recursive walkers pass ~e.children[i]~, the entry points
pass a unit's ast.

Kept, with a REM saying why: ns_const_order_deps' ~e~. Its caller walks
~d.default_value~, and a namespace member with no initializer has none --
the "optional AST field" case CLAUDE.md names as real control flow.

Noted while surveying: emit_funcsig_typedefs has NO callers at all. Dead
code, left alone here rather than deleted on a whim.

CORRECTION to batch 16: it claimed Context.interp_ret_dest was the one
step-1 field NOT blocked on #324. That is wrong, and attempting it proved
so. Making the ~destination~ parameter chain
(eval_call / eval_expr_raw / eval_expr_field_raw / eval_user_func_call /
eval_callable_call / raw_result_finish) take ~Option(ref Dynamic)~
compiles until raw_result_finish has to hand the destination back:

: error: cannot return ref variable from non-ref function

It returns an owned ~Dynamic~, and the destination is now a BORROWED
payload. There is no laundering route -- ~to_ptr~ is itself
~returns ref Dynamic~ -- and returning ~Option(ref Dynamic)~ only moves
the same wall one frame up, since the callers' own returns are owned
~Dynamic~ too. Identical to the Context.return_value and FFIEntry-buffer
walls: the raw layer passes pointers around by hand and the type system
cannot express "a borrowed pointer value I am only forwarding".

So step 1 is now uniformly blocked on #324, with no exceptions:
constfolder_known, ns_fields, InterpSession.global, FFIEntry's two
buffers, Context.return_value AND Context.interp_ret_dest.

**** Batch 18: the last null ref return, and why the owned ones cannot follow

arg_addressed_ident (typer.til) was the final ~returns ref T~ in the
compiler still handing back NULL; it is now ~Option(ref Str)~. Verified
after the migration: NO ~returns ref~ function in src/ returns NULL any
more. The 56 surviving ~return NULL~ split cleanly by return type --
37 ~returns Expr~ (constfolder 24, loader 10, typer 2, garbager 1) and
19 ~returns Dynamic~ (interpreter 16, constfolder 3). The Dynamic ones
are exactly the quarantined raw layer the Decisions section rules out
of scope, so step 2 has nothing left to migrate.

The owned-Expr 37 are a DIFFERENT blocker, and it is a codegen one, not
a design one. Measured on a probe (tmp, not committed): an owned
~Option(T)~ can be moved INTO cheaply but never OUT of. Construction is
already a shallow steal --

: Option__Box *Option__Box_Some(Box * val) {
:     Option__Box *r = malloc(sizeof(Option__Box));
:     r->tag = Option__Box_TAG_Some;
:     r->data.Some = *val;
:     free(val);
:     return r;
: }

-- but every extraction path deep-clones. ~unwrap~ emits
~Box_clone(v)~, and so does a hand-written ~own self~ variant:

: Box * take(Option__Box * self) {
:     case Option__Box_TAG_Some: {
:         Box *v = ((void *)((U8 *)(self) + offsetof(Option__Box, data)));
:         Box *hoisted__Box_5676 = Box_clone(v);
:         Option__Box_delete(self, 1);
:         return hoisted__Box_5676;

Correct, but a deep copy. The cause is the general rule "returning a
ref binding from a by-value function clones it", which is right
everywhere else: a switch case payload binding IS a borrow into the
enum's data. For ~own self~ it is needlessly conservative -- self is
about to be deleted, so the payload can be stolen instead, the exact
mirror of the ~Some~ constructor above (malloc the payload shell,
shallow-copy the union member into it, free the enum without running
the payload's destructor).

Until that lands, migrating the 37 is not an option: an ~Expr~ clone is
a whole AST subtree, so it would be both a large slowdown and a
semantic change (the passes compare and mutate nodes by identity). So
the ~NULL~ keyword removal in step 4 now has a concrete, named
prerequisite: move-out-of-owned-Option in the codegen.

**** Batch 19: move-out of an owned Option, and a correction to batch 18

CORRECTION to batch 18: it concluded the codegen could not move an owned
payload out of an Option. That is wrong, and testing it properly proved
so. The codegen could already do it; only the IMPLICIT spelling clones.
Written out by hand, the move compiles to exactly the right C today:

: take := func(own self: Option(Box)) returns Box {
:     switch self {
:     case Option(Box).Some(v):
:         out : Box = malloc(Box.size())
:         memcpy(out, v, Box.size())
:         free(own self)
:         return out
:     case: panic(__LOC__, "none")
:     }
: }

-> ~Box *out = malloc(24); memcpy(out, v, 24); free(self); return out;~
-- no ~Box_clone~, no ~Option__Box_delete~, ASAN clean. So the pieces
were all there: ~free~ is the ext_func wrapping C's free, which releases
the enum SHELL without running the payload's destructor, and
~free(own self)~ is an own-transfer the garbager already reads, so it
stands the param's exit delete down on that path by itself.

What was actually missing was the natural spelling. ~case Some(v):
return v~ hit infer_return_stmt's alias rule and cloned, and there is no
~return own v~ (the parser rejects ~own~ outside a call argument). Fixed
by making the typer rewrite the return when -- and only when -- the
switch subject is an ~own~ PARAM, the payload is owned (not a ref
payload), and the return type IS the payload type:

: { <tmp> : T = malloc(T.size())
:   memcpy(<tmp>, <binding>, T.size())
:   free(own <param>)
:   return <tmp> }

steal_own_param_payload_returns + make_payload_steal_block in typer.til,
run from infer_clike_switch just before prepend_clike_payload_binding.
The rewrite emits ordinary til, so neither backend learns a new node and
the garbager needs no change. A borrowed subject keeps cloning (it does
not own the enum), and a ref payload keeps copying (a borrow the enum
never owned -- there is nothing to move).

Verified: interpreted and compiled agree, ASAN clean, 194/194, and pass
1 == pass 2 (no compiler source matches the gate, so this is not a
two-pass commit). Regression tests in test/option.til:
test_option_take_owned_payload_moves and, as the gate's control,
test_option_peek_borrowed_subject_still_copies -- which calls a
borrowed-subject peek TWICE on the same Option, so a steal there would
read freed memory on the second call.

Note what the tests can and cannot pin: the clone path is behaviourally
identical to the steal and differs only in cost, so no til-level test
distinguishes them. The evidence for the move is the emitted C above.

**** Batch 20: all 37 owned-Expr returns, and what the layout costs

Every ~returns Expr~ ~return NULL~ in src/ is now ~Option(Expr)~:
loader (lazy_single_return_value, lazy_body_as_expr, lazy_build_prelude
-- 10), typer + garbager (make_delete_call, gc_make_delete -- 3),
constfolder (raw_to_expr, try_fast_fold_call, try_eval_call -- 24).
~Option.take~ (own self, batch 19's steal) is what makes the callers
cheap; the receiver has to be spelled as a namespace call,
~Option(Expr).take(own x)~, because ~own~ is not accepted on a UFCS
receiver in expression position (~own x.take()~ parses as ~own~ on the
whole argument).

Only 19 ~return NULL~ remain in src/, all ~returns Dynamic~: 16 in
interpreter.til, 3 in constfolder.til (expr_to_known_raw, is_known).
That is exactly the quarantined raw layer the Decisions section rules
out of scope, so step 2 is finished in every sense.

PERF: this costs ~3% on a self-translate (26288/26821 ms before,
27323/27509 ms after; the loader and typer/garbager batches measured
clean on their own, so the cost is the constfolder's fold path). The
cause is layout, not the migration. An owned ~Option(T)~ stores its
payload INLINE, so a ~Some~ + ~take~ round trip is two malloc/free
pairs and two struct copies where a bare ~Expr *~ used to pass straight
through. Nothing in the call sites can recover it.

What WOULD recover it, and make the rest of #321 free: extend #320's
niche rule so a two-variant enum with one payload-less variant and one
OWNED AGGREGATE payload also lowers to a bare pointer, None == null --
Rust's ~Option<Box<T>>~ guarantee, the owned twin of the ~ref~ niche
already implemented. til's owned aggregates are already heap boxes
(~Expr *~) as params, returns and locals; storing them inline inside an
enum is the only place that differs, and it is what forces the copies.
That is a layout ruling, not a call-site fix, so it is the human's call
-- noted here rather than taken.

**** Batch 21: owned-payload niche, stage 1 -- the deref gate

The owned niche (batch 20's perf note) is a STAGED change, and the first
attempt at it in one go proved why. Flipping ~enum_is_niche~ alone builds
and gets the layout right --

: struct Option__Box { Box *data; };
: Option__Box Option__Box_Some(Box * val) { Option__Box r; r.data = val; return r; }

-- but every payload ACCESS then reads the wrong thing:

: Box *v = ((void *)((U8 *)(self) + offsetof(Option__Box, data)));
: Box_delete((U8 *)self + offsetof(Option__Box, data), 0);

~get_payload~ yields the ADDRESS OF THE POINTER SLOT (~Box **~ used as
~Box *~). A ref payload is saved by the ~deref~ wrapper
make_payload_ref_decl adds -- but that is gated on
~enum_variant_payload_ref~, which is false for an owned payload. The
probe panicked with "take of a None Option".

The root cause is that ONE predicate was answering two questions that
happen to coincide today:
  - is this payload stored as a POINTER? (the binding must load it)
  - is this payload BORROWED? (delete/clone must not touch the pointee)
A niche is only ever a ref payload today, so ~payload_ref~ answers both.
The owned niche breaks the coincidence.

Stage 1 separates them, with no behaviour change:
~enum_payload_is_pointer_slot(enum_def, idx)~ =
~payload_ref OR enum_is_niche~ (and false for a payload-less variant, so
the niche arm cannot answer for the wrong variant). The five deref gates
now ask it -- desugarer's two payload-binding sites, initer's generated
eq / to_str / clone -- plus the typer's steal gate, which must skip a
pointer slot because the steal block copies bytes OUT of an inline
payload and frees the enum box; for a niche that would leak the pointee.
The ownership sites (generated delete's skip, the ctor param own_type,
the garbager's borrow source) still ask ~enum_variant_payload_ref~.

Identical today by construction, and verified so: 196/196, pass 1 ==
pass 2, and test/constfold.c -- a committed golden of emitted C -- came
back byte-identical.

**** Batch 22: owned-payload niche, stage 2 -- delete and clone

Still preparation, still behaviour-identical (196/196, pass 1 == pass 2,
test/constfold.c byte-identical), and it found a real latent bug stage 1
had left behind.

The generated DELETE now asks the storage question where it was reading
the slot raw. For a payload in a pointer slot it derefs -- passing the
slot's ADDRESS would have deleted the pointer variable as if it were the
payload, the same mistake the first niche attempt made -- and it passes
call_free TRUE, because such a payload is its own heap box rather than
bytes inside the enum. An inline payload keeps false: its storage IS the
enum's. Ref payloads never reach either branch; they `continue` earlier,
since the enum does not own their pointee.

The generated CLONE is where stage 1 had introduced the bug. It had one
~payload_is_ref~ variable feeding BOTH the binding (storage) and
~payload_needs_clone~ (ownership), and stage 1 pointed that variable at
the storage predicate. Harmless today, but the moment an owned payload
is niched it would have made a clone ALIAS the original's payload and
then double-free it. Split into ~payload_in_slot~ (binding) and
~payload_is_borrowed~ (deep-clone decision); only a borrowed payload is
shared by a clone.

The lesson generalises: every remaining stage has to be read with "which
of the two questions is this?" in hand, because today every wrong answer
is invisible.

Remaining stages, in order (REORDERED by batch 23 -- the interpreter
mirror has to come BEFORE the flip, not after it):
  3. DONE (batch 24): the interpreter's raw-layer mirror.
  4. flip ~enum_is_niche~ AND give the niche a zero-copy take, together
     in one commit -- batch 25 showed they cannot be separated
     (excluding primitives, whose 0 is a legal value; Dynamic, the
     quarantined layer where null is legal; Str, whose literals are
     `static Str` rather than boxes, so the niche's delete would free a
     static address -- the tagged ctor's TIL_CAP_LIT check is what
     guards that today; and funcptr payloads, a distinct C shape the
     generated delete/clone already special-case). Two-pass, since it
     changes emit.
  5. a niche-aware ~take~: ~return self.data~ instead of the steal
     block, which needs a lowering for "hand this pointer back as
     owned". Until then a niche take keeps the copy stage 1 preserves.
  6. the garbager's view of a by-value enum that owns a heap payload.

**** Batch 23: stage 3 attempted, and the two bugs it flushed out

Flipping ~enum_is_niche~ was tried and REVERTED; what it uncovered was
kept. Two latent bugs, both invisible today because every niche payload
is currently a ~ref~ one, both of which the flip would have turned live:

1. The typer only recognised a payload binding in its BARE shape,
   ~get_payload(<subject>)~. A payload in a pointer slot binds
   ~deref(get_payload(<subject>))~, which was therefore not marked as an
   alias -- so a by-value ~return v~ of it hit the hard error "cannot
   return ref variable from non-ref function" instead of cloning. Every
   Option method broke at once under the flip (unwrap, unwrap_or, take).
   It never showed before because a ref payload lives in an enum whose
   ~unwrap~ returns ~ref T~, so the return path never asked the
   question. Fixed by looking through ~deref~
   (rhs_is_payload_binding).

2. The constfolder's raw_to_expr read a variant tag as an I32 at offset
   0. A niche stores NO tag -- so it decoded the low bytes of the stored
   pointer as a variant index, and the fold replaced a whole call with a
   bogus ~{.data = NULL}~ literal. It also read the payload inline
   instead of following the pointer. Both fixed; the interpreter's own
   enum_data_tag_key had the niche case right all along, which is why
   nothing else had noticed.

The flip still does not work after those two, and the reason is why the
stage order above changed: the INTERPRETER's enum ctor writes an owned
payload inline at the flat payload offset. Its ref-payload arm already
writes the pointer at offset 0 for a niche; the owned arm has no such
case, so a folded ~Option(Box).Some(...)~ never had a payload to find.
The raw-layer mirror is a prerequisite for the flip, not a follow-up to
it.

196/196, pass 1 == pass 2, test/constfold.c byte-identical -- both
fixes are corrections to code no live path reaches yet.

**** Batch 24: owned-payload niche, stage 3 -- the interpreter's raw layer

The prerequisite batch 23 uncovered. Three sites in the interpreter's
raw layer assumed a payload sits INLINE at ENUM_PAYLOAD_OFFSET, which is
the tagged layout; a niche stores a POINTER to it at offset 0 instead.

The enum CTOR is the one that made the flip fail outright.
write_enum_flat deliberately only ZEROES a niche slot -- it expects the
ctor to fill the pointer -- and until now only the ref-payload arm did,
so a constructed owned niche always read back as None. The owned arm now
writes the payload box at offset 0 and, crucially, does NOT free it: the
enum has adopted it, where the tagged path frees the source after
copying the bytes in. What differs between the two arms is ownership,
not layout.

enum_payload_deep_free frees the box as well as the pointee. For a
tagged payload the storage IS the enum's, so there is nothing extra to
reclaim; for a niche the box is a separate allocation and leaks
otherwise. (A borrowed slot payload still returns early -- the enum
never owned it.)

enum_payload_deep_clone gives the copy its OWN box rather than copying
bytes into shared storage, which would have left two enums pointing at
one allocation and double-freeing it -- the raw-layer twin of the
generated-clone bug batch 22 fixed.

Still preparation: 196/196, pass 1 == pass 2, test/constfold.c
byte-identical. Nothing reaches these paths until stage 4 flips
enum_is_niche, which is now the next commit.

**** Batch 25: the flip attempted again -- it works, and still cannot land alone

With stages 1-3 in, the flip DOES produce the intended layout. Verified
on a probe before reverting:

: struct Option__Box { Box *data; };
: Option__Box Option__Box_Some(Box * val) { Option__Box r; r.data = val; return r; }
: void Option__Box_delete(Option__Box * self, Bool call_free) {
:     if (self->data != NULL) { Box_delete(deref(&self->data), 1); }
:     if (call_free) { free(self); }
: }

One word, the ctor adopts the pointer with no malloc and no copy, the
delete follows the pointer and frees the box -- and interpreted and
compiled modes agree on the value. Stages 1-3 did their job. Two things
still block it, and the second is the important one.

1. An emit bug the flip exposes. At the ~is_n~ tag-check site the
   builder resolves the enum with ~find_struct_def(enum_c, ctx)~, where
   ~enum_c~ came from ~til_type_name~ -- the MANGLED name for a ~priv~
   enum. The lookup misses, ~isn_niche~ stays false, and it emits
   ~->tag~ against a struct that has no tag:

   : error: 'priv___src_self_typer_til__CtorArg' has no member named 'tag'

   The sibling default-value emit in the same expression rendered
   ~{.data = NULL}~ correctly, so two lookups in one statement disagree
   about whether the same enum is a niche. (CtorArg, a priv enum in
   typer.til, is the first non-Option enum the rule ever matched.)

2. The flip makes ~take~ REGRESS, which is why stage 4 and stage 5 are
   one commit and not two. Stage 1 deliberately made the steal gate skip
   pointer slots -- the steal block copies bytes OUT of an inline payload
   and frees the enum box, which would leak a niche's pointee -- with the
   note that "the niche gets its own zero-copy take later". Later is now:
   with the flip, ~Option(Expr).take(...)~ falls back to the CLONE path,
   so every take deep-copies a whole AST subtree. That is worse than the
   tagged layout it replaces (a shallow malloc+memcpy of the Expr header)
   and it is a semantic change, since the passes compare and mutate nodes
   by identity. It is exactly the cost the niche exists to remove.

   ASAN made it visible on the probe: ~Option__Box_take~ -> ~Box_clone~
   -> ~Str_clone~, allocating a copy where the steal had moved the
   original.

So the next commit has to do both: flip the rule AND lower a niche take
to ~return self.data~. That lowering is the open piece -- til still has
no way to spell "hand this pointer back as owned", which is the same
wall this issue opened with, now confined to one place.

*** What is left before the forbid can land

0. DONE (batch 19): move-out of an owned ~Option(T)~.
   DONE (batch 20): all 37 ~returns Expr~ ~return NULL~ sites, at a ~3%
   self-translate cost that an owned-payload niche would remove.
1. #324 (a ~ref~ FIELD may be passed to an ~own~ param) -> then the
   three Scope-typed owning fields get a real ownership answer.
2. Flip check_ref_field_owned_default: reject ~= NULL~ on a ref field
   whose type is not Dynamic (the quarantine exemption above), and
   convert the test fixtures in the same commit.
3. Step 2 is DONE (batch 13). Steps 3-4 remain: remove ~is_null()~ on
   refs, then the ~NULL~ keyword itself. Their own prerequisites:
   - the nullable ref PARAMS the batches kept flagging (a null scope
     ARGUMENT in the interpreter's walk loops, ~sdef~-style def params);
   - making the #303 noreturn seed order-independent, so core files
     typed before core/exit.til can instantiate Option (see batch 13);
   - the Dynamic quarantine has to be spelled out in the language, not
     just in this issue, since ~ref Dynamic~ keeps NULL by design.
4. FIXED: the auto-shallow dangling-borrow hole. A function returning an
   enum with a borrowed payload hands a borrow back exactly like
   ~returns ref~ does, so the four param-demotion gates in
   infer_func_def_expr now ask ~fdef_return_carries_borrow~ instead of
   ~return_own_type.is(OwnType.Ref)~. Regression test:
   test_option_ref_return_param_not_shallowed (test/option.til), which
   pins the ~returns ref~ twin as the control and fails on the unfixed
   compiler with "expected '1', found '42'".

   STILL OPEN, and worse than its ~returns ref~ equivalent: returning a
   borrow of a LOCAL through the Option. ~returns ref~ is caught by gcc
   (-Werror=return-local-addr); through an enum the address is stored in
   a struct, so gcc cannot see it and the program silently reads garbage.
   Reusing check_ref_function_returns does NOT work -- tried and backed
   out: its rule is "the returned EXPRESSION must be a borrow source",
   which is right for ~returns ref T~ and wrong for an enum VALUE whose
   payload is the borrow, so it rejects the legitimate
   ~return Option(ref T).Some(param)~. What this needs is a payload-aware
   check: validate the borrow SOURCE of a ref-payload ctor arg against
   the function's own scope, the same question #147 tracks for ref
   returns generally.

*** Decisions (2026-07-21 design discussion)

- NO unsafe blocks. Rust needs unsafe REGIONS because its raw
  pointers allow dangerous operations anywhere in user code; til's
  trusted surface is already confined to CONSTRUCTS --
  ext_func/ext_proc/ext_struct declarations (inherently unchecked
  claims about C: arity, types, ownership) and Dynamic laundering
  (~to_ptr~/~ptr_add~/~ref x : T = dyn~). The niche'd Option closes
  the nullability hole at that boundary with no new syntax. The rest
  of that trusted surface stays ambient, as today -- unsafe blocks
  would be an orthogonal auditability feature, not a dependency of
  this plan.
- The niche is a GENERAL enum-layout rule, not Option-by-name
  special casing -- see #320 "Niche layout" for the rule and its
  codegen consequences. Option(ref T) is pointer-sized (same as
  ~ref T~); behind the scenes generated C still uses C's NULL, so C
  interop costs nothing and other user enums of the same shape get
  the optimization for free.
- NO separate Nullable(ref T) / c_ptr type for the C boundary. Being
  layout-identical to Option(ref T), it would only add a second
  maybe-type, a conversion tax between the two, and bifurcated APIs;
  the ext declaration site already marks the C boundary.
- ext signatures and ext structs use Option(ref T) DIRECTLY for
  nullable C pointers; a bare ~ref T~ in ext contexts means "non-null
  by C contract". The bindings generator (src/self/binder.til:330-351
  currently emits ~ref mut field : T = NULL~ for every pointer field)
  switches to ~mut field : Option(ref T) = Option(ref T).None~. Note
  the trick that makes the default legal: that is an inline ENUM
  field, not a ref field, so it escapes check_ref_field_owned_default
  and can carry an ordinary default -- the ref moved inside an enum
  that has a no-borrow variant, which dissolves the whole "no
  instance to borrow from at default-value time" forced exception.
  Generated bindings are then hand-tightened to mandatory
  ~ref field : T~ where the C docs guarantee non-null -- the same
  manual tuning workflow bindings already need for ref vs own and
  func vs proc. Nullable C callbacks become ~Option(Fn(...))~ (fn
  pointers are niche-eligible too).
- Concrete C-boundary cases driving the above (surveyed 2026-07-21):
  - tinyfiledialogs (src/modes/gui.til:95): NULL crosses BOTH ways --
    passed as "no filter" args and returned for "user cancelled";
    ~ref result : I8 = ...; if result.is_null() { return "" }~
    becomes a switch on the returned Option.
  - nng_url (vendor/bindings/nng.til:948): ext struct whose C layout
    mandates per-field nullability ("will be NULL if not specified");
    niche'd Option fields keep the struct byte-identical to C's.
  - File.handle (src/std/file.til): NOT actually a nullable-from-C
    case -- cfile_open ~exit(1)~s on failure, so its NULL uses are
    only the field default (becomes a mandatory field; both
    constructors always pass a handle) and the ~self.handle = NULL~
    poison in close (dead state; nothing reads the handle after
    delete).
  - Vec.data lazy-empty is spelled ~own data : U8 = 0~ (integer
    zero, not the NULL keyword) and ~realloc(NULL, n)~ semantics
    live in C; container internals are unaffected.
