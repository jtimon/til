# priority: low
# category: features
** WIP #262 lazy_func / lazy_proc: lazily-evaluated call arguments

Numbering note: this issue was originally filed as #260 and renumbered to
#262 when #260 was reassigned to "Add cursor-relative File seek helpers"
(issues/fixed/260.org). Older chat / commit references to "the lazy issue as
#260" predate the renumber -- the lazy work is #262, and #260 is an unrelated,
already-fixed issue. They are NOT the same.

Status summary (what is merged):
  DONE: lazy_func/lazy_proc callables; a pre-typer inline pass with
  call-by-name semantics; inlinable body shapes = single ~return <expr>~,
  ~if/else~ with single returns, and N-clause guard chains; bare and UFCS
  method-call inlining; Lisp-style N-ary ~and~/~or~ folding; core ~and~/~or~
  migrated to ~lazy_func~ so ~.and~/~.or~/~&&~/~||~ short-circuit (#117, now
  FIXED); throwing calls inside a lazy arg lower correctly into the match
  arm. Tests in test/lazy.til and test/typer_errors.til.

  DONE (thunk fallback, Design 2): a non-inlinable lazy body (loops, early
  returns inside control flow) is now lowered to an ordinary closure-taking
  function instead of hard-erroring. Each param ~p: T~ becomes a zero-arg
  thunk ~p: func() returns T~ (the synthetic funcsig ~_Fn_ret_T~), every
  value-use of ~p~ in the body is auto-forced to ~p()~, and at each call site
  the argument is wrapped in a synthesized closure ~func() returns T [caps]
  { return arg }~ (call-by-name; ~caps~ computed by free-variable analysis
  against the root scope). Non-capturing thunks lift to plain top-level
  lambdas; capturing ones reuse the #54 closure machinery, whose escape
  checks gate any captured local that would escape its defining call. Scope:
  TOP-LEVEL defs, BARE and UFCS calls, EXACT (non-variadic) arity, no
  param-name shadowing inside the body. Lives in ~expand_lazy_thunks_in_program~
  / ~lazy_thunk_lower_def~ / ~lazy_wrap_thunk_calls~ (src/self/loader.til),
  pre-typer. Tests: test/lazy.til (~both~ loop+early-return body, captured
  locals, UFCS); test/typer_errors.til (~terr_bad_lazy~, a param-shadowing
  body that stays lazy and still hard-errors). The old "non-inlinable bodies
  HARD-ERROR" rule now applies only to the residual cases below.

  Related fix merged alongside (#223): the redundant-~shallow~-on-primitive-
  return diagnostic was not idempotent (the auto-shallow pass overwrites
  return_own_type, so a re-inferred def -- e.g. a capturing thunk used as a
  UFCS-receiver-call arg -- misfired). Now keyed off a parser-set
  ~return_shallow_explicit~ flag.

  DONE (declared-variadic lazy, compile-time unroll). ~lazy_func(xs: ..Bool)~
  works with THREE equivalent body spellings, each unrolled at the call site
  over its compile-time-known operands (no thunks, no Array is ever built --
  the Array-of-thunks Design 2 route and its Array-of-funcptr blocker became
  moot for variadic; Design 1's compile-time expansion landed instead):
    - ITERATIVE: ~for x in xs { if x.not() { return false } } return true~
      -- each for-in becomes one guard per operand
      (~inline_variadic_lazy_call~, src/self/loader.til).
    - RECURSIVE match (the GHC Prelude shape, ~and (x:xs) = x && and xs~):
      ~return match xs { case []: true case [x, ..tl]: match x { case true:
      vand(..tl) case: false } }~. New surface syntax: compile-time list
      patterns ~case []:~ / ~case [x, ..tl]:~ (NodeType.ListPattern, legal
      only on the variadic param of such a body) + the existing ~f(..xs)~
      splat as the recursive tail call. Bottom-up unroll
      (~inline_recursive_variadic_lazy_call~); the def itself is a
      compile-time template (never expanded in place, typed, or emitted).
    - RECURSIVE statement/switch (if/else clause style): ~switch xs { case
      []: return true case [x, ..tl]: if x.not() { return false } return
      vand(..tl) }~ -- same unroll, the cons arm's guard chain splices via
      ~lazy_body_as_expr~.
  Core ~vand~/~vor~ (src/core/bool.til) use the recursive match form -- all
  three benchmarked indistinguishable (20M 4-operand calls, identical
  outputs), so the Haskell idiom won on readability. The other two spellings
  are test-local coverage in test/lazy.til (~vand_iter~/~vor_iter~,
  ~vand_rec_if~/~vor_rec_if~). The old N-ary FOLD (binary defs silently
  accepting N args) is DELETED: ~and~/~or~ are strictly binary and a 3+-arg
  call is an arity error.

  REMAINING -- each blocked on a separate prerequisite or a non-goal:
    1. FIRST-CLASS lazy callables. PARTLY DONE. A thunk-lowered lazy func can
       now be bound to a LOCAL VALUE and called through that value with full
       short-circuit, in both the interpreter and the C backend:
         g := both        # value binding of a thunk-lowered lazy func
         h := g           # transitive alias
         g(false, bump()) # short-circuits: bump() never runs
       Mechanism (all keyed off the loader's `_Fn_ret_T` thunk lowering, no new
       runtime cost): the loader's call-wrapping pass (lazy_wrap_thunk_calls,
       src/self/loader.til) tracks local `x := <lowered-def>` aliases -- scoped
       by clone-on-descent so a name reused in another function does not leak --
       and wraps the value call's args in thunk closures exactly like a direct
       call; the typer's funcptr-call arg-check (src/self/typer.til) accepts the
       thunk args via the wildcard-aware funcptr_eq (a bare FuncPtr matches the
       named `_Fn_ret_T` param sig); the builder resolves the value's signature
       -- transitively for a chain, via resolve_local_fn_sig -- and emits the
       closure-call cast treating an `_Fn_ret_T` (= `TilClosure *`) arg by value
       instead of appending a spurious extra pointer. Tests: test/lazy.til
       (test_first_class_value / _chain / _short_circuits).
       STILL REMAINING: first-class through an EXPLICITLY-NAMED funcsig type --
       returning a lazy func (`func() returns MySig { return both }`), a struct
       field of that type, or a HOF param. These need the user's surface funcsig
       (`func(Bool, Bool)`) to be lowered to the thunk signature
       (`func(_Fn_ret_Bool, _Fn_ret_Bool)`) -- i.e. a lazy-funcsig-type lowering
       and call-through-lazy-funcsig-value wrapping -- which is a distinct piece
       (new surface: a `lazy_func(...)` bodyless funcsig). Escaping thunks that
       capture locals BY REFERENCE remain gated by #54's escape checks (shallow/
       own captures); and passing a lazy func through an EAGER HOF cannot
       preserve the HOF's own arg short-circuit by semantics.
    2. Param-name shadowing inside a thunk-lowered body: currently the def is
       left lazy (hard-errors) rather than risk mis-rewriting a shadowed use.
       A minor self-imposed limitation; needs scope-aware auto-force. Low
       value (shadowing a param inside a lazy body is rare).
    3. call-by-need / memoization: a NON-GOAL under the chosen call-by-name
       semantics (see below) -- it would break the inline == thunk
       equivalence and only helps params reused within a body.

v1 status (implemented):
  - ~lazy_func~ / ~lazy_proc~ keywords parse to ~FuncType.LazyFunc~ /
    ~FuncType.LazyProc~ (lexer, parser, ~FuncType~ enum + name mappers).
  - A lazy callable's body is a single ~return <expr>~. Bare-name calls and
    UFCS-style method calls are inlined by ~expand_lazy_calls_in_program~
    (src/self/loader.til), a pre-typer pass: it clones ~<expr>~ and
    substitutes each parameter Ident with the matching argument expression
    (call-by-name), then the typer types and lowers the result normally.
    When ~<expr>~ is a ~match~, only the taken arm runs, so unused args never
    evaluate -> short-circuit.
  - Core ~and~ / ~or~ are now ~lazy_func~ definitions in ~src/core/bool.til~;
    this gives short-circuit semantics to ~a.and(b)~, ~a.or(b)~, ~a && b~,
    and ~a || b~ (#117).
  - The lazy defs themselves are left in place and type/emit as ordinary
    (uncalled) functions, like macros, so the builder needs no changes.
  - A throwing call in an argument (subscript ~v[i]~ -> ~get(i)!~, an ~f()?~,
    or an ~f()!~) works: ~cond.and(v[i])~ inlines to ~match cond { case true:
    v[i] ... }~, putting the throwing call in a match arm body. The throw/bang
    lowering pass (~process_match_arms_in~ in src/self/typer.til) descends into
    match arm bodies just like switch case bodies, so the call is lowered INTO
    the arm: it gets its hidden _err_kind/_err_<T> out-args and only runs when
    that arm is taken (short-circuit preserved -- a guarded ~v[i]~ never
    out-of-bounds). Previously the arm body was visited by neither the bang
    hoister (skips Body nodes) nor the nested-body recursion (handled
    if/while/for/switch/catch, not a match expression), so the call reached
    infer unlowered and failed with "missing argument for parameter
    '_err_kind'".
  - Lisp-style N-ary ~and~ / ~or~: a binary (2-param, non-variadic) lazy
    callable called with more than two effective args right-folds into nested
    binary calls before inlining (~lazy_try_fold_nary~ / ~lazy_build_fold~ in
    src/self/loader.til), e.g. ~and(a, b, c)~ -> ~and(a, and(b, c))~. The
    outer call inlines to a ~match~ and the spliced inner call is inlined by
    the re-expansion pass, so the whole chain short-circuits. Works for both
    bare ~and(a, b, c)~ and method ~a.and(b, c)~ forms. Purely additive:
    N<=2 is unchanged, so the pervasive binary ~.and()~/~.or()~ path is
    untouched. Folding assumes associativity (true for ~and~/~or~).
  - Richer inlinable body shapes (the inline-when-possible side of the
    hybrid). Beyond a single ~return <expr>~, ~lazy_body_as_expr~
    (src/self/loader.til) now also inlines:
      * ~if cond { return X } else { return Y }~ -> ~match cond { case true: X
        case: Y }~; and
      * a guard chain ~if c1 { return X1 } ... if ck { return Xk } return Y~
        -> right-nested ~match~es, so each clause short-circuits the ones
        after it.
    til's ~if~ is a statement (not a value-expression), hence the rewrite to
    ~match~. All these inline at zero runtime cost and short-circuit.
  - Statement-inlining (zero-cost; covers the common local-binding case
    without thunks). A single-exit body that is NOT a single expression --
    a prelude of safe statements (local bindings, assignments, calls)
    followed by an inlinable tail, e.g. ~x := f(a); return x.and(b)~ -- is
    inlined at statement-position calls (a Decl RHS or Return operand): the
    substituted prelude goes into a fresh block and the tail flows through a
    result temp:
        r := lazyf(p, q)
      becomes
        mut _lzr_N : R = <default>
        { x := f(p); _lzr_N = <tail expr> }
        r := _lzr_N
    The block scopes the body's locals (so repeated calls don't collide), the
    prelude runs unconditionally (matching the body), and the tail is
    short-circuited by the expression inliner. ~lazy_body_split_point~ /
    ~lazy_body_stmt_inlinable~ (src/core/ast.til) classify the shape;
    ~lazy_stmt_inline_walk~ (src/self/loader.til) does the rewrite, a pre-typer
    pass that runs before the expression inliner. Call-by-name, semantically
    exact. Verified short-circuit: in ~x := costly(a); return x.and(b)~,
    ~costly(a)~ runs once (used unconditionally) and ~b~ only when reached.
  - Non-inlinable lazy bodies HARD-ERROR instead of silently degrading to an
    eager call. The typer (~infer_func_def_expr~) rejects any body that is
    neither ~lazy_body_inlinable~ nor ~lazy_body_stmt_inlinable~ -- i.e. loops,
    early returns, and first-class/recursive use. These are the only cases
    left; they need the thunk fallback (or are genuinely rare/exotic).
  - Regression test: test/lazy.til (functional and/or, nested lazy calls,
    core Bool and/or, operator syntax, N-ary fold and its short-circuit,
    if/else-body and guard-chain lazy funcs, statement-inlined local-binding
    bodies and their short-circuit, decisive short-circuit proofs where an
    unused side-effecting argument must not run, and a throwing call in a lazy
    and/or argument that must compile and short-circuit). Negative:
    test/typer_errors.til (terr_bad_lazy) checks a residual hard-error case.

(The "Remaining" list above -- variadic, first-class, shadowing, call-by-need
-- supersedes the older notes that follow. The thunk fallback that those notes
described as "not yet done" is now MERGED for the synchronous fixed-arity case;
see the Status summary. The Design discussion below is kept for the variadic /
first-class work that is still blocked on its prerequisites.)

Introduce ~lazy_func~ and ~lazy_proc~ as new callable kinds (alongside
  ~func~/~proc~ and ~ext_func~/~ext_proc~). When you call a lazy callable,
  its arguments are NOT evaluated eagerly at the call site; each argument
  expression is treated as a thunk and is only evaluated if and when the
  body uses it. Most of the work should live in the desugarer.

Motivation:
  - It unblocks short-circuiting ~and~/~or~ (#117). With lazy args, ~and~
    is just:
        and := lazy_func(a: Bool, b: Bool) returns Bool {
            if a { return b } else { return false }
        }
    and ~b~ is never evaluated when ~a~ is false. ~or~ is the dual.
  - It enables a variadic, Lisp-style ~and~/~or~ that short-circuits across
    N operands, e.g. ~and(p, q, r, ...)~ folding to nested ~if~s, instead of
    the chained ~.and(...).and(...)~ method form.

Sketch of the desugaring approach:
  - At a lazy call site, the desugarer inlines the callee's body and splices
    each argument expression in at the points where the corresponding
    parameter is used (call-by-name / macro-style expansion). For ~and~/~or~
    this collapses to the ~if~ forms above with no thunk allocation.
  - Variadic lazy and/or desugar by right-folding the operand list into
    nested conditionals.

Open questions / honest caveats (these are why this is its own issue, not a
  trivial rewrite):
  - "All in the desugarer" only holds when the lazy callee can be inlined at
    the call site. A first-class lazy func (stored in a var, passed as a
    funcptr) or a recursive one cannot be fully desugared by inlining; those
    cases need real thunks/closures or must be disallowed initially.
  - Side-effect / evaluation-order semantics change by design: an unused lazy
    argument's side effects never run. This needs to be documented clearly.
  - Each lazy parameter that is used more than once in the body would
    re-evaluate its argument unless we bind it once; decide whether lazy means
    call-by-name (re-eval) or call-by-need (memoized once).

Design options considered (no approach picked -- documented for the human to
decide):

  Core constraint. Ordinary variadic args become a runtime collection of
  VALUES (today an ~Array(Bool)~ built in ~desugar_user_func_fcall_args~).
  Building that collection evaluates every operand, which destroys
  short-circuiting. Switching ~Array~ -> ~Vec~ does NOT help: the trap is
  storing values, not the container. To defer evaluation an operand must be
  stored as something that is not yet the value -- either no runtime
  collection at all (compile-time expansion, Design 1) or a collection of
  thunks (Design 2).

  Design 1 -- compile-time unroll (generalize the current fold; zero runtime
  cost). Keep the variadic as a compile-time list of argument ASTs; the
  author writes the fold as head/tail recursion and the desugarer unrolls it
  by the call's known arity:
      and := lazy_func(xs: ..Bool) returns Bool {
          return match xs {
          case []:        true                                  // identity
          case [x, ..tl]: match x { case true: and(tl...) case: false }
          }
      }
  ~and(a, b, c)~ expands to nested ~match~es bottoming at ~true~: no
  allocation, full short-circuit, and the AUTHOR (not the compiler) defines
  the fold shape and base case, so associativity is no longer assumed.
  Needs new surface features: (a) a spread in arg position (~and(tl...)~),
  (b) compile-time head/tail list patterns (~[]~, ~[x, ..tl]~), (c)
  recursion-unrolling in the expander. Could stay a pre-typer structural
  pass. Limits: arity must be statically known (fine for direct calls, not
  first-class); generated code grows ~linearly with operand count. Cannot be
  done with ~match~/~switch~ today -- the patterns and spread are genuinely
  new features (worth their own issue, useful beyond lazy_func).

  Design 2 -- array of thunks (call-by-name; first-class; runtime cost).
  Make the variadic ~Array(() -> T)~: each operand is wrapped in a zero-arg
  closure at the call site (no evaluation, just closure construction), and
  the body forces on demand:
      and := lazy_func(xs: ..Bool) returns Bool {
          for x in xs { if x.not() { return false } }   // x auto-forced
          return true
      }
  Fully general: runtime arity, first-class (storable/passable), no
  recursion needed. Reuses til closures (#54; ~typer_lift_lambdas~ in
  src/self/desugarer.til). Costs: one closure allocation per operand plus
  indirect calls; must live in desugarer.til (type-directed -- it builds
  typed closures). Needs auto-force (the compiler inserts the call at each
  use of a lazy param so the body reads ~x~, not ~force(x)~) and a thunk
  ownership/lifetime story (who owns the closure + captured env, when freed).

  Design 3 -- recursion with pop. Same REPRESENTATION as Design 2, just a
  recursive body instead of a loop: ~match force(xs.pop()) { case true:
  and(xs) case: false }~ with ~xs~ a ~Vec~ of thunks. NOT a separate option:
  a ~Vec~ of VALUES would be eager (see core constraint); only a ~Vec~ of
  thunks works, which is Design 2 with a different body shape.

  Promises vs thunks. A Scheme-style ~promise~ (~delay~ / ~force~) is exactly
  a MEMOIZED thunk (call-by-need): the first force caches the result, later
  forces reuse it. So "array of promises" = Design 2 + memoization, not a
  separate mechanism. (JS / std::future async promises are orthogonal AND
  wrong here: they start eagerly and defer only delivery, giving no
  short-circuit.) Memoization only pays off when a lazy param is used MORE
  THAN ONCE in a body; ~and~/~or~ use each operand at most once, so plain
  call-by-name thunks already evaluate each operand 0-or-1 times and a
  promise's cache cell would never earn its keep there. Call-by-need also
  adds the "not-forced-yet" representation + cached-value ownership cost.

  One candidate -- "inline when possible, thunk otherwise", with semantics
  = call-by-name:
    - Pick call-by-name. Under it, inline-splice and thunk-call are
      OBSERVATIONALLY IDENTICAL (an unused arg never runs; an arg used N
      times runs N times), so the compiler may choose either lowering per
      call site invisibly to the user. Call-by-need would break this
      equivalence (inline re-evaluates per use, a promise evaluates once),
      which is a further reason to defer it.
    - Mental model: a lazy_func IS a function whose args are thunks (the
      always-correct general lowering); INLINING is a pure optimization
      applied where the body is inlinable and arity is statically known.
      ~and~/~or~ keep the zero-cost inline path; everything else falls back
      to thunks (still lazy, still short-circuit).
    - Benefits: (a) generic lazy_func / lazy_proc with no user-visible
      special cases; (b) ~and~/~or~ stay binary and inlined -- no
      ~var_and~/~var_or~ split needed; (c) it fixes the silent-eager bug
      below for free (a non-inlinable body falls back to THUNKS, not eager).
    - Gotchas to nail down: thunk ownership/lifetime in til's manual model;
      auto-force insertion; and capture-by-REFERENCE so a thunk that reads a
      variable the body mutates before forcing agrees with the inline splice
      (matters only for side-effecting / mutating captures; moot for pure
      args like and/or operands). The mechanism spans two phases: inline
      stays pre-typer (needs raw bodies), thunk-lowering is type-directed
      (desugarer.til).

  Silent-eager fallback bug (FIXED). A lazy_func whose body was not inlinable
  used to be silently left un-inlined and degrade to an EAGER call (right
  value, but all args evaluated, no diagnostic -- a correctness bug, since a
  guarded throwing / out-of-bounds operand would panic). Now: the if/else
  single-return shape inlines (see v1 status), and ANY other body shape
  (multi-statement, loops, ...) is rejected with a HARD ERROR at the lazy
  definition -- ~lazy_body_inlinable~ (src/core/ast.til) is the shared shape
  predicate; the typer checks it in ~infer_func_def_expr~ before the body is
  lowered. Negative test: test/typer_errors.til (terr_bad_lazy). When the
  thunk fallback lands, this hard error becomes a THUNK lowering instead, so
  those bodies start working rather than erroring.

  A possible ordering (for the human to decide, not chosen): (1) DONE --
  stopped the silent-eager fallback (hard error) and widened the inline path
  (return-expr, if/else, guard chains, N-ary fold, UFCS); (2) Design 2 thunks
  as the generic fallback, keeping the inline path as the fast path; (3)
  Design 1's compile-time list-patterns + spread; (4) call-by-need / promises.

Note: the binary short-circuit ~and~/~or~ from #117 could also be done as a
  narrow, special-cased desugar rewrite (~a.and(b)~ -> ~if a { b } else
  { false }~) without any of this general machinery. This issue is the
  general path that additionally buys user-defined lazy callables and the
  variadic form; #117 can be satisfied by either.
