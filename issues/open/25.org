# priority: low
# category: deferred
** WIP #25 Support for the 4 main operative systems

:DEPENDENCIES: [[../fixed/12.org][#12]] (self-hosting, FIXED)
:RELATED: [[./151.org][#151]] (TempleOS), [[./152.org][#152]] (SDK-less macOS cross), [[./232.org][#232]] (hosted 32-bit)

  til should build and run on the 4 main operative systems, and not
  just natively: the goal is full cross-compilation -- build a til
  binary for any target from any host. Hosts are GNU/Linux, macOS and
  Windows; targets add TempleOS. The matrix is 3x4 + 1x1 = 13
  combinations (TempleOS as a host only targets itself; replacing
  gcc/clang on TempleOS is out of scope).

  The original priority order was Linux, TempleOS, macOS, Windows.
  Reality inverted it: mingw-w64 made Windows cheap, so it landed
  second. macOS is now the active front and the greatest remaining
  challenge; TempleOS goes last ([[./151.org][#151]]).

  Status (2026-07):

  - GNU/Linux: DONE. Native host plus 6 Linux targets (x64, arm64,
    riscv64, x86, arm32, riscv32). CI cross-compiles all six and runs
    x86/arm32/riscv32 under qemu. The --cc override supports odd
    toolchains (the riscv32 job uses a Bootlin gcc).
  - Windows as a target: DONE. mingw-w64 cross from Linux; vendored
    libraylib-win64.a / libtinyfd-win64.a; mode gui links
    gdi32/opengl32/winmm/...; [[file:../../src/c/ext.c][src/c/ext.c]] has full _WIN32 coverage
    (CreateProcess, LoadLibrary/GetProcAddress FFI loading, psapi
    RSS). CI cross-builds hello_script.exe and runs it on a real
    windows-latest runner. make build_win sweeps all examples except
    the ones that link Linux-only libs directly (raylib.til FFI demo,
    nng_*.til -- no Windows nng build yet). 32-bit windows-x86 is
    tracked in [[./232.org][#232]].
  - wasm32 (bonus target beyond the 4): DONE via emscripten
    (make build_wasm, single-file .html output; was #228).
  - Windows as a host (bin/til.exe compiling programs ON Windows):
    open. Needs libffi built for win64 (target_ffi_lib returns "" for
    windows, and the compiler links libffi for its ext_func
    interpreter dispatch), an nng-win64 build for mode server, and a
    CI job that builds and runs the compiler itself on windows-latest.
    The same libffi-win64 build unlocks runtime ext_func in
    cross-compiled user programs.
  - macOS: not started in practice. Plumbing exists in
    [[file:../../src/self/targets.til][src/self/targets.til]] (Target.MacosX64/MacosArm64, clang -target
    flags, -lm -lpthread), but nothing has ever been built or tested
    on a Mac, and cross from Linux cannot work as-is: no Apple SDK on
    the host. Detailed plan below.
  - TempleOS: deferred, split out to [[./151.org][#151]]. #12 is fixed so the hard
    dependency is satisfied; what remains is the HolyC backend itself
    (Lang.HolyC and the holyc toolchain entry are placeholders today).

*** Progress

   - 2026-07 (Linux session): phase 1 groundwork that could be done and
     verified from Linux, all host-gated so Linux behavior is
     unchanged: Makefile uname gating (-lrt/-latomic dropped and
     desktop frameworks added on Darwin, clang -Wno extras on the cc
     lines, xvfb-run wrapper Linux-only, stable libffi-macos.a copy),
     ext.c (__linux__ gate for the /proc RSS walkers,
     _NSGetExecutablePath + realpath for get_bin_dir), targets.til
     (shared_lib_extension helper, macos target_ffi_lib /
     target_gui_libs), builder.til (macos link-flag rewrite, per-target
     shared-lib suffix), interpreter.til (.dylib sonames for -l dlopens
     on mac hosts), and an experimental non-blocking macos-arm64 CI job
     running make test_nogui.
   - CI iterations so far: (1) vendored libffi 3.4.6 does not assemble
     under Apple clang -- backported upstream 8308bed (cfi_startproc
     after the CNAME label), tracked for removal by the libffi upgrade
     issue #304; (2) boot/til.c tripped -Wgnu-null-pointer-arithmetic --
     fixed in the emitter (NULL.ptr_add now lowers to a standard
     (void *)(UPtr) cast, two-pass); (3) til_boot link failed on the
     glibc leak probe's undefined-weak symbols (an ELF idiom Mach-O
     rejects) -- probe now gated on __GLIBC__. A local
     clang -Werror -fsyntax-only sweep with the mac warning set
     reproduces the mac compile faithfully and is part of the loop.
   - Both macos CI jobs (arm64 on macos-latest, x64 on macos-15-intel)
     run make test_nogui plus a --target=macos-* build/file/run check,
     non-blocking via continue-on-error.
   - CI iterations 4-5: macos-x64 ran the ENTIRE compiler pipeline
     (til_boot lexed/typed/emitted src/til.til, clang compiled the gen
     C) and only tripped at the final link on a dangling ./src/self/
     token -- root-caused to interpreter.til's hardcoded Linux-triple
     libffi link() falling back to a bogus importer-relative path on
     mac; the directive is gone (target_ffi_lib supplies libffi per
     target). macos-arm64 instead aborts (silent SIGABRT) ~65s into
     the til_boot self-build; both mac jobs now dump the .ips crash
     reports on failure since the reason lands there, not on stderr.
   - The arm64 abort is darwin-arm64 SPECIFIC: the identical in-process
     work (typer, constfolder incl. live libffi ext_func evals,
     garbager, emitter) completed fine on aarch64-LINUX under
     qemu-user from an x64 box -- cross-built aarch64 vendor libs +
     til_boot, interpreted hello and self-built src/til.til, exit 0.
     Suspects shortlist for darwin-arm64: variadic-call ABI (darwin
     passes variadics on the stack, unlike AAPCS64), Apple-Silicon-only
     malloc/JIT strictness. The .ips backtrace decides.
   - New matrix cell from that detour: aarch64-linux as a native HOST
     works; CI gains linux-arm64-native (ubuntu-24.04-arm, full
     make test + target build check, experimental) with targets.til
     LinuxArm64 ffi/gui tables and a Makefile stable-path libffi copy,
     mirroring the mac pattern.
   - Iteration bottleneck: the GitHub App cannot dispatch workflow runs
     (403 on actions -- needs Actions write permission), so mac runs
     only happen on master pushes; grant the permission or trigger
     'Run workflow' on the branch manually to unblock bot-driven
     iteration without merges.
   - CI iteration 7: the arm64-mac silent SIGABRT is GONE -- it was the
     bogus libffi link() fallout after all (cured by removing the
     directive; the crash-dump step found no .ips, consistent with the
     abort never recurring). macos-arm64 now reaches a REAL frontier:
     til_boot fails compiling src/til.til with
     "./src/self/interpreter.til:5187:5: error: undefined variable
     '_va_Array_3546_eo'" -- a desugarer-generated variadic err slot
     (expect()'s OutOfBounds slot) dangling after the constfolder's
     rewrite + re-inference, on this host ONLY (linux-x64, darwin-x64
     and linux-aarch64-under-qemu all type the same file fine). So a
     constfolder fold decision diverges on darwin-arm64 -- fold
     decisions consume in-process FFI eval results, which is the only
     host-dependent input -- and some fold shape then breaks
     va_build_dce's never-dangle invariant. Two bugs to hunt: the
     darwin-arm64 eval divergence, and the DCE hole that lets ANY
     divergence dangle a reference. (A long-double hypothesis was
     checked and dropped: nothing in the compiler or bindings touches
     long double.)
   - macos-x64 meanwhile runs test_nogui well past every previous
     failure point (the libffi link() removal unblocked the final
     link); first suite results pending.
   - PHASE 2 LANDED (SDK-less cross-compilation from Linux):
     vendor/macos/ carries zig's curated darwin libc headers plus the
     libSystem.tbd linker stub (provenance in vendor/macos/README.org,
     zig commit 738d2be); macos targets from a non-mac host compile
     with the stock clang against those headers and link with lld
     (which ad-hoc code-signs arm64). make build_mac on Linux
     cross-builds every portable-mode example (cli/script/pure/lib)
     for BOTH mac arches with -macos-arm64/-macos-x64 name suffixes;
     verified locally: 16 examples x 2 arches, all Mach-O, plus a
     mode lib .dylib. mode gui stays SDK-bound (frameworks) and the
     nng custom modes need a mac-built libnng -- both remain
     native-mac-only until phase 3. Running the cross output on a real
     mac is the next CI wiring step (artifact download job, like the
     windows pair).
   - First full-suite results on real aarch64-linux (the renamed
     linux-arm64 (native) job, 3.5h on the free runner): 161/165.
     The four failures -- cast.til, operators.til (interp+run) and
     cli_scalars (i+r) -- look like float-to-int cast semantics
     diverging on aarch64 (FCVTZS saturates where x86 wraps to
     INT_MIN); to fix next, darwin-arm64 will need the same answer.
     The job keeps continue-on-error ONLY until those four are fixed.
   - The build_mac sweeps exposed one example broken on every platform
     and built by no CI job: bench_hashmap.til (parse error, stale
     HashMap(...).hasher = syntax; not filed yet). custom_modes.til is
     also excluded from the sweeps but is NOT broken -- it is a
     mode-definitions file consumed via --extra-modes (the
     build_sender/... tests in src/tests.til), not a standalone
     program; an earlier version of this entry misdiagnosed it.
   - MATRIX CELL DONE: linux-arm64 as a native host runs the FULL
     suite green (the -fsigned-char contract fixed all four bring-up
     failures); the CI job is blocking like the other linux jobs.
     continue-on-error is gone from every job -- failed mac jobs now
     fail the run honestly.
   - Current darwin frontier (both arches, one suspected bug): the til
     compiler itself SIGSEGVs intermittently while compiling tests on
     the mac runners -- macos-x64 is at 150/153 and macos-arm64 at
     98/153 with the failing-test SET CHURNING between runs, and the
     .ips crash reports all name procName til, Segmentation fault at
     0x100000008 ("not in any region"). That address is exactly a
     little-endian read of two adjacent U32s {count=8, cap=1} as a
     64-bit pointer -- i.e. somewhere a Str's (count,cap) words get
     read as its c_str, smelling like a struct-offset disagreement or
     a stale-pointer read that darwin's nano-malloc layout turns into
     this signature. The mac jobs now upload the crashing binaries
     plus the .ips reports on failure so the frame offsets can be
     symbolicated offline; that artifact is the next actionable input.
   - Phase 2 CI landed: macos-cross-build cross-compiles hello_script
     from linux-x64 to BOTH mac targets with the vendored SDK-less
     toolchain and macos-{arm64,x64}-cross-run execute the artifacts
     on the real mac runners -- the windows build/run pair shape.
     linux-x64 stays the single cross host to avoid a combinatorial
     host-x-target explosion.
   - Issue #266 (drop per-test xvfb-run; GUI tests inherit the suite's
     outer Xvfb) meshes with the mac design: on mac the outer wrapper
     is empty (XVFB_RUN) and raylib talks to Cocoa directly, so GUI
     tests need a window server session rather than any wrapper. It
     also root-causes the build_minesweeper exit=5 flake seen during
     phase 1 (nested Xvfb teardown race). One new mac dependency to
     solve in phase 3: GUI test commands now call `timeout` directly,
     which stock macOS does not ship (GNU coreutils; runner images
     have it via brew, bare macs do not).
   - Darwin crash FINALLY caught on camera (run 29027527473, arm64):
     the .ips symbolicates to abort() via
     BUG_IN_CLIENT_OF_LIBMALLOC_POINTER_BEING_FREED_WAS_NOT_ALLOCATED
     inside Vec__Str_delete(user_argv, 1) at main's cleanup epilogue,
     during a plain `til build src/test_runner.til`. user_argv is
     created once in main and never touched again on the build path,
     so its Vec struct (or data ptr) was clobbered earlier by
     something else: heap corruption whose abort site is the victim,
     not the culprit. The same inputs on linux run valgrind-clean
     (26.4M allocs, 0 errors) and the full linux suite is ASAN-clean,
     so the corrupting write lives in code only executed on mac
     (host_os()=="macos" til branches, __APPLE__ ext.c, darwin libffi,
     Apple clang codegen of boot/til.c). get_bin_dir/__APPLE__ and the
     builder's macos rewrite were audited clean by hand. The next run
     (b5301ec) crashed even earlier -- til_boot itself SIGSEGV'd
     building src/til.til -- and ReportCrash wrote nothing within the
     10s window ("total 0" twice), hence the CI forensics below.
   - CI forensics upgraded to make the mac runners name the culprit:
     (1) both mac suites now run under ASAN (bin/til_asan, same as
     linux `make test` minus gui) so the corrupting write is caught
     in-process with a full report in the log; (2) on failure a Guard
     Malloc probe (DYLD_INSERT_LIBRARIES=libgmalloc) reruns a
     previously-crashing compile with every allocation on its own
     guarded page, catching even writes from uninstrumented code
     (libffi asm) at the culprit instruction; (3) crash-report capture
     hardened (sleep 30, user + system DiagnosticReports dirs).
   - First ASAN-suite run (29050250037) results: (a) macos-x64 ran the
     whole 46-min ASAN suite; its many run-phase failures all trace to
     ASAN aborting AT STARTUP of the compiled test binaries --
     __asan::InitializeFlags() -> Die() -- because asan_child_prefix
     forced ASAN_OPTIONS=detect_leaks=1 and Apple's ASAN runtime has
     no LeakSanitizer. Fixed: detect_leaks=0 on a macos host. (b) The
     compile-phase failures (closures_compile etc.) had their reason
     discarded: test_runner redirected every phase's stderr to
     /dev/null, so ASAN reports never reached any log -- on linux too.
     Fixed: stderr goes to gen/out/*.err and failing tests echo it
     (tail-truncated). (c) The arm64 job died in the build step and
     the Guard Malloc probe crashed in 52s (a catch!), but macOS-26
     .ips files are >5000 lines each and the raw cats buried both the
     probe's report and the build error beyond the log API's tail
     window; artifact download is proxy-blocked from the bot session.
     Fixed: the dump step prints a compact per-report digest
     (procName/exception/frames) instead of raw cats; full reports
     still go to the artifact.
   - Next: read the ASAN/gmalloc culprit digest from the next mac run,
     fix it, iterate until the suite is green on both arches; then the
     gui suite natively on a mac (phase 3) and the `timeout`
     dependency.

*** macOS plan

   Why it is the hard one: unlike Windows there is no mingw equivalent
   for Mach-O. Apple's SDK (libc headers, libSystem .tbd stubs, Cocoa/
   OpenGL frameworks) is not redistributable, so the windows-x64
   recipe -- vendor a cross compiler plus static libs, link on Linux --
   does not transfer. Split the work so the SDK problem only blocks
   the parts that genuinely need the SDK:

**** Phase 1: native macOS host (arm64 first, x64 second)

     Build and test til ON a Mac using GitHub's macOS runners
     (macos-latest is arm64 today; macos-15-intel is x64). No SDK
     problem there: the runner ships Xcode clang. Work items:

     - Makefile portability: -lrt (in RAYLIB_FLAGS, linked into
       til_boot too) and -latomic (NNG_FLAGS) do not exist on macOS;
       gate on uname -s. xvfb-run is Linux-only: mac runners are
       headless, so run the suite without the wrapper and without gui
       tests at first (bin/tests --no-gui -- same reason Linux CI
       needs xvfb at all). -rdynamic/-ldl reportedly work with Apple
       clang; verify.
     - The til_boot cc line uses only -Wall -Wextra -Werror; Apple
       clang needs the -Wno-* extras toolchain_extra_args already adds
       for clang builds (sometimes-uninitialized, self-assign,
       unused-function) -- or the warnings fixed for real.
     - ext.c: gate the /proc RSS walkers (proc_rss_bytes,
       is_process_leader, proc_tree_rss_bytes) on __linux__; on macOS
       they compile as unused static functions and trip
       -Werror=unused-function. The rest is POSIX or already forked
       per platform (_WIN32 / __EMSCRIPTEN__ / __APPLE__ ru_maxrss
       units, host_os); audit for stragglers while there.
     - Vendor builds on the mac runner: libffi (./configure lands in
       vendor/libffi/aarch64-apple-darwin*/.libs), nng (cmake), raylib
       (upstream supports PLATFORM_OS=OSX; links -framework
       Cocoa/IOKit/OpenGL/CoreVideo), tinyfd (plain cc, osascript
       backend).
     - targets.til: fill target_ffi_lib and target_gui_libs for the
       two macos targets (mac .a paths plus framework flags, mirroring
       the -win64 pattern).
     - Un-hardcode the libffi link() path: [[file:../../src/self/interpreter.til][src/self/interpreter.til]]
       pins link("vendor/libffi/x86_64-pc-linux-gnu/.libs/libffi.a");
       the builder already rewrites Linux link flags per target for
       windows/wasm -- extend that to pick the host's libffi dir.
     - .dylib vs .so: interpreter.til formats "lib<name>.so" for
       ffi_load_global_lib, and builder.til emits gen/lib/lib<name>.so
       for mode lib; the macOS shared-lib suffix is .dylib.
     - ASAN: clang ASAN works on macOS but LeakSanitizer does not
       exist on arm64 darwin; make test's --asan run needs a mac
       carve-out for leak checks (or run test_fast there until
       sorted).
     - CI: a macos-latest job running the no-gui suite; add
       macos-15-intel once arm64 is green.

**** Phase 2: SDK-less cross-compilation Linux -> macOS (CLI/server)

     No zig dependency -- replicate the mechanism zig uses internally.
     What zig actually does (zig cc is clang underneath):

     - vendors the darwin libc headers in its own tree
       (lib/libc/include/any-darwin-any/ in ziglang/zig, harvested
       from Apple SDKs by the now-archived
       ziglang/fetch-them-macos-headers tool);
     - vendors lib/libc/darwin/libSystem.tbd, a YAML text stub listing
       libSystem's exported symbols: the linker resolves -lSystem
       against the stub and the real libSystem.dylib binds at runtime
       (stable ABI -- same idea as mingw import libs);
     - links with its own Mach-O linker and ad-hoc code-signs arm64
       output (mandatory on Apple Silicon);
     - compiles compiler-rt builtins from source for the target;
     - bundles no frameworks: gui cross is SDK-bound even for zig.

     Every piece has a freely available non-zig equivalent: clang is
     already a Mach-O cross-compiler on any host, ld64.lld
     (clang -fuse-ld=lld) links Mach-O and ad-hoc-signs arm64, and
     libSystem.tbd is one vendorable text file. The vendored header
     tree is the heavy part -- and precompiled per-target ext objects
     ([[./152.org][#152]], macOS-only; linux/windows targets keep the current
     compile-ext.c-with-the-cross-gcc flow that already works) make it
     mostly unnecessary:

     - Precompile ext-macos-arm64.o / ext-macos-x64.o (plus
       libffi-macos-*.a) on the Phase 1 mac CI job, where the real SDK
       lives, and commit them like libraylib-win64.a. That absorbs
       every system-header need in ext.c (dlfcn, spawn, sysconf, ...).
     - The generated C is nearly freestanding already: gen/til/*.c
       includes only stdio/stdlib/string/stdarg/limits plus ext.h, and
       ext.h includes only stdbool/stdint (clang provides those
       itself, stdarg too). Finish the job -- #152 step 1: route the
       remaining direct libc uses (printf family, fprintf(stderr,...),
       malloc/free/realloc, mem*/str*) through ext.h prototypes or
       til_* wrappers so the generated C compiles with clang's builtin
       headers alone, no Apple headers on the host. One darwin quirk
       forces part of this anyway: Apple's stdio.h does
       #define stderr __stderrp, so raw fprintf(stderr, ...) in
       generated C cannot be hand-prototyped portably -- stderr users
       must go through ext.o.
     - Vendor a libSystem.tbd (checked against zig's copy) and link:
       clang -target arm64-apple-macos11 -fuse-ld=lld -lSystem plus
       the precompiled objects. No crt1.o exists on modern macOS
       (LC_MAIN entry point), so no CRT objects to obtain. If the
       generated C ends up referencing compiler-rt builtins, build the
       few needed ones from llvm compiler-rt (permissive license) --
       Linux clang packages do not ship libclang_rt.osx.a.
     - Host toolchain deps: distro clang + lld, nothing else. Plan B
       if shrinking the generated C's libc surface drags on: vendor
       the darwin libc headers the way zig does (do the license
       diligence at vendoring time; the tree already vendors X11
       headers, so there is precedent).
     - osxcross (user-extracted SDK) stays a documented --cc
       alternative for whoever wants Apple's real toolchain; never a
       dependency.
     - CI: a linux job cross-builds examples/hello_script.til for
       macos-arm64 and macos-x64 (file output grepped for Mach-O),
       uploads artifacts; a macos job downloads and runs them -- the
       exact windows-x64-build / windows-x64-run pattern.

**** Phase 3: the rest of the mac matrix

     - mode gui cross from Linux stays blocked on Apple frameworks
       (the Cocoa/OpenGL .tbd stubs are SDK content). GUI programs for
       macOS are built natively on a Mac (Phase 1 covers that);
       document osxcross plus user-supplied SDK for whoever needs gui
       cross builds.
     - macOS as a host targeting the others: windows-x64 via
       brew mingw-w64; linux-* needs a Linux cross toolchain on the
       mac (Apple clang cannot link glibc ELF without a sysroot --
       homebrew musl cross-gcc for static musl binaries is the simple
       first step, and the Phase 2 trick works in reverse too:
       precompiled ext-linux-*.o plus vendored stubs). Verify on the
       macos-latest CI job once Phase 1 lands.

*** TempleOS

   Unchanged in spirit, now tracked in [[./151.org][#151]]: HolyC codegen backend
   (the Lang.HolyC placeholder exists), TempleOS-native gui primitives
   instead of raylib, QEMU-based test loop. Still the most interesting
   target, still last in line.
