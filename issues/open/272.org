# priority: high
# category: cleanups
** WIP #272 Garbager system: single ownership authority, no manual memory outside core

Architectural cleanup driven by the 2026-06 leak-hunting sessions. The
leaks were fixed point-by-point, but several fixes (and pre-existing
code) violate the intended layering. The principles, as stated by Jorge:

  - ~Dynamic~ is just ~void*~ in C. A Dynamic value does NOT imply a
    malloc'd box; it is only a pointer. Error messages / checks that say
    "raw malloc'd memory typed Dynamic" conflate the two.
  - ~ref~ must never be malloc'd. (There may already be a typer error
    for this; verify and extend coverage.)
  - ~malloc~ / ~free~ / raw pointer surgery belong ONLY in the core
    collections (Str, Array, Vec, Set, Map) and the C bridge. Nothing in
    parser/initer/typer/loader/builder/interpreter/dispatch should call
    them directly.
  - ~delete~ is called manually only from other ~delete~ implementations
    (the destructor tree). Every other delete is the garbager's job.
  - The builder and the interpreter must not invent clone/malloc/delete
    calls; when they do, they paper over garbager gaps and create
    double-free/leak coupling. The garbager is the single authority for
    where a value dies (Mojo-style ASAP, see doc/ownership.org's
    "Relation to Mojo").

*** Known violations / smells to remove (inventory, not exhaustive)

  - ~Expr.replace_with~ (core/ast.til): added to fix the in-place
    rewrite leaks (move() overwrites). It works, but it is a manual-
    memory primitive in disguise. The cleaner shape is rewriting via the
    parent: ~parent.children.set(i, own new_expr)~ (Vec.set already
    deep-deletes the old element). Audit the 6 call sites; most have the
    parent at hand. Sites that rewrite a node they only have a ~ref~ to
    (init_substitute_idents' walk) need the walk to carry the parent.
    Then delete replace_with and the move()-onto-managed typer error can
    point at children.set instead.
  - ~ext_dispatch_ffi~ scalar boxes + boxed_flags array (dispatch.til):
    hand-rolled malloc/flag/free. Works, but the marshaling layer should
    own a typed buffer abstraction instead.
  - Interpreter Value model: make_str_value / shallow_copy_value /
    borrow_value memcpy aliasing; scope_free cannot deep-delete Struct
    payloads because returning a local hands out an UNFLAGGED alias of
    the cell payload (see NOTE in scope_free; deep-delete attempt
    segfaults the compile pipeline). This is the dominant remaining leak
    (make_str_value ~8.8K, eval_call ~5K, widen_numeric ~3.4K on
    strings.til interpret). Fix = give returns a move/borrow contract
    (null the cell on return-of-local, or flag the alias borrowed), then
    scope_free can deep-delete and the manual Enum-only special case
    dies.
  - builder.til / interpreter.til: audit for emitted clone/malloc/delete
    that compensate for garbager behavior (e.g. emit_as_ptr's
    malloc-copy wrap for own+stack_local; the POD-enum clone shortcut
    interactions documented in garbager.til). Each is either a garbager
    gap or dead compensation.
  - free(own p) on a Dynamic local at PROGRAM scope emits invalid C
    (`*_oa = malloc(sizeof(void))` own-box). Builder bug, reproduced
    2026-06-10; works inside proc bodies.

*** Garbager structural weaknesses (the bug class behind the leaks)

  - Liveness/transfer tracking is STATEMENT-INDEX based (decl_index /
    last_use / own_transfer as I32 indexes into one body's children),
    not path-based. Every fixed bug was this model leaking at a path the
    indexes cannot express: conditional transfer (sinking), transfer
    after early exits (this week's fix), hoisted-view aliasing (#238 /
    #250 taint), FieldAccess-base FCalls (never had an index at all
    until hoisted). A per-path (CFG) liveness pass would subsume the
    accumulated special cases. Mojo does ASAP destruction on a real CFG
    with use-def chains; doc/ownership.org has the references.
  - The garbager only sees one body at a time and trusts hoisting to
    surface every owned temp as a named local. Any expression shape the
    hoister misses is invisible (the f().field class). Inverting the
    contract -- garbager walks expressions, not just statements -- would
    remove the hoist coupling.

*** Suggested staging

  1. Inventory pass: grep malloc/free/delete call sites outside core
     collections + C bridge; classify each (legit island / compensation /
     garbager gap) per doc/ownership.org's audit procedure. Record here.
  2. Replace replace_with call sites with parent children.set; drop
     replace_with; retarget the move() typer error message.
  3. Interpreter return move/borrow contract; then deep scope_free; then
     remove free_value/cleanup special cases.
  4. CFG-based liveness in garbager; fold sink_conditional_transfer_
     deletes, early-exit-before-transfer, extend_*_lifetimes into it.
  5. Builder/interpreter compensation removal, guarded by ASAN baseline
     (doc/bots/asan.org) at every step.

*** Progress (step 3, in flight)

  - Step 2 landed: Expr.replace_with removed; all rewrites go through
    the owning slot (children.set / assignment / field assignment).
  - Step 3 reproduced with a clean ASAN debug compiler
    (bin/til_boot build --asan -o tmp/til_dbg src/til.til, then
    ASAN_OPTIONS=detect_leaks=0:abort_on_error=1 tmp/til_dbg interpret
    test/strings.til). With deep scope_free (b.cell.val = val_none()):
    heap-use-after-free, shape:
      - alloc: result = c.return_value.clone() at the FieldAccess-callee
        eval_call return boundary (interpreter.til ~1497) -- an
        INDEPENDENT clone that becomes some local v's cell payload.
      - free: scope_free(call_scope2) of a LATER direct-ident user call
        (interpreter.til ~1714): that call's scope held an UNFLAGGED
        alias of v's payload in one of its cells (param or local),
        so the deep delete freed v's bytes.
      - read: read_field on v.field after the call -> UAF
        (Value_Uint32 / read_field / eval_expr FieldAccess).
    Next: find which arg/local binding path in eval_call (direct-ident
    branch, call_scope2) stores an alias of a caller local's payload
    without borrowed=true or without nulling the source. Suspects: the
    non-Ident arg eval branch (gen 211955), own-arg consumption for
    user calls (ext calls null the source cell, user calls may not),
    reinterpret_ptr_value / widen_numeric struct paths.

*** Step 3 findings, round 6 -- the wall is mapped end to end (2026-06-12)

  - THE DOUBLE-OWNER ROOT CAUSE, probe-proven (full ownership history
    of one box): hoisted Str temp owns it -> Ident-arg binding to a
    Dynamic param duplicates it UNFLAGGED via box_scalar_to_dynamic's
    `case: return shallow_copy_value(v)` pass-through (twice, nested
    calls) -> garbager free(own val) frees one copy -> deep teardown
    of the other re-frees. FIXES THAT WORK: (a) own params MOVE --
    null the caller's cell right after scope_set_owned in the
    Ident-arg Dynamic-box paths; (b) non-own params BORROW -- flag the
    pass-through copy. With (a)+(b): kwargs dynamic tuples asting ALL
    PASS under ASAN deep scope_free.
  - BUT (b) breaks the #211 enum payload-binding write-through (plain
    interp: enums/errors corrupt payload Strs -> suite 152/154), and
    (a) alone breaks 8 files (146/154) while asting/enums/errors UAF
    under ASAN. The payload-alias subsystem (get_payload bindings,
    payload_aliases mirror) needs the same move/borrow contract before
    deep scope_free can land. Neither config is suite-green, so the
    interpreter experiments are REVERTED to master in this commit; the
    fixes are fully described above for clean re-application.
  - Probe recipes that cracked it (reusable): dbg_owned(tag,name,val)
    printing si.data as U64 via an 8-byte malloc slot + write_ptr +
    `ref a : U64 = ptr_add(slot, 0)` (direct to_ptr on payload fields
    overflows); h_free/scope_free address probes; match the ASAN
    report's hex address converted to decimal against the probe log.
  - Workflow: synchronous one-command-at-a-time flow re-verified all
    day (builds + sweeps + suites in-band); bin/til_boot must be
    rebuilt after rebases that change the parser (children[] syntax).

*** Step 3 findings, round 5*** Step 3 findings, round 5 (owner probes, 2026-06-12)

  - Probes added (eprint to stderr): h_free Ident branch prints
    ident + si.data address; scope_free deep delete prints binding
    name + si.data. scope_free is temporarily a proc for eprint.
  - CATCH: the h_free Ident calls in the kwargs run are
    free(own self) / free(own val) from INTERPRETED CORE DELETE
    METHOD BODIES (the `if call_free { free(own self) }` tail and
    Map/Vec internals). self binds BORROWED (caller's cell);
    free_value_full through it frees the caller's payload box and
    nulls the CALLER's cell -- interaction with the deep scope_free
    teardown of the callee/caller scopes double-frees. data=0 lines
    show some frees hit already-nulled values (benign); the UAF
    fires later at teardown.
  - NOTE: the probe itself then crashed reading a freed cell while
    printing (probe-induced, ignore that frame; remove probes before
    committing).
  - Direction: interp h_free must mirror compiled free(own x)
    semantics EXACTLY: box-only release (compiled frees the malloc'd
    wrapper; field destructors already ran inside the delete body).
    free_value (box only) instead of free_value_full for the
    Value.Struct case? -- and the cell-nulling must propagate to the
    OWNING cell (it does: scope_get walks to it). Then deep
    scope_free skips nulled cells naturally. Test against kwargs,
    dynamic, tuples; asting is a separate ext_dispatch_ffi read
    shape (tmp/wall_asting.log).
  - Build notes: bin/til_boot must be rebuilt (make bin/til_boot)
    after rebasing over master's Expr.child removal -- the old boot
    parser cannot read children[0].method() statements.

*** Step 3 findings, round 4*** Step 3 findings, round 4 (synchronous flow restored, 2026-06-12)

  - WORKFLOW: the morning's plain synchronous flow is restored and
    re-verified end to end: bin/til_boot build --asan (build: 0
    in-band) + full 15-file ASAN sweep completed in one synchronous
    command each. The managed-runner experiment is abandoned; its
    enforcement block was removed from block-background-bash.sh.
  - Deep scope_free + h_free FieldAccess slot-zero fix in tree.
    Sweep: PASS maps hashmaps sets aliases first_class regression
    own_fields structs strings ref; FAIL kwargs dynamic tuples asting
    splat (5 left, was 13).
  - kwargs UAF (tmp/wall_kwargs.log): alloc = top-level Str literal
    Decl (make_str_value via program eval_body); freed via h_free's
    IDENT branch (frees + nulls that cell correctly); re-freed by
    scope_free deep delete => a SECOND owned cell shares the payload
    unflagged. Find which binding path duplicated a program-scope
    Str payload into another owned cell without borrow/move (suspect:
    interp arg binding for core calls feeding KwargsMap/Array paths,
    or the Decl bare-Ident move not covering some shape).
  - splat: FALSE FAIL -- test/splat.til does not exist (stale wall
    list). Real remaining: kwargs, dynamic, tuples (one shared shape)
    + asting (distinct, via ext_dispatch_ffi read).
  - The shared shape: h_free's IDENT branch is reached by GARBAGER-
    INSERTED free calls (make_free_call FCalls in the typed AST, e.g.
    the Shallow-field value-copy WRAPPER free from
    insert_post_stmt_deletes). Compiled semantics: free(x) releases
    ONLY the box; the value-copied field owns the contents. Interp
    h_free does free_value_full + nulls the cell -- but a SECOND owner
    of the same si.data box exists (alloc = top-level Str literal
    Decl; re-free = a call scope's deep teardown). Find who the second
    box owner is (shadowed binding? borrowed binding made owned?) --
    probe: print cell identity vs payload address at free time, or
    audit scope_set_borrowed/scope_get paths for the test's shape.
  - Next: read tmp/wall_dynamic.log  - Next: read tmp/wall_dynamic.log, tuples, asting, splat the same
    way; fix each alias at creation; re-sweep synchronously; when all
    PASS: make clean && make test synchronously; then ASAN measure
    (expect the ~17K interpret-mode cell-payload class to drop);
    close #272 (git mv to fixed/, post-mortem) and squash EVERYTHING
    into one commit per Jorge's instruction.

*** Step 3 findings, round 3*** Step 3 findings, round 3 (deep scope_free wall sweep, 2026-06-12)

  - swap_replace move semantics merged (332c559); mut-param assignment
    no longer aliases. Deep scope_free re-applied in the lab tree;
    sweeping the 13 wall files with tmp/til_dbg (clean ASAN build).
  - kwargs UAF diagnosed: interpreted CORE code calls
    `free(own holder.field)`; h_free's non-Ident branch evals the
    FieldAccess into a borrowed copy, frees through it, nulls the COPY
    only -- the FIELD SLOT inside the holder still stores the freed
    pointer, so the holder's deep delete re-frees it. Fix: after
    free_value_full in the non-Ident branch, when the freed expr is a
    FieldAccess, resolve base+offset (same machinery as the
    FieldAssign handler / write_field) and zero the slot.
  - dynamic also FAILS (trace in tmp/wall_dynamic.log, not yet read).
    Sweep results land in tmp/wall_<file>.log; PASS/FAIL list in
    tmp/job.log.

*** Step 3 findings, round 2 (lab state in working tree, NOT committed)

  - Minimal repro: tmp/mp.til (mut-param concat: greet(mut out: Str)
    { out = out.concat(" world") }; caller prints s after). UAF under
    deep scope_free. test/strings.til's test_mut_param_concat is the
    same shape; test/ref.til crashes too (ref-primitive write-through,
    fixed in the lab tree by restricting the interp bare-Ident assign
    move to unflagged Struct/Enum payloads + Dynamic).
  - Probe evidence (eprint_single to stderr -- println/print_flush are
    LOST on ASAN abort, print_flush is putchar not fflush): greet's
    scope_free deep-deletes binding "out" with cell_is_local=TRUE,
    borrowed=false, data non-null. concat's params "a"/"b" are also
    owned+local. But ALL THREE eval_call branches bind Ident args via
    scope_set_borrowed (cell_is_local=false). So some path creates an
    OWNED "out" binding: next probe = binding creation (scope_set_owned
    is called for "out" somewhere -- note scope_set_owned/scope_free
    cannot call eprint_single while funcs; the lab tree made scope_free
    a proc, scope_set_owned cascades into constfolder funcs, so probe the
    CALLERS instead, or check whether constfolder's compile-time folding
    scopes (constfolder.til scope_set_owned sites) are the ones tearing
    down).
  - Suspect to check next: constfolder try_eval_call folds greet at compile
    time? (probe output may mix constfolder-phase scope_frees with runtime
    ones; correlate with the ASAN free trace, which is RUNTIME
    eval_call -> scope_free.)
  - Lab tree changes (uncommitted): deep scope_free via
    b.cell.val = val_none(); bare-Ident assign MOVE for unflagged
    heap payloads (fixes the garbager-move/interp mismatch; keeps #227
    ref write-through working -- ref.til passes); stderr probes in
    scope_free; scope_free is temporarily a proc. tmp/til_dbg =
    bin/til_boot build --asan of the lab tree; rebuild ~1min.
