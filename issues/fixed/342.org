# priority: medium
# category: bugs
** FIXED #342 Restore mixed-splat variadic calls (a `..` splat beside other args in the block)

*** Post-mortem

Restored in three commits, staged because ~FCallData~'s layout is
committed-boot ABI.

1. PREPARATION -- record the position. ~splat_args~, a ~U64~ bitmap indexed
   like ~own_args~ / ~ref_args~ and appended at the END of ~FCallData~ so
   the committed boot's layout stays stable. ~is_splat~ says only THAT some
   argument carried ~..~; this says WHICH, which is the fact the lowering
   was missing.

2. The helper. Of the issue's two candidate lowerings, the concat route won
   over a runtime-capacity collector plus a hand-built copy loop, because
   ~desugar_one_variadic_fcall~ runs inside ~infer_body~ and its emitted
   nodes are never re-inferred -- a loop built there must carry correct
   til_types, throwing-call err slots and ownership by hand, while
   ~Array.concat~ written in til is typed and lowered by the ordinary
   pipeline. concat clones every element exactly as ~clone~ does, which
   settles question 3: nothing is moved out of either source, so no double
   free and no shredded elements, and a splatted array stays reusable.

3. The wiring. ~desugar_mixed_splat_variadic_call~ walks the block in source
   order: each run of consecutive non-splat arguments becomes its own
   collector Array (the ordinary machinery, filled from a SUBSET -- which is
   why ~build_variadic_array_set~ now takes an absolute source index and a
   separate destination slot instead of assuming a contiguous block), each
   splatted Array is taken as is, and the pieces fold left to right with
   concat. The call is then handed the single combined Array, i.e. exactly
   the pure-splat shape the desugar already knew how to pass. The typer
   stopgap is gone.

Question 4 (the comptime path) needed nothing: the constfolder folds
all-comptime variadic calls while they are still raw, and a splat of a
runtime array is not comptime, so it never reaches that path.

Tests: test_mixed_splat / _multiple / _source_intact in test/variadic.til
(splat first, last, middle; two splats; a splatted array reused afterwards),
both backends and ASAN-clean. The ~sp_mixed~ rejection fixture is removed
from test/typer_errors.til (count 184 -> 183) and ~mv_variadic_callee~ is
back to the direct ~format(mv_head, ..mv_rest)~ the stopgap had forced into
a nested workaround.

*** Follow-up: the three blocks the fix above did not reach

The lowering was right; what was wrong was WHERE it was consulted. Three
call shapes still produced the original garbage -- silently, since the
stopgap that would have rejected them was gone -- and a fourth had never
gone through any splat path at all. All four are one commit.

~splat_args~ is indexed by CHILD POSITION, so every pass that inserts,
drops or reorders call children has to renumber it exactly as it
renumbers ~own_args~. Two did not, and each left the bit naming the
argument beside the splat, so the splatted Array was wrapped as one
element again (the first of the two was fixed on master in parallel --
see below):

- UFCS inserts the receiver at position 1 (~infer~'s method-call
  rewrite). It shifted ~own_args~ and left ~splat_args~ on the parsed
  positions, so ~h.join("x", ..a)~ was garbage while the written-out
  ~Holder.join(h, "x", ..a)~ was correct. This one landed on master
  independently while the work below was in flight: #326's
  ~shift_fcall_bitmap_for_ufcs~ shifts every child-indexed bitmap,
  ~splat_args~ among them, which is the same shift generalized. Only the
  regression test for it comes from here.
- Monomorphizing drops the ~Type~ argument (~generic_rewrite_call~ in
  initer.til for the explicit application, ~generic_rewrite_call_in_place~
  in typer.til for the deduced one). It carried ~is_splat~ and the own
  bits across but not ~splat_args~, so ~gjoin(Str, "x", ..a)~ quietly lost
  an element. (Deduced type arguments still reject any splat, pure or
  mixed, with the pre-existing "pass it explicitly" error -- unchanged
  here.)
- The dyn-dispatch rewrite builds a wholly different ~dyn_call~; its
  ~splat_args~ is now cleared with ~own_args~ rather than left pointing at
  the new argument list.

~get_splat_arg~ joins ~set_splat_arg~ in parser.til so these read the
bitmap the way ~get_own_arg~ / ~get_ref_arg~ are read, instead of the
desugar's own copy of the bit test.

The fourth: ~array()~ and ~vec()~ are themselves declared variadic
(~elements: ..Dynamic~), so their element list IS a variadic block -- but
the collection-literal desugar predates the splat paths and indexed it as
contiguous elements. ~array(I64, 1, ..xs, 9)~ and ~vec(...)~ alike stored
the splatted Array as ONE element, in every position: literal in a
declaration, literal as a call argument, splat alone in the block.
~build_splat_folded_array~ (the fold factored out of
~desugar_mixed_splat_variadic_call~) now serves both, and two things the
variadic path never had to face turned up here:

- A block that is nothing but one splat folds to a single piece, so there
  is no concat to do the cloning. An Ident there is clone-wrapped, the way
  the pure-splat path wraps its argument.
- A Vec literal has to end at a Vec. ~Array.to_vec~ (src/core/array.til)
  converts, cloning every element like ~concat~ does; the desugar folds to
  an Array and calls it. Pushing element by element instead would have
  meant a hand-built loop over a runtime length in a pass whose emitted
  nodes are never re-inferred -- the same reason ~concat~ won the first
  time. It is a method on Array rather than a ~Vec.extend~ taking an Array
  because the dependency then runs Array -> Vec: the compiler instantiates
  ~Vec__T~ for over a thousand types and ~Array__T~ for a few dozen.

Two traps worth recording, both found by crashing:

- ~_va_Array_~ is not a free name. The constfolder reads that prefix as
  "an Array.new collector, interpret its build region" (~is_va_array_decl~
  ignores the RHS shape entirely), so naming the Vec path's folded temp
  that way made it interpret a concat chain in the fold scope, where the
  hoisted temp of an inner concat is unbound. The temp uses ~_array_~,
  the collection literals' own prefix.
- A local moved on with ~own~ into a call is deleted again at the end of
  the enclosing body unless the transfer leaves that body, so the fold is
  handed to ~bind_folded_array_literal~ / ~bind_folded_vec_literal~ as an
  ~own~ parameter rather than bound and moved in the caller.

Tests: test_mixed_splat_ufcs / _generic in test/variadic.til, and
test_to_vec / _empty, test_array_literal_splat, test_vec_literal_splat,
test_collection_literal_splat_in_arg in test/arrays.til.

*** Follow-up 2: what a splat says about `..T`, and where the bitmap ends

Two more, both the same confusion as the original -- a splat read as ONE
element instead of the run it stands for.

1. Type-argument DEDUCTION. With the type argument omitted, evidence for
   `..T` came off the splatted argument's own type, so ~gcount(..xs)~
   deduced T = ~Array__Str~ and then demanded an ~Array__Array__Str~
   instance ("pass it explicitly"), while ~gcount("x", ..xs)~ reported
   ~Str~ and ~Array__Str~ as conflicting evidence. A splat's evidence is
   its ELEMENT type: ~variadic_array_elem_type_name~ (the inverse of
   ~variadic_array_type_name~) decodes it, applied only inside the
   variadic run, which is the only place a splat can appear. Genuine
   mismatches still conflict, and now say ~Str~ and ~I64~ rather than
   ~Str~ and ~Array__I64~.

2. The bitmaps END at child 63. ~own_args~ / ~ref_args~ / ~splat_args~ /
   ~keep_outer_args~ are ~U64~s indexed by child position, so the shift
   that builds a bit for position 64 overflows to zero and the set is a
   silent no-op. A ~..~ there reached the lowering as an ordinary element
   and the callee read the Array's header bytes -- the original garbage,
   reappearing purely because of WHERE the argument sat. Verified exactly:
   child 63 is the last that works, child 64 is garbage. The parser now
   refuses a source-written ~own~ / ~ref~ / ~..~ past ~FCALL_ARG_BITS~
   rather than drop it; long calls without qualifiers, and a splat early
   in a long call, are untouched. Widening the bitmaps would be an
   ~FCallData~ layout change, i.e. the staged migration this issue already
   did once -- worth doing if the limit is ever hit in earnest.

Found while testing (1) and NOT fixed here, since it has nothing to do
with splats: applying one generic both explicitly and by deduction in a
single unit synthesizes the monomorphization TWICE, and the C compiler
rejects the duplicate definition. It reproduces with plain runtime
arguments and no splat anywhere (~gc(Str, s)~ beside ~gc(t)~), so it
predates this work; the deduction fix merely adds a splat call as one more
way to reach it. The two dedup routes are ~ctx.generic_func_synths~ keyed
~path#mono~ at init time and the late instantiation in
~generic_finish_fcall~. test/variadic.til's deduced tests use their own
generic to stay clear of it.

Tests: test_splat_deduced_type_arg in test/variadic.til, and the
splat_arg_limit fixture (test/splat_arg_limit_error.til).

A ~..~ splat passed alongside other arguments in the same variadic
block is currently a type error:

    arr := array(Str, "a", "b")
    format("<", ..arr)
    // error: a '..' splat must be the only argument in the variadic
    // block for 'parts'; splat one array holding every element, or
    // combine them in a nested call

The intended meaning is obvious and used to hold when the internals
were different: the splat expands the array's elements in place among
the other variadic args, so ~format("<", ..arr)~ prints ~<ab~. The
rejection is a stopgap, not the semantics we want. Before it landed,
the call COMPILED and produced garbage -- the splatted Array's own
header bytes read back as the element type, varying run to run:

    format("<", ..arr)     -->  <\xa0""       (expected <ab)
    format(..arr, ">")     -->  \xc0"">

No error, no crash, wrong output, on both backends alike (the lowering
runs in the shared desugar, so interpretation and compilation
corrupted identically).

*** Why the current internals cannot express it

Three facts combine:

- ~is_splat~ is ONE Bool on ~FCallData~, set by the parser
  (~set_fcall_splat~) when ANY argument carries ~..~. WHICH argument
  carried it is recorded nowhere.
- The variadic lowering (~desugar_one_variadic_fcall~) wraps the
  call's variadic block into a collector Array: ~Array.new(cap)~ with
  a compile-time capacity, one throwing ~Array.set~ per element, then
  the block is replaced by the collector
  (~rewrite_variadic_fcall_args~). Every element is assumed to BE one
  element.
- The only splat-aware path, ~desugar_pure_splat_variadic_call~, can
  recover which arg is the splat only when the block holds exactly one
  argument (~vc == 1~): then "the splat" is unambiguous, and the
  splatted array is passed through directly (clone-wrapped when it is
  an Ident, so the caller's array is not moved out from under it).

With a sibling inside the block, ~vc > 1~, the pure-splat path
declines, the ordinary path wraps the splatted Array as a single
ELEMENT of the collector, and the callee reads the Array header's
bytes as its element type.

Note the boundary is the variadic BLOCK, not the argument list --
which is why the corpus never caught it. test/kwargs.til's
~combo_sum("x", ..arr, bias = 100)~ always worked: ~"x"~ binds a fixed
param and ~bias~ is kwargs, so ~..arr~ sits alone in its block and
takes the pure-splat path. Only a sibling INSIDE the block broke, and
no test had one.

(In-repo git history is truncated, so the era where this worked is not
reachable for archaeology; within the reachable history the ~vc == 1~
limitation is present throughout.)

*** What restoring it needs (solution to be decided)

1. Record the splat's position. DONE (2026-08-03), as the preparation
   commit this staging calls for: ~splat_args~ is a ~U64~ bitmap on
   ~FCallData~ indexed like ~own_args~ / ~ref_args~, appended at the END
   so the committed boot's layout stays stable (the same placement
   ~ref_args~ documents). The parser sets it in the same branch that
   sets ~is_splat~, via ~set_splat_arg~ beside ~set_ref_arg~. Nothing
   READS it yet -- that is step 2 -- so behaviour is unchanged and the
   stopgap error still stands; ~is_splat~ can become the derived
   ~splat_args != 0~ once the reader lands.

2. Lower a block whose total length is not compile-time known. Today's
   collector is ~Array.new(<literal cap>)~ + one ~set~ per element.
   With a splat among the elements the capacity is
   ~fixed_count + splatted.len()~, a runtime expression, and the
   splatted elements need a copy loop instead of one ~set~. Candidate
   shapes:
   - teach ~build_variadic_array_decl~ a runtime capacity and emit a
     per-splat element-copy loop next to the ~set~ calls; or
   - have the desugar do what the current error message tells the user
     to do by hand: build the combined array first (concat helper),
     then feed the call the pure-splat form it already handles.
     CHOSEN, and its helper has landed (2026-08-03): ~Array.concat~
     (src/core/array.til) returns a fresh Array of self's elements then
     other's, cloning each so both sources stay intact -- the same
     element discipline ~clone~ uses, which answers question 3 for this
     route (every element is copied, nothing is moved out of the
     caller's array, so no double free). Written in til, so it types and
     lowers normally: the desugar never hand-builds a loop, which the
     runtime-capacity alternative would have required in a pass whose
     emitted nodes are not re-inferred. Tests: test_concat /
     test_concat_empty in test/arrays.til, ASAN-clean.

     STILL TO DO -- the wiring: in ~desugar_one_variadic_fcall~, when the
     block is mixed (~is_splat~ and ~vc > 1~), walk the block in source
     order grouping consecutive non-splat args into their own collector
     Array and taking each splatted Array as-is, fold the groups with
     ~concat~, and hand the call the single-splat form. The existing
     ~build_variadic_array_decl~ / ~build_variadic_array_set~ index off
     ~fcall.children[vi + j]~ and so assume the block is contiguous
     elements; they need a subset-aware variant. Then remove the typer
     stopgap and flip ~sp_mixed~ per "Where the stopgap lives" below.

3. Define ownership for the mixed form. The pure-splat path
   clone-wraps an Ident splat because moving it would zero the
   caller's array; an element-wise copy has the same question per
   element (clone vs move-out for owned temporaries), and the
   collector's elements are MOVED into it by ~Array.set~ -- see the
   borrow-clone note in ~build_variadic_array_set~. Whatever lowering
   is chosen must not double-free the source array or shred its
   elements.

4. Check the comptime path too: the constfolder folds all-comptime
   variadic calls while they are still RAW (pre-lowering, via eval),
   so mixed splats at compile time need the same defined semantics as
   the lowered runtime form.

*** Where the stopgap lives

The rejection is in the typer's ~infer_and_validate_fcall_args_for_fdef~
(it has ~fvc~ and ~is_splat~ in hand there). The regression fixture is
~sp_mixed~ in test/typer_errors.til. When the feature is restored:
remove the check, flip that fixture into a positive test of the mixed
form, and ~mv_variadic_callee~'s body (same file) can return to the
direct ~format(mv_head, ..mv_rest)~ it originally had before the
rejection forced the nested workaround.
