# priority: high
# category: cleanups
** FIXED #165 Declaration order should not matter inside a file or across imports

*** Post-mortem

    The old analysis predated the declaration seed pass (#271), Context-owned
    layouts (#147), and the graph-wide field/declaration/body typer sweeps
    (#338). Those changes had already removed the original ~USize~ ordering
    failure and made ordinary fields wait until every file's aliases were
    registered.

    One order-dependent path remained. Enum constructors and derived methods
    were generated in ~init_declarations_unit~, while recursively visiting an
    imported file, before the importing file registered its aliases. An enum
    payload naming such an alias was consequently treated as an unresolved
    owned aggregate: its constructor had the wrong ownership, its delete body
    tried to call a nonexistent namespace, and its C storage retained an
    undefined type name.

    Enum generation now runs in the typer's graph-wide method-preparation
    sweep, after initialization has completed for the entire import graph but
    before field inference (which needs enum namespace constants such as
    ~Type.Unknown~). At that barrier, ordinary enum payload aliases are
    canonicalized in the existing
    ~VariantDef.payload_type~ slot before layout, niche selection, method
    generation, interpretation, and codegen consume it. ~USize~ and ~UPtr~ keep
    their target-independent interface spelling.

    Regression: ~test_alias_from_importer~ in ~test/forward_refs.til~, with its
    imported enum in ~test/imports/forward_alias_enum.til~, passes in both the
    interpreter and compiled modes and exercises generated construction, ~eq~,
    ~delete~, and ~to_str~ through an importer-declared alias.

    The detailed text below is retained as historical analysis; several of its
    architectural TODOs were completed by the later issues named above.

    The same typer sweep now owns interface box/lifecycle/default expansion
    and all enum constructors and generated methods. The REPL uses the same
    methods -> fields -> declarations ordering. No type method generation runs
    from init; the clean full suite passed 414/414 before integration.

    The final follow-up removed ~init_declarations_global~. After method
    generation moved to the typer, function/value registration was already
    performed by ~init_declarations_unit~. The normal loader now runs only the
    narrow AST-definition pointer refresh needed after unit initialization can
    grow an AST; the REPL delta path uses the unit registration directly.

    The narrow refresh exposed a separate defect:
    ~init_refresh_seeded_scope_defs~ can run before private-name mangling and
    mistake another unit's same-named public binding for a seed of the private
    declaration, replacing the public FuncDef pointer. Unmangled source-private
    declarations are now skipped until they own their canonical mangled name.
    ~test_circular_private_public_registration~ covers opposite signatures on
    the public and private functions across an import cycle.

    Re-analysis while updating #174 identified two additional order-sensitive
    method-generation paths not exercised by the alias regression. Issue
    [[./380.org][#380]] fixed both: nested generated-method capability now
    follows field types recursively, and field-only inference can use an
    eligible generated method's provisional return signature before strict
    body-sweep validation. These paths were separate from both the fixed
    importer-alias failure and #174's resolved method-body/import-cycle
    failure.

   Symptom (historical, kept for context):

   [[file:../../src/core/core.til][src/core/core.til]] contained the alias ~USize := U32~ wedged
   between two groups of ~import("...")~ calls. Moving that single
   line either to the top of the file (before the imports) or to the
   bottom (after every import) broke the build. The only "safe"
   position was right after ~import("u32.til")~ and before
   ~import("i64.til")~. That was forward-declaration discipline by
   another name. til is supposed to have an init pass precisely so
   source order does not matter.

   Why this happened (historical analysis):

   - The loader pre-extracts ~import("...")~ calls from each file's AST
     (see ~extract_imports~ in [[file:../../src/self/loader.til:97][src/self/loader.til:97]]) and builds an
     import list per file.
   - ~init_file~ ([[file:../../src/self/initer.til:2319][src/self/initer.til:2319]]) recurses into the dep list
     first, then runs ~init_declarations_unit~ on the importing file.
     The dep list is iterated in source order.
   - Sibling imports inside the same file are therefore order-sensitive:
     ~core.til~ relies on ~u32.til~ being init'd before any later code
     that mentions ~U32~. The ~USize := U32~ alias only works because it
     sits between ~import("u32.til")~ and the next group; if it moves
     above ~u32.til~, ~scope.find("U32")~ in
     ~register_type_alias_for_stmt~ ([[file:../../src/self/initer.til:1097][src/self/initer.til:1097]]) returns
     ~ScopeFind.NotFound~ and the alias is silently dropped (or produces a
     downstream type error). If it moves below, then ~i64.til~ /
     ~range.til~ / ~bool.til~ / ~mem.til~ / ... are init'd before USize is
     in scope, so any of their decls that reference USize through derived
     methods (size methods, ~for~ desugaring against ~Vec.len()~, etc.)
      resolve USize as ~Type.Unknown~ at init time.
   - Even within a single file, ~init_declarations_unit~ does the AST walk
     in source order: ~register_struct_def_for_stmt~ runs for struct decls
     immediately during the walk, but alias decls (~NodeType.Ident~ on the
     RHS) are bucketed and processed only after the walk. That asymmetry
     means a struct that mentions an alias defined later in the same file
     sees ~Unknown~ during ~register_struct_def_for_stmt~ /
     ~gen_struct_clone_delete_for_stmt~ / ~compute_struct_layout~ -- those
     all read types via ~type_from_name_init~ before any alias bucket
     drains.

   Root cause in one line:

   ~initer.til~ does three jobs at once -- declaration indexing, struct
   layout computation, and clone/delete/size/eq/to_str method synthesis --
   and the last two need scope-resolved types, which forces the first
   into a load-bearing source order. The init pass is supposed to be
   pure name indexing; everything that needs to know "what is the type of
   X?" belongs in the typer.

   Architectural goal:

   - Make declaration order inside a single file and across imports
     irrelevant by moving every type-resolving job out of initer and
     into typer, in line with [[./158.org][#158]] (one pass per phase). Initer keeps a
     single AST walk per file that registers names by kind (struct,
      enum, func, alias, value); typer owns coordinated graph-wide field,
      declaration, and body sweeps that perform type-aware work with full
      scope. These are internal typer traversals, not new pipeline phases.

   How later phases see auto-generated methods:

   - The field sweep first stamps inferred field types. The declaration
     sweep then computes layouts and pushes missing struct methods into
     the type AST's ~ns_decls~ / ~ns_index~. The body sweep registers and
     types those namespace signatures and bodies like hand-written
     members. Method lookup follows the receiver type through
     ~scope.get_struct~ to that namespace index; there is no separate flat
     ~Type.method~ scope binding. ~Context.struct_layouts~ is the cross-phase
     layout memo, matching the pattern [[./147.org][#147]] calls out.

   How rstil informs the design (~../rstil/~):

   - rstil's ~init.rs:init_context~ walks each file's body once and, on
     hitting ~import("path")~, recurses via
     ~init_import_declarations~ at that point in the walk. Imports and
     local declarations interleave in source order; ~imports_init_done~
     handles cycles and re-imports. There is no separately pre-computed
     import list driving a different order than the AST itself.
   - rstil's ~preinit~ phase synthesizes ~clone~ / ~delete~ as a pure
     AST-to-AST rewrite using only field shape (mut?, primitive name?,
     FuncSig?). til does not need this as a separate pipeline phase:
     the typer's internal declaration sweep knows field types and is
     the natural home.
   - In rstil, init never asks "what is the type of X?". That is what
     makes USize position irrelevant. til adopts the same phase boundary.

   Plan (incremental, smallest landable steps first; each lands green):

   1. [MOSTLY DONE] Strip every ~type_from_name_init~ call out of
      ~initer.til~. ~register_struct_def_for_stmt~,
      ~register_enum_def_for_stmt~, ~register_type_alias_for_stmt~,
      ~register_function_def_for_stmt~ and
      ~register_top_level_value_for_stmt~ should record names and
      shapes only. The alias case stores the target name as a Str on
      the binding without resolving it; the typer dereferences on use.
      After this step, initer is one walk per file with no scope-typed
      reads.

      Done in c4b8064: ~register_type_alias_for_stmt~ no longer
      silently drops on NotFound for builtin-primitive targets.
      Stores ~alias_target~ + ~is_type_alias = true~ when
      ~til_type_from_explicit_type(target)~ returns a non-Custom
      Type (so ~USize := U32~ works wherever it is in the file).
      ce0a584 moved ~USize := U32~ to the top of core.til to validate
      end-to-end.

      Done in c5fa48a: ~register_function_def_for_stmt~ and
      ~infer_top_level_decl_type~ now use the scope-free
      ~til_type_from_explicit_type~ for function return types and
      explicit type annotations. To make ~Custom(name)~ written by
      these helpers compare equal to ~Struct(name)~/~Enum(name)~
      written elsewhere in the typer:

      - ast.til adds a custom ~Type.eq~ that treats Custom <->
        Struct/Enum as equal when names match. Uses ~.is()~ throughout
        (tag-only) because switch-on-enum desugars to recursive eq.
      - typer.til ~type_prefix~ gets a ~Custom(s)~ case that returns
        the name (matching Struct/Enum behavior).
      - typer.til ~normalize_scope_bindings~ walks the global scope
        once after init and rewrites any binding with type
        ~Custom(name)~ to ~Struct(name)~ or ~Enum(name)~ when scope
        resolves the name. Loader calls it between
        ~init_declarations_global~ and the first ~type_file~.

      Did NOT land: NotFound-permissive registration for user-defined
      type aliases. The CamelCase-vs-snake_case heuristic to
      distinguish type aliases from value aliases was rejected -- til
      intentionally has no compiler-level case sensitivity, and
      ALL_CAPS value constants like ~NOTE_ROW_Y : U64 = GAP~ break
      any first-letter heuristic. The Found arm in
      ~register_type_alias~ already handles user-defined type aliases
      when the target file is init'd before the alias file.

   2. [DONE] Move ~gen_struct_clone_delete_for_stmt~,
      ~gen_enum_nonclone_methods_for_stmt~,
      ~gen_missing_enum_clone_for_stmt~,
      ~gen_struct_size_method_for_stmt~,
      ~gen_enum_size_method_for_stmt~, ~gen_cmp_derived_for_stmt~ and
      ~gen_unity_derived_for_stmt~ into the typer's struct/enum visit.
      The typer pushes synthesized methods into ~ns_decls~ / ~ns_index~;
      the body sweep then registers and types them as ordinary namespace
      members, so subsequent phases see no difference from user-written
      methods.

      Landed through #338: a graph-wide field sweep stamps field
      types, a graph-wide declaration sweep computes layouts and
      synthesizes struct clone/delete, size, cmp and unity methods,
      and only then does the graph-wide body sweep type-check method
      bodies. This is option (b) below and fixes #174. Enum
      nonclone/clone generation and interface clone/delete/default
      expansion subsequently moved into the typer's pre-field sweep.

      Re-analysis after #174 found two additional source-order holes, fixed
      by [[./380.org][#380]]:

      - ~struct_field_supports_generated_method~ recognizes a nested
        struct's cmp/eq/to_str only if that method is already in the nested
        struct's namespace. Since ~type_decls_file~ generates in source
        order, a parent declared before its child can miss those methods
        permanently. Capability checking/materialization must be
        dependency-aware rather than visit-order-dependent.
      - ~type_fields_file~ infers an unannotated field default by running
        expression inference before ~type_decls_file~ has generated any
        methods. A default that calls an auto-generated method therefore
        still depends on whether some earlier phase happened to create it.
        Field type inference must not require declaration-sweep products,
        or the required signatures must be made available first.

      First attempt (pre [[./162.org][#162]]): moved scope-free
      ~gen_cmp_derived_for_stmt~ and ~gen_unity_derived_for_stmt~ into
      ~infer_decl_stmt~ before ~infer_expr~ walks ns_decls. Failed with
      ~no method 'lt' for type 'U32'~ on ~ast.til:431~ because
      ~ast.til~ was typer-visited before ~u32.til~. Diagnosed as a
      cross-file ordering problem and parked behind ~[[./162.org][#162]]~.

      Second attempt (post [[./162.org][#162]]): same change, expecting per-file
      imports to topo-order the typer's recursion. New failures:

      #+begin_example
      ./src/core/vec.til:25:22: error: no method 'eq' for type 'Str'
      ./src/core/vec.til:49:71: error: no method 'eq' for type 'Str'
      ./src/core/str.til:55:18: error: no method 'neq' for type 'I64'
      ./src/core/str.til:254:18: error: no method 'lt' for type 'I64'
      #+end_example

      Diagnosis: ~vec.til~ has a ~Str elem_type~ field and calls
      ~Str.eq~ in ~Vec.append~; ~str.til~'s ~Str.split~ returns ~Vec~
      and uses I64-derived methods. Both directions are in
      ~ctx.imports_typer_done~ as soon as ~type_file~ enters them, but
      ~type_check_unit~ on the second visitor runs while the first is
      still mid-recursion -- so the first's auto-generated methods
      don't exist yet when the second's bodies type-check.

      This is a real cycle, not an ordering bug. Per-file imports
      don't solve it; rstil avoids it by *design* -- rstil's ~Vec~
      stores a ~Ptr~ with ~elem_size~ + ~elem_type~ as a C string
      pointer, never a ~Str~. til's ~Vec~ stores ~Str elem_type~,
      which forces the cycle.

      Three real options for unblocking step 2:

      a) Restructure til's collection types (~Vec~, ~Map~, ~Set~,
         ~Tuple~, ~DynMap~) to match rstil's ~Ptr~-based design. Big
         work; touches every container.
      b) Two-sweep typer: first sweep across all files visits
         struct/enum decls only and synthesizes methods; second sweep
         type-checks function bodies. Same cyclic Vec/Str shape, but
         the methods exist by the time any body is checked. Adds a
         walk inside the typer driver -- arguably still "one pass per
         phase" since both sub-walks are typer work.
      c) Lazy on-demand synthesis: when the typer encounters
         ~X.method()~ and ~method~ is missing from ~X~'s ~ns_decls~,
         eagerly call the relevant ~gen_*~ for ~X~'s struct decl.
         Closest to rstil's spirit (synthesis localized at use site)
         but adds bookkeeping for "is this struct already swept?".

      Option (b) landed. Enum/interface preparation, struct
      clone/delete, and the scope-free cmp/unity/size generators now
      all run inside the typer.

      Sub-finding from the attempts: ~gen_cmp_derived_for_stmt~ and
      ~gen_unity_derived_for_stmt~ are scope-free pure AST transforms
      (they read only the struct's own ~ns_decls~). The actual
      scope-using prize for step 2 was ~gen_struct_clone_delete~ and
      ~gen_enum_nonclone_methods~. Both now run in typer-owned sweeps.

   3. [DONE] Replace ~compute_struct_layout~ + ~compute_enum_layout~ with a
      ~Context.struct_layouts~ map populated by the typer on first
      visit (subsumes ~[[./147.org][#147]]~ steps 3-4: ~Declaration.field_offset~,
      ~field_size~, ~field_struct_def~ and
      ~StructDefData.total_struct_size~ all migrate to that Context
      side table). Layout computation now runs from the typer's
      declaration sweep and all later phases read the Context tables.

   4. [DONE] Drop ~init_declarations_global~ entirely. Layout and method
      generation live in typer sweeps; function/value registration already
      happens in ~init_declarations_unit~. The loader retains only the narrow
      AST-definition pointer refresh required after initialization grows a
      unit AST.

   5. [DONE] Replace ~extract_imports~ + the pre-built ~iu.imports~
      list with rstil-style in-walk recursion. ~init_file~ becomes
      ~init_body~: walk the AST in source order, and on
      ~import("...")~ FCalls recurse through ~ctx.imports_init_done~.
      Removes the second source of ordering (sibling import order
      driven by extract_imports rather than the AST itself) and is a
      precondition for circular imports (see ~[[./163.org][#163]]~).

      Loader discovery, ~init_file~, and all typer sweeps now resolve each
      ~import("...")~ call directly from its owning AST using per-unit base and
      library roots plus the Context cwd. Namespace-import declarations retain
      their FCall as a bare dependency statement after alias rewriting. Mode
      auto-imports remain the one explicit edge field because no source call
      represents them. ~ProgramUnit.imports~, ~ImportUnit.imports~,
      ~extract_imports~, and the positional count/index machinery are gone.

   6. [DONE] Alphabetize [[file:../../src/core/core.til][src/core/core.til]]'s imports and put
      ~USize := U32~ wherever (its own file, top of core.til,
      anywhere). Add regression tests covering both directions: (a)
      two files where the importer declares ~MyAlias := U32~ and an
      importee struct uses ~MyAlias~ in a field type; (b) USize-style
      alias declared above every ~import("...")~ in the importing
      file. Both must build.

      Done:
      - [[file:../../src/core/core.til][src/core/core.til]] alphabetized; it is now a pure import
        list with no top-level decls.
      - ~USize := U32~ moved into its own [[file:../../src/core/usize.til][src/core/usize.til]].
      - Scenario (b) regression tests landed in
        test/forward_refs.til as ~test_alias_top_of_file~ and
        ~test_alias_bottom_of_file~ (covers builtin-primitive alias
        at top of a file and after the struct that uses it).
      - Scenario (a) landed as ~test_alias_from_importer~, with the imported
        enum in ~test/imports/forward_alias_enum.til~. It exercises the
        importer-declared alias through generated construction, eq, delete,
        and to_str in interpreted and compiled modes.

      Discovery: alphabetizing was a no-op on the build because init
      registers every name before the graph-wide typer sweeps run.
      Per-file imports ([[./162.org][#162]]) and the separate field,
      declaration, and body sweeps supplied the graph-wide barriers needed
      to move methodgen; the nested-method eligibility ordering hole remains
      in step 2 above.

   Validation:

   - After each step, ~make clean && make test~ must pass.
   - The regression tests in step 6 must continue to pass.

   Related issues:

   - ~[[./158.org][#158]]~ One pass per phase across the whole pipeline -- this issue
     is the forcing function for initer: collapse three jobs into one
     walk by moving the type-dependent two into typer.
   - ~[[./147.org][#147]]~ AST/Context redesign: lessons from rstil -- step 3 is
     literally that issue's steps 3-4 (~Context.struct_layouts~).
     Step 1's "store alias target as Str, resolve in typer" mirrors
     that issue's ~explicit_type -> til_type + struct_name~ split.
   - ~[[./163.org][#163]]~ Per-file AST/import graph + ~Context.path~ -- step 5 reuses
     that infrastructure and removes the last excuse to keep the
     pre-extracted import list.
   - ~[[./162.org][#162]]~ Core files should import semantic dependencies explicitly
     -- FIXED. Per-file imports for every [[file:../../src/core/*.til][src/core/*.til]] now declare
      the dependency edges that ~type_file~ traverses. Acyclic dependencies
      are visited first; an import cycle is cut by the sweep memo and relies
      on the graph-wide barrier before the next sweep starts. This enabled
      the struct half of step 2 to move without hitting the ~ast.til~->U32
      wall; enum/interface generation remains.
   - ~[[../open/116.org][#116]]~ Imported files are parsed in isolation -- same root-cause
     family (cross-file visibility vs. phase ordering).
