# priority: low
# category: features
** FIXED #114 Source docs + repl help/introspection

* Post-mortem
  - Root cause: this early umbrella issue stayed open after its implementation
    moved into the more detailed issues #157 and #233, so its progress section
    no longer described the documentation system on master.
  - Fix: #157 added ~///~ declaration docs, AST storage, qualified REPL
    ~help()~, field/member docs, and ~til doc~. #233 completed the shared doc
    catalog, generated Org and searchable HTML output, metadata, links,
    navigation, and doctests. No second documentation path is needed.
  - Regression coverage: parser doc-comment tests,
    ~verify_doc_html_body_renderer~, generated doc index assertions,
    ~write_doc_doctests~, and ~test/repl_help.til~.

Add source-level documentation that can later be surfaced from the REPL and
  tooling. The long-term goal is that once ~[[./10.org][#10]]~ exists, users can ask for:

  - ~help("Vec")~
  - ~help("Vec.get")~
  - ~help("Str.replace")~

  and get the doc text directly from the declarations, not from a separate
  manual.

  This should follow the code, not drift away from it. Think of a small,
  language-native equivalent of:

  - Doxygen/Javadoc-style declaration docs
  - Python docstrings / `help(...)`

  The documentation model should cover at least:

  - types (`struct`, `enum`, `namespace`)
  - functions / procs / methods
  - fields when useful
  - modules or imported files if we later want file-level help

  Suggested shape:

  - parser keeps doc comments attached to the next declaration
  - AST stores the docs on declarations / definitions
  - builder / bindings can emit doc metadata if needed
  - REPL `help(...)` and tooling read from that metadata

  This is separate from current org docs like `doc/pipeline.org`.
  Those are still useful, but they are not a substitute for docs attached to
  the source and available interactively.

   Depends conceptually on ~[[./10.org][#10]]~, but the syntax/storage side can be designed
   and implemented before the REPL command itself.

   Final state:

   - ~///~ doc comments are attached to declarations, fields, enum variants,
     and namespace members.
   - [[file:../../src/std/doc.til][src/std/doc.til]] provides REPL ~help(name)~ using the same generated
     source documentation consumed by tooling.
   - ~til doc~ emits Org and searchable HTML under ~doc/gen/~ from the shared
     typed-AST catalog.
   - Normal ~interpret/build/run/test/translate~ pipelines deliberately do not
     populate the REPL registry or carry documentation into compiled programs.
