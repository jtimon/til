# priority: medium
# category: features
** FIXED #233 Improve til documentation system (Python/Elixir model: markup, metadata, doctests, hiding, HTML)

*   Post-mortem:
    - Root cause: documentation features had accumulated as separate partial paths. The HTML renderer understood only paragraphs, links, and source blocks; doctest discovery was hard-coded to ~src/core/str.til~; generated pages had search but no catalog navigation or version context.
    - Fix: completed org-style HTML rendering for headings, lists, inline code/verbatim, example blocks, language-tagged source blocks, paragraphs, and links; generated responsive navigation from the existing ~DocCatalog~; stamped org/HTML output with the existing compiler version; and generalized test-only doctest discovery across tracked documented source roots with source locations.
    - Regression coverage: ~verify_doc_html_body_renderer~, generated index output assertions, the synthetic doctest extraction regression in ~write_doc_doctests~, ~test/repl_help.til~, and the full interpreted/compiled suite.

*   Current state:
    - /// doc comments support org-style metadata lines: ~#+doc: false~, ~#+group:~, ~#+since:~, and ~#+deprecated:~.
    - Packed blobs for runtime ~help()~ (linear lookup), loaded from generated org docs for core declarations and rebuilt from live REPL declarations when user docs change.
    - ~til doc~ -> org-mode and searchable static HTML output under ~doc/gen/~, mirroring source tree, with stable ~:CUSTOM_ID:~ anchors, grouped navigation, compiler versioning, resolved ~[[til:...]]~ links, and org-style block/inline rendering.
    - ~make test~ extracts ~#+begin_src til :doctest~ blocks from tracked documented source roots and runs them through interpreted and compiled test mode.

*   Proposed vision (Python/Elixir-inspired):
    - Adopt explicit org-mode doc contract metadata in comments.
    - Org markup in docs (verbatim, lists, headings, links).
    - Rich metadata: ~#+since:~, ~#+deprecated:~, ~#+group:~.
    - Internals hiding (~#+doc: false~).
    - Doctests verified by ~make test~.
    - Structured interactive introspection through REPL ~help()~.
    - Generate searchable HTML with nav sidebar + grouping + versioning.

*   Completion checklist:
    - [X] Org metadata, hiding, stable anchors, cross-references, and grouped index.
    - [X] Searchable HTML and REPL ~help()~ backed by generated org docs.
    - [X] Render headings, lists, inline verbatim/code, example blocks, source blocks, paragraphs, and links in HTML.
    - [X] Generate responsive catalog navigation with groups, source units, declaration anchors, and current-page highlighting.
    - [X] Show the existing compiler version in generated org and HTML output.
    - [X] Discover doctests across tracked documented source roots with source locations and no public CLI.
    - [X] Pass focused doc/REPL checks and the full interpreted/compiled suite.

*   Progress:
    - First slice landed org-style doc metadata parsing/rendering and runtime filtering.
    - Second slice explored pre-generated escaped core ~help()~ blobs under ~doc/~; this was superseded by loading the visible generated org docs directly.
    - Third slice generated ~doc/gen/index.org~ as the first navigation layer over public docs, grouped by ~#+group:~.
    - Fourth slice factored ~til doc~ through a reusable in-memory catalog so org pages, the index, and later HTML/search output share one collected doc model.
    - Fifth slice added stable org anchors and default groups for the generated docs index.
    - Sixth slice resolved ~[[til:Name]]~ doc-comment links through the catalog into generated org links.
    - Seventh slice emitted static HTML pages and an HTML index from the same doc catalog.
    - Eighth slice emitted ~doc/gen/search.js~ and wired a client-side search box into the generated HTML.
    - Ninth slice rendered org source blocks in HTML and added a test-suite-only doctest MVP.
    - Tenth slice removed the ad-hoc ~.esc~ REPL doc cache files. ~help()~ lazily packs core docs from ~doc/gen/*.org~, reuses them in the session, and refreshes user docs only after declarations change.
    - Final slice completed org-style HTML rendering, responsive catalog navigation, versioned output, and repository-wide test-only doctest discovery.

*   Notes:
    - Issue #223's auto-shallow exclusion for ext_*/ext_proc is correct:
      ~shallow~ on ext functions is part of the C ABI contract, not
      redundant noise.  See issues/fixed/223.org for concrete examples
      (U32_clone crash, spawn_cmd heap pointer).

*   Architectural Learnings (from Python/Sphinx and Elixir/ExDoc):
    - *Cross-Referencing*: Deterministic object IDs based on qualified names (for example, ~domain:module.class.func~) eliminate the need for manual link mapping. Sphinx domains and ExDoc nodes heavily utilize this.
    - *Docstring Parsing*: Docstrings are treated as markup and parsed dynamically. ExDoc compiles an intermediate HTML AST (~ExDoc.DocAST~) in memory before writing to disk, allowing metadata injection (anchors, cross-refs).
    - *Build Integration*: Python/Sphinx imports modules at build time (~autodoc~), uses ~inspect~ to read ~__doc__~, and parses as reST. Elixir/ExDoc embeds docs in compiled ~.beam~ files as EEP-48 ~Docs~ chunks; at runtime, ~Code.fetch_docs/1~ loads them.
    - *Output Formatting*: Separation of concerns. Sphinx uses Jinja2 templates; ExDoc uses EEx templates. Both walk a format-agnostic internal AST.
    - *Search/Navigation*: ExDoc dumps an inverted index of all documentation into a JSON blob embedded in Javascript for instant client-side search. Navigation sidebars are data-driven from module groupings.
    - *Doctests*: Elixir dynamically extracts docs via ~Code.fetch_docs/1~, parses lines starting with ~iex>~, executes them, and asserts the output matches the following lines. Handled by the test runner (~ExUnit.DocTest~), not the doc generator itself.
