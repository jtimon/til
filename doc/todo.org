* DONE Interpreter/codegen consistency
  The interpreter now heap-allocates all values (I64, Bool, Str, U8) just like
  codegen. free() semantics are identical in both modes.

* DONE Eliminate manual free(_wcond) in while-true transform
  insert_free_calls now handles NODE_BREAK: direct-child breaks get frees like
  NODE_RETURN, and nested breaks inside if/else branches get frees via
  insert_break_frees helper. Manual free removed from while-true transform.
