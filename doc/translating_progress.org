* C to til translation -- remaining work

** builder.c -- FULLY TRANSLATED AND REMOVED

** Remaining translations -- interpreter.c (15 functions in C)

*** Done (translated to interpreter.til)
- free_value, needs_widen, widen_numeric (with shallow_copy_value via memcpy)
- scope_get, interp_fa_is_ns
- str_view (memcpy from StructInstance.data + CAP_VIEW)
- val_none, val_i64, val_u8, val_i16, val_i32, val_u32, val_u64, val_f32, val_bool
- val_enum_flat, val_enum_simple
- clone_struct_instance, ns_get, ns_set, ns_qname, find_field_decl, enum_has_payloads
- cached_str/array/vec_def/name globals (moved from C to til)
- shallow_copy_value helper (memcpy-based bitwise Value copy)

*** Leaves (no deps on other untranslated C functions)
- make_str_value(data, len)           line 223 -- builds Str StructInstance, uses write_ptr + ptr_add
- make_str_value_own(data, len)       line 240 -- same pattern, no strndup
  Translation attempted, write_ptr works but result had garbage c_str.
  Bug: StructInstance constructor in til may not preserve data field correctly.
  Needs debugging.
- scope_new(parent)                   line 112 -- malloc + Map_new + field init
  Failed: til rejects ref-cast from malloc'd Dynamic. Needs investigation.
- scope_free(s)                       line 119 -- iterates bindings, frees cells, Map_delete
  Failed: double free from destructor overlap. Needs careful ownership handling.
- scope_set_owned(s, name, val)       line 134 -- Map_has/get + malloc Cell + Map_set
  Failed: segfault from Binding constructor + memcpy. Needs investigation.
- scope_set_borrowed(s, name, cell)   line 147 -- malloc Binding + Map_set (same pattern)
- read_field(inst, fdecl)             line 167 -- reads Value from StructInstance.data at offset
  Leaf: only calls val_* (done) and enum_has_payloads (done). Large (80 lines).
- write_field(inst, fdecl, val)       line 248 -- writes Value into StructInstance.data at offset
  Leaf: calls Value_clone (done). Large (53 lines).

*** Depend on make_str_value
- parse_cli_arg(type_name, str_val)   line 1186 -- make_str_value + strtoll etc.
- value_to_buf(dest, v, type_name)    line 1244 -- raw memcpy of Value fields to buffer
- build_argv_array(argv, ...)         line 1265 -- parse_cli_arg

*** Core interpreter loop (mutually recursive -- must translate together)
- eval_call(s, e) -> Value            line 414 (~305 lines)
- eval_expr(s, e) -> Value            line 719 (~133 lines)
- eval_body(s, body)                  line 852 (~298 lines)
Depend on: scope_set_owned, scope_set_borrowed, widen_numeric (done),
read_field, write_field, ext_function_dispatch, enum_method_dispatch,
make_str_value, interp_fa_is_ns (done)

*** Top-level (depend on eval_* core)
- interpreter_init_ns(global, prog)   line 1150 -- eval_expr
- interpret(program, ...)             line 1301 -- eval_body, ffi_init, scope_*,
  parse_cli_arg, build_argv_array

** Remaining translations -- dispatch.c (15 functions in C)

*** Done (translated to dispatch.til)
- ffi_type_pointer_ref .. ffi_type_float_ref (8 ref accessors)
- ffi_dlsym, ffi_init_link_libs, ffi_init_user_so, ffi_init_struct_defs
- ffi_cleanup, ffi_close_user_so, ffi_open_user_so, ffi_load_global_lib
- value_to_i64, value_to_u64, value_to_f32, eval_arg, val_to_ptr
- h_Bool_and/or/not, h_U8/I16/I32/F32/U32_from_i64
- h_print_single, h_print_flush, h_exit
- h_readfile, h_writefile, h_spawn_cmd
- h_dyn_has_method, h_dyn_fn, h_check_cmd_status
- h_sleep, h_file_mtime, h_clock_ms, h_get_thread_count, h_cfile_open
- shallow_ffi_type, field_ffi_type, build_struct_ffi_type

*** Depend on eval_expr/eval_call (translate after interpreter core)
- h_free(s, e, r)                    line 55
- h_dyn_call(s, e, r)                line 80
- get_elem_size(s, name, src)        line 128
- h_array(s, e, r)                   line 167
- h_vec(s, e, r)                     line 223
- h_ptr_add(s, e, r)                 line 282
- h_cfile_close(s, e, r)             line 294
- h_cfile_write_str(s, e, r)         line 302
- h_cfile_read_all(s, e, r)          line 312
- enum_method_dispatch(...)           line 587

*** Function pointer dispatch (keep in C or rewrite as if/else chain)
- dispatch_init()                     line 323 -- stores C function pointers via REG macro
- ext_dispatch_builtin(...)           line 390 -- calls function pointer from map
- ext_dispatch_ffi(...)               line 398 -- ffi_call/FFI_FN/FFI_DEFAULT_ABI macros
- ext_function_dispatch(...)          line 580 -- depends on above three

*** FFI registration chain
- ffi_register(name, fn, fdef)       line 634 -- ffi_prep_cif call
- ffi_init_scan_program(program)     line 675 -- ffi_register + ffi_dlsym + AST traversal
- ffi_init(program, ...)             line 733 -- ffi_init_scan_program + til funcs

** Remaining translations -- precomp.c (2 functions)

- try_eval_call                       line 12  -- depends on eval_expr
- precomp                             line 62  -- depends on ffi_init, interpreter_init_ns,
  scope_new, scope_free

** Translation order

Best next targets (leaves, no untranslated C deps):
1. read_field -- large but leaf, only calls val_* and enum_has_payloads (all done)
2. write_field -- leaf, calls Value_clone (done)
3. make_str_value / make_str_value_own -- debug the c_str pointer bug
4. scope_new/free/set_owned/set_borrowed -- debug the allocation patterns

After those, the interpreter core (eval_call/eval_expr/eval_body) can be
translated as a unit, which unblocks everything else.
