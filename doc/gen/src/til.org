#+TITLE: src/til.til
#+OPTIONS: toc:t

* BASE_VERSION
:PROPERTIES:
:CUSTOM_ID: til.src.til.BASE_VERSION
:group: CLI
:END:

#+begin_src til
BASE_VERSION is a Str
#+end_src

* get_version
:PROPERTIES:
:CUSTOM_ID: til.src.til.get_version
:group: CLI
:END:

#+begin_src til
macro get_version() returns shallow Str
#+end_src

* VERSION
:PROPERTIES:
:CUSTOM_ID: til.src.til.VERSION
:group: CLI
:END:

#+begin_src til
VERSION is a Str
#+end_src

* get_bin_dir
:PROPERTIES:
:CUSTOM_ID: til.src.til.get_bin_dir
:group: CLI
:END:

#+begin_src til
ext_proc get_bin_dir() returns Str
#+end_src

* get_cwd_str
:PROPERTIES:
:CUSTOM_ID: til.src.til.get_cwd_str
:group: CLI
:END:

#+begin_src til
ext_proc get_cwd_str() returns Str
#+end_src

* repl_read_line
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_read_line
:group: CLI
:END:

#+begin_src til
proc repl_read_line(mode_name: Str) returns Str
#+end_src

* repl_typecheck
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_typecheck
:group: CLI
:END:

#+begin_src til
proc repl_typecheck(mut lp: LoadedProgram) returns shallow I32
#+end_src

* repl_capture_eval_current
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_capture_eval_current
:group: CLI
:END:

#+begin_src til
proc repl_capture_eval_current(mut session: InterpSession, mut lp: LoadedProgram, out_path: Str, shallow block_start_line: U32, shallow eval_doc_line: Bool) returns Str
#+end_src

* repl_source_line_count
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_source_line_count
:group: CLI
:END:

#+begin_src til
func repl_source_line_count(s: Str) returns shallow U32
#+end_src

* repl_parse_allows_wrap
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_parse_allows_wrap
:group: CLI
:END:

#+begin_src til
func repl_parse_allows_wrap(peek_ast: Expr) returns shallow Bool
#+end_src

* repl_ast_has_help_call
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_ast_has_help_call
:group: CLI
:END:

#+begin_src til
func repl_ast_has_help_call(e: Expr) returns shallow Bool
#+end_src

* repl_ast_updates_doc_cache
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_ast_updates_doc_cache
:group: CLI
:END:

#+begin_src til
func repl_ast_updates_doc_cache(e: Expr) returns shallow Bool
#+end_src

* repl_refresh_user_doc_cache
:PROPERTIES:
:CUSTOM_ID: til.src.til.repl_refresh_user_doc_cache
:group: CLI
:END:

#+begin_src til
proc repl_refresh_user_doc_cache(mode_str: Str, accum: Str, tmp_path: Str, bin_dir: Str, cwd: Str, ext_c_path: Str, extra_modes: Vec__Str, mut user_info_cache: Str, mut user_docs_cache: Str)
#+end_src

* run_repl_session
:PROPERTIES:
:CUSTOM_ID: til.src.til.run_repl_session
:group: CLI
:END:

#+begin_src til
proc run_repl_session(mode_name_in: Str, mut next_mode_out: Str, extra_modes: Vec__Str)
#+end_src

* run_repl
:PROPERTIES:
:CUSTOM_ID: til.src.til.run_repl
:group: CLI
:END:

#+begin_src til
proc run_repl(initial_mode: Str, extra_modes: Vec__Str)
#+end_src

* usage
:PROPERTIES:
:CUSTOM_ID: til.src.til.usage
:group: CLI
:END:

#+begin_src til
proc usage()
#+end_src

* CliArgs
:PROPERTIES:
:CUSTOM_ID: til.src.til.CliArgs
:group: CLI
:END:

#+begin_src til
struct CliArgs {
    mut command : Str
    mut path : Str
    mut custom_bin : Str
    mut custom_c : Str
    mut target_str : Str
    mut cc : Str
    mut install_prefix : Str
    mut path_idx : U32
    mut early_return : Bool
    mut asan : Bool
    mut prof : Bool
    own mut extra_modes
namespace:
    func clone(self: CliArgs) returns CliArgs
    func delete(own self: CliArgs, shallow call_free: Bool)
    func size() returns shallow U32
}
#+end_src

* parse_args
:PROPERTIES:
:CUSTOM_ID: til.src.til.parse_args
:group: CLI
:END:

#+begin_src til
proc parse_args(args: Array__Str) returns CliArgs
#+end_src

* collect_user_argv
:PROPERTIES:
:CUSTOM_ID: til.src.til.collect_user_argv
:group: CLI
:END:

#+begin_src til
func collect_user_argv(mut lp: LoadedProgram, args: Array__Str, shallow start_idx: U32) returns Vec__Str
#+end_src

* main
:PROPERTIES:
:CUSTOM_ID: til.src.til.main
:group: CLI
:END:

#+begin_src til
proc main(args: ..Str)
#+end_src
