#+TITLE: src/core/u8.til
#+OPTIONS: toc:t

* U8
Unsigned 8-bit integer.

#+begin_src til
struct U8 {
namespace:
    ext_func to_i64(shallow val: U8) returns shallow I64
    ext_func to_u64(shallow val: U8) returns shallow U64
    ext_func to_i32(shallow val: U8) returns shallow I32
    ext_func to_u32(shallow val: U8) returns shallow U32
    ext_func to_usize(shallow val: U8) returns shallow U32
    ext_func to_f32(shallow val: U8) returns shallow F32
    /// Convert to decimal string representation.
    func to_str(val: U8) returns Str
    ext_func add(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func sub(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func mul(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func div(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func mod(shallow a: U8, shallow b: U8) returns shallow U8
    func inc(mut self: U8)
    func dec(mut self: U8)
    /// Return the multiplicative identity (1) for this type.
    func unity(_self: U8) returns U8
    ext_func eq(shallow a: U8, shallow b: U8) returns shallow Bool
    ext_func cmp(shallow a: U8, shallow b: U8) returns shallow I64
    ext_func and(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func or(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func xor(shallow a: U8, shallow b: U8) returns shallow U8
    ext_func clone(val: U8) returns shallow U8
    /// Absolute difference between two values, useful for Range iteration.
    func distance(a: Dynamic, b: Dynamic) returns shallow U64
    func delete(own self: U8, call_free: Bool)
    func size() returns U32
    func lt(a: U8, b: U8) returns shallow Bool
    func gt(a: U8, b: U8) returns shallow Bool
    func neq(a: U8, b: U8) returns shallow Bool
    func lte(a: U8, b: U8) returns shallow Bool
    func gte(a: U8, b: U8) returns shallow Bool
}
#+end_src

