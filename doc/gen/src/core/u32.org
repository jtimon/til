#+TITLE: src/core/u32.til
#+OPTIONS: toc:t

* U32
Unsigned 32-bit integer.

#+begin_src til
struct U32 {
namespace:
    ext_func to_i64(shallow val: U32) returns shallow I64
    ext_func to_u64(shallow val: U32) returns shallow U64
    ext_func to_i32(shallow val: U32) returns shallow I32
    ext_func to_f32(shallow val: U32) returns shallow F32
    /// Convert to decimal string representation.
    func to_str(val: U32) returns Str
    ext_func add(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func sub(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func mul(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func div(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func mod(shallow a: U32, shallow b: U32) returns shallow U32
    func inc(mut self: U32)
    func dec(mut self: U32)
    /// Return the multiplicative identity (1) for this type.
    func unity(_self: U32) returns U32
    ext_func eq(shallow a: U32, shallow b: U32) returns shallow Bool
    ext_func cmp(shallow a: U32, shallow b: U32) returns shallow I64
    ext_func and(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func or(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func xor(shallow a: U32, shallow b: U32) returns shallow U32
    ext_func clone(val: U32) returns shallow U32
    /// Absolute difference between two values, useful for Range iteration.
    func distance(a: Dynamic, b: Dynamic) returns shallow U64
    func delete(own self: U32, call_free: Bool)
    func size() returns U32
    func lt(a: U32, b: U32) returns shallow Bool
    func gt(a: U32, b: U32) returns shallow Bool
    func neq(a: U32, b: U32) returns shallow Bool
    func lte(a: U32, b: U32) returns shallow Bool
    func gte(a: U32, b: U32) returns shallow Bool
}
#+end_src

