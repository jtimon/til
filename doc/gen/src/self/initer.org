#+TITLE: src/self/initer.til
#+OPTIONS: toc:t

* register_struct_def_for_stmt
#+begin_src til
proc register_struct_def_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope) returns shallow I32
#+end_src

* gen_struct_clone_delete_for_stmt
#+begin_src til
func gen_struct_clone_delete_for_stmt(stmt: Expr, scope: TypeScope, ctx: Context)
#+end_src

* register_enum_definition
#+begin_src til
proc register_enum_definition(ctx: Context, stmt: Expr, scope: TypeScope) returns shallow I32
#+end_src

* collect_enum_variants
#+begin_src til
func collect_enum_variants(enum_def: Expr, mut variant_names: Vec, mut variant_types: Vec, mut has_payloads: Bool)
#+end_src

* generate_enum_eq_method
#+begin_src til
func generate_enum_eq_method(enum_def: Expr, ename: Str, shallow line: U32, shallow col: U32, variant_names: Vec, variant_types: Vec, scope: TypeScope)
#+end_src

* generate_enum_delete_method
#+begin_src til
func generate_enum_delete_method(enum_def: Expr, ename: Str, shallow line: U32, shallow col: U32, variant_names: Vec, variant_types: Vec, shallow has_payloads: Bool, scope: TypeScope)
#+end_src

* generate_enum_to_str_method
#+begin_src til
func generate_enum_to_str_method(enum_def: Expr, ename: Str, shallow line: U32, shallow col: U32, variant_names: Vec, variant_types: Vec, shallow has_payloads: Bool, scope: TypeScope)
#+end_src

* generate_enum_clone_method
#+begin_src til
func generate_enum_clone_method(enum_def: Expr, ename: Str, shallow line: U32, shallow col: U32, variant_names: Vec, variant_types: Vec, shallow has_payloads: Bool, scope: TypeScope)
#+end_src

* register_enum_def_for_stmt
#+begin_src til
proc register_enum_def_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope) returns shallow I32
#+end_src

* gen_enum_nonclone_methods_for_stmt
#+begin_src til
func gen_enum_nonclone_methods_for_stmt(stmt: Expr, scope: TypeScope)
#+end_src

* register_enum_and_gen_nonclone_methods_for_stmt
#+begin_src til
proc register_enum_and_gen_nonclone_methods_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope) returns shallow I32
#+end_src

* register_enums_and_generate_nonclone_methods
#+begin_src til
proc register_enums_and_generate_nonclone_methods(ctx: Context, program: Expr, scope: TypeScope) returns shallow I32
#+end_src

* gen_missing_enum_clone_for_stmt
#+begin_src til
func gen_missing_enum_clone_for_stmt(stmt: Expr, scope: TypeScope)
#+end_src

* generate_missing_enum_clones
#+begin_src til
func generate_missing_enum_clones(program: Expr, scope: TypeScope)
#+end_src

* register_funcsig_alias_for_stmt
#+begin_src til
func register_funcsig_alias_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope)
#+end_src

* register_funcsig_aliases
#+begin_src til
func register_funcsig_aliases(ctx: Context, program: Expr, scope: TypeScope)
#+end_src

* register_type_alias_for_stmt
#+begin_src til
func register_type_alias_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope)
#+end_src

* register_type_aliases
#+begin_src til
func register_type_aliases(ctx: Context, program: Expr, scope: TypeScope)
#+end_src

* register_top_level_value_for_stmt
#+begin_src til
func register_top_level_value_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope)
#+end_src

* register_top_level_values
#+begin_src til
func register_top_level_values(ctx: Context, program: Expr, scope: TypeScope)
#+end_src

* register_function_def_for_stmt
#+begin_src til
func register_function_def_for_stmt(ctx: Context, stmt: Expr, scope: TypeScope)
#+end_src

* register_function_definitions
#+begin_src til
func register_function_definitions(ctx: Context, program: Expr, scope: TypeScope)
#+end_src

* gen_struct_size_method_for_stmt
#+begin_src til
func gen_struct_size_method_for_stmt(stmt: Expr, ctx: Context)
#+end_src

* gen_enum_size_method_for_stmt
#+begin_src til
proc gen_enum_size_method_for_stmt(stmt: Expr, scope: TypeScope, ctx: Context)
#+end_src

* gen_unity_derived_for_stmt
#+begin_src til
func gen_unity_derived_for_stmt(stmt: Expr)
#+end_src

* gen_cmp_derived_for_stmt
#+begin_src til
func gen_cmp_derived_for_stmt(stmt: Expr)
#+end_src

* compute_struct_layout
#+begin_src til
proc compute_struct_layout(name: Str, mut struct_def: Expr, scope: TypeScope, ctx: Context)
#+end_src

* type_size_align
#+begin_src til
proc type_size_align(ftype: Str, scope: TypeScope, ctx: Context, mut sz: I32, mut al: I32)
#+end_src

* compute_enum_layout
#+begin_src til
proc compute_enum_layout(name: Str, enum_def: Expr, scope: TypeScope, ctx: Context) returns shallow I32
#+end_src

* ctx_total_size
#+begin_src til
func ctx_total_size(name: Str, ctx: Context) returns shallow I32
#+end_src

* ctx_field_layout
#+begin_src til
func ctx_field_layout(sname: Str, field_name: Str, ctx: Context) returns FieldLayout
#+end_src

* infer_top_level_decl_type
#+begin_src til
func infer_top_level_decl_type(stmt: Expr, scope: TypeScope, mut out_type: Type) returns shallow Bool
#+end_src

* init_declarations
#+begin_src til
proc init_declarations(ctx: Context, program: Expr, scope: TypeScope) returns shallow I32
#+end_src

* init_declarations_unit
#+begin_src til
proc init_declarations_unit(path: Str, program: Expr, scope: TypeScope, ctx: Context) returns shallow I32
#+end_src

* init_declarations_global
#+begin_src til
func init_declarations_global(ctx: Context, program: Expr, scope: TypeScope) returns shallow I32
#+end_src

* count_ast_imports
#+begin_src til
func count_ast_imports(body: Expr) returns shallow U32
#+end_src

* init_file
#+begin_src til
proc init_file(path: Str, ctx: Context) returns shallow I32
#+end_src

* enum_variant_is_payload_less
#+begin_src til
func enum_variant_is_payload_less(enum_def: Expr, variant_name: Str) returns shallow Bool
#+end_src

* clone_call_type_name
#+begin_src til
func clone_call_type_name(e: Expr) returns Str
#+end_src

* is_pod_enum_clone_of
#+begin_src til
func is_pod_enum_clone_of(e: Expr, edef: Expr) returns shallow Bool
#+end_src

