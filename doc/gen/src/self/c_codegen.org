#+TITLE: src/self/c_codegen.til
#+SUBTITLE: til 0.0.1-1b5c7faf9
#+OPTIONS: toc:t

* emit_funcsig_typedefs_prog
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.emit_funcsig_typedefs_prog
:group: Compiler backend
:END:

#+begin_src til
proc emit_funcsig_typedefs_prog(f: File, prog: Expr, mut ctx: Context)
#+end_src

* build_forward_header_lp
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.build_forward_header_lp
:group: Compiler backend
:END:

#+begin_src til
proc build_forward_header_lp(fwd_path: Str, mut lp: LoadedProgram) returns shallow I32
#+end_src

* builder_prepare_emit
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.builder_prepare_emit
:group: Compiler backend
:END:

#+begin_src til
proc builder_prepare_emit(mut lp: LoadedProgram)
#+end_src

* c_codegen_translate_outputs
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.c_codegen_translate_outputs
:group: Compiler backend
:END:

#+begin_src til
proc c_codegen_translate_outputs(mut lp: LoadedProgram, c_path: Str, fwd_path: Str, h_path: Str, til_path: Str, py_path: Str, lib_name: Str, shallow do_lib: Bool) returns shallow I32
#+end_src

* Map__Sym_Str
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Map__Sym_Str
:group: Compiler backend
:END:

#+begin_src til
Map__Sym_Str is a None
#+end_src

* Set__Sym
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__Sym
:group: Compiler backend
:END:

#+begin_src til
Set__Sym is a None
#+end_src

* Vec__DynCallInfo
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__DynCallInfo
:group: Compiler backend
:END:

#+begin_src til
Vec__DynCallInfo is a None
#+end_src

* Vec__CollectionInfo
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__CollectionInfo
:group: Compiler backend
:END:

#+begin_src til
Vec__CollectionInfo is a None
#+end_src

* Set__USize
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize
:group: Compiler backend
:END:

#+begin_src til
struct Set__USize {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Set__USize
    func with_capacity(shallow n: USize) returns Set__USize
    func len(self: Set__USize) returns shallow USize
    func clear(mut self: Set__USize)
    func has(self: Set__USize, shallow val: USize) returns shallow Bool
    func add(mut self: Set__USize, own val: USize)
    func remove(mut self: Set__USize, shallow val: USize) returns shallow Bool
    func delete(own self: Set__USize, shallow call_free: Bool)
    func clone(self: Set__USize) returns Set__USize
    func size() returns shallow USize
}
#+end_src

* Set__USize.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize.new
:group: Compiler backend
:END:

Create an empty Set. T must implement ~cmp~. Allocates nothing:
like Vec.new, an empty Set is lazy (cap 0) and ~add~ seeds the
first slot. Short-lived empty Sets are common -- the garbager
builds four per statement for its per-statement name facts, most
of them never added to.

#+begin_src til
func Set__USize.new() returns Set__USize
#+end_src

* Set__USize.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize.with_capacity
:group: Compiler backend
:END:

Create an empty Set pre-sized for ~n~ elements, so adding up to
~n~ reallocs zero times (Vec.with_capacity's counterpart). Use it
where the final size is known or well predicted; ~new()~ grows
0->1->2->4, which is the right default but costs a realloc per
doubling. n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Set__USize.with_capacity(shallow n: USize) returns Set__USize
#+end_src

* Set__USize.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize.clear
:group: Compiler backend
:END:

Drop all live elements but keep the allocated storage so the Set can be reused.

#+begin_src til
func Set__USize.clear(mut self: Set__USize)
#+end_src

* Set__USize.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize.has
:group: Compiler backend
:END:

True if the set contains an element equal to ~val~.

#+begin_src til
func Set__USize.has(self: Set__USize, shallow val: USize) returns shallow Bool
#+end_src

* Set__USize.add
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize.add
:group: Compiler backend
:END:

Insert ~val~ into the set (sorted order). If an equal element
already exists, ~val~ is dropped. ~val~ is owned.

#+begin_src til
func Set__USize.add(mut self: Set__USize, own val: USize)
#+end_src

* Set__USize.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Set__USize.remove
:group: Compiler backend
:END:

Remove the element equal to ~val~, dropping it. Returns false if
absent (Map.remove's counterpart: ~add~ is the Set's ~set~, so
this is its removal half). Later elements shift left to keep the
storage sorted and dense.

#+begin_src til
func Set__USize.remove(mut self: Set__USize, shallow val: USize) returns shallow Bool
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo
:group: Compiler backend
:END:

#+begin_src til
struct Vec__priv___src_self_c_codegen_til__CollectionInfo {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__priv___src_self_c_codegen_til__CollectionInfo
    func with_capacity(shallow n: USize) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
    func len(self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns shallow USize
    func clear(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo)
    func delete_marked(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, marks: Dynamic)
    func push(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, own val: priv___src_self_c_codegen_til__CollectionInfo)
    func append(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, own other: Vec__priv___src_self_c_codegen_til__CollectionInfo)
    func take(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
    func move_to(own self: Vec__priv___src_self_c_codegen_til__CollectionInfo, mut dest: Vec__priv___src_self_c_codegen_til__CollectionInfo)
    func move_from(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, own other: Vec__priv___src_self_c_codegen_til__CollectionInfo)
    func unsafe_get(self: Vec__priv___src_self_c_codegen_til__CollectionInfo, i: USize) returns ref priv___src_self_c_codegen_til__CollectionInfo
    func get(self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_c_codegen_til__CollectionInfo
    func pop(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns priv___src_self_c_codegen_til__CollectionInfo
    func remove_at(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow n: USize) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
    func truncate(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow n: USize)
    func unsafe_set(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__CollectionInfo)
    func set(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__CollectionInfo, mut _err_kind: I64)
    func push_take(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, mut src: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize)
    func delete(own self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow call_free: Bool)
    func clone(self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
    func size() returns shallow USize
}
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.new
:group: Compiler backend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.new() returns Vec__priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.with_capacity
:group: Compiler backend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.with_capacity(shallow n: USize) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.clear
:group: Compiler backend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.clear(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.delete_marked
:group: Compiler backend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.delete_marked(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, marks: Dynamic)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.push
:group: Compiler backend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.push(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, own val: priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.append
:group: Compiler backend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.append(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, own other: Vec__priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.take
:group: Compiler backend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.take(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.move_to
:group: Compiler backend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.move_to(own self: Vec__priv___src_self_c_codegen_til__CollectionInfo, mut dest: Vec__priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.move_from
:group: Compiler backend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.move_from(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, own other: Vec__priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.unsafe_get
:group: Compiler backend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.unsafe_get(self: Vec__priv___src_self_c_codegen_til__CollectionInfo, i: USize) returns ref priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.get
:group: Compiler backend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.get(self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.pop
:group: Compiler backend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.pop(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.remove_at
:group: Compiler backend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.remove_at(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.take_prefix
:group: Compiler backend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.take_prefix(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow n: USize) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.truncate
:group: Compiler backend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.truncate(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow n: USize)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.unsafe_set
:group: Compiler backend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.unsafe_set(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.set
:group: Compiler backend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.set(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__CollectionInfo, mut _err_kind: I64)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.push_take
:group: Compiler backend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.push_take(mut self: Vec__priv___src_self_c_codegen_til__CollectionInfo, mut src: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow i: USize)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.delete
:group: Compiler backend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.delete(own self: Vec__priv___src_self_c_codegen_til__CollectionInfo, shallow call_free: Bool)
#+end_src

* Vec__priv___src_self_c_codegen_til__CollectionInfo.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__CollectionInfo.clone
:group: Compiler backend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__CollectionInfo.clone(self: Vec__priv___src_self_c_codegen_til__CollectionInfo) returns Vec__priv___src_self_c_codegen_til__CollectionInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo
:group: Compiler backend
:END:

#+begin_src til
struct Vec__priv___src_self_c_codegen_til__DynCallInfo {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__priv___src_self_c_codegen_til__DynCallInfo
    func with_capacity(shallow n: USize) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
    func len(self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns shallow USize
    func clear(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo)
    func delete_marked(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, marks: Dynamic)
    func push(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, own val: priv___src_self_c_codegen_til__DynCallInfo)
    func append(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, own other: Vec__priv___src_self_c_codegen_til__DynCallInfo)
    func take(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
    func move_to(own self: Vec__priv___src_self_c_codegen_til__DynCallInfo, mut dest: Vec__priv___src_self_c_codegen_til__DynCallInfo)
    func move_from(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, own other: Vec__priv___src_self_c_codegen_til__DynCallInfo)
    func unsafe_get(self: Vec__priv___src_self_c_codegen_til__DynCallInfo, i: USize) returns ref priv___src_self_c_codegen_til__DynCallInfo
    func get(self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_c_codegen_til__DynCallInfo
    func pop(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns priv___src_self_c_codegen_til__DynCallInfo
    func remove_at(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow n: USize) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
    func truncate(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow n: USize)
    func unsafe_set(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__DynCallInfo)
    func set(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__DynCallInfo, mut _err_kind: I64)
    func push_take(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, mut src: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize)
    func delete(own self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow call_free: Bool)
    func clone(self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
    func size() returns shallow USize
}
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.new
:group: Compiler backend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.new() returns Vec__priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.with_capacity
:group: Compiler backend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.with_capacity(shallow n: USize) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.clear
:group: Compiler backend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.clear(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.delete_marked
:group: Compiler backend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.delete_marked(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, marks: Dynamic)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.push
:group: Compiler backend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.push(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, own val: priv___src_self_c_codegen_til__DynCallInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.append
:group: Compiler backend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.append(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, own other: Vec__priv___src_self_c_codegen_til__DynCallInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.take
:group: Compiler backend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.take(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.move_to
:group: Compiler backend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.move_to(own self: Vec__priv___src_self_c_codegen_til__DynCallInfo, mut dest: Vec__priv___src_self_c_codegen_til__DynCallInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.move_from
:group: Compiler backend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.move_from(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, own other: Vec__priv___src_self_c_codegen_til__DynCallInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.unsafe_get
:group: Compiler backend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.unsafe_get(self: Vec__priv___src_self_c_codegen_til__DynCallInfo, i: USize) returns ref priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.get
:group: Compiler backend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.get(self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.pop
:group: Compiler backend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.pop(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.remove_at
:group: Compiler backend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.remove_at(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.take_prefix
:group: Compiler backend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.take_prefix(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow n: USize) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.truncate
:group: Compiler backend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.truncate(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow n: USize)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.unsafe_set
:group: Compiler backend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.unsafe_set(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__DynCallInfo)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.set
:group: Compiler backend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.set(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize, own val: priv___src_self_c_codegen_til__DynCallInfo, mut _err_kind: I64)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.push_take
:group: Compiler backend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.push_take(mut self: Vec__priv___src_self_c_codegen_til__DynCallInfo, mut src: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow i: USize)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.delete
:group: Compiler backend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.delete(own self: Vec__priv___src_self_c_codegen_til__DynCallInfo, shallow call_free: Bool)
#+end_src

* Vec__priv___src_self_c_codegen_til__DynCallInfo.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.Vec__priv___src_self_c_codegen_til__DynCallInfo.clone
:group: Compiler backend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__priv___src_self_c_codegen_til__DynCallInfo.clone(self: Vec__priv___src_self_c_codegen_til__DynCallInfo) returns Vec__priv___src_self_c_codegen_til__DynCallInfo
#+end_src

* adopt__priv___src_self_c_codegen_til__CollectionInfo
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.adopt__priv___src_self_c_codegen_til__CollectionInfo
:group: Compiler backend
:END:

#+begin_src til
func adopt__priv___src_self_c_codegen_til__CollectionInfo(mut dest: Dynamic, own src: priv___src_self_c_codegen_til__CollectionInfo)
#+end_src

* adopt__priv___src_self_c_codegen_til__DynCallInfo
:PROPERTIES:
:CUSTOM_ID: til.src.self.c_codegen.adopt__priv___src_self_c_codegen_til__DynCallInfo
:group: Compiler backend
:END:

#+begin_src til
func adopt__priv___src_self_c_codegen_til__DynCallInfo(mut dest: Dynamic, own src: priv___src_self_c_codegen_til__DynCallInfo)
#+end_src
