#+TITLE: src/self/parser.til
#+SUBTITLE: til 0.0.1-4e407a330
#+OPTIONS: toc:t

* SymbolPool
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.SymbolPool
:group: Compiler frontend
:END:

#+begin_src til
struct SymbolPool {
    mut names : Vec__Str
    mut buckets : Vec__U32
    mut next : U32
namespace:
    func rebuild(mut self: SymbolPool, shallow count: USize)
    func lookup(self: SymbolPool, name: Str) returns Option__U32
    func intern(mut self: SymbolPool, name: Str) returns shallow U32
    func resolve(self: SymbolPool, sym: U32) returns ref Str
    func size() returns shallow USize
    func clone(self: SymbolPool) returns SymbolPool
    func delete(own self: SymbolPool, shallow call_free: Bool)
    func hash(self: SymbolPool, hasher: HashFn) returns shallow U64
}
#+end_src

* builtin_primitive_from_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.builtin_primitive_from_name
:group: Compiler frontend
:END:

#+begin_src til
func builtin_primitive_from_name(name: Str, mut out: Primitive) returns shallow Bool
#+end_src

* builtin_primitive_size_align
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.builtin_primitive_size_align
:group: Compiler frontend
:END:

#+begin_src til
func builtin_primitive_size_align(prim: Primitive, mut sz: USize, mut al: USize)
#+end_src

* builtin_type_from_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.builtin_type_from_name
:group: Compiler frontend
:END:

#+begin_src til
func builtin_type_from_name(name: Str, mut out: Type) returns shallow Bool
#+end_src

* decl_typed
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.decl_typed
:group: Compiler frontend
:END:

#+begin_src til
func decl_typed(type_name: Str, name: Str, doc: Str, shallow is_mut: Bool, shallow redundant_mut: Bool, shallow is_priv: Bool, shallow used: Bool, own_type: OwnType) returns Declaration
#+end_src

* take_expr
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.take_expr
:group: Compiler frontend
:END:

#+begin_src til
func take_expr(mut e: Expr) returns Expr
#+end_src

* parse
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.parse
:group: Compiler frontend
:END:

#+begin_src til
proc parse(tokens: Vec__Token, source: Str, path: Str, mut mode_out: Str, mut anon_type_counter: I64, symbols: SymbolPool, mut parse_errors: I32, shallow quiet: Bool) returns Expr
#+end_src

* expr_type_name_ref
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.expr_type_name_ref
:group: Compiler frontend
:END:

#+begin_src til
func expr_type_name_ref(e: Expr) returns shallow Option__ref_Str
#+end_src

* til_type_name_c
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.til_type_name_c
:group: Compiler frontend
:END:

#+begin_src til
func til_type_name_c(t: Type) returns Str
#+end_src

* node_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.node_name
:group: Compiler frontend
:END:

#+begin_src til
func node_name(data: NodeType) returns Str
#+end_src

* ident_node
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.ident_node
:group: Compiler frontend
:END:

#+begin_src til
func ident_node(shallow name: U32) returns NodeType
#+end_src

* literal_num_node
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.literal_num_node
:group: Compiler frontend
:END:

#+begin_src til
func literal_num_node(text: Str) returns NodeType
#+end_src

* field_access_node
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.field_access_node
:group: Compiler frontend
:END:

#+begin_src til
func field_access_node(shallow name: U32) returns NodeType
#+end_src

* FCALL_ARG_BITS
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.FCALL_ARG_BITS
:group: Compiler frontend
:END:

#+begin_src til
FCALL_ARG_BITS is a USize
#+end_src

* set_own_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.set_own_arg
:group: Compiler frontend
:END:

#+begin_src til
func set_own_arg(mut fcall: Expr, shallow arg_index: USize)
#+end_src

* get_own_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.get_own_arg
:group: Compiler frontend
:END:

#+begin_src til
func get_own_arg(fcall: Expr, shallow arg_index: USize) returns shallow Bool
#+end_src

* set_splat_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.set_splat_arg
:group: Compiler frontend
:END:

#+begin_src til
func set_splat_arg(mut fcall: Expr, shallow arg_index: USize)
#+end_src

* get_splat_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.get_splat_arg
:group: Compiler frontend
:END:

#+begin_src til
func get_splat_arg(fcall: Expr, shallow arg_index: USize) returns shallow Bool
#+end_src

* set_ref_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.set_ref_arg
:group: Compiler frontend
:END:

#+begin_src til
func set_ref_arg(mut fcall: Expr, shallow arg_index: USize)
#+end_src

* get_ref_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.get_ref_arg
:group: Compiler frontend
:END:

#+begin_src til
func get_ref_arg(fcall: Expr, shallow arg_index: USize) returns shallow Bool
#+end_src

* fcall_has_ref_args
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.fcall_has_ref_args
:group: Compiler frontend
:END:

#+begin_src til
func fcall_has_ref_args(fcall: Expr) returns shallow Bool
#+end_src

* set_keep_outer_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.set_keep_outer_arg
:group: Compiler frontend
:END:

#+begin_src til
func set_keep_outer_arg(mut fcall: Expr, shallow arg_index: USize)
#+end_src

* get_keep_outer_arg
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.get_keep_outer_arg
:group: Compiler frontend
:END:

#+begin_src til
func get_keep_outer_arg(fcall: Expr, shallow arg_index: USize) returns shallow Bool
#+end_src

* func_type_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.func_type_name
:group: Compiler frontend
:END:

#+begin_src til
func func_type_name(ft: FuncType) returns Str
#+end_src

* Option__Sym
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Option__Sym
:group: Compiler frontend
:END:

#+begin_src til
Option__Sym is a None
#+end_src

* Vec__U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__U32 {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__U32
    func with_capacity(shallow n: USize) returns Vec__U32
    func len(self: Vec__U32) returns shallow USize
    func clear(mut self: Vec__U32)
    func delete_marked(mut self: Vec__U32, marks: Dynamic)
    func push(mut self: Vec__U32, own val: U32)
    func append(mut self: Vec__U32, own other: Vec__U32)
    func take(mut self: Vec__U32) returns Vec__U32
    func move_to(own self: Vec__U32, mut dest: Vec__U32)
    func move_from(mut self: Vec__U32, own other: Vec__U32)
    func unsafe_get(self: Vec__U32, i: USize) returns ref U32
    func get(self: Vec__U32, shallow i: USize, mut _err_kind: I64) returns ref U32
    func pop(mut self: Vec__U32) returns shallow U32
    func remove_at(mut self: Vec__U32, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__U32, shallow n: USize) returns Vec__U32
    func truncate(mut self: Vec__U32, shallow n: USize)
    func unsafe_set(mut self: Vec__U32, shallow i: USize, own val: U32)
    func set(mut self: Vec__U32, shallow i: USize, own val: U32, mut _err_kind: I64)
    func push_take(mut self: Vec__U32, mut src: Vec__U32, shallow i: USize)
    func delete(own self: Vec__U32, shallow call_free: Bool)
    func clone(self: Vec__U32) returns Vec__U32
    func size() returns shallow USize
}
#+end_src

* Vec__U32.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__U32.new() returns Vec__U32
#+end_src

* Vec__U32.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__U32.with_capacity(shallow n: USize) returns Vec__U32
#+end_src

* Vec__U32.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__U32.clear(mut self: Vec__U32)
#+end_src

* Vec__U32.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__U32.delete_marked(mut self: Vec__U32, marks: Dynamic)
#+end_src

* Vec__U32.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.push
:group: Compiler frontend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__U32.push(mut self: Vec__U32, own val: U32)
#+end_src

* Vec__U32.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.append
:group: Compiler frontend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__U32.append(mut self: Vec__U32, own other: Vec__U32)
#+end_src

* Vec__U32.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__U32.take(mut self: Vec__U32) returns Vec__U32
#+end_src

* Vec__U32.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.move_to
:group: Compiler frontend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__U32.move_to(own self: Vec__U32, mut dest: Vec__U32)
#+end_src

* Vec__U32.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.move_from
:group: Compiler frontend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__U32.move_from(mut self: Vec__U32, own other: Vec__U32)
#+end_src

* Vec__U32.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__U32.unsafe_get(self: Vec__U32, i: USize) returns ref U32
#+end_src

* Vec__U32.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.get
:group: Compiler frontend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__U32.get(self: Vec__U32, shallow i: USize, mut _err_kind: I64) returns ref U32
#+end_src

* Vec__U32.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__U32.pop(mut self: Vec__U32) returns shallow U32
#+end_src

* Vec__U32.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__U32.remove_at(mut self: Vec__U32, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__U32.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.take_prefix
:group: Compiler frontend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__U32.take_prefix(mut self: Vec__U32, shallow n: USize) returns Vec__U32
#+end_src

* Vec__U32.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.truncate
:group: Compiler frontend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__U32.truncate(mut self: Vec__U32, shallow n: USize)
#+end_src

* Vec__U32.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__U32.unsafe_set(mut self: Vec__U32, shallow i: USize, own val: U32)
#+end_src

* Vec__U32.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__U32.set(mut self: Vec__U32, shallow i: USize, own val: U32, mut _err_kind: I64)
#+end_src

* Vec__U32.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__U32.push_take(mut self: Vec__U32, mut src: Vec__U32, shallow i: USize)
#+end_src

* Vec__U32.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__U32.delete(own self: Vec__U32, shallow call_free: Bool)
#+end_src

* Vec__U32.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__U32.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__U32.clone(self: Vec__U32) returns Vec__U32
#+end_src

* Option__U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Option__U32
:group: Compiler frontend
:END:

#+begin_src til
enum Option__U32 {
    None
    Some: U32
namespace:
    func is_some(self: Option__U32) returns shallow Bool
    func is_none(self: Option__U32) returns shallow Bool
    func unwrap_or(self: Option__U32, shallow default_v: U32) returns shallow U32
    func unwrap(self: Option__U32) returns shallow U32
    func take(own self: Option__U32) returns shallow U32
    ext_func None() returns Option__U32
    ext_func Some(val: U32) returns Option__U32
    core_func is(self: Option__U32, other: Option__U32) returns shallow Bool
    func eq(self: Option__U32, other: Option__U32) returns shallow Bool
    func delete(own self: Option__U32, shallow call_free: Bool)
    func cmp(a: Option__U32, b: Option__U32) returns shallow I64
    func to_str(self: Option__U32) returns Str
    func clone(self: Option__U32) returns Option__U32
    func size() returns shallow USize
    func lt(a: Option__U32, b: Option__U32) returns shallow Bool
    func gt(a: Option__U32, b: Option__U32) returns shallow Bool
    func neq(a: Option__U32, b: Option__U32) returns shallow Bool
    func lte(a: Option__U32, b: Option__U32) returns shallow Bool
    func gte(a: Option__U32, b: Option__U32) returns shallow Bool
}
#+end_src

* Option__U32.unwrap
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Option__U32.unwrap
:group: Compiler frontend
:END:

The contained value; PANICS on None, ala Rust's unwrap. For the
~if o.is_none() { ... }~ + ~o.unwrap()~ idiom, where the caller
has already handled absence and unwrapping cannot fail. Prefer
a ~switch~ when both arms do real work, and ~unwrap_or~ when a
default is meaningful -- reach for this only when None is
unreachable by construction, so it fails loudly if that ever
stops being true.

#+begin_src til
func Option__U32.unwrap(self: Option__U32) returns shallow U32
#+end_src

* Option__U32.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Option__U32.take
:group: Compiler frontend
:END:

The contained value, MOVED out; PANICS on None, like unwrap.
unwrap only BORROWS self, so for an owned payload it must hand
back a deep clone; this consumes the Option instead, so the
payload's bytes move and only the enum shell is freed (#321).
Use it wherever the caller takes ownership of what the Option
held -- an ~Option(Expr)~ returned by an AST helper, say, where
a clone would copy a whole subtree and break node identity.

#+begin_src til
func Option__U32.take(own self: Option__U32) returns shallow U32
#+end_src

* Set__Str
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str
:group: Compiler frontend
:END:

#+begin_src til
struct Set__Str {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Set__Str
    func with_capacity(shallow n: USize) returns Set__Str
    func len(self: Set__Str) returns shallow USize
    func clear(mut self: Set__Str)
    func has(self: Set__Str, val: Str) returns shallow Bool
    func add(mut self: Set__Str, own val: Str)
    func remove(mut self: Set__Str, val: Str) returns shallow Bool
    func delete(own self: Set__Str, shallow call_free: Bool)
    func clone(self: Set__Str) returns Set__Str
    func size() returns shallow USize
}
#+end_src

* Set__Str.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str.new
:group: Compiler frontend
:END:

Create an empty Set. T must implement ~cmp~. Allocates nothing:
like Vec.new, an empty Set is lazy (cap 0) and ~add~ seeds the
first slot. Short-lived empty Sets are common -- the garbager
builds four per statement for its per-statement name facts, most
of them never added to.

#+begin_src til
func Set__Str.new() returns Set__Str
#+end_src

* Set__Str.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str.with_capacity
:group: Compiler frontend
:END:

Create an empty Set pre-sized for ~n~ elements, so adding up to
~n~ reallocs zero times (Vec.with_capacity's counterpart). Use it
where the final size is known or well predicted; ~new()~ grows
0->1->2->4, which is the right default but costs a realloc per
doubling. n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Set__Str.with_capacity(shallow n: USize) returns Set__Str
#+end_src

* Set__Str.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str.clear
:group: Compiler frontend
:END:

Drop all live elements but keep the allocated storage so the Set can be reused.

#+begin_src til
func Set__Str.clear(mut self: Set__Str)
#+end_src

* Set__Str.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str.has
:group: Compiler frontend
:END:

True if the set contains an element equal to ~val~.

#+begin_src til
func Set__Str.has(self: Set__Str, val: Str) returns shallow Bool
#+end_src

* Set__Str.add
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str.add
:group: Compiler frontend
:END:

Insert ~val~ into the set (sorted order). If an equal element
already exists, ~val~ is dropped. ~val~ is owned.

#+begin_src til
func Set__Str.add(mut self: Set__Str, own val: Str)
#+end_src

* Set__Str.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Set__Str.remove
:group: Compiler frontend
:END:

Remove the element equal to ~val~, dropping it. Returns false if
absent (Map.remove's counterpart: ~add~ is the Set's ~set~, so
this is its removal half). Later elements shift left to keep the
storage sorted and dense.

#+begin_src til
func Set__Str.remove(mut self: Set__Str, val: Str) returns shallow Bool
#+end_src

* Map__Str_Str
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_Str {
    mut keys : Vec__Str
    mut values : Vec__Str
namespace:
    func new() returns Map__Str_Str
    func with_capacity(shallow n: USize) returns Map__Str_Str
    func len(self: Map__Str_Str) returns shallow USize
    func clear(mut self: Map__Str_Str)
    func has(self: Map__Str_Str, key: Str) returns shallow Bool
    func get(self: Map__Str_Str, key: Str, mut _err_kind: I64) returns ref Str
    func set(mut self: Map__Str_Str, key: Str, own val: Str)
    func remove(mut self: Map__Str_Str, key: Str) returns shallow Bool
    func delete(own self: Map__Str_Str, shallow call_free: Bool)
    func clone(self: Map__Str_Str) returns Map__Str_Str
    func size() returns shallow USize
    func hash(self: Map__Str_Str, hasher: HashFn) returns shallow U64
}
#+end_src

* Map__Str_Str.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement ~cmp~.

#+begin_src til
func Map__Str_Str.new() returns Map__Str_Str
#+end_src

* Map__Str_Str.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for ~n~ entries, so inserting up to
~n~ reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_Str.with_capacity(shallow n: USize) returns Map__Str_Str
#+end_src

* Map__Str_Str.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_Str.clear(mut self: Map__Str_Str)
#+end_src

* Map__Str_Str.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_Str.has(self: Map__Str_Str, key: Str) returns shallow Bool
#+end_src

* Map__Str_Str.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.get
:group: Compiler frontend
:END:

Return a ref to the value for ~key~. Throws KeyNotFound if
~key~ is absent; call as ~map.get(k)!~ (or ~map[k]~) to panic
on a missing key.

#+begin_src til
func Map__Str_Str.get(self: Map__Str_Str, key: Str, mut _err_kind: I64) returns ref Str
#+end_src

* Map__Str_Str.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.set
:group: Compiler frontend
:END:

Insert or overwrite the value for ~key~. The value is owned.

#+begin_src til
func Map__Str_Str.set(mut self: Map__Str_Str, key: Str, own val: Str)
#+end_src

* Map__Str_Str.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Map__Str_Str.remove
:group: Compiler frontend
:END:

Remove ~key~, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_Str.remove(mut self: Map__Str_Str, key: Str) returns shallow Bool
#+end_src

* Vec__Bool
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__Bool {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__Bool
    func with_capacity(shallow n: USize) returns Vec__Bool
    func len(self: Vec__Bool) returns shallow USize
    func clear(mut self: Vec__Bool)
    func delete_marked(mut self: Vec__Bool, marks: Dynamic)
    func push(mut self: Vec__Bool, own val: Bool)
    func append(mut self: Vec__Bool, own other: Vec__Bool)
    func take(mut self: Vec__Bool) returns Vec__Bool
    func move_to(own self: Vec__Bool, mut dest: Vec__Bool)
    func move_from(mut self: Vec__Bool, own other: Vec__Bool)
    func unsafe_get(self: Vec__Bool, i: USize) returns ref Bool
    func get(self: Vec__Bool, shallow i: USize, mut _err_kind: I64) returns ref Bool
    func pop(mut self: Vec__Bool) returns shallow Bool
    func remove_at(mut self: Vec__Bool, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__Bool, shallow n: USize) returns Vec__Bool
    func truncate(mut self: Vec__Bool, shallow n: USize)
    func unsafe_set(mut self: Vec__Bool, shallow i: USize, own val: Bool)
    func set(mut self: Vec__Bool, shallow i: USize, own val: Bool, mut _err_kind: I64)
    func push_take(mut self: Vec__Bool, mut src: Vec__Bool, shallow i: USize)
    func delete(own self: Vec__Bool, shallow call_free: Bool)
    func clone(self: Vec__Bool) returns Vec__Bool
    func size() returns shallow USize
}
#+end_src

* Vec__Bool.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__Bool.new() returns Vec__Bool
#+end_src

* Vec__Bool.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__Bool.with_capacity(shallow n: USize) returns Vec__Bool
#+end_src

* Vec__Bool.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__Bool.clear(mut self: Vec__Bool)
#+end_src

* Vec__Bool.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__Bool.delete_marked(mut self: Vec__Bool, marks: Dynamic)
#+end_src

* Vec__Bool.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.push
:group: Compiler frontend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__Bool.push(mut self: Vec__Bool, own val: Bool)
#+end_src

* Vec__Bool.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.append
:group: Compiler frontend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__Bool.append(mut self: Vec__Bool, own other: Vec__Bool)
#+end_src

* Vec__Bool.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__Bool.take(mut self: Vec__Bool) returns Vec__Bool
#+end_src

* Vec__Bool.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.move_to
:group: Compiler frontend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__Bool.move_to(own self: Vec__Bool, mut dest: Vec__Bool)
#+end_src

* Vec__Bool.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.move_from
:group: Compiler frontend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__Bool.move_from(mut self: Vec__Bool, own other: Vec__Bool)
#+end_src

* Vec__Bool.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__Bool.unsafe_get(self: Vec__Bool, i: USize) returns ref Bool
#+end_src

* Vec__Bool.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.get
:group: Compiler frontend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__Bool.get(self: Vec__Bool, shallow i: USize, mut _err_kind: I64) returns ref Bool
#+end_src

* Vec__Bool.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__Bool.pop(mut self: Vec__Bool) returns shallow Bool
#+end_src

* Vec__Bool.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__Bool.remove_at(mut self: Vec__Bool, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__Bool.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.take_prefix
:group: Compiler frontend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__Bool.take_prefix(mut self: Vec__Bool, shallow n: USize) returns Vec__Bool
#+end_src

* Vec__Bool.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.truncate
:group: Compiler frontend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__Bool.truncate(mut self: Vec__Bool, shallow n: USize)
#+end_src

* Vec__Bool.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__Bool.unsafe_set(mut self: Vec__Bool, shallow i: USize, own val: Bool)
#+end_src

* Vec__Bool.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__Bool.set(mut self: Vec__Bool, shallow i: USize, own val: Bool, mut _err_kind: I64)
#+end_src

* Vec__Bool.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__Bool.push_take(mut self: Vec__Bool, mut src: Vec__Bool, shallow i: USize)
#+end_src

* Vec__Bool.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__Bool.delete(own self: Vec__Bool, shallow call_free: Bool)
#+end_src

* Vec__Bool.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.Vec__Bool.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__Bool.clone(self: Vec__Bool) returns Vec__Bool
#+end_src

* swap__Expr
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.swap__Expr
:group: Compiler frontend
:END:

#+begin_src til
func swap__Expr(mut a: Expr, mut b: Expr)
#+end_src

* adopt__U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.adopt__U32
:group: Compiler frontend
:END:

#+begin_src til
func adopt__U32(mut dest: Dynamic, own src: U32)
#+end_src

* adopt__Bool
:PROPERTIES:
:CUSTOM_ID: til.src.self.parser.adopt__Bool
:group: Compiler frontend
:END:

#+begin_src til
func adopt__Bool(mut dest: Dynamic, own src: Bool)
#+end_src
