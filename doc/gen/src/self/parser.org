#+TITLE: src/self/parser.til
#+OPTIONS: toc:t

* builtin_type_table
#+begin_src til
builtin_type_table is a Map
#+end_src

* builtin_type_from_name
#+begin_src til
func builtin_type_from_name(name: Str, mut out: Type) returns shallow Bool
#+end_src

* til_type_from_explicit_type
#+begin_src til
func til_type_from_explicit_type(name: Str) returns Type
#+end_src

* decl_typed
#+begin_src til
func decl_typed(explicit_type: Str, name: Str, doc: Str, shallow is_mut: Bool, shallow redundant_mut: Bool, shallow is_priv: Bool, shallow used: Bool, own_type: OwnType) returns Declaration
#+end_src

* anon_struct_counter
#+begin_src til
anon_struct_counter is a I64
#+end_src

* parse
#+begin_src til
proc parse(tokens: Vec, source: Str, path: Str, mut mode_out: Str) returns Expr
#+end_src

