#+TITLE: src/self/loader.til
#+SUBTITLE: til 0.0.1-c5918d787
#+OPTIONS: toc:t

* realpath_str
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.realpath_str
:group: Compiler frontend
:END:

#+begin_src til
ext_proc realpath_str(path: Str) returns Str
#+end_src

* REPL_DOC_INDEX
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.REPL_DOC_INDEX
:group: Compiler frontend
:END:

#+begin_src til
REPL_DOC_INDEX is a Str
#+end_src

* forward_header_path
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.forward_header_path
:group: Compiler frontend
:END:

#+begin_src til
func forward_header_path(path: Str) returns Str
#+end_src

* ProgramUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.ProgramUnit
:group: Compiler frontend
:END:

#+begin_src til
struct ProgramUnit {
    mut path : Str
    mut mode : Mode
    mut imports : Vec__Str
namespace:
    func clone(self: ProgramUnit) returns ProgramUnit
    func delete(own self: ProgramUnit, shallow call_free: Bool)
    func hash(self: ProgramUnit, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* LoadedProgram
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.LoadedProgram
:group: Compiler frontend
:END:

#+begin_src til
struct LoadedProgram {
    own mut core_units : Vec__ProgramUnit
    own mut units : Vec__ProgramUnit
    mut mode_files : Vec__Str
    mut target : Target
    mut cur_mode : Mode
    mut ctx : Context
    mut skip_core : Bool
    mut mode_str : Str
    mut path : Str
    mut ext_c_path : Str
    mut link_flags : Str
    mut include_flags : Str
    mut link_c_paths : Str
    mut run_tests : Bool
    mut load_errors : I32
    mut usize_override_bits : I64
namespace:
    func unit_paths(self: LoadedProgram) returns Vec__Str
    func delete(own self: LoadedProgram, shallow call_free: Bool)
    func clone(self: LoadedProgram) returns LoadedProgram
    func size() returns shallow USize
}
#+end_src

* repl_clone_loaded_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.repl_clone_loaded_program
:group: Compiler frontend
:END:

#+begin_src til
func repl_clone_loaded_program(src: LoadedProgram) returns LoadedProgram
#+end_src

* retarget_loaded_aliases
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.retarget_loaded_aliases
:group: Compiler frontend
:END:

#+begin_src til
func retarget_loaded_aliases(mut lp: LoadedProgram)
#+end_src

* load_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.load_program
:group: Compiler frontend
:END:

#+begin_src til
proc load_program(path: Str, bin_dir: Str, cwd: Str, ext_c_path: Str, extra_modes: Vec__Str, import_base: Str) returns LoadedProgram
#+end_src

* DocMeta
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.DocMeta
:group: Compiler frontend
:END:

#+begin_src til
struct DocMeta {
    mut body : Str
    mut group : Str
    mut since : Str
    mut deprecated : Str
    mut hidden : Bool
namespace:
    func clone(self: DocMeta) returns DocMeta
    func delete(own self: DocMeta, shallow call_free: Bool)
    func hash(self: DocMeta, hasher: HashFn) returns shallow U64
    func cmp(a: DocMeta, b: DocMeta) returns shallow I64
    func to_str(self: DocMeta) returns Str
    func size() returns shallow USize
    func eq(a: DocMeta, b: DocMeta) returns shallow Bool
    func lt(a: DocMeta, b: DocMeta) returns shallow Bool
    func gt(a: DocMeta, b: DocMeta) returns shallow Bool
    func neq(a: DocMeta, b: DocMeta) returns shallow Bool
    func lte(a: DocMeta, b: DocMeta) returns shallow Bool
    func gte(a: DocMeta, b: DocMeta) returns shallow Bool
}
#+end_src

* doc_keyword_value
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.doc_keyword_value
:group: Compiler frontend
:END:

#+begin_src til
func doc_keyword_value(line: Str, key: Str) returns Str
#+end_src

* doc_false_value
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.doc_false_value
:group: Compiler frontend
:END:

#+begin_src til
func doc_false_value(value: Str) returns shallow Bool
#+end_src

* doc_meta_line
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.doc_meta_line
:group: Compiler frontend
:END:

#+begin_src til
func doc_meta_line(line: Str, mut meta: DocMeta) returns shallow Bool
#+end_src

* parse_doc_meta
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.parse_doc_meta
:group: Compiler frontend
:END:

#+begin_src til
func parse_doc_meta(doc: Str) returns DocMeta
#+end_src

* format_doc_for_help
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.format_doc_for_help
:group: Compiler frontend
:END:

#+begin_src til
func format_doc_for_help(doc: Str) returns Str
#+end_src

* format_decl_info
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.format_decl_info
:group: Compiler frontend
:END:

#+begin_src til
func format_decl_info(stmt: Expr, scope: TypeScope) returns Str
#+end_src

* format_member_info
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.format_member_info
:group: Compiler frontend
:END:

#+begin_src til
func format_member_info(type_name: Str, dname: Str, d: Declaration) returns Str
#+end_src

* format_field_info
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.format_field_info
:group: Compiler frontend
:END:

#+begin_src til
func format_field_info(type_name: Str, field_name: Str, field: Declaration) returns Str
#+end_src

* format_variant_info
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.format_variant_info
:group: Compiler frontend
:END:

#+begin_src til
func format_variant_info(type_name: Str, variant: VariantDef) returns Str
#+end_src

* collect_decl_packed
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.collect_decl_packed
:group: Compiler frontend
:END:

#+begin_src til
func collect_decl_packed(body: Expr, scope: TypeScope, mut info: Str, mut docs: Str)
#+end_src

* build_doc_init_line
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.build_doc_init_line
:group: Compiler frontend
:END:

#+begin_src til
func build_doc_init_line(packed_info: Str, packed_docs: Str) returns Str
#+end_src

* build_doc_init_line_org_files
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.build_doc_init_line_org_files
:group: Compiler frontend
:END:

#+begin_src til
func build_doc_init_line_org_files(index_path: Str, packed_user_info: Str, packed_user_docs: Str) returns Str
#+end_src

* lazy_recursive_variadic_def
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.lazy_recursive_variadic_def
:group: Compiler frontend
:END:

#+begin_src til
func lazy_recursive_variadic_def(fdef: Expr) returns shallow Bool
#+end_src

* type_program_errors
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.type_program_errors
:group: Compiler frontend
:END:

#+begin_src til
proc type_program_errors(mut lp: LoadedProgram, shallow run_tests: Bool) returns shallow I32
#+end_src

* finish_init_and_type_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.finish_init_and_type_program
:group: Compiler frontend
:END:

#+begin_src til
proc finish_init_and_type_program(mut lp: LoadedProgram)
#+end_src

* init_and_type_program_errors
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.init_and_type_program_errors
:group: Compiler frontend
:END:

#+begin_src til
proc init_and_type_program_errors(mut lp: LoadedProgram, shallow run_tests: Bool) returns shallow I32
#+end_src

* init_and_type_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.init_and_type_program
:group: Compiler frontend
:END:

#+begin_src til
proc init_and_type_program(mut lp: LoadedProgram, shallow run_tests: Bool)
#+end_src

* finish_prepare_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.finish_prepare_program
:group: Compiler frontend
:END:

#+begin_src til
proc finish_prepare_program(mut lp: LoadedProgram)
#+end_src

* repl_finish_prepare_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.repl_finish_prepare_program
:group: Compiler frontend
:END:

#+begin_src til
proc repl_finish_prepare_program(mut lp: LoadedProgram)
#+end_src

* repl_init_cached_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.repl_init_cached_program
:group: Compiler frontend
:END:

#+begin_src til
proc repl_init_cached_program(mut lp: LoadedProgram) returns shallow I32
#+end_src

* repl_type_delta_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.repl_type_delta_program
:group: Compiler frontend
:END:

#+begin_src til
proc repl_type_delta_program(mut lp: LoadedProgram, mut delta: Expr) returns shallow I32
#+end_src

* repl_prepare_delta_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.repl_prepare_delta_program
:group: Compiler frontend
:END:

#+begin_src til
proc repl_prepare_delta_program(mut lp: LoadedProgram, mut delta: Expr)
#+end_src

* prepare_program
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.prepare_program
:group: Compiler frontend
:END:

#+begin_src til
proc prepare_program(mut lp: LoadedProgram, shallow run_tests: Bool)
#+end_src

* cmd_ast
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.cmd_ast
:group: Compiler frontend
:END:

#+begin_src til
proc cmd_ast(lp: LoadedProgram)
#+end_src

* Vec__ProgramUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__ProgramUnit {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__ProgramUnit
    func with_capacity(shallow n: USize) returns Vec__ProgramUnit
    func len(self: Vec__ProgramUnit) returns shallow USize
    func clear(mut self: Vec__ProgramUnit)
    func delete_marked(mut self: Vec__ProgramUnit, marks: Dynamic)
    func push(mut self: Vec__ProgramUnit, own val: ProgramUnit)
    func append(mut self: Vec__ProgramUnit, own other: Vec__ProgramUnit)
    func take(mut self: Vec__ProgramUnit) returns Vec__ProgramUnit
    func move_to(own self: Vec__ProgramUnit, mut dest: Vec__ProgramUnit)
    func move_from(mut self: Vec__ProgramUnit, own other: Vec__ProgramUnit)
    func unsafe_get(self: Vec__ProgramUnit, i: USize) returns ref ProgramUnit
    func get(self: Vec__ProgramUnit, shallow i: USize, mut _err_kind: I64) returns ref ProgramUnit
    func pop(mut self: Vec__ProgramUnit) returns ProgramUnit
    func remove_at(mut self: Vec__ProgramUnit, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__ProgramUnit, shallow n: USize) returns Vec__ProgramUnit
    func truncate(mut self: Vec__ProgramUnit, shallow n: USize)
    func unsafe_set(mut self: Vec__ProgramUnit, shallow i: USize, own val: ProgramUnit)
    func set(mut self: Vec__ProgramUnit, shallow i: USize, own val: ProgramUnit, mut _err_kind: I64)
    func push_take(mut self: Vec__ProgramUnit, mut src: Vec__ProgramUnit, shallow i: USize)
    func delete(own self: Vec__ProgramUnit, shallow call_free: Bool)
    func clone(self: Vec__ProgramUnit) returns Vec__ProgramUnit
    func size() returns shallow USize
}
#+end_src

* Vec__ProgramUnit.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__ProgramUnit.new() returns Vec__ProgramUnit
#+end_src

* Vec__ProgramUnit.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__ProgramUnit.with_capacity(shallow n: USize) returns Vec__ProgramUnit
#+end_src

* Vec__ProgramUnit.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__ProgramUnit.clear(mut self: Vec__ProgramUnit)
#+end_src

* Vec__ProgramUnit.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__ProgramUnit.delete_marked(mut self: Vec__ProgramUnit, marks: Dynamic)
#+end_src

* Vec__ProgramUnit.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.push
:group: Compiler frontend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__ProgramUnit.push(mut self: Vec__ProgramUnit, own val: ProgramUnit)
#+end_src

* Vec__ProgramUnit.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.append
:group: Compiler frontend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__ProgramUnit.append(mut self: Vec__ProgramUnit, own other: Vec__ProgramUnit)
#+end_src

* Vec__ProgramUnit.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__ProgramUnit.take(mut self: Vec__ProgramUnit) returns Vec__ProgramUnit
#+end_src

* Vec__ProgramUnit.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.move_to
:group: Compiler frontend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__ProgramUnit.move_to(own self: Vec__ProgramUnit, mut dest: Vec__ProgramUnit)
#+end_src

* Vec__ProgramUnit.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.move_from
:group: Compiler frontend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__ProgramUnit.move_from(mut self: Vec__ProgramUnit, own other: Vec__ProgramUnit)
#+end_src

* Vec__ProgramUnit.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__ProgramUnit.unsafe_get(self: Vec__ProgramUnit, i: USize) returns ref ProgramUnit
#+end_src

* Vec__ProgramUnit.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.get
:group: Compiler frontend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__ProgramUnit.get(self: Vec__ProgramUnit, shallow i: USize, mut _err_kind: I64) returns ref ProgramUnit
#+end_src

* Vec__ProgramUnit.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__ProgramUnit.pop(mut self: Vec__ProgramUnit) returns ProgramUnit
#+end_src

* Vec__ProgramUnit.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__ProgramUnit.remove_at(mut self: Vec__ProgramUnit, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__ProgramUnit.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.take_prefix
:group: Compiler frontend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__ProgramUnit.take_prefix(mut self: Vec__ProgramUnit, shallow n: USize) returns Vec__ProgramUnit
#+end_src

* Vec__ProgramUnit.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.truncate
:group: Compiler frontend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__ProgramUnit.truncate(mut self: Vec__ProgramUnit, shallow n: USize)
#+end_src

* Vec__ProgramUnit.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__ProgramUnit.unsafe_set(mut self: Vec__ProgramUnit, shallow i: USize, own val: ProgramUnit)
#+end_src

* Vec__ProgramUnit.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__ProgramUnit.set(mut self: Vec__ProgramUnit, shallow i: USize, own val: ProgramUnit, mut _err_kind: I64)
#+end_src

* Vec__ProgramUnit.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__ProgramUnit.push_take(mut self: Vec__ProgramUnit, mut src: Vec__ProgramUnit, shallow i: USize)
#+end_src

* Vec__ProgramUnit.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__ProgramUnit.delete(own self: Vec__ProgramUnit, shallow call_free: Bool)
#+end_src

* Vec__ProgramUnit.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ProgramUnit.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__ProgramUnit.clone(self: Vec__ProgramUnit) returns Vec__ProgramUnit
#+end_src

* Vec__ImportCheckEntry
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__ImportCheckEntry {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__ImportCheckEntry
    func with_capacity(shallow n: USize) returns Vec__ImportCheckEntry
    func len(self: Vec__ImportCheckEntry) returns shallow USize
    func clear(mut self: Vec__ImportCheckEntry)
    func delete_marked(mut self: Vec__ImportCheckEntry, marks: Dynamic)
    func push(mut self: Vec__ImportCheckEntry, own val: priv___src_self_loader_til__ImportCheckEntry)
    func append(mut self: Vec__ImportCheckEntry, own other: Vec__ImportCheckEntry)
    func take(mut self: Vec__ImportCheckEntry) returns Vec__ImportCheckEntry
    func move_to(own self: Vec__ImportCheckEntry, mut dest: Vec__ImportCheckEntry)
    func move_from(mut self: Vec__ImportCheckEntry, own other: Vec__ImportCheckEntry)
    func unsafe_get(self: Vec__ImportCheckEntry, i: USize) returns ref priv___src_self_loader_til__ImportCheckEntry
    func get(self: Vec__ImportCheckEntry, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_loader_til__ImportCheckEntry
    func pop(mut self: Vec__ImportCheckEntry) returns priv___src_self_loader_til__ImportCheckEntry
    func remove_at(mut self: Vec__ImportCheckEntry, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__ImportCheckEntry, shallow n: USize) returns Vec__ImportCheckEntry
    func truncate(mut self: Vec__ImportCheckEntry, shallow n: USize)
    func unsafe_set(mut self: Vec__ImportCheckEntry, shallow i: USize, own val: priv___src_self_loader_til__ImportCheckEntry)
    func set(mut self: Vec__ImportCheckEntry, shallow i: USize, own val: priv___src_self_loader_til__ImportCheckEntry, mut _err_kind: I64)
    func push_take(mut self: Vec__ImportCheckEntry, mut src: Vec__ImportCheckEntry, shallow i: USize)
    func delete(own self: Vec__ImportCheckEntry, shallow call_free: Bool)
    func clone(self: Vec__ImportCheckEntry) returns Vec__ImportCheckEntry
    func size() returns shallow USize
}
#+end_src

* Vec__ImportCheckEntry.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__ImportCheckEntry.new() returns Vec__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__ImportCheckEntry.with_capacity(shallow n: USize) returns Vec__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__ImportCheckEntry.clear(mut self: Vec__ImportCheckEntry)
#+end_src

* Vec__ImportCheckEntry.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__ImportCheckEntry.delete_marked(mut self: Vec__ImportCheckEntry, marks: Dynamic)
#+end_src

* Vec__ImportCheckEntry.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.push
:group: Compiler frontend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__ImportCheckEntry.push(mut self: Vec__ImportCheckEntry, own val: priv___src_self_loader_til__ImportCheckEntry)
#+end_src

* Vec__ImportCheckEntry.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.append
:group: Compiler frontend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__ImportCheckEntry.append(mut self: Vec__ImportCheckEntry, own other: Vec__ImportCheckEntry)
#+end_src

* Vec__ImportCheckEntry.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__ImportCheckEntry.take(mut self: Vec__ImportCheckEntry) returns Vec__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.move_to
:group: Compiler frontend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__ImportCheckEntry.move_to(own self: Vec__ImportCheckEntry, mut dest: Vec__ImportCheckEntry)
#+end_src

* Vec__ImportCheckEntry.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.move_from
:group: Compiler frontend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__ImportCheckEntry.move_from(mut self: Vec__ImportCheckEntry, own other: Vec__ImportCheckEntry)
#+end_src

* Vec__ImportCheckEntry.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__ImportCheckEntry.unsafe_get(self: Vec__ImportCheckEntry, i: USize) returns ref priv___src_self_loader_til__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.get
:group: Compiler frontend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__ImportCheckEntry.get(self: Vec__ImportCheckEntry, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_loader_til__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__ImportCheckEntry.pop(mut self: Vec__ImportCheckEntry) returns priv___src_self_loader_til__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__ImportCheckEntry.remove_at(mut self: Vec__ImportCheckEntry, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__ImportCheckEntry.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.take_prefix
:group: Compiler frontend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__ImportCheckEntry.take_prefix(mut self: Vec__ImportCheckEntry, shallow n: USize) returns Vec__ImportCheckEntry
#+end_src

* Vec__ImportCheckEntry.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.truncate
:group: Compiler frontend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__ImportCheckEntry.truncate(mut self: Vec__ImportCheckEntry, shallow n: USize)
#+end_src

* Vec__ImportCheckEntry.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__ImportCheckEntry.unsafe_set(mut self: Vec__ImportCheckEntry, shallow i: USize, own val: priv___src_self_loader_til__ImportCheckEntry)
#+end_src

* Vec__ImportCheckEntry.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.set
:group: Compiler frontend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__ImportCheckEntry.set(mut self: Vec__ImportCheckEntry, shallow i: USize, own val: priv___src_self_loader_til__ImportCheckEntry, mut _err_kind: I64)
#+end_src

* Vec__ImportCheckEntry.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__ImportCheckEntry.push_take(mut self: Vec__ImportCheckEntry, mut src: Vec__ImportCheckEntry, shallow i: USize)
#+end_src

* Vec__ImportCheckEntry.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__ImportCheckEntry.delete(own self: Vec__ImportCheckEntry, shallow call_free: Bool)
#+end_src

* Vec__ImportCheckEntry.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.Vec__ImportCheckEntry.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__ImportCheckEntry.clone(self: Vec__ImportCheckEntry) returns Vec__ImportCheckEntry
#+end_src

* adopt__ProgramUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.adopt__ProgramUnit
:group: Compiler frontend
:END:

#+begin_src til
func adopt__ProgramUnit(dest: Dynamic, own src: ProgramUnit)
#+end_src

* adopt__priv___src_self_loader_til__ImportCheckEntry
:PROPERTIES:
:CUSTOM_ID: til.src.self.loader.adopt__priv___src_self_loader_til__ImportCheckEntry
:group: Compiler frontend
:END:

#+begin_src til
func adopt__priv___src_self_loader_til__ImportCheckEntry(dest: Dynamic, own src: priv___src_self_loader_til__ImportCheckEntry)
#+end_src
