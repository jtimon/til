#+TITLE: src/self/context.til
#+SUBTITLE: til 0.0.1-61e33ef8f
#+OPTIONS: toc:t

* BorrowRoot
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.BorrowRoot
:group: Compiler frontend
:END:

#+begin_src til
struct BorrowRoot {
    mut name : Str
    mut path : Str
    mut line : U32
    mut col : U32
namespace:
    func eq(a: BorrowRoot, b: BorrowRoot) returns shallow Bool
    func to_str(self: BorrowRoot) returns Str
    func clone(self: BorrowRoot) returns BorrowRoot
    func delete(own self: BorrowRoot, shallow call_free: Bool)
    func hash(self: BorrowRoot, hasher: HashFn) returns shallow U64
    func cmp(a: BorrowRoot, b: BorrowRoot) returns shallow I64
    func size() returns shallow USize
    func lt(a: BorrowRoot, b: BorrowRoot) returns shallow Bool
    func gt(a: BorrowRoot, b: BorrowRoot) returns shallow Bool
    func neq(a: BorrowRoot, b: BorrowRoot) returns shallow Bool
    func lte(a: BorrowRoot, b: BorrowRoot) returns shallow Bool
    func gte(a: BorrowRoot, b: BorrowRoot) returns shallow Bool
}
#+end_src

* TypeBinding
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.TypeBinding
:group: Compiler frontend
:END:

#+begin_src til
struct TypeBinding {
    mut name : Str
    mut type : Type
    mut is_mut : Bool
    mut is_priv : Bool
    mut path : Str
    mut line : U32
    mut col : U32
    mut is_param : Bool
    mut own_type : OwnType
    mut is_alias : Bool
    mut is_type_alias : Bool
    mut is_func_decl : Bool
    mut alias_target : Str
    mut func_type : FuncType
    mut is_builtin : Bool
    mut used : Bool
    mut written : Bool
    mut mut_explicit : Bool
    mut orig_name : Str
    mut dynvec_elem : Str
    mut declared_type_name : Str
    mut borrow_roots : Vec__BorrowRoot
namespace:
    func eq(a: TypeBinding, b: TypeBinding) returns shallow Bool
    func to_str(self: TypeBinding) returns Str
    func clone(self: TypeBinding) returns TypeBinding
    func delete(own self: TypeBinding, shallow call_free: Bool)
    func hash(self: TypeBinding, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* ScopeFind
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.ScopeFind
:group: Compiler frontend
:END:

#+begin_src til
enum ScopeFind {
    NotFound
    Found: TypeBinding
namespace:
    ext_func NotFound() returns shallow ScopeFind
    ext_func Found(own val: TypeBinding) returns shallow ScopeFind
    core_func is(self: ScopeFind, other: ScopeFind) returns shallow Bool
    func eq(shallow self: ScopeFind, shallow other: ScopeFind) returns shallow Bool
    func delete(own self: ScopeFind, shallow call_free: Bool)
    func to_str(shallow self: ScopeFind) returns Str
    func clone(shallow self: ScopeFind) returns shallow ScopeFind
    func size() returns shallow USize
}
#+end_src

* name_fingerprint
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.name_fingerprint
:group: Compiler frontend
:END:

#+begin_src til
func name_fingerprint(name: Str) returns shallow U64
#+end_src

* TypeScope
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.TypeScope
:group: Compiler frontend
:END:

#+begin_src til
struct TypeScope {
    mut bindings : Map__Str_TypeBinding
    mut target_usize_pname : Str
    mut target_uptr_pname : Str
    mut func_defs : Map__Str_Dynamic
    mut struct_defs : Map__Str_Dynamic
    mut parent : Option__ref_TypeScope
    mut is_func_root : Bool
    mut bindings_filt : U64
    mut func_defs_filt : U64
    mut struct_defs_filt : U64
namespace:
    func get_binding(self: TypeScope, name: Str) returns shallow Option__ref_TypeBinding
    func get_binding_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_TypeBinding
    func in_function(self: TypeScope) returns shallow Bool
    func bound_in_this_func(self: TypeScope, name: Str) returns shallow Bool
    func bound_in_this_func_fp(self: TypeScope, name: Str, shallow fp: U64) returns shallow Bool
    func find(self: TypeScope, name: Str) returns shallow ScopeFind
    func get_type(self: TypeScope, name: Str) returns Type
    func get_func_type(self: TypeScope, name: Str) returns shallow FuncType
    func get_struct(self: TypeScope, name: Str) returns shallow Option__ref_Expr
    func get_struct_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_Expr
    func struct_def_of(self: TypeScope, name: Str) returns shallow Option__ref_Expr
    func struct_def_of_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_Expr
    func lookup_func(self: TypeScope, name: Str) returns shallow Option__ref_Expr
    func lookup_func_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_Expr
    func any_func_def(self: TypeScope, name: Str) returns shallow Bool
    func any_func_def_fp(self: TypeScope, name: Str, shallow fp: U64) returns shallow Bool
    func local_struct_def_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_Expr
    func local_func_def_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_Expr
    func lookup_func_symbol(self: TypeScope, name: Str) returns shallow Option__ref_Expr
    func lookup_func_symbol_fp(self: TypeScope, name: Str, fp: U64) returns shallow Option__ref_Expr
    func set_func_def(mut self: TypeScope, name: Str, fdef: Expr)
    func set_func_symbol(mut self: TypeScope, name: Str, fdef: Expr)
    func set_struct_def(mut self: TypeScope, name: Str, sdef: Expr)
    func is_mut(self: TypeScope, name: Str) returns shallow Bool
    func erase_func_def(mut self: TypeScope, name: Str)
    func erase_binding(mut self: TypeScope, name: Str)
    func erase_struct_def(mut self: TypeScope, name: Str)
    func erase_ast_decl(mut self: TypeScope, name: Str, shallow erase_decl_binding: Bool)
    func refresh_lookup_filters(mut self: TypeScope)
    func set(mut self: TypeScope, name: Str, type: Type, shallow is_mut: Bool, path: Str, shallow line: U32, shallow col: U32, shallow is_param: Bool, shallow own_type: OwnType)
    func clone(self: TypeScope) returns TypeScope
    func delete(own self: TypeScope, shallow call_free: Bool)
    func hash(self: TypeScope, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* binding_func_type
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.binding_func_type
:group: Compiler frontend
:END:

#+begin_src til
func binding_func_type(b: TypeBinding) returns shallow FuncType
#+end_src

* type_binding_is_type_token
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.type_binding_is_type_token
:group: Compiler frontend
:END:

#+begin_src til
func type_binding_is_type_token(scope: TypeScope, b: TypeBinding) returns shallow Bool
#+end_src

* init_func_is_generic
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.init_func_is_generic
:group: Compiler frontend
:END:

#+begin_src til
func init_func_is_generic(rhs: Expr) returns shallow Bool
#+end_src

* init_stmt_is_generic_source
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.init_stmt_is_generic_source
:group: Compiler frontend
:END:

#+begin_src til
func init_stmt_is_generic_source(stmt: Expr) returns shallow Bool
#+end_src

* type_name_mentions_param
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.type_name_mentions_param
:group: Compiler frontend
:END:

#+begin_src til
func type_name_mentions_param(name: Str, pname: Str) returns shallow Bool
#+end_src

* generic_source_throws_depend_on_type
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.generic_source_throws_depend_on_type
:group: Compiler frontend
:END:

#+begin_src til
func generic_source_throws_depend_on_type(rhs: Expr) returns shallow Bool
#+end_src

* GenericFuncSource
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.GenericFuncSource
:group: Compiler frontend
:END:

#+begin_src til
struct GenericFuncSource {
    mut name : Str
    mut path : Str
    mut is_priv : Bool
namespace:
    func eq(a: GenericFuncSource, b: GenericFuncSource) returns shallow Bool
    func to_str(self: GenericFuncSource) returns Str
    func clone(self: GenericFuncSource) returns GenericFuncSource
    func delete(own self: GenericFuncSource, shallow call_free: Bool)
    func hash(self: GenericFuncSource, hasher: HashFn) returns shallow U64
    func cmp(a: GenericFuncSource, b: GenericFuncSource) returns shallow I64
    func size() returns shallow USize
    func lt(a: GenericFuncSource, b: GenericFuncSource) returns shallow Bool
    func gt(a: GenericFuncSource, b: GenericFuncSource) returns shallow Bool
    func neq(a: GenericFuncSource, b: GenericFuncSource) returns shallow Bool
    func lte(a: GenericFuncSource, b: GenericFuncSource) returns shallow Bool
    func gte(a: GenericFuncSource, b: GenericFuncSource) returns shallow Bool
}
#+end_src

* func_def_type
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.func_def_type
:group: Compiler frontend
:END:

#+begin_src til
func func_def_type(fdef: Expr) returns shallow FuncType
#+end_src

* ImportUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.ImportUnit
:group: Compiler frontend
:END:

#+begin_src til
struct ImportUnit {
    mut mode_str : Str
    mut mode : Mode
    own mut ast : Expr
    mut imports : Vec__Str
namespace:
    func clone(self: ImportUnit) returns ImportUnit
    func delete(own self: ImportUnit, shallow call_free: Bool)
    func size() returns shallow USize
}
#+end_src

* BuilderFuncScratch
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.BuilderFuncScratch
:group: Compiler frontend
:END:

#+begin_src til
struct BuilderFuncScratch {
    mut local_fn_sigs : Map__Str_Dynamic
    mut body_refs : Set__Str
    mut body_multi_decls : Set__Str
    mut stack_locals : Set__Str
    mut stack_local_types : Map__Str_Str
    mut stack_lit_str_locals : Set__Str
    mut force_heap_stack_lit_str_own : Bool
    mut unsafe_to_hoist : Set__Str
    mut ref_locals : Set__Str
    mut ref_dyn_locals : Set__Str
    mut ptr_locals : Set__Str
    mut shadowed_params : Set__Str
namespace:
    func clone(self: BuilderFuncScratch) returns BuilderFuncScratch
    func delete(own self: BuilderFuncScratch, shallow call_free: Bool)
    func hash(self: BuilderFuncScratch, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* InternedTypes
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.InternedTypes
:group: Compiler frontend
:END:

#+begin_src til
struct InternedTypes {
    mut simples : Vec__Dynamic
    mut prims : Vec__Dynamic
    mut structs : Map__Str_Dynamic
    mut enums : Map__Str_Dynamic
    mut customs : Map__Str_Dynamic
    mut funcsigs : Map__Str_Dynamic
namespace:
    func clone(_self: InternedTypes) returns InternedTypes
    func delete(own self: InternedTypes, shallow call_free: Bool)
    func hash(self: InternedTypes, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Sym
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Sym
:group: Compiler frontend
:END:

#+begin_src til
Sym
#+end_src

* SymbolPool
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.SymbolPool
:group: Compiler frontend
:END:

#+begin_src til
struct SymbolPool {
    mut by_name : Map__Str_U32
    mut names : Vec__Str
    mut next : U32
namespace:
    func intern(mut self: SymbolPool, name: Str) returns shallow U32
    func resolve(self: SymbolPool, shallow sym: U32) returns Str
    func clone(self: SymbolPool) returns SymbolPool
    func delete(own self: SymbolPool, shallow call_free: Bool)
    func hash(self: SymbolPool, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Context
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Context
:group: Compiler frontend
:END:

#+begin_src til
struct Context {
    mut mode : Mode
    mut path : Str
    mut path_modes : Map__Str_Mode
    mut mode_registry : Map__Str_Mode
    mut eval_heap : EvalHeap
    mut target_usize_pname : Str
    mut target_uptr_pname : Str
    mut target_usize_bytes : USize
    mut anon_type_counter : I64
    mut compile_mode : Bool
    mut constfolder_foldables : Map__Str_FuncType
    mut constfolder_known : Option__Scope
    mut constfolder_assigned : Set__Str
    mut has_return : Bool
    mut has_break : Bool
    mut has_continue : Bool
    ref mut return_value : Dynamic
    mut interp_ret_is_ref : Bool
    ref mut interp_ret_dest : Dynamic
    mut interp_ret_type : Option__ref_Str
    mut interp_ret_own_type : OwnType
    mut constfold_active : Bool
    mut eval_aborted : Bool
    mut repl_session : Bool
    mut ns_fields : Option__Scope
    mut imported : Map__Str_ImportUnit
    mut check_unused_imports : Bool
    mut import_use_edges : Set__Str
    mut imports_init_seed_done : Set__Str
    mut imports_init_active : Set__Str
    mut imports_init_done : Set__Str
    mut imports_typer_decls_done : Set__Str
    mut imports_typer_bodies_done : Set__Str
    mut type_gen_synths : Set__Str
    mut generic_funcs : Map__Str_Expr
    mut generic_func_synths : Set__Str
    own mut generic_pending : Expr
    mut lambda_counter : I64
    mut scope : TypeScope
    mut is_repl : Bool
    mut struct_layouts : Map__Str_StructLayout
    mut typing_namespace_member : Bool
    mut closure_emit_env : Str
    mut closure_emit_captures : Set__Str
    mut funcsig_names : Set__U32
    mut closure_value_names : Set__Str
    mut script_globals : Set__Str
    mut ref_globals : Set__Str
    mut throw_type_registry : Vec__Str
    mut throws_global : Map__Str_call_Vec_Str
    mut bang_counter : I64
    mut lowering_param_types : Map__Str_Str
    mut typer_in_shallow_return : Bool
    mut typer_return_type_name : Str
    mut ctor_seq : I32
    mut in_func_def : Bool
    mut in_main_func : Bool
    mut proc_calls_count : I32
    mut proc_def_depth : I32
    mut hoist_counter : I32
    mut va_counter : I32
    mut kw_counter : I32
    mut coll_counter : I32
    mut errors : I32
    mut current_type_name : Str
    mut current_top_func_name : Str
    mut auto_gen_depth : I32
    mut throw_used_local_names : Set__Str
    mut priv_ref_edges : Map__Str_call_Vec_Str
    mut builder_func : BuilderFuncScratch
    mut builder_str_lit_symbols : Map__Str_Str
    mut builder_str_lit_values : Vec__Str
    mut builder_str_lit_ident_symbols : Map__Str_Str
    mut builder_reflect_inventory : Set__Str
    mut builder_dyn_fn_targets : Set__Str
    mut builder_ns_member_sites : Set__Str
    mut builder_collection_elem_types : Set__Str
    mut builder_keep_all_exports : Bool
    mut builder_lit_guard_used : Bool
    mut current_fdef : Option__ref_Expr
    mut cached_str_name : Str
    mut interp_type_defs : Map__Str_Dynamic
    mut interned_types : InternedTypes
    mut dispatch_map : Map__Str_Dynamic
    mut dispatch_inited : Bool
    mut ffi_map : Map__Str_FFIEntry
    mut ffi_map_inited : Bool
    mut ffi_loaded : Bool
    mut ffi_struct_defs : Map__Str_ExprPtrBox
    mut ffi_type_cache : Vec__FFITypePtrBox
    mut ffi_type_cache_inited : Bool
    mut symbols : SymbolPool
namespace:
    func clone(self: Context) returns Context
    func delete(own self: Context, shallow call_free: Bool)
    func size() returns shallow USize
}
#+end_src

* context_register_target_int_alias_types
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.context_register_target_int_alias_types
:group: Compiler frontend
:END:

#+begin_src til
func context_register_target_int_alias_types(mut ctx: Context, usize_name: Str, uptr_name: Str)
#+end_src

* context_reset_type_scope
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.context_reset_type_scope
:group: Compiler frontend
:END:

#+begin_src til
func context_reset_type_scope(mut ctx: Context)
#+end_src

* note_cross_file_use
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.note_cross_file_use
:group: Compiler frontend
:END:

#+begin_src til
func note_cross_file_use(mut ctx: Context, decl_path: Str)
#+end_src

* note_cross_file_type_use
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.note_cross_file_type_use
:group: Compiler frontend
:END:

#+begin_src til
func note_cross_file_type_use(scope: TypeScope, mut ctx: Context, type_name: Str)
#+end_src

* scope_target_usize_pname
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.scope_target_usize_pname
:group: Compiler frontend
:END:

#+begin_src til
func scope_target_usize_pname(scope: TypeScope) returns ref Str
#+end_src

* scope_target_uptr_pname
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.scope_target_uptr_pname
:group: Compiler frontend
:END:

#+begin_src til
func scope_target_uptr_pname(scope: TypeScope) returns ref Str
#+end_src

* priv_c_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.priv_c_name
:group: Compiler frontend
:END:

#+begin_src til
func priv_c_name(path: Str, name: Str) returns Str
#+end_src

* align_up
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.align_up
:group: Compiler frontend
:END:

#+begin_src til
func align_up(shallow offset: USize, shallow align: USize) returns shallow USize
#+end_src

* PRIM_NAMES
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.PRIM_NAMES
:group: Compiler frontend
:END:

#+begin_src til
PRIM_NAMES is a Array__Str
#+end_src

* _PRIM_NAMES_ek
:PROPERTIES:
:CUSTOM_ID: til.src.self.context._PRIM_NAMES_ek
:group: Compiler frontend
:END:

#+begin_src til
_PRIM_NAMES_ek is a I64
#+end_src

* WIDTH_ALIAS_NAMES
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.WIDTH_ALIAS_NAMES
:group: Compiler frontend
:END:

#+begin_src til
WIDTH_ALIAS_NAMES is a Array__Str
#+end_src

* _WIDTH_ALIAS_NAMES_ek
:PROPERTIES:
:CUSTOM_ID: til.src.self.context._WIDTH_ALIAS_NAMES_ek
:group: Compiler frontend
:END:

#+begin_src til
_WIDTH_ALIAS_NAMES_ek is a I64
#+end_src

* is_scalar_type_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_scalar_type_name
:group: Compiler frontend
:END:

#+begin_src til
func is_scalar_type_name(n: Str) returns shallow Bool
#+end_src

* resolved_type_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.resolved_type_name
:group: Compiler frontend
:END:

#+begin_src til
func resolved_type_name(t: Type) returns Str
#+end_src

* unsigned_prim_name_for_width
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.unsigned_prim_name_for_width
:group: Compiler frontend
:END:

#+begin_src til
func unsigned_prim_name_for_width(shallow w: USize) returns Str
#+end_src

* unsigned_prim_width_for_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.unsigned_prim_width_for_name
:group: Compiler frontend
:END:

#+begin_src til
func unsigned_prim_width_for_name(name: Str) returns shallow USize
#+end_src

* host_usize_pname
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.host_usize_pname
:group: Compiler frontend
:END:

#+begin_src til
func host_usize_pname() returns Str
#+end_src

* host_uptr_pname
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.host_uptr_pname
:group: Compiler frontend
:END:

#+begin_src til
func host_uptr_pname() returns Str
#+end_src

* ENUM_PAYLOAD_OFFSET
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.ENUM_PAYLOAD_OFFSET
:group: Compiler frontend
:END:

#+begin_src til
ENUM_PAYLOAD_OFFSET is a U32
#+end_src

* size_intrinsics_use_sizeof
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.size_intrinsics_use_sizeof
:group: Compiler frontend
:END:

#+begin_src til
func size_intrinsics_use_sizeof(ctx: Context) returns shallow Bool
#+end_src

* constfolder_classify_stmt
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.constfolder_classify_stmt
:group: Compiler frontend
:END:

#+begin_src til
func constfolder_classify_stmt(stmt: Expr, mut ctx: Context)
#+end_src

* make_method_call_fcall
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.make_method_call_fcall
:group: Compiler frontend
:END:

#+begin_src til
func make_method_call_fcall(own recv: Expr, method: Str, shallow line: U32, shallow col: U32) returns Expr
#+end_src

* make_get_payload_fcall
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.make_get_payload_fcall
:group: Compiler frontend
:END:

#+begin_src til
func make_get_payload_fcall(own arg: Expr, shallow payload_type: Type, shallow line: U32, shallow col: U32) returns Expr
#+end_src

* make_is_tag_check_fcall
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.make_is_tag_check_fcall
:group: Compiler frontend
:END:

#+begin_src til
func make_is_tag_check_fcall(arg_name: Str, ename: Str, vname: Str, shallow tag_size: USize, shallow line: U32, shallow col: U32) returns Expr
#+end_src

* make_payload_ref_decl
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.make_payload_ref_decl
:group: Compiler frontend
:END:

#+begin_src til
func make_payload_ref_decl(own arg: Expr, bind_name: Str, vtype: Str, shallow line: U32, shallow col: U32, shallow is_mut: Bool, shallow is_ref_payload: Bool, shallow used: Bool) returns Expr
#+end_src

* make_payload_ref_decls
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.make_payload_ref_decls
:group: Compiler frontend
:END:

#+begin_src til
func make_payload_ref_decls(own arg: Expr, bind_name: Str, fresh_name: Str, vtype: Str, shallow line: U32, shallow col: U32, shallow is_mut: Bool, shallow is_ref_payload: Bool, shallow used: Bool) returns Vec__Expr
#+end_src

* type_is_primitive
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.type_is_primitive
:group: Compiler frontend
:END:

#+begin_src til
func type_is_primitive(t: Type, p: Primitive) returns shallow Bool
#+end_src

* is_type_metatype
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_type_metatype
:group: Compiler frontend
:END:

#+begin_src til
func is_type_metatype(t: Type) returns shallow Bool
#+end_src

* funcptr_eq
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.funcptr_eq
:group: Compiler frontend
:END:

#+begin_src til
func funcptr_eq(a: Type, b: Type) returns shallow Bool
#+end_src

* type_is_callable_storage
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.type_is_callable_storage
:group: Compiler frontend
:END:

#+begin_src til
func type_is_callable_storage(t: Type) returns shallow Bool
#+end_src

* type_from_name_init
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.type_from_name_init
:group: Compiler frontend
:END:

#+begin_src til
func type_from_name_init(name: Str, scope: TypeScope) returns Type
#+end_src

* init_type_from_explicit_type
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.init_type_from_explicit_type
:group: Compiler frontend
:END:

#+begin_src til
func init_type_from_explicit_type(name: Str, scope: TypeScope) returns Type
#+end_src

* field_inferred_type_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.field_inferred_type_name
:group: Compiler frontend
:END:

#+begin_src til
func field_inferred_type_name(fd: Declaration) returns Str
#+end_src

* array_vec_elem_type_name
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.array_vec_elem_type_name
:group: Compiler frontend
:END:

#+begin_src til
func array_vec_elem_type_name(fcall: Expr) returns Str
#+end_src

* is_pod_type_for_clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_pod_type_for_clone
:group: Compiler frontend
:END:

#+begin_src til
func is_pod_type_for_clone(type_name: Str, scope: TypeScope) returns shallow Bool
#+end_src

* is_struct_or_enum
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_struct_or_enum
:group: Compiler frontend
:END:

#+begin_src til
func is_struct_or_enum(stmt: Expr) returns shallow Bool
#+end_src

* is_func_decl
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_func_decl
:group: Compiler frontend
:END:

#+begin_src til
func is_func_decl(stmt: Expr) returns shallow Bool
#+end_src

* is_def
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_def
:group: Compiler frontend
:END:

#+begin_src til
func is_def(stmt: Expr) returns shallow Bool
#+end_src

* member_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.member_get
:group: Compiler frontend
:END:

#+begin_src til
func member_get(idx: Map__Str_USize, v: Vec__Declaration, name: Str) returns shallow Option__ref_Declaration
#+end_src

* def_ns_decls
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.def_ns_decls
:group: Compiler frontend
:END:

#+begin_src til
func def_ns_decls(sdef: Expr) returns ref Vec__Declaration
#+end_src

* def_ns_name_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.def_ns_name_at
:group: Compiler frontend
:END:

#+begin_src til
func def_ns_name_at(sdef: Expr, i: USize) returns ref Str
#+end_src

* def_ns_has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.def_ns_has
:group: Compiler frontend
:END:

#+begin_src til
func def_ns_has(sdef: Expr, name: Str) returns shallow Bool
#+end_src

* def_ns_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.def_ns_get
:group: Compiler frontend
:END:

#+begin_src til
func def_ns_get(sdef: Expr, name: Str) returns shallow Option__ref_Declaration
#+end_src

* def_ns_pos
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.def_ns_pos
:group: Compiler frontend
:END:

#+begin_src til
func def_ns_pos(sdef: Expr, name: Str) returns shallow I64
#+end_src

* ns_const_eval_order
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.ns_const_eval_order
:group: Compiler frontend
:END:

#+begin_src til
func ns_const_eval_order(tname: Str, sdef: Expr) returns Vec__I64
#+end_src

* enum_has_payloads
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_has_payloads
:group: Compiler frontend
:END:

#+begin_src til
func enum_has_payloads(enum_def: Expr) returns shallow Bool
#+end_src

* enum_tag_type
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_tag_type
:group: Compiler frontend
:END:

#+begin_src til
func enum_tag_type(enum_def: Expr) returns Str
#+end_src

* enum_tag_size
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_tag_size
:group: Compiler frontend
:END:

#+begin_src til
func enum_tag_size(enum_def: Expr) returns shallow USize
#+end_src

* enum_variant_tag
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_tag
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_tag(enum_def: Expr, variant_name: Str) returns shallow I64
#+end_src

* enum_variant_tag_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_tag_at
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_tag_at(enum_def: Expr, shallow idx: I32) returns shallow I64
#+end_src

* enum_variant_index
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_index
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_index(enum_def: Expr, variant_name: Str) returns shallow I32
#+end_src

* enum_variant_type
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_type
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_type(enum_def: Expr, shallow idx: I32) returns Str
#+end_src

* enum_variant_payload_const
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_payload_const
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_payload_const(enum_def: Expr, shallow idx: I32) returns shallow Bool
#+end_src

* niche_payload_is_owned_box
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.niche_payload_is_owned_box
:group: Compiler frontend
:END:

#+begin_src til
func niche_payload_is_owned_box(vtype: Str) returns shallow Bool
#+end_src

* enum_variant_payload_funcsig
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_payload_funcsig
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_payload_funcsig(enum_def: Expr, shallow idx: I32) returns shallow Bool
#+end_src

* enum_is_niche
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_is_niche
:group: Compiler frontend
:END:

#+begin_src til
func enum_is_niche(enum_def: Expr) returns shallow Bool
#+end_src

* enum_niche_some_index
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_niche_some_index
:group: Compiler frontend
:END:

#+begin_src til
func enum_niche_some_index(enum_def: Expr) returns shallow I32
#+end_src

* enum_variant_payload_ref
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_variant_payload_ref
:group: Compiler frontend
:END:

#+begin_src til
func enum_variant_payload_ref(enum_def: Expr, shallow idx: I32) returns shallow Bool
#+end_src

* enum_payload_is_pointer_slot
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_payload_is_pointer_slot
:group: Compiler frontend
:END:

#+begin_src til
func enum_payload_is_pointer_slot(enum_def: Expr, shallow idx: I32) returns shallow Bool
#+end_src

* enum_has_ref_payload
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.enum_has_ref_payload
:group: Compiler frontend
:END:

#+begin_src til
func enum_has_ref_payload(enum_def: Expr) returns shallow Bool
#+end_src

* rhs_is_allocator_call
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.rhs_is_allocator_call
:group: Compiler frontend
:END:

#+begin_src til
func rhs_is_allocator_call(e: Expr) returns shallow Bool
#+end_src

* fn_never_returns
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.fn_never_returns
:group: Compiler frontend
:END:

#+begin_src til
func fn_never_returns(body: Expr) returns shallow Bool
#+end_src

* stmt_always_diverges
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.stmt_always_diverges
:group: Compiler frontend
:END:

#+begin_src til
func stmt_always_diverges(e: Expr) returns shallow Bool
#+end_src

* lazy_body_inlinable
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.lazy_body_inlinable
:group: Compiler frontend
:END:

#+begin_src til
func lazy_body_inlinable(body: Expr) returns shallow Bool
#+end_src

* lazy_body_split_point
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.lazy_body_split_point
:group: Compiler frontend
:END:

#+begin_src til
func lazy_body_split_point(body: Expr) returns shallow I32
#+end_src

* lazy_body_stmt_inlinable
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.lazy_body_stmt_inlinable
:group: Compiler frontend
:END:

#+begin_src til
func lazy_body_stmt_inlinable(body: Expr) returns shallow Bool
#+end_src

* lazy_variadic_body_unrollable
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.lazy_variadic_body_unrollable
:group: Compiler frontend
:END:

#+begin_src til
func lazy_variadic_body_unrollable(body: Expr, vname: Str) returns shallow Bool
#+end_src

* is_primitive
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_primitive
:group: Compiler frontend
:END:

#+begin_src til
func is_primitive(t: Type) returns shallow Bool
#+end_src

* is_funcptr
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_funcptr
:group: Compiler frontend
:END:

#+begin_src til
func is_funcptr(t: Type) returns shallow Bool
#+end_src

* is_aggregate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_aggregate
:group: Compiler frontend
:END:

#+begin_src til
func is_aggregate(t: Type) returns shallow Bool
#+end_src

* is_range_new_call
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.is_range_new_call
:group: Compiler frontend
:END:

#+begin_src til
func is_range_new_call(e: Expr) returns shallow Bool
#+end_src

* ast_print
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.ast_print
:group: Compiler frontend
:END:

#+begin_src til
proc ast_print(e: Expr, shallow indent: U32)
#+end_src

* fcall_kwargs_count
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.fcall_kwargs_count
:group: Compiler frontend
:END:

#+begin_src til
func fcall_kwargs_count(fcall: Expr) returns shallow USize
#+end_src

* fcall_variadic_count
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.fcall_variadic_count
:group: Compiler frontend
:END:

#+begin_src til
func fcall_variadic_count(fcall: Expr, shallow nparam: USize, shallow callee_has_kwargs: Bool) returns shallow USize
#+end_src

* qname_cmp
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.qname_cmp
:group: Compiler frontend
:END:

#+begin_src til
func qname_cmp(key: Str, a: Str, sep: Str, b: Str) returns shallow I64
#+end_src

* qname_keys_index
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.qname_keys_index
:group: Compiler frontend
:END:

#+begin_src til
func qname_keys_index(keys: Vec__Str, a: Str, sep: Str, b: Str) returns shallow I64
#+end_src

* qname_in_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.qname_in_set
:group: Compiler frontend
:END:

#+begin_src til
func qname_in_set(s: Set__Str, a: Str, sep: Str, b: Str) returns shallow Bool
#+end_src

* pod_struct_is
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.pod_struct_is
:group: Compiler frontend
:END:

#+begin_src til
func pod_struct_is(struct_name: Str, scope: TypeScope) returns shallow Bool
#+end_src

* pod_ctor_args_safe
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.pod_ctor_args_safe
:group: Compiler frontend
:END:

#+begin_src til
func pod_ctor_args_safe(ctor: Expr, scope: TypeScope) returns shallow Bool
#+end_src

* pod_ctor_decl_is_stack
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.pod_ctor_decl_is_stack
:group: Compiler frontend
:END:

#+begin_src til
func pod_ctor_decl_is_stack(rhs: Expr, scope: TypeScope) returns shallow Bool
#+end_src

* Vec__BorrowRoot
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__BorrowRoot {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__BorrowRoot
    func with_capacity(shallow n: USize) returns Vec__BorrowRoot
    func len(self: Vec__BorrowRoot) returns shallow USize
    func clear(mut self: Vec__BorrowRoot)
    func delete_marked(mut self: Vec__BorrowRoot, marks: Dynamic)
    func push(mut self: Vec__BorrowRoot, own val: BorrowRoot)
    func append(mut self: Vec__BorrowRoot, own other: Vec__BorrowRoot)
    func take(mut self: Vec__BorrowRoot) returns Vec__BorrowRoot
    func move_to(own self: Vec__BorrowRoot, mut dest: Vec__BorrowRoot)
    func move_from(mut self: Vec__BorrowRoot, own other: Vec__BorrowRoot)
    func unsafe_get(self: Vec__BorrowRoot, i: USize) returns ref BorrowRoot
    func get(self: Vec__BorrowRoot, i: USize, mut _err_kind: I64) returns ref BorrowRoot
    func pop(mut self: Vec__BorrowRoot) returns BorrowRoot
    func remove_at(mut self: Vec__BorrowRoot, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__BorrowRoot, shallow n: USize) returns Vec__BorrowRoot
    func truncate(mut self: Vec__BorrowRoot, shallow n: USize)
    func unsafe_set(mut self: Vec__BorrowRoot, shallow i: USize, own val: BorrowRoot)
    func set(mut self: Vec__BorrowRoot, shallow i: USize, own val: BorrowRoot, mut _err_kind: I64)
    func push_take(mut self: Vec__BorrowRoot, mut src: Vec__BorrowRoot, shallow i: USize)
    func delete(own self: Vec__BorrowRoot, shallow call_free: Bool)
    func clone(self: Vec__BorrowRoot) returns Vec__BorrowRoot
    func size() returns shallow USize
}
#+end_src

* Vec__BorrowRoot.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__BorrowRoot.new() returns Vec__BorrowRoot
#+end_src

* Vec__BorrowRoot.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__BorrowRoot.with_capacity(shallow n: USize) returns Vec__BorrowRoot
#+end_src

* Vec__BorrowRoot.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__BorrowRoot.clear(mut self: Vec__BorrowRoot)
#+end_src

* Vec__BorrowRoot.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__BorrowRoot.delete_marked(mut self: Vec__BorrowRoot, marks: Dynamic)
#+end_src

* Vec__BorrowRoot.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__BorrowRoot.push(mut self: Vec__BorrowRoot, own val: BorrowRoot)
#+end_src

* Vec__BorrowRoot.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__BorrowRoot.append(mut self: Vec__BorrowRoot, own other: Vec__BorrowRoot)
#+end_src

* Vec__BorrowRoot.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__BorrowRoot.take(mut self: Vec__BorrowRoot) returns Vec__BorrowRoot
#+end_src

* Vec__BorrowRoot.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__BorrowRoot.move_to(own self: Vec__BorrowRoot, mut dest: Vec__BorrowRoot)
#+end_src

* Vec__BorrowRoot.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__BorrowRoot.move_from(mut self: Vec__BorrowRoot, own other: Vec__BorrowRoot)
#+end_src

* Vec__BorrowRoot.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__BorrowRoot.unsafe_get(self: Vec__BorrowRoot, i: USize) returns ref BorrowRoot
#+end_src

* Vec__BorrowRoot.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__BorrowRoot.get(self: Vec__BorrowRoot, i: USize, mut _err_kind: I64) returns ref BorrowRoot
#+end_src

* Vec__BorrowRoot.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__BorrowRoot.pop(mut self: Vec__BorrowRoot) returns BorrowRoot
#+end_src

* Vec__BorrowRoot.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__BorrowRoot.remove_at(mut self: Vec__BorrowRoot, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__BorrowRoot.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__BorrowRoot.take_prefix(mut self: Vec__BorrowRoot, shallow n: USize) returns Vec__BorrowRoot
#+end_src

* Vec__BorrowRoot.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__BorrowRoot.truncate(mut self: Vec__BorrowRoot, shallow n: USize)
#+end_src

* Vec__BorrowRoot.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__BorrowRoot.unsafe_set(mut self: Vec__BorrowRoot, shallow i: USize, own val: BorrowRoot)
#+end_src

* Vec__BorrowRoot.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__BorrowRoot.set(mut self: Vec__BorrowRoot, shallow i: USize, own val: BorrowRoot, mut _err_kind: I64)
#+end_src

* Vec__BorrowRoot.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__BorrowRoot.push_take(mut self: Vec__BorrowRoot, mut src: Vec__BorrowRoot, shallow i: USize)
#+end_src

* Vec__BorrowRoot.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__BorrowRoot.delete(own self: Vec__BorrowRoot, shallow call_free: Bool)
#+end_src

* Vec__BorrowRoot.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__BorrowRoot.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__BorrowRoot.clone(self: Vec__BorrowRoot) returns Vec__BorrowRoot
#+end_src

* Map__Str_TypeBinding
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_TypeBinding {
    mut keys : Vec__Str
    mut values : Vec__TypeBinding
namespace:
    func new() returns Map__Str_TypeBinding
    func with_capacity(shallow n: USize) returns Map__Str_TypeBinding
    func len(self: Map__Str_TypeBinding) returns shallow USize
    func clear(mut self: Map__Str_TypeBinding)
    func has(self: Map__Str_TypeBinding, key: Str) returns shallow Bool
    func get(self: Map__Str_TypeBinding, key: Str, mut _err_kind: I64) returns ref TypeBinding
    func set(mut self: Map__Str_TypeBinding, key: Str, own val: TypeBinding)
    func remove(mut self: Map__Str_TypeBinding, key: Str) returns shallow Bool
    func delete(own self: Map__Str_TypeBinding, shallow call_free: Bool)
    func clone(self: Map__Str_TypeBinding) returns Map__Str_TypeBinding
    func hash(self: Map__Str_TypeBinding, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_TypeBinding.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_TypeBinding.new() returns Map__Str_TypeBinding
#+end_src

* Map__Str_TypeBinding.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_TypeBinding.with_capacity(shallow n: USize) returns Map__Str_TypeBinding
#+end_src

* Map__Str_TypeBinding.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_TypeBinding.clear(mut self: Map__Str_TypeBinding)
#+end_src

* Map__Str_TypeBinding.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_TypeBinding.has(self: Map__Str_TypeBinding, key: Str) returns shallow Bool
#+end_src

* Map__Str_TypeBinding.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_TypeBinding.get(self: Map__Str_TypeBinding, key: Str, mut _err_kind: I64) returns ref TypeBinding
#+end_src

* Map__Str_TypeBinding.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_TypeBinding.set(mut self: Map__Str_TypeBinding, key: Str, own val: TypeBinding)
#+end_src

* Map__Str_TypeBinding.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_TypeBinding.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_TypeBinding.remove(mut self: Map__Str_TypeBinding, key: Str) returns shallow Bool
#+end_src

* Option__ref_TypeScope
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_TypeScope
:group: Compiler frontend
:END:

#+begin_src til
enum Option__ref_TypeScope {
    None
    Some: TypeScope
namespace:
    func is_some(shallow self: Option__ref_TypeScope) returns shallow Bool
    func is_none(shallow self: Option__ref_TypeScope) returns shallow Bool
    func unwrap_or(self: Option__ref_TypeScope, default_v: TypeScope) returns ref TypeScope
    func unwrap(self: Option__ref_TypeScope) returns ref TypeScope
    func take(own self: Option__ref_TypeScope) returns ref TypeScope
    ext_func None() returns shallow Option__ref_TypeScope
    ext_func Some(val: TypeScope) returns shallow Option__ref_TypeScope
    core_func is(self: Option__ref_TypeScope, other: Option__ref_TypeScope) returns shallow Bool
    func delete(own self: Option__ref_TypeScope, shallow call_free: Bool)
    func clone(self: Option__ref_TypeScope) returns shallow Option__ref_TypeScope
    func size() returns shallow USize
}
#+end_src

* Option__ref_TypeScope.unwrap
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_TypeScope.unwrap
:group: Compiler frontend
:END:

The contained value; PANICS on None, ala Rust's unwrap. For the
`if o.is_none() { ... }` + `o.unwrap()` idiom, where the caller
has already handled absence and unwrapping cannot fail. Prefer
a `switch` when both arms do real work, and `unwrap_or` when a
default is meaningful -- reach for this only when None is
unreachable by construction, so it fails loudly if that ever
stops being true.

#+begin_src til
func Option__ref_TypeScope.unwrap(self: Option__ref_TypeScope) returns ref TypeScope
#+end_src

* Option__ref_TypeScope.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_TypeScope.take
:group: Compiler frontend
:END:

The contained value, MOVED out; PANICS on None, like unwrap.
unwrap only BORROWS self, so for an owned payload it must hand
back a deep clone; this consumes the Option instead, so the
payload's bytes move and only the enum shell is freed (#321).
Use it wherever the caller takes ownership of what the Option
held -- an `Option(Expr)` returned by an AST helper, say, where
a clone would copy a whole subtree and break node identity.

#+begin_src til
func Option__ref_TypeScope.take(own self: Option__ref_TypeScope) returns ref TypeScope
#+end_src

* Option__ref_TypeBinding
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_TypeBinding
:group: Compiler frontend
:END:

#+begin_src til
enum Option__ref_TypeBinding {
    None
    Some: TypeBinding
namespace:
    func is_some(shallow self: Option__ref_TypeBinding) returns shallow Bool
    func is_none(shallow self: Option__ref_TypeBinding) returns shallow Bool
    func unwrap_or(self: Option__ref_TypeBinding, default_v: TypeBinding) returns ref TypeBinding
    func unwrap(self: Option__ref_TypeBinding) returns ref TypeBinding
    func take(own self: Option__ref_TypeBinding) returns ref TypeBinding
    ext_func None() returns shallow Option__ref_TypeBinding
    ext_func Some(val: TypeBinding) returns shallow Option__ref_TypeBinding
    core_func is(self: Option__ref_TypeBinding, other: Option__ref_TypeBinding) returns shallow Bool
    func eq(shallow self: Option__ref_TypeBinding, shallow other: Option__ref_TypeBinding) returns shallow Bool
    func delete(own self: Option__ref_TypeBinding, shallow call_free: Bool)
    func to_str(shallow self: Option__ref_TypeBinding) returns Str
    func clone(self: Option__ref_TypeBinding) returns shallow Option__ref_TypeBinding
    func size() returns shallow USize
}
#+end_src

* Option__ref_TypeBinding.unwrap
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_TypeBinding.unwrap
:group: Compiler frontend
:END:

The contained value; PANICS on None, ala Rust's unwrap. For the
`if o.is_none() { ... }` + `o.unwrap()` idiom, where the caller
has already handled absence and unwrapping cannot fail. Prefer
a `switch` when both arms do real work, and `unwrap_or` when a
default is meaningful -- reach for this only when None is
unreachable by construction, so it fails loudly if that ever
stops being true.

#+begin_src til
func Option__ref_TypeBinding.unwrap(self: Option__ref_TypeBinding) returns ref TypeBinding
#+end_src

* Option__ref_TypeBinding.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_TypeBinding.take
:group: Compiler frontend
:END:

The contained value, MOVED out; PANICS on None, like unwrap.
unwrap only BORROWS self, so for an owned payload it must hand
back a deep clone; this consumes the Option instead, so the
payload's bytes move and only the enum shell is freed (#321).
Use it wherever the caller takes ownership of what the Option
held -- an `Option(Expr)` returned by an AST helper, say, where
a clone would copy a whole subtree and break node identity.

#+begin_src til
func Option__ref_TypeBinding.take(own self: Option__ref_TypeBinding) returns ref TypeBinding
#+end_src

* Map__Str_U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_U32 {
    mut keys : Vec__Str
    mut values : Vec__U32
namespace:
    func new() returns Map__Str_U32
    func with_capacity(shallow n: USize) returns Map__Str_U32
    func len(self: Map__Str_U32) returns shallow USize
    func clear(mut self: Map__Str_U32)
    func has(self: Map__Str_U32, key: Str) returns shallow Bool
    func get(self: Map__Str_U32, key: Str, mut _err_kind: I64) returns ref U32
    func set(mut self: Map__Str_U32, key: Str, own val: U32)
    func remove(mut self: Map__Str_U32, key: Str) returns shallow Bool
    func delete(own self: Map__Str_U32, shallow call_free: Bool)
    func clone(self: Map__Str_U32) returns Map__Str_U32
    func hash(self: Map__Str_U32, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_U32.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_U32.new() returns Map__Str_U32
#+end_src

* Map__Str_U32.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_U32.with_capacity(shallow n: USize) returns Map__Str_U32
#+end_src

* Map__Str_U32.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_U32.clear(mut self: Map__Str_U32)
#+end_src

* Map__Str_U32.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_U32.has(self: Map__Str_U32, key: Str) returns shallow Bool
#+end_src

* Map__Str_U32.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_U32.get(self: Map__Str_U32, key: Str, mut _err_kind: I64) returns ref U32
#+end_src

* Map__Str_U32.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_U32.set(mut self: Map__Str_U32, key: Str, own val: U32)
#+end_src

* Map__Str_U32.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_U32.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_U32.remove(mut self: Map__Str_U32, key: Str) returns shallow Bool
#+end_src

* Map__Str_Mode
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_Mode {
    mut keys : Vec__Str
    mut values : Vec__Mode
namespace:
    func new() returns Map__Str_Mode
    func with_capacity(shallow n: USize) returns Map__Str_Mode
    func len(self: Map__Str_Mode) returns shallow USize
    func clear(mut self: Map__Str_Mode)
    func has(self: Map__Str_Mode, key: Str) returns shallow Bool
    func get(self: Map__Str_Mode, key: Str, mut _err_kind: I64) returns ref Mode
    func set(mut self: Map__Str_Mode, key: Str, own val: Mode)
    func remove(mut self: Map__Str_Mode, key: Str) returns shallow Bool
    func delete(own self: Map__Str_Mode, shallow call_free: Bool)
    func clone(self: Map__Str_Mode) returns Map__Str_Mode
    func hash(self: Map__Str_Mode, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_Mode.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_Mode.new() returns Map__Str_Mode
#+end_src

* Map__Str_Mode.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_Mode.with_capacity(shallow n: USize) returns Map__Str_Mode
#+end_src

* Map__Str_Mode.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_Mode.clear(mut self: Map__Str_Mode)
#+end_src

* Map__Str_Mode.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_Mode.has(self: Map__Str_Mode, key: Str) returns shallow Bool
#+end_src

* Map__Str_Mode.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_Mode.get(self: Map__Str_Mode, key: Str, mut _err_kind: I64) returns ref Mode
#+end_src

* Map__Str_Mode.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_Mode.set(mut self: Map__Str_Mode, key: Str, own val: Mode)
#+end_src

* Map__Str_Mode.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Mode.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_Mode.remove(mut self: Map__Str_Mode, key: Str) returns shallow Bool
#+end_src

* Map__Str_FuncType
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_FuncType {
    mut keys : Vec__Str
    mut values : Vec__FuncType
namespace:
    func new() returns Map__Str_FuncType
    func with_capacity(shallow n: USize) returns Map__Str_FuncType
    func len(self: Map__Str_FuncType) returns shallow USize
    func clear(mut self: Map__Str_FuncType)
    func has(self: Map__Str_FuncType, key: Str) returns shallow Bool
    func get(self: Map__Str_FuncType, key: Str, mut _err_kind: I64) returns ref FuncType
    func set(mut self: Map__Str_FuncType, key: Str, own val: FuncType)
    func remove(mut self: Map__Str_FuncType, key: Str) returns shallow Bool
    func delete(own self: Map__Str_FuncType, shallow call_free: Bool)
    func clone(self: Map__Str_FuncType) returns Map__Str_FuncType
    func hash(self: Map__Str_FuncType, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_FuncType.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_FuncType.new() returns Map__Str_FuncType
#+end_src

* Map__Str_FuncType.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_FuncType.with_capacity(shallow n: USize) returns Map__Str_FuncType
#+end_src

* Map__Str_FuncType.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_FuncType.clear(mut self: Map__Str_FuncType)
#+end_src

* Map__Str_FuncType.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_FuncType.has(self: Map__Str_FuncType, key: Str) returns shallow Bool
#+end_src

* Map__Str_FuncType.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_FuncType.get(self: Map__Str_FuncType, key: Str, mut _err_kind: I64) returns ref FuncType
#+end_src

* Map__Str_FuncType.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_FuncType.set(mut self: Map__Str_FuncType, key: Str, own val: FuncType)
#+end_src

* Map__Str_FuncType.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FuncType.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_FuncType.remove(mut self: Map__Str_FuncType, key: Str) returns shallow Bool
#+end_src

* Option__Scope
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__Scope
:group: Compiler frontend
:END:

#+begin_src til
enum Option__Scope {
    None
    Some: Scope
namespace:
    func is_some(shallow self: Option__Scope) returns shallow Bool
    func is_none(shallow self: Option__Scope) returns shallow Bool
    func unwrap_or(shallow self: Option__Scope, default_v: Scope) returns Scope
    func unwrap(shallow self: Option__Scope) returns Scope
    func take(own self: Option__Scope) returns Scope
    ext_func None() returns shallow Option__Scope
    ext_func Some(own val: Scope) returns shallow Option__Scope
    core_func is(self: Option__Scope, other: Option__Scope) returns shallow Bool
    func delete(own self: Option__Scope, shallow call_free: Bool)
    func clone(shallow self: Option__Scope) returns shallow Option__Scope
    func size() returns shallow USize
}
#+end_src

* Option__Scope.unwrap
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__Scope.unwrap
:group: Compiler frontend
:END:

The contained value; PANICS on None, ala Rust's unwrap. For the
`if o.is_none() { ... }` + `o.unwrap()` idiom, where the caller
has already handled absence and unwrapping cannot fail. Prefer
a `switch` when both arms do real work, and `unwrap_or` when a
default is meaningful -- reach for this only when None is
unreachable by construction, so it fails loudly if that ever
stops being true.

#+begin_src til
func Option__Scope.unwrap(shallow self: Option__Scope) returns Scope
#+end_src

* Option__Scope.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__Scope.take
:group: Compiler frontend
:END:

The contained value, MOVED out; PANICS on None, like unwrap.
unwrap only BORROWS self, so for an owned payload it must hand
back a deep clone; this consumes the Option instead, so the
payload's bytes move and only the enum shell is freed (#321).
Use it wherever the caller takes ownership of what the Option
held -- an `Option(Expr)` returned by an AST helper, say, where
a clone would copy a whole subtree and break node identity.

#+begin_src til
func Option__Scope.take(own self: Option__Scope) returns Scope
#+end_src

* Map__Str_ImportUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_ImportUnit {
    mut keys : Vec__Str
    mut values : Vec__ImportUnit
namespace:
    func new() returns Map__Str_ImportUnit
    func with_capacity(shallow n: USize) returns Map__Str_ImportUnit
    func len(self: Map__Str_ImportUnit) returns shallow USize
    func clear(mut self: Map__Str_ImportUnit)
    func has(self: Map__Str_ImportUnit, key: Str) returns shallow Bool
    func get(self: Map__Str_ImportUnit, key: Str, mut _err_kind: I64) returns ref ImportUnit
    func set(mut self: Map__Str_ImportUnit, key: Str, own val: ImportUnit)
    func remove(mut self: Map__Str_ImportUnit, key: Str) returns shallow Bool
    func delete(own self: Map__Str_ImportUnit, shallow call_free: Bool)
    func clone(self: Map__Str_ImportUnit) returns Map__Str_ImportUnit
    func hash(self: Map__Str_ImportUnit, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_ImportUnit.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_ImportUnit.new() returns Map__Str_ImportUnit
#+end_src

* Map__Str_ImportUnit.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_ImportUnit.with_capacity(shallow n: USize) returns Map__Str_ImportUnit
#+end_src

* Map__Str_ImportUnit.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_ImportUnit.clear(mut self: Map__Str_ImportUnit)
#+end_src

* Map__Str_ImportUnit.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_ImportUnit.has(self: Map__Str_ImportUnit, key: Str) returns shallow Bool
#+end_src

* Map__Str_ImportUnit.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_ImportUnit.get(self: Map__Str_ImportUnit, key: Str, mut _err_kind: I64) returns ref ImportUnit
#+end_src

* Map__Str_ImportUnit.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_ImportUnit.set(mut self: Map__Str_ImportUnit, key: Str, own val: ImportUnit)
#+end_src

* Map__Str_ImportUnit.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ImportUnit.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_ImportUnit.remove(mut self: Map__Str_ImportUnit, key: Str) returns shallow Bool
#+end_src

* Map__Str_Expr
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_Expr {
    mut keys : Vec__Str
    mut values : Vec__Expr
namespace:
    func new() returns Map__Str_Expr
    func with_capacity(shallow n: USize) returns Map__Str_Expr
    func len(self: Map__Str_Expr) returns shallow USize
    func clear(mut self: Map__Str_Expr)
    func has(self: Map__Str_Expr, key: Str) returns shallow Bool
    func get(self: Map__Str_Expr, key: Str, mut _err_kind: I64) returns ref Expr
    func set(mut self: Map__Str_Expr, key: Str, own val: Expr)
    func remove(mut self: Map__Str_Expr, key: Str) returns shallow Bool
    func delete(own self: Map__Str_Expr, shallow call_free: Bool)
    func clone(self: Map__Str_Expr) returns Map__Str_Expr
    func hash(self: Map__Str_Expr, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_Expr.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_Expr.new() returns Map__Str_Expr
#+end_src

* Map__Str_Expr.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_Expr.with_capacity(shallow n: USize) returns Map__Str_Expr
#+end_src

* Map__Str_Expr.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_Expr.clear(mut self: Map__Str_Expr)
#+end_src

* Map__Str_Expr.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_Expr.has(self: Map__Str_Expr, key: Str) returns shallow Bool
#+end_src

* Map__Str_Expr.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_Expr.get(self: Map__Str_Expr, key: Str, mut _err_kind: I64) returns ref Expr
#+end_src

* Map__Str_Expr.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_Expr.set(mut self: Map__Str_Expr, key: Str, own val: Expr)
#+end_src

* Map__Str_Expr.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Expr.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_Expr.remove(mut self: Map__Str_Expr, key: Str) returns shallow Bool
#+end_src

* Map__Str_StructLayout
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_StructLayout {
    mut keys : Vec__Str
    mut values : Vec__StructLayout
namespace:
    func new() returns Map__Str_StructLayout
    func with_capacity(shallow n: USize) returns Map__Str_StructLayout
    func len(self: Map__Str_StructLayout) returns shallow USize
    func clear(mut self: Map__Str_StructLayout)
    func has(self: Map__Str_StructLayout, key: Str) returns shallow Bool
    func get(self: Map__Str_StructLayout, key: Str, mut _err_kind: I64) returns ref StructLayout
    func set(mut self: Map__Str_StructLayout, key: Str, own val: StructLayout)
    func remove(mut self: Map__Str_StructLayout, key: Str) returns shallow Bool
    func delete(own self: Map__Str_StructLayout, shallow call_free: Bool)
    func clone(self: Map__Str_StructLayout) returns Map__Str_StructLayout
    func hash(self: Map__Str_StructLayout, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_StructLayout.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_StructLayout.new() returns Map__Str_StructLayout
#+end_src

* Map__Str_StructLayout.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_StructLayout.with_capacity(shallow n: USize) returns Map__Str_StructLayout
#+end_src

* Map__Str_StructLayout.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_StructLayout.clear(mut self: Map__Str_StructLayout)
#+end_src

* Map__Str_StructLayout.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_StructLayout.has(self: Map__Str_StructLayout, key: Str) returns shallow Bool
#+end_src

* Map__Str_StructLayout.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_StructLayout.get(self: Map__Str_StructLayout, key: Str, mut _err_kind: I64) returns ref StructLayout
#+end_src

* Map__Str_StructLayout.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_StructLayout.set(mut self: Map__Str_StructLayout, key: Str, own val: StructLayout)
#+end_src

* Map__Str_StructLayout.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_StructLayout.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_StructLayout.remove(mut self: Map__Str_StructLayout, key: Str) returns shallow Bool
#+end_src

* Set__U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32
:group: Compiler frontend
:END:

#+begin_src til
struct Set__U32 {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Set__U32
    func with_capacity(shallow n: USize) returns Set__U32
    func len(self: Set__U32) returns shallow USize
    func clear(mut self: Set__U32)
    func has(self: Set__U32, shallow val: U32) returns shallow Bool
    func add(mut self: Set__U32, own val: U32)
    func remove(mut self: Set__U32, shallow val: U32) returns shallow Bool
    func delete(own self: Set__U32, shallow call_free: Bool)
    func clone(self: Set__U32) returns Set__U32
    func size() returns shallow USize
}
#+end_src

* Set__U32.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32.new
:group: Compiler frontend
:END:

Create an empty Set. T must implement `cmp`. Allocates nothing:
like Vec.new, an empty Set is lazy (cap 0) and `add` seeds the
first slot. Short-lived empty Sets are common -- the garbager
builds four per statement for its per-statement name facts, most
of them never added to.

#+begin_src til
func Set__U32.new() returns Set__U32
#+end_src

* Set__U32.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32.with_capacity
:group: Compiler frontend
:END:

Create an empty Set pre-sized for `n` elements, so adding up to
`n` reallocs zero times (Vec.with_capacity's counterpart). Use it
where the final size is known or well predicted; `new()` grows
0->1->2->4, which is the right default but costs a realloc per
doubling. n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Set__U32.with_capacity(shallow n: USize) returns Set__U32
#+end_src

* Set__U32.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32.clear
:group: Compiler frontend
:END:

Drop all live elements but keep the allocated storage so the Set can be reused.

#+begin_src til
func Set__U32.clear(mut self: Set__U32)
#+end_src

* Set__U32.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32.has
:group: Compiler frontend
:END:

True if the set contains an element equal to `val`.

#+begin_src til
func Set__U32.has(self: Set__U32, shallow val: U32) returns shallow Bool
#+end_src

* Set__U32.add
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32.add
:group: Compiler frontend
:END:

Insert `val` into the set (sorted order). If an equal element
already exists, `val` is dropped. `val` is owned.

#+begin_src til
func Set__U32.add(mut self: Set__U32, own val: U32)
#+end_src

* Set__U32.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Set__U32.remove
:group: Compiler frontend
:END:

Remove the element equal to `val`, dropping it. Returns false if
absent (Map.remove's counterpart: `add` is the Set's `set`, so
this is its removal half). Later elements shift left to keep the
storage sorted and dense.

#+begin_src til
func Set__U32.remove(mut self: Set__U32, shallow val: U32) returns shallow Bool
#+end_src

* Map__Str_call_Vec_Str
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_call_Vec_Str {
    mut keys : Vec__Str
    mut values : Vec__call_Vec_Str
namespace:
    func new() returns Map__Str_call_Vec_Str
    func with_capacity(shallow n: USize) returns Map__Str_call_Vec_Str
    func len(self: Map__Str_call_Vec_Str) returns shallow USize
    func clear(mut self: Map__Str_call_Vec_Str)
    func has(self: Map__Str_call_Vec_Str, key: Str) returns shallow Bool
    func get(self: Map__Str_call_Vec_Str, key: Str, mut _err_kind: I64) returns ref Vec__Str
    func set(mut self: Map__Str_call_Vec_Str, key: Str, own val: Vec__Str)
    func remove(mut self: Map__Str_call_Vec_Str, key: Str) returns shallow Bool
    func delete(own self: Map__Str_call_Vec_Str, shallow call_free: Bool)
    func clone(self: Map__Str_call_Vec_Str) returns Map__Str_call_Vec_Str
    func hash(self: Map__Str_call_Vec_Str, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_call_Vec_Str.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_call_Vec_Str.new() returns Map__Str_call_Vec_Str
#+end_src

* Map__Str_call_Vec_Str.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_call_Vec_Str.with_capacity(shallow n: USize) returns Map__Str_call_Vec_Str
#+end_src

* Map__Str_call_Vec_Str.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_call_Vec_Str.clear(mut self: Map__Str_call_Vec_Str)
#+end_src

* Map__Str_call_Vec_Str.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_call_Vec_Str.has(self: Map__Str_call_Vec_Str, key: Str) returns shallow Bool
#+end_src

* Map__Str_call_Vec_Str.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_call_Vec_Str.get(self: Map__Str_call_Vec_Str, key: Str, mut _err_kind: I64) returns ref Vec__Str
#+end_src

* Map__Str_call_Vec_Str.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_call_Vec_Str.set(mut self: Map__Str_call_Vec_Str, key: Str, own val: Vec__Str)
#+end_src

* Map__Str_call_Vec_Str.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_call_Vec_Str.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_call_Vec_Str.remove(mut self: Map__Str_call_Vec_Str, key: Str) returns shallow Bool
#+end_src

* Option__ref_Expr
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_Expr
:group: Compiler frontend
:END:

#+begin_src til
enum Option__ref_Expr {
    None
    Some: Expr
namespace:
    func is_some(shallow self: Option__ref_Expr) returns shallow Bool
    func is_none(shallow self: Option__ref_Expr) returns shallow Bool
    func unwrap_or(self: Option__ref_Expr, default_v: Expr) returns ref Expr
    func unwrap(self: Option__ref_Expr) returns ref Expr
    func take(own self: Option__ref_Expr) returns ref Expr
    ext_func None() returns shallow Option__ref_Expr
    ext_func Some(val: Expr) returns shallow Option__ref_Expr
    core_func is(self: Option__ref_Expr, other: Option__ref_Expr) returns shallow Bool
    func eq(shallow self: Option__ref_Expr, shallow other: Option__ref_Expr) returns shallow Bool
    func delete(own self: Option__ref_Expr, shallow call_free: Bool)
    func to_str(shallow self: Option__ref_Expr) returns Str
    func clone(self: Option__ref_Expr) returns shallow Option__ref_Expr
    func size() returns shallow USize
}
#+end_src

* Option__ref_Expr.unwrap
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_Expr.unwrap
:group: Compiler frontend
:END:

The contained value; PANICS on None, ala Rust's unwrap. For the
`if o.is_none() { ... }` + `o.unwrap()` idiom, where the caller
has already handled absence and unwrapping cannot fail. Prefer
a `switch` when both arms do real work, and `unwrap_or` when a
default is meaningful -- reach for this only when None is
unreachable by construction, so it fails loudly if that ever
stops being true.

#+begin_src til
func Option__ref_Expr.unwrap(self: Option__ref_Expr) returns ref Expr
#+end_src

* Option__ref_Expr.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_Expr.take
:group: Compiler frontend
:END:

The contained value, MOVED out; PANICS on None, like unwrap.
unwrap only BORROWS self, so for an owned payload it must hand
back a deep clone; this consumes the Option instead, so the
payload's bytes move and only the enum shell is freed (#321).
Use it wherever the caller takes ownership of what the Option
held -- an `Option(Expr)` returned by an AST helper, say, where
a clone would copy a whole subtree and break node identity.

#+begin_src til
func Option__ref_Expr.take(own self: Option__ref_Expr) returns ref Expr
#+end_src

* Map__Str_Dynamic
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_Dynamic {
    mut keys : Vec__Str
    mut values : Vec__Dynamic
namespace:
    func new() returns Map__Str_Dynamic
    func with_capacity(shallow n: USize) returns Map__Str_Dynamic
    func len(self: Map__Str_Dynamic) returns shallow USize
    func clear(mut self: Map__Str_Dynamic)
    func has(self: Map__Str_Dynamic, key: Str) returns shallow Bool
    func get(self: Map__Str_Dynamic, key: Str, mut _err_kind: I64) returns ref Dynamic
    func set(mut self: Map__Str_Dynamic, key: Str, own val: Dynamic)
    func remove(mut self: Map__Str_Dynamic, key: Str) returns shallow Bool
    func delete(own self: Map__Str_Dynamic, shallow call_free: Bool)
    func clone(self: Map__Str_Dynamic) returns Map__Str_Dynamic
    func hash(self: Map__Str_Dynamic, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_Dynamic.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_Dynamic.new() returns Map__Str_Dynamic
#+end_src

* Map__Str_Dynamic.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_Dynamic.with_capacity(shallow n: USize) returns Map__Str_Dynamic
#+end_src

* Map__Str_Dynamic.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_Dynamic.clear(mut self: Map__Str_Dynamic)
#+end_src

* Map__Str_Dynamic.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_Dynamic.has(self: Map__Str_Dynamic, key: Str) returns shallow Bool
#+end_src

* Map__Str_Dynamic.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_Dynamic.get(self: Map__Str_Dynamic, key: Str, mut _err_kind: I64) returns ref Dynamic
#+end_src

* Map__Str_Dynamic.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_Dynamic.set(mut self: Map__Str_Dynamic, key: Str, own val: Dynamic)
#+end_src

* Map__Str_Dynamic.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_Dynamic.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_Dynamic.remove(mut self: Map__Str_Dynamic, key: Str) returns shallow Bool
#+end_src

* Map__Str_FFIEntry
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_FFIEntry {
    mut keys : Vec__Str
    mut values : Vec__FFIEntry
namespace:
    func new() returns Map__Str_FFIEntry
    func with_capacity(shallow n: USize) returns Map__Str_FFIEntry
    func len(self: Map__Str_FFIEntry) returns shallow USize
    func clear(mut self: Map__Str_FFIEntry)
    func has(self: Map__Str_FFIEntry, key: Str) returns shallow Bool
    func get(self: Map__Str_FFIEntry, key: Str, mut _err_kind: I64) returns ref FFIEntry
    func set(mut self: Map__Str_FFIEntry, key: Str, own val: FFIEntry)
    func remove(mut self: Map__Str_FFIEntry, key: Str) returns shallow Bool
    func delete(own self: Map__Str_FFIEntry, shallow call_free: Bool)
    func clone(self: Map__Str_FFIEntry) returns Map__Str_FFIEntry
    func hash(self: Map__Str_FFIEntry, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_FFIEntry.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_FFIEntry.new() returns Map__Str_FFIEntry
#+end_src

* Map__Str_FFIEntry.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_FFIEntry.with_capacity(shallow n: USize) returns Map__Str_FFIEntry
#+end_src

* Map__Str_FFIEntry.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_FFIEntry.clear(mut self: Map__Str_FFIEntry)
#+end_src

* Map__Str_FFIEntry.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_FFIEntry.has(self: Map__Str_FFIEntry, key: Str) returns shallow Bool
#+end_src

* Map__Str_FFIEntry.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_FFIEntry.get(self: Map__Str_FFIEntry, key: Str, mut _err_kind: I64) returns ref FFIEntry
#+end_src

* Map__Str_FFIEntry.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_FFIEntry.set(mut self: Map__Str_FFIEntry, key: Str, own val: FFIEntry)
#+end_src

* Map__Str_FFIEntry.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_FFIEntry.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_FFIEntry.remove(mut self: Map__Str_FFIEntry, key: Str) returns shallow Bool
#+end_src

* Map__Str_ExprPtrBox
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox
:group: Compiler frontend
:END:

#+begin_src til
struct Map__Str_ExprPtrBox {
    mut keys : Vec__Str
    mut values : Vec__ExprPtrBox
namespace:
    func new() returns Map__Str_ExprPtrBox
    func with_capacity(shallow n: USize) returns Map__Str_ExprPtrBox
    func len(self: Map__Str_ExprPtrBox) returns shallow USize
    func clear(mut self: Map__Str_ExprPtrBox)
    func has(self: Map__Str_ExprPtrBox, key: Str) returns shallow Bool
    func get(self: Map__Str_ExprPtrBox, key: Str, mut _err_kind: I64) returns ref ExprPtrBox
    func set(mut self: Map__Str_ExprPtrBox, key: Str, own val: ExprPtrBox)
    func remove(mut self: Map__Str_ExprPtrBox, key: Str) returns shallow Bool
    func delete(own self: Map__Str_ExprPtrBox, shallow call_free: Bool)
    func clone(self: Map__Str_ExprPtrBox) returns Map__Str_ExprPtrBox
    func hash(self: Map__Str_ExprPtrBox, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_ExprPtrBox.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.new
:group: Compiler frontend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement `cmp`.

#+begin_src til
func Map__Str_ExprPtrBox.new() returns Map__Str_ExprPtrBox
#+end_src

* Map__Str_ExprPtrBox.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.with_capacity
:group: Compiler frontend
:END:

Create an empty Map pre-sized for `n` entries, so inserting up to
`n` reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_ExprPtrBox.with_capacity(shallow n: USize) returns Map__Str_ExprPtrBox
#+end_src

* Map__Str_ExprPtrBox.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.clear
:group: Compiler frontend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_ExprPtrBox.clear(mut self: Map__Str_ExprPtrBox)
#+end_src

* Map__Str_ExprPtrBox.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.has
:group: Compiler frontend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_ExprPtrBox.has(self: Map__Str_ExprPtrBox, key: Str) returns shallow Bool
#+end_src

* Map__Str_ExprPtrBox.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.get
:group: Compiler frontend
:END:

Return a ref to the value for `key`. Throws KeyNotFound if
`key` is absent; call as `map.get(k)!` (or `map[k]`) to panic
on a missing key.

#+begin_src til
func Map__Str_ExprPtrBox.get(self: Map__Str_ExprPtrBox, key: Str, mut _err_kind: I64) returns ref ExprPtrBox
#+end_src

* Map__Str_ExprPtrBox.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.set
:group: Compiler frontend
:END:

Insert or overwrite the value for `key`. The value is owned.

#+begin_src til
func Map__Str_ExprPtrBox.set(mut self: Map__Str_ExprPtrBox, key: Str, own val: ExprPtrBox)
#+end_src

* Map__Str_ExprPtrBox.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Map__Str_ExprPtrBox.remove
:group: Compiler frontend
:END:

Remove `key`, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_ExprPtrBox.remove(mut self: Map__Str_ExprPtrBox, key: Str) returns shallow Bool
#+end_src

* Vec__FFITypePtrBox
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__FFITypePtrBox {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__FFITypePtrBox
    func with_capacity(shallow n: USize) returns Vec__FFITypePtrBox
    func len(self: Vec__FFITypePtrBox) returns shallow USize
    func clear(mut self: Vec__FFITypePtrBox)
    func delete_marked(mut self: Vec__FFITypePtrBox, marks: Dynamic)
    func push(mut self: Vec__FFITypePtrBox, own val: FFITypePtrBox)
    func append(mut self: Vec__FFITypePtrBox, own other: Vec__FFITypePtrBox)
    func take(mut self: Vec__FFITypePtrBox) returns Vec__FFITypePtrBox
    func move_to(own self: Vec__FFITypePtrBox, mut dest: Vec__FFITypePtrBox)
    func move_from(mut self: Vec__FFITypePtrBox, own other: Vec__FFITypePtrBox)
    func unsafe_get(self: Vec__FFITypePtrBox, i: USize) returns ref FFITypePtrBox
    func get(self: Vec__FFITypePtrBox, i: USize, mut _err_kind: I64) returns ref FFITypePtrBox
    func pop(mut self: Vec__FFITypePtrBox) returns FFITypePtrBox
    func remove_at(mut self: Vec__FFITypePtrBox, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__FFITypePtrBox, shallow n: USize) returns Vec__FFITypePtrBox
    func truncate(mut self: Vec__FFITypePtrBox, shallow n: USize)
    func unsafe_set(mut self: Vec__FFITypePtrBox, shallow i: USize, own val: FFITypePtrBox)
    func set(mut self: Vec__FFITypePtrBox, shallow i: USize, own val: FFITypePtrBox, mut _err_kind: I64)
    func push_take(mut self: Vec__FFITypePtrBox, mut src: Vec__FFITypePtrBox, shallow i: USize)
    func delete(own self: Vec__FFITypePtrBox, shallow call_free: Bool)
    func clone(self: Vec__FFITypePtrBox) returns Vec__FFITypePtrBox
    func size() returns shallow USize
}
#+end_src

* Vec__FFITypePtrBox.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__FFITypePtrBox.new() returns Vec__FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__FFITypePtrBox.with_capacity(shallow n: USize) returns Vec__FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__FFITypePtrBox.clear(mut self: Vec__FFITypePtrBox)
#+end_src

* Vec__FFITypePtrBox.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__FFITypePtrBox.delete_marked(mut self: Vec__FFITypePtrBox, marks: Dynamic)
#+end_src

* Vec__FFITypePtrBox.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__FFITypePtrBox.push(mut self: Vec__FFITypePtrBox, own val: FFITypePtrBox)
#+end_src

* Vec__FFITypePtrBox.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__FFITypePtrBox.append(mut self: Vec__FFITypePtrBox, own other: Vec__FFITypePtrBox)
#+end_src

* Vec__FFITypePtrBox.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__FFITypePtrBox.take(mut self: Vec__FFITypePtrBox) returns Vec__FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__FFITypePtrBox.move_to(own self: Vec__FFITypePtrBox, mut dest: Vec__FFITypePtrBox)
#+end_src

* Vec__FFITypePtrBox.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__FFITypePtrBox.move_from(mut self: Vec__FFITypePtrBox, own other: Vec__FFITypePtrBox)
#+end_src

* Vec__FFITypePtrBox.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__FFITypePtrBox.unsafe_get(self: Vec__FFITypePtrBox, i: USize) returns ref FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__FFITypePtrBox.get(self: Vec__FFITypePtrBox, i: USize, mut _err_kind: I64) returns ref FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__FFITypePtrBox.pop(mut self: Vec__FFITypePtrBox) returns FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__FFITypePtrBox.remove_at(mut self: Vec__FFITypePtrBox, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__FFITypePtrBox.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__FFITypePtrBox.take_prefix(mut self: Vec__FFITypePtrBox, shallow n: USize) returns Vec__FFITypePtrBox
#+end_src

* Vec__FFITypePtrBox.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__FFITypePtrBox.truncate(mut self: Vec__FFITypePtrBox, shallow n: USize)
#+end_src

* Vec__FFITypePtrBox.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__FFITypePtrBox.unsafe_set(mut self: Vec__FFITypePtrBox, shallow i: USize, own val: FFITypePtrBox)
#+end_src

* Vec__FFITypePtrBox.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__FFITypePtrBox.set(mut self: Vec__FFITypePtrBox, shallow i: USize, own val: FFITypePtrBox, mut _err_kind: I64)
#+end_src

* Vec__FFITypePtrBox.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__FFITypePtrBox.push_take(mut self: Vec__FFITypePtrBox, mut src: Vec__FFITypePtrBox, shallow i: USize)
#+end_src

* Vec__FFITypePtrBox.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__FFITypePtrBox.delete(own self: Vec__FFITypePtrBox, shallow call_free: Bool)
#+end_src

* Vec__FFITypePtrBox.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFITypePtrBox.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__FFITypePtrBox.clone(self: Vec__FFITypePtrBox) returns Vec__FFITypePtrBox
#+end_src

* Option__ref_Declaration
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_Declaration
:group: Compiler frontend
:END:

#+begin_src til
enum Option__ref_Declaration {
    None
    Some: Declaration
namespace:
    func is_some(shallow self: Option__ref_Declaration) returns shallow Bool
    func is_none(shallow self: Option__ref_Declaration) returns shallow Bool
    func unwrap_or(self: Option__ref_Declaration, default_v: Declaration) returns ref Declaration
    func unwrap(self: Option__ref_Declaration) returns ref Declaration
    func take(own self: Option__ref_Declaration) returns ref Declaration
    ext_func None() returns shallow Option__ref_Declaration
    ext_func Some(val: Declaration) returns shallow Option__ref_Declaration
    core_func is(self: Option__ref_Declaration, other: Option__ref_Declaration) returns shallow Bool
    func eq(shallow self: Option__ref_Declaration, shallow other: Option__ref_Declaration) returns shallow Bool
    func delete(own self: Option__ref_Declaration, shallow call_free: Bool)
    func to_str(shallow self: Option__ref_Declaration) returns Str
    func clone(self: Option__ref_Declaration) returns shallow Option__ref_Declaration
    func size() returns shallow USize
}
#+end_src

* Option__ref_Declaration.unwrap
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_Declaration.unwrap
:group: Compiler frontend
:END:

The contained value; PANICS on None, ala Rust's unwrap. For the
`if o.is_none() { ... }` + `o.unwrap()` idiom, where the caller
has already handled absence and unwrapping cannot fail. Prefer
a `switch` when both arms do real work, and `unwrap_or` when a
default is meaningful -- reach for this only when None is
unreachable by construction, so it fails loudly if that ever
stops being true.

#+begin_src til
func Option__ref_Declaration.unwrap(self: Option__ref_Declaration) returns ref Declaration
#+end_src

* Option__ref_Declaration.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Option__ref_Declaration.take
:group: Compiler frontend
:END:

The contained value, MOVED out; PANICS on None, like unwrap.
unwrap only BORROWS self, so for an owned payload it must hand
back a deep clone; this consumes the Option instead, so the
payload's bytes move and only the enum shell is freed (#321).
Use it wherever the caller takes ownership of what the Option
held -- an `Option(Expr)` returned by an AST helper, say, where
a clone would copy a whole subtree and break node identity.

#+begin_src til
func Option__ref_Declaration.take(own self: Option__ref_Declaration) returns ref Declaration
#+end_src

* Vec__I64
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__I64 {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__I64
    func with_capacity(shallow n: USize) returns Vec__I64
    func len(self: Vec__I64) returns shallow USize
    func clear(mut self: Vec__I64)
    func delete_marked(mut self: Vec__I64, marks: Dynamic)
    func push(mut self: Vec__I64, own val: I64)
    func append(mut self: Vec__I64, own other: Vec__I64)
    func take(mut self: Vec__I64) returns Vec__I64
    func move_to(own self: Vec__I64, mut dest: Vec__I64)
    func move_from(mut self: Vec__I64, own other: Vec__I64)
    func unsafe_get(self: Vec__I64, i: USize) returns ref I64
    func get(self: Vec__I64, i: USize, mut _err_kind: I64) returns ref I64
    func pop(mut self: Vec__I64) returns shallow I64
    func remove_at(mut self: Vec__I64, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__I64, shallow n: USize) returns Vec__I64
    func truncate(mut self: Vec__I64, shallow n: USize)
    func unsafe_set(mut self: Vec__I64, shallow i: USize, own val: I64)
    func set(mut self: Vec__I64, shallow i: USize, own val: I64, mut _err_kind: I64)
    func push_take(mut self: Vec__I64, mut src: Vec__I64, shallow i: USize)
    func delete(own self: Vec__I64, shallow call_free: Bool)
    func clone(self: Vec__I64) returns Vec__I64
    func size() returns shallow USize
}
#+end_src

* Vec__I64.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__I64.new() returns Vec__I64
#+end_src

* Vec__I64.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__I64.with_capacity(shallow n: USize) returns Vec__I64
#+end_src

* Vec__I64.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__I64.clear(mut self: Vec__I64)
#+end_src

* Vec__I64.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__I64.delete_marked(mut self: Vec__I64, marks: Dynamic)
#+end_src

* Vec__I64.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__I64.push(mut self: Vec__I64, own val: I64)
#+end_src

* Vec__I64.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__I64.append(mut self: Vec__I64, own other: Vec__I64)
#+end_src

* Vec__I64.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__I64.take(mut self: Vec__I64) returns Vec__I64
#+end_src

* Vec__I64.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__I64.move_to(own self: Vec__I64, mut dest: Vec__I64)
#+end_src

* Vec__I64.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__I64.move_from(mut self: Vec__I64, own other: Vec__I64)
#+end_src

* Vec__I64.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__I64.unsafe_get(self: Vec__I64, i: USize) returns ref I64
#+end_src

* Vec__I64.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__I64.get(self: Vec__I64, i: USize, mut _err_kind: I64) returns ref I64
#+end_src

* Vec__I64.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__I64.pop(mut self: Vec__I64) returns shallow I64
#+end_src

* Vec__I64.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__I64.remove_at(mut self: Vec__I64, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__I64.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__I64.take_prefix(mut self: Vec__I64, shallow n: USize) returns Vec__I64
#+end_src

* Vec__I64.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__I64.truncate(mut self: Vec__I64, shallow n: USize)
#+end_src

* Vec__I64.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__I64.unsafe_set(mut self: Vec__I64, shallow i: USize, own val: I64)
#+end_src

* Vec__I64.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__I64.set(mut self: Vec__I64, shallow i: USize, own val: I64, mut _err_kind: I64)
#+end_src

* Vec__I64.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__I64.push_take(mut self: Vec__I64, mut src: Vec__I64, shallow i: USize)
#+end_src

* Vec__I64.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__I64.delete(own self: Vec__I64, shallow call_free: Bool)
#+end_src

* Vec__I64.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__I64.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__I64.clone(self: Vec__I64) returns Vec__I64
#+end_src

* Vec__TypeBinding
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__TypeBinding {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__TypeBinding
    func with_capacity(shallow n: USize) returns Vec__TypeBinding
    func len(self: Vec__TypeBinding) returns shallow USize
    func clear(mut self: Vec__TypeBinding)
    func delete_marked(mut self: Vec__TypeBinding, marks: Dynamic)
    func push(mut self: Vec__TypeBinding, own val: TypeBinding)
    func append(mut self: Vec__TypeBinding, own other: Vec__TypeBinding)
    func take(mut self: Vec__TypeBinding) returns Vec__TypeBinding
    func move_to(own self: Vec__TypeBinding, mut dest: Vec__TypeBinding)
    func move_from(mut self: Vec__TypeBinding, own other: Vec__TypeBinding)
    func unsafe_get(self: Vec__TypeBinding, i: USize) returns ref TypeBinding
    func get(self: Vec__TypeBinding, i: USize, mut _err_kind: I64) returns ref TypeBinding
    func pop(mut self: Vec__TypeBinding) returns TypeBinding
    func remove_at(mut self: Vec__TypeBinding, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__TypeBinding, shallow n: USize) returns Vec__TypeBinding
    func truncate(mut self: Vec__TypeBinding, shallow n: USize)
    func unsafe_set(mut self: Vec__TypeBinding, shallow i: USize, own val: TypeBinding)
    func set(mut self: Vec__TypeBinding, shallow i: USize, own val: TypeBinding, mut _err_kind: I64)
    func push_take(mut self: Vec__TypeBinding, mut src: Vec__TypeBinding, shallow i: USize)
    func delete(own self: Vec__TypeBinding, shallow call_free: Bool)
    func clone(self: Vec__TypeBinding) returns Vec__TypeBinding
    func size() returns shallow USize
}
#+end_src

* Vec__TypeBinding.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__TypeBinding.new() returns Vec__TypeBinding
#+end_src

* Vec__TypeBinding.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__TypeBinding.with_capacity(shallow n: USize) returns Vec__TypeBinding
#+end_src

* Vec__TypeBinding.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__TypeBinding.clear(mut self: Vec__TypeBinding)
#+end_src

* Vec__TypeBinding.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__TypeBinding.delete_marked(mut self: Vec__TypeBinding, marks: Dynamic)
#+end_src

* Vec__TypeBinding.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__TypeBinding.push(mut self: Vec__TypeBinding, own val: TypeBinding)
#+end_src

* Vec__TypeBinding.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__TypeBinding.append(mut self: Vec__TypeBinding, own other: Vec__TypeBinding)
#+end_src

* Vec__TypeBinding.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__TypeBinding.take(mut self: Vec__TypeBinding) returns Vec__TypeBinding
#+end_src

* Vec__TypeBinding.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__TypeBinding.move_to(own self: Vec__TypeBinding, mut dest: Vec__TypeBinding)
#+end_src

* Vec__TypeBinding.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__TypeBinding.move_from(mut self: Vec__TypeBinding, own other: Vec__TypeBinding)
#+end_src

* Vec__TypeBinding.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__TypeBinding.unsafe_get(self: Vec__TypeBinding, i: USize) returns ref TypeBinding
#+end_src

* Vec__TypeBinding.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__TypeBinding.get(self: Vec__TypeBinding, i: USize, mut _err_kind: I64) returns ref TypeBinding
#+end_src

* Vec__TypeBinding.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__TypeBinding.pop(mut self: Vec__TypeBinding) returns TypeBinding
#+end_src

* Vec__TypeBinding.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__TypeBinding.remove_at(mut self: Vec__TypeBinding, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__TypeBinding.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__TypeBinding.take_prefix(mut self: Vec__TypeBinding, shallow n: USize) returns Vec__TypeBinding
#+end_src

* Vec__TypeBinding.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__TypeBinding.truncate(mut self: Vec__TypeBinding, shallow n: USize)
#+end_src

* Vec__TypeBinding.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__TypeBinding.unsafe_set(mut self: Vec__TypeBinding, shallow i: USize, own val: TypeBinding)
#+end_src

* Vec__TypeBinding.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__TypeBinding.set(mut self: Vec__TypeBinding, shallow i: USize, own val: TypeBinding, mut _err_kind: I64)
#+end_src

* Vec__TypeBinding.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__TypeBinding.push_take(mut self: Vec__TypeBinding, mut src: Vec__TypeBinding, shallow i: USize)
#+end_src

* Vec__TypeBinding.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__TypeBinding.delete(own self: Vec__TypeBinding, shallow call_free: Bool)
#+end_src

* Vec__TypeBinding.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__TypeBinding.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__TypeBinding.clone(self: Vec__TypeBinding) returns Vec__TypeBinding
#+end_src

* Vec__U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__U32 {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__U32
    func with_capacity(shallow n: USize) returns Vec__U32
    func len(self: Vec__U32) returns shallow USize
    func clear(mut self: Vec__U32)
    func delete_marked(mut self: Vec__U32, marks: Dynamic)
    func push(mut self: Vec__U32, own val: U32)
    func append(mut self: Vec__U32, own other: Vec__U32)
    func take(mut self: Vec__U32) returns Vec__U32
    func move_to(own self: Vec__U32, mut dest: Vec__U32)
    func move_from(mut self: Vec__U32, own other: Vec__U32)
    func unsafe_get(self: Vec__U32, i: USize) returns ref U32
    func get(self: Vec__U32, i: USize, mut _err_kind: I64) returns ref U32
    func pop(mut self: Vec__U32) returns shallow U32
    func remove_at(mut self: Vec__U32, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__U32, shallow n: USize) returns Vec__U32
    func truncate(mut self: Vec__U32, shallow n: USize)
    func unsafe_set(mut self: Vec__U32, shallow i: USize, own val: U32)
    func set(mut self: Vec__U32, shallow i: USize, own val: U32, mut _err_kind: I64)
    func push_take(mut self: Vec__U32, mut src: Vec__U32, shallow i: USize)
    func delete(own self: Vec__U32, shallow call_free: Bool)
    func clone(self: Vec__U32) returns Vec__U32
    func size() returns shallow USize
}
#+end_src

* Vec__U32.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__U32.new() returns Vec__U32
#+end_src

* Vec__U32.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__U32.with_capacity(shallow n: USize) returns Vec__U32
#+end_src

* Vec__U32.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__U32.clear(mut self: Vec__U32)
#+end_src

* Vec__U32.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__U32.delete_marked(mut self: Vec__U32, marks: Dynamic)
#+end_src

* Vec__U32.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__U32.push(mut self: Vec__U32, own val: U32)
#+end_src

* Vec__U32.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__U32.append(mut self: Vec__U32, own other: Vec__U32)
#+end_src

* Vec__U32.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__U32.take(mut self: Vec__U32) returns Vec__U32
#+end_src

* Vec__U32.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__U32.move_to(own self: Vec__U32, mut dest: Vec__U32)
#+end_src

* Vec__U32.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__U32.move_from(mut self: Vec__U32, own other: Vec__U32)
#+end_src

* Vec__U32.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__U32.unsafe_get(self: Vec__U32, i: USize) returns ref U32
#+end_src

* Vec__U32.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__U32.get(self: Vec__U32, i: USize, mut _err_kind: I64) returns ref U32
#+end_src

* Vec__U32.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__U32.pop(mut self: Vec__U32) returns shallow U32
#+end_src

* Vec__U32.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__U32.remove_at(mut self: Vec__U32, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__U32.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__U32.take_prefix(mut self: Vec__U32, shallow n: USize) returns Vec__U32
#+end_src

* Vec__U32.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__U32.truncate(mut self: Vec__U32, shallow n: USize)
#+end_src

* Vec__U32.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__U32.unsafe_set(mut self: Vec__U32, shallow i: USize, own val: U32)
#+end_src

* Vec__U32.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__U32.set(mut self: Vec__U32, shallow i: USize, own val: U32, mut _err_kind: I64)
#+end_src

* Vec__U32.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__U32.push_take(mut self: Vec__U32, mut src: Vec__U32, shallow i: USize)
#+end_src

* Vec__U32.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__U32.delete(own self: Vec__U32, shallow call_free: Bool)
#+end_src

* Vec__U32.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__U32.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__U32.clone(self: Vec__U32) returns Vec__U32
#+end_src

* Vec__Mode
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__Mode {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__Mode
    func with_capacity(shallow n: USize) returns Vec__Mode
    func len(self: Vec__Mode) returns shallow USize
    func clear(mut self: Vec__Mode)
    func delete_marked(mut self: Vec__Mode, marks: Dynamic)
    func push(mut self: Vec__Mode, own val: Mode)
    func append(mut self: Vec__Mode, own other: Vec__Mode)
    func take(mut self: Vec__Mode) returns Vec__Mode
    func move_to(own self: Vec__Mode, mut dest: Vec__Mode)
    func move_from(mut self: Vec__Mode, own other: Vec__Mode)
    func unsafe_get(self: Vec__Mode, i: USize) returns ref Mode
    func get(self: Vec__Mode, i: USize, mut _err_kind: I64) returns ref Mode
    func pop(mut self: Vec__Mode) returns Mode
    func remove_at(mut self: Vec__Mode, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__Mode, shallow n: USize) returns Vec__Mode
    func truncate(mut self: Vec__Mode, shallow n: USize)
    func unsafe_set(mut self: Vec__Mode, shallow i: USize, own val: Mode)
    func set(mut self: Vec__Mode, shallow i: USize, own val: Mode, mut _err_kind: I64)
    func push_take(mut self: Vec__Mode, mut src: Vec__Mode, shallow i: USize)
    func delete(own self: Vec__Mode, shallow call_free: Bool)
    func clone(self: Vec__Mode) returns Vec__Mode
    func size() returns shallow USize
}
#+end_src

* Vec__Mode.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__Mode.new() returns Vec__Mode
#+end_src

* Vec__Mode.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__Mode.with_capacity(shallow n: USize) returns Vec__Mode
#+end_src

* Vec__Mode.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__Mode.clear(mut self: Vec__Mode)
#+end_src

* Vec__Mode.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__Mode.delete_marked(mut self: Vec__Mode, marks: Dynamic)
#+end_src

* Vec__Mode.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__Mode.push(mut self: Vec__Mode, own val: Mode)
#+end_src

* Vec__Mode.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__Mode.append(mut self: Vec__Mode, own other: Vec__Mode)
#+end_src

* Vec__Mode.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__Mode.take(mut self: Vec__Mode) returns Vec__Mode
#+end_src

* Vec__Mode.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__Mode.move_to(own self: Vec__Mode, mut dest: Vec__Mode)
#+end_src

* Vec__Mode.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__Mode.move_from(mut self: Vec__Mode, own other: Vec__Mode)
#+end_src

* Vec__Mode.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__Mode.unsafe_get(self: Vec__Mode, i: USize) returns ref Mode
#+end_src

* Vec__Mode.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__Mode.get(self: Vec__Mode, i: USize, mut _err_kind: I64) returns ref Mode
#+end_src

* Vec__Mode.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__Mode.pop(mut self: Vec__Mode) returns Mode
#+end_src

* Vec__Mode.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__Mode.remove_at(mut self: Vec__Mode, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__Mode.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__Mode.take_prefix(mut self: Vec__Mode, shallow n: USize) returns Vec__Mode
#+end_src

* Vec__Mode.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__Mode.truncate(mut self: Vec__Mode, shallow n: USize)
#+end_src

* Vec__Mode.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__Mode.unsafe_set(mut self: Vec__Mode, shallow i: USize, own val: Mode)
#+end_src

* Vec__Mode.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__Mode.set(mut self: Vec__Mode, shallow i: USize, own val: Mode, mut _err_kind: I64)
#+end_src

* Vec__Mode.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__Mode.push_take(mut self: Vec__Mode, mut src: Vec__Mode, shallow i: USize)
#+end_src

* Vec__Mode.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__Mode.delete(own self: Vec__Mode, shallow call_free: Bool)
#+end_src

* Vec__Mode.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__Mode.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__Mode.clone(self: Vec__Mode) returns Vec__Mode
#+end_src

* Vec__FuncType
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__FuncType {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__FuncType
    func with_capacity(shallow n: USize) returns Vec__FuncType
    func len(self: Vec__FuncType) returns shallow USize
    func clear(mut self: Vec__FuncType)
    func delete_marked(mut self: Vec__FuncType, marks: Dynamic)
    func push(mut self: Vec__FuncType, own val: FuncType)
    func append(mut self: Vec__FuncType, own other: Vec__FuncType)
    func take(mut self: Vec__FuncType) returns Vec__FuncType
    func move_to(own self: Vec__FuncType, mut dest: Vec__FuncType)
    func move_from(mut self: Vec__FuncType, own other: Vec__FuncType)
    func unsafe_get(self: Vec__FuncType, i: USize) returns ref FuncType
    func get(self: Vec__FuncType, i: USize, mut _err_kind: I64) returns ref FuncType
    func pop(mut self: Vec__FuncType) returns FuncType
    func remove_at(mut self: Vec__FuncType, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__FuncType, shallow n: USize) returns Vec__FuncType
    func truncate(mut self: Vec__FuncType, shallow n: USize)
    func unsafe_set(mut self: Vec__FuncType, shallow i: USize, own val: FuncType)
    func set(mut self: Vec__FuncType, shallow i: USize, own val: FuncType, mut _err_kind: I64)
    func push_take(mut self: Vec__FuncType, mut src: Vec__FuncType, shallow i: USize)
    func delete(own self: Vec__FuncType, shallow call_free: Bool)
    func clone(self: Vec__FuncType) returns Vec__FuncType
    func size() returns shallow USize
}
#+end_src

* Vec__FuncType.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__FuncType.new() returns Vec__FuncType
#+end_src

* Vec__FuncType.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__FuncType.with_capacity(shallow n: USize) returns Vec__FuncType
#+end_src

* Vec__FuncType.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__FuncType.clear(mut self: Vec__FuncType)
#+end_src

* Vec__FuncType.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__FuncType.delete_marked(mut self: Vec__FuncType, marks: Dynamic)
#+end_src

* Vec__FuncType.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__FuncType.push(mut self: Vec__FuncType, own val: FuncType)
#+end_src

* Vec__FuncType.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__FuncType.append(mut self: Vec__FuncType, own other: Vec__FuncType)
#+end_src

* Vec__FuncType.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__FuncType.take(mut self: Vec__FuncType) returns Vec__FuncType
#+end_src

* Vec__FuncType.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__FuncType.move_to(own self: Vec__FuncType, mut dest: Vec__FuncType)
#+end_src

* Vec__FuncType.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__FuncType.move_from(mut self: Vec__FuncType, own other: Vec__FuncType)
#+end_src

* Vec__FuncType.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__FuncType.unsafe_get(self: Vec__FuncType, i: USize) returns ref FuncType
#+end_src

* Vec__FuncType.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__FuncType.get(self: Vec__FuncType, i: USize, mut _err_kind: I64) returns ref FuncType
#+end_src

* Vec__FuncType.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__FuncType.pop(mut self: Vec__FuncType) returns FuncType
#+end_src

* Vec__FuncType.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__FuncType.remove_at(mut self: Vec__FuncType, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__FuncType.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__FuncType.take_prefix(mut self: Vec__FuncType, shallow n: USize) returns Vec__FuncType
#+end_src

* Vec__FuncType.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__FuncType.truncate(mut self: Vec__FuncType, shallow n: USize)
#+end_src

* Vec__FuncType.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__FuncType.unsafe_set(mut self: Vec__FuncType, shallow i: USize, own val: FuncType)
#+end_src

* Vec__FuncType.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__FuncType.set(mut self: Vec__FuncType, shallow i: USize, own val: FuncType, mut _err_kind: I64)
#+end_src

* Vec__FuncType.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__FuncType.push_take(mut self: Vec__FuncType, mut src: Vec__FuncType, shallow i: USize)
#+end_src

* Vec__FuncType.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__FuncType.delete(own self: Vec__FuncType, shallow call_free: Bool)
#+end_src

* Vec__FuncType.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FuncType.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__FuncType.clone(self: Vec__FuncType) returns Vec__FuncType
#+end_src

* Vec__ImportUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__ImportUnit {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__ImportUnit
    func with_capacity(shallow n: USize) returns Vec__ImportUnit
    func len(self: Vec__ImportUnit) returns shallow USize
    func clear(mut self: Vec__ImportUnit)
    func delete_marked(mut self: Vec__ImportUnit, marks: Dynamic)
    func push(mut self: Vec__ImportUnit, own val: ImportUnit)
    func append(mut self: Vec__ImportUnit, own other: Vec__ImportUnit)
    func take(mut self: Vec__ImportUnit) returns Vec__ImportUnit
    func move_to(own self: Vec__ImportUnit, mut dest: Vec__ImportUnit)
    func move_from(mut self: Vec__ImportUnit, own other: Vec__ImportUnit)
    func unsafe_get(self: Vec__ImportUnit, i: USize) returns ref ImportUnit
    func get(self: Vec__ImportUnit, i: USize, mut _err_kind: I64) returns ref ImportUnit
    func pop(mut self: Vec__ImportUnit) returns ImportUnit
    func remove_at(mut self: Vec__ImportUnit, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__ImportUnit, shallow n: USize) returns Vec__ImportUnit
    func truncate(mut self: Vec__ImportUnit, shallow n: USize)
    func unsafe_set(mut self: Vec__ImportUnit, shallow i: USize, own val: ImportUnit)
    func set(mut self: Vec__ImportUnit, shallow i: USize, own val: ImportUnit, mut _err_kind: I64)
    func push_take(mut self: Vec__ImportUnit, mut src: Vec__ImportUnit, shallow i: USize)
    func delete(own self: Vec__ImportUnit, shallow call_free: Bool)
    func clone(self: Vec__ImportUnit) returns Vec__ImportUnit
    func size() returns shallow USize
}
#+end_src

* Vec__ImportUnit.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__ImportUnit.new() returns Vec__ImportUnit
#+end_src

* Vec__ImportUnit.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__ImportUnit.with_capacity(shallow n: USize) returns Vec__ImportUnit
#+end_src

* Vec__ImportUnit.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__ImportUnit.clear(mut self: Vec__ImportUnit)
#+end_src

* Vec__ImportUnit.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__ImportUnit.delete_marked(mut self: Vec__ImportUnit, marks: Dynamic)
#+end_src

* Vec__ImportUnit.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__ImportUnit.push(mut self: Vec__ImportUnit, own val: ImportUnit)
#+end_src

* Vec__ImportUnit.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__ImportUnit.append(mut self: Vec__ImportUnit, own other: Vec__ImportUnit)
#+end_src

* Vec__ImportUnit.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__ImportUnit.take(mut self: Vec__ImportUnit) returns Vec__ImportUnit
#+end_src

* Vec__ImportUnit.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__ImportUnit.move_to(own self: Vec__ImportUnit, mut dest: Vec__ImportUnit)
#+end_src

* Vec__ImportUnit.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__ImportUnit.move_from(mut self: Vec__ImportUnit, own other: Vec__ImportUnit)
#+end_src

* Vec__ImportUnit.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__ImportUnit.unsafe_get(self: Vec__ImportUnit, i: USize) returns ref ImportUnit
#+end_src

* Vec__ImportUnit.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__ImportUnit.get(self: Vec__ImportUnit, i: USize, mut _err_kind: I64) returns ref ImportUnit
#+end_src

* Vec__ImportUnit.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__ImportUnit.pop(mut self: Vec__ImportUnit) returns ImportUnit
#+end_src

* Vec__ImportUnit.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__ImportUnit.remove_at(mut self: Vec__ImportUnit, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__ImportUnit.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__ImportUnit.take_prefix(mut self: Vec__ImportUnit, shallow n: USize) returns Vec__ImportUnit
#+end_src

* Vec__ImportUnit.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__ImportUnit.truncate(mut self: Vec__ImportUnit, shallow n: USize)
#+end_src

* Vec__ImportUnit.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__ImportUnit.unsafe_set(mut self: Vec__ImportUnit, shallow i: USize, own val: ImportUnit)
#+end_src

* Vec__ImportUnit.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__ImportUnit.set(mut self: Vec__ImportUnit, shallow i: USize, own val: ImportUnit, mut _err_kind: I64)
#+end_src

* Vec__ImportUnit.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__ImportUnit.push_take(mut self: Vec__ImportUnit, mut src: Vec__ImportUnit, shallow i: USize)
#+end_src

* Vec__ImportUnit.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__ImportUnit.delete(own self: Vec__ImportUnit, shallow call_free: Bool)
#+end_src

* Vec__ImportUnit.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ImportUnit.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__ImportUnit.clone(self: Vec__ImportUnit) returns Vec__ImportUnit
#+end_src

* Vec__StructLayout
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__StructLayout {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__StructLayout
    func with_capacity(shallow n: USize) returns Vec__StructLayout
    func len(self: Vec__StructLayout) returns shallow USize
    func clear(mut self: Vec__StructLayout)
    func delete_marked(mut self: Vec__StructLayout, marks: Dynamic)
    func push(mut self: Vec__StructLayout, own val: StructLayout)
    func append(mut self: Vec__StructLayout, own other: Vec__StructLayout)
    func take(mut self: Vec__StructLayout) returns Vec__StructLayout
    func move_to(own self: Vec__StructLayout, mut dest: Vec__StructLayout)
    func move_from(mut self: Vec__StructLayout, own other: Vec__StructLayout)
    func unsafe_get(self: Vec__StructLayout, i: USize) returns ref StructLayout
    func get(self: Vec__StructLayout, i: USize, mut _err_kind: I64) returns ref StructLayout
    func pop(mut self: Vec__StructLayout) returns StructLayout
    func remove_at(mut self: Vec__StructLayout, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__StructLayout, shallow n: USize) returns Vec__StructLayout
    func truncate(mut self: Vec__StructLayout, shallow n: USize)
    func unsafe_set(mut self: Vec__StructLayout, shallow i: USize, own val: StructLayout)
    func set(mut self: Vec__StructLayout, shallow i: USize, own val: StructLayout, mut _err_kind: I64)
    func push_take(mut self: Vec__StructLayout, mut src: Vec__StructLayout, shallow i: USize)
    func delete(own self: Vec__StructLayout, shallow call_free: Bool)
    func clone(self: Vec__StructLayout) returns Vec__StructLayout
    func size() returns shallow USize
}
#+end_src

* Vec__StructLayout.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__StructLayout.new() returns Vec__StructLayout
#+end_src

* Vec__StructLayout.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__StructLayout.with_capacity(shallow n: USize) returns Vec__StructLayout
#+end_src

* Vec__StructLayout.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__StructLayout.clear(mut self: Vec__StructLayout)
#+end_src

* Vec__StructLayout.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__StructLayout.delete_marked(mut self: Vec__StructLayout, marks: Dynamic)
#+end_src

* Vec__StructLayout.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__StructLayout.push(mut self: Vec__StructLayout, own val: StructLayout)
#+end_src

* Vec__StructLayout.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__StructLayout.append(mut self: Vec__StructLayout, own other: Vec__StructLayout)
#+end_src

* Vec__StructLayout.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__StructLayout.take(mut self: Vec__StructLayout) returns Vec__StructLayout
#+end_src

* Vec__StructLayout.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__StructLayout.move_to(own self: Vec__StructLayout, mut dest: Vec__StructLayout)
#+end_src

* Vec__StructLayout.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__StructLayout.move_from(mut self: Vec__StructLayout, own other: Vec__StructLayout)
#+end_src

* Vec__StructLayout.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__StructLayout.unsafe_get(self: Vec__StructLayout, i: USize) returns ref StructLayout
#+end_src

* Vec__StructLayout.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__StructLayout.get(self: Vec__StructLayout, i: USize, mut _err_kind: I64) returns ref StructLayout
#+end_src

* Vec__StructLayout.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__StructLayout.pop(mut self: Vec__StructLayout) returns StructLayout
#+end_src

* Vec__StructLayout.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__StructLayout.remove_at(mut self: Vec__StructLayout, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__StructLayout.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__StructLayout.take_prefix(mut self: Vec__StructLayout, shallow n: USize) returns Vec__StructLayout
#+end_src

* Vec__StructLayout.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__StructLayout.truncate(mut self: Vec__StructLayout, shallow n: USize)
#+end_src

* Vec__StructLayout.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__StructLayout.unsafe_set(mut self: Vec__StructLayout, shallow i: USize, own val: StructLayout)
#+end_src

* Vec__StructLayout.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__StructLayout.set(mut self: Vec__StructLayout, shallow i: USize, own val: StructLayout, mut _err_kind: I64)
#+end_src

* Vec__StructLayout.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__StructLayout.push_take(mut self: Vec__StructLayout, mut src: Vec__StructLayout, shallow i: USize)
#+end_src

* Vec__StructLayout.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__StructLayout.delete(own self: Vec__StructLayout, shallow call_free: Bool)
#+end_src

* Vec__StructLayout.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__StructLayout.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__StructLayout.clone(self: Vec__StructLayout) returns Vec__StructLayout
#+end_src

* Vec__call_Vec_Str
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__call_Vec_Str {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__call_Vec_Str
    func with_capacity(shallow n: USize) returns Vec__call_Vec_Str
    func len(self: Vec__call_Vec_Str) returns shallow USize
    func clear(mut self: Vec__call_Vec_Str)
    func delete_marked(mut self: Vec__call_Vec_Str, marks: Dynamic)
    func push(mut self: Vec__call_Vec_Str, own val: Vec__Str)
    func append(mut self: Vec__call_Vec_Str, own other: Vec__call_Vec_Str)
    func take(mut self: Vec__call_Vec_Str) returns Vec__call_Vec_Str
    func move_to(own self: Vec__call_Vec_Str, mut dest: Vec__call_Vec_Str)
    func move_from(mut self: Vec__call_Vec_Str, own other: Vec__call_Vec_Str)
    func unsafe_get(self: Vec__call_Vec_Str, i: USize) returns ref Vec__Str
    func get(self: Vec__call_Vec_Str, i: USize, mut _err_kind: I64) returns ref Vec__Str
    func pop(mut self: Vec__call_Vec_Str) returns Vec__Str
    func remove_at(mut self: Vec__call_Vec_Str, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__call_Vec_Str, shallow n: USize) returns Vec__call_Vec_Str
    func truncate(mut self: Vec__call_Vec_Str, shallow n: USize)
    func unsafe_set(mut self: Vec__call_Vec_Str, shallow i: USize, own val: Vec__Str)
    func set(mut self: Vec__call_Vec_Str, shallow i: USize, own val: Vec__Str, mut _err_kind: I64)
    func push_take(mut self: Vec__call_Vec_Str, mut src: Vec__call_Vec_Str, shallow i: USize)
    func delete(own self: Vec__call_Vec_Str, shallow call_free: Bool)
    func clone(self: Vec__call_Vec_Str) returns Vec__call_Vec_Str
    func size() returns shallow USize
}
#+end_src

* Vec__call_Vec_Str.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__call_Vec_Str.new() returns Vec__call_Vec_Str
#+end_src

* Vec__call_Vec_Str.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__call_Vec_Str.with_capacity(shallow n: USize) returns Vec__call_Vec_Str
#+end_src

* Vec__call_Vec_Str.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__call_Vec_Str.clear(mut self: Vec__call_Vec_Str)
#+end_src

* Vec__call_Vec_Str.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__call_Vec_Str.delete_marked(mut self: Vec__call_Vec_Str, marks: Dynamic)
#+end_src

* Vec__call_Vec_Str.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__call_Vec_Str.push(mut self: Vec__call_Vec_Str, own val: Vec__Str)
#+end_src

* Vec__call_Vec_Str.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__call_Vec_Str.append(mut self: Vec__call_Vec_Str, own other: Vec__call_Vec_Str)
#+end_src

* Vec__call_Vec_Str.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__call_Vec_Str.take(mut self: Vec__call_Vec_Str) returns Vec__call_Vec_Str
#+end_src

* Vec__call_Vec_Str.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__call_Vec_Str.move_to(own self: Vec__call_Vec_Str, mut dest: Vec__call_Vec_Str)
#+end_src

* Vec__call_Vec_Str.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__call_Vec_Str.move_from(mut self: Vec__call_Vec_Str, own other: Vec__call_Vec_Str)
#+end_src

* Vec__call_Vec_Str.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__call_Vec_Str.unsafe_get(self: Vec__call_Vec_Str, i: USize) returns ref Vec__Str
#+end_src

* Vec__call_Vec_Str.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__call_Vec_Str.get(self: Vec__call_Vec_Str, i: USize, mut _err_kind: I64) returns ref Vec__Str
#+end_src

* Vec__call_Vec_Str.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__call_Vec_Str.pop(mut self: Vec__call_Vec_Str) returns Vec__Str
#+end_src

* Vec__call_Vec_Str.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__call_Vec_Str.remove_at(mut self: Vec__call_Vec_Str, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__call_Vec_Str.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__call_Vec_Str.take_prefix(mut self: Vec__call_Vec_Str, shallow n: USize) returns Vec__call_Vec_Str
#+end_src

* Vec__call_Vec_Str.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__call_Vec_Str.truncate(mut self: Vec__call_Vec_Str, shallow n: USize)
#+end_src

* Vec__call_Vec_Str.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__call_Vec_Str.unsafe_set(mut self: Vec__call_Vec_Str, shallow i: USize, own val: Vec__Str)
#+end_src

* Vec__call_Vec_Str.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__call_Vec_Str.set(mut self: Vec__call_Vec_Str, shallow i: USize, own val: Vec__Str, mut _err_kind: I64)
#+end_src

* Vec__call_Vec_Str.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__call_Vec_Str.push_take(mut self: Vec__call_Vec_Str, mut src: Vec__call_Vec_Str, shallow i: USize)
#+end_src

* Vec__call_Vec_Str.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__call_Vec_Str.delete(own self: Vec__call_Vec_Str, shallow call_free: Bool)
#+end_src

* Vec__call_Vec_Str.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__call_Vec_Str.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__call_Vec_Str.clone(self: Vec__call_Vec_Str) returns Vec__call_Vec_Str
#+end_src

* Vec__FFIEntry
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__FFIEntry {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__FFIEntry
    func with_capacity(shallow n: USize) returns Vec__FFIEntry
    func len(self: Vec__FFIEntry) returns shallow USize
    func clear(mut self: Vec__FFIEntry)
    func delete_marked(mut self: Vec__FFIEntry, marks: Dynamic)
    func push(mut self: Vec__FFIEntry, own val: FFIEntry)
    func append(mut self: Vec__FFIEntry, own other: Vec__FFIEntry)
    func take(mut self: Vec__FFIEntry) returns Vec__FFIEntry
    func move_to(own self: Vec__FFIEntry, mut dest: Vec__FFIEntry)
    func move_from(mut self: Vec__FFIEntry, own other: Vec__FFIEntry)
    func unsafe_get(self: Vec__FFIEntry, i: USize) returns ref FFIEntry
    func get(self: Vec__FFIEntry, i: USize, mut _err_kind: I64) returns ref FFIEntry
    func pop(mut self: Vec__FFIEntry) returns FFIEntry
    func remove_at(mut self: Vec__FFIEntry, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__FFIEntry, shallow n: USize) returns Vec__FFIEntry
    func truncate(mut self: Vec__FFIEntry, shallow n: USize)
    func unsafe_set(mut self: Vec__FFIEntry, shallow i: USize, own val: FFIEntry)
    func set(mut self: Vec__FFIEntry, shallow i: USize, own val: FFIEntry, mut _err_kind: I64)
    func push_take(mut self: Vec__FFIEntry, mut src: Vec__FFIEntry, shallow i: USize)
    func delete(own self: Vec__FFIEntry, shallow call_free: Bool)
    func clone(self: Vec__FFIEntry) returns Vec__FFIEntry
    func size() returns shallow USize
}
#+end_src

* Vec__FFIEntry.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__FFIEntry.new() returns Vec__FFIEntry
#+end_src

* Vec__FFIEntry.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__FFIEntry.with_capacity(shallow n: USize) returns Vec__FFIEntry
#+end_src

* Vec__FFIEntry.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__FFIEntry.clear(mut self: Vec__FFIEntry)
#+end_src

* Vec__FFIEntry.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__FFIEntry.delete_marked(mut self: Vec__FFIEntry, marks: Dynamic)
#+end_src

* Vec__FFIEntry.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__FFIEntry.push(mut self: Vec__FFIEntry, own val: FFIEntry)
#+end_src

* Vec__FFIEntry.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__FFIEntry.append(mut self: Vec__FFIEntry, own other: Vec__FFIEntry)
#+end_src

* Vec__FFIEntry.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__FFIEntry.take(mut self: Vec__FFIEntry) returns Vec__FFIEntry
#+end_src

* Vec__FFIEntry.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__FFIEntry.move_to(own self: Vec__FFIEntry, mut dest: Vec__FFIEntry)
#+end_src

* Vec__FFIEntry.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__FFIEntry.move_from(mut self: Vec__FFIEntry, own other: Vec__FFIEntry)
#+end_src

* Vec__FFIEntry.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__FFIEntry.unsafe_get(self: Vec__FFIEntry, i: USize) returns ref FFIEntry
#+end_src

* Vec__FFIEntry.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__FFIEntry.get(self: Vec__FFIEntry, i: USize, mut _err_kind: I64) returns ref FFIEntry
#+end_src

* Vec__FFIEntry.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__FFIEntry.pop(mut self: Vec__FFIEntry) returns FFIEntry
#+end_src

* Vec__FFIEntry.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__FFIEntry.remove_at(mut self: Vec__FFIEntry, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__FFIEntry.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__FFIEntry.take_prefix(mut self: Vec__FFIEntry, shallow n: USize) returns Vec__FFIEntry
#+end_src

* Vec__FFIEntry.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__FFIEntry.truncate(mut self: Vec__FFIEntry, shallow n: USize)
#+end_src

* Vec__FFIEntry.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__FFIEntry.unsafe_set(mut self: Vec__FFIEntry, shallow i: USize, own val: FFIEntry)
#+end_src

* Vec__FFIEntry.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__FFIEntry.set(mut self: Vec__FFIEntry, shallow i: USize, own val: FFIEntry, mut _err_kind: I64)
#+end_src

* Vec__FFIEntry.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__FFIEntry.push_take(mut self: Vec__FFIEntry, mut src: Vec__FFIEntry, shallow i: USize)
#+end_src

* Vec__FFIEntry.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__FFIEntry.delete(own self: Vec__FFIEntry, shallow call_free: Bool)
#+end_src

* Vec__FFIEntry.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__FFIEntry.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__FFIEntry.clone(self: Vec__FFIEntry) returns Vec__FFIEntry
#+end_src

* Vec__ExprPtrBox
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox
:group: Compiler frontend
:END:

#+begin_src til
struct Vec__ExprPtrBox {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__ExprPtrBox
    func with_capacity(shallow n: USize) returns Vec__ExprPtrBox
    func len(self: Vec__ExprPtrBox) returns shallow USize
    func clear(mut self: Vec__ExprPtrBox)
    func delete_marked(mut self: Vec__ExprPtrBox, marks: Dynamic)
    func push(mut self: Vec__ExprPtrBox, own val: ExprPtrBox)
    func append(mut self: Vec__ExprPtrBox, own other: Vec__ExprPtrBox)
    func take(mut self: Vec__ExprPtrBox) returns Vec__ExprPtrBox
    func move_to(own self: Vec__ExprPtrBox, mut dest: Vec__ExprPtrBox)
    func move_from(mut self: Vec__ExprPtrBox, own other: Vec__ExprPtrBox)
    func unsafe_get(self: Vec__ExprPtrBox, i: USize) returns ref ExprPtrBox
    func get(self: Vec__ExprPtrBox, i: USize, mut _err_kind: I64) returns ref ExprPtrBox
    func pop(mut self: Vec__ExprPtrBox) returns ExprPtrBox
    func remove_at(mut self: Vec__ExprPtrBox, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__ExprPtrBox, shallow n: USize) returns Vec__ExprPtrBox
    func truncate(mut self: Vec__ExprPtrBox, shallow n: USize)
    func unsafe_set(mut self: Vec__ExprPtrBox, shallow i: USize, own val: ExprPtrBox)
    func set(mut self: Vec__ExprPtrBox, shallow i: USize, own val: ExprPtrBox, mut _err_kind: I64)
    func push_take(mut self: Vec__ExprPtrBox, mut src: Vec__ExprPtrBox, shallow i: USize)
    func delete(own self: Vec__ExprPtrBox, shallow call_free: Bool)
    func clone(self: Vec__ExprPtrBox) returns Vec__ExprPtrBox
    func size() returns shallow USize
}
#+end_src

* Vec__ExprPtrBox.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.new
:group: Compiler frontend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__ExprPtrBox.new() returns Vec__ExprPtrBox
#+end_src

* Vec__ExprPtrBox.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.with_capacity
:group: Compiler frontend
:END:

Create an empty Vec pre-sized for `n` elements, so pushing up to `n`
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
`new()` + repeated `push` pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__ExprPtrBox.with_capacity(shallow n: USize) returns Vec__ExprPtrBox
#+end_src

* Vec__ExprPtrBox.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.clear
:group: Compiler frontend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__ExprPtrBox.clear(mut self: Vec__ExprPtrBox)
#+end_src

* Vec__ExprPtrBox.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.delete_marked
:group: Compiler frontend
:END:

Drop the elements flagged in `marks` (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__ExprPtrBox.delete_marked(mut self: Vec__ExprPtrBox, marks: Dynamic)
#+end_src

* Vec__ExprPtrBox.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.push
:group: Compiler frontend
:END:

Append an element to the end. `val` is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__ExprPtrBox.push(mut self: Vec__ExprPtrBox, own val: ExprPtrBox)
#+end_src

* Vec__ExprPtrBox.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.append
:group: Compiler frontend
:END:

Consume the contents of `other` by appending them to `self`.
`other` is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__ExprPtrBox.append(mut self: Vec__ExprPtrBox, own other: Vec__ExprPtrBox)
#+end_src

* Vec__ExprPtrBox.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.take
:group: Compiler frontend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(`mut old := body.children.take()`): a field cannot be an `own`
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__ExprPtrBox.take(mut self: Vec__ExprPtrBox) returns Vec__ExprPtrBox
#+end_src

* Vec__ExprPtrBox.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.move_to
:group: Compiler frontend
:END:

Give self's contents to `dest`, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: `Vec(T).move_to(own src, dest)`.

The three fields move as BYTES on purpose: `dest.data =
self.data` drops dest's old pointer (right) and then
`self.data = 0` drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__ExprPtrBox.move_to(own self: Vec__ExprPtrBox, mut dest: Vec__ExprPtrBox)
#+end_src

* Vec__ExprPtrBox.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.move_from
:group: Compiler frontend
:END:

Take `other`'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: `dest.move_from(own src)`.

#+begin_src til
func Vec__ExprPtrBox.move_from(mut self: Vec__ExprPtrBox, own other: Vec__ExprPtrBox)
#+end_src

* Vec__ExprPtrBox.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.unsafe_get
:group: Compiler frontend
:END:

Unchecked element access: return a reference to the element at
index `i` with NO bounds check. The caller must prove `i < len()`.
This is the unchecked core that `get` wraps; for-in uses it
directly because its `_fi < len()` termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__ExprPtrBox.unsafe_get(self: Vec__ExprPtrBox, i: USize) returns ref ExprPtrBox
#+end_src

* Vec__ExprPtrBox.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.get
:group: Compiler frontend
:END:

Return a reference to the element at index `i`. Throws
OutOfBounds if `i` is past the end; call as `vec[i]`
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__ExprPtrBox.get(self: Vec__ExprPtrBox, i: USize, mut _err_kind: I64) returns ref ExprPtrBox
#+end_src

* Vec__ExprPtrBox.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.pop
:group: Compiler frontend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__ExprPtrBox.pop(mut self: Vec__ExprPtrBox) returns ExprPtrBox
#+end_src

* Vec__ExprPtrBox.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.remove_at
:group: Compiler frontend
:END:

Remove and drop the element at `i`, shifting later elements left.

#+begin_src til
func Vec__ExprPtrBox.remove_at(mut self: Vec__ExprPtrBox, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__ExprPtrBox.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.take_prefix
:group: Compiler frontend
:END:

Split off the first `n` elements into a new Vec. Panics if `n`
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__ExprPtrBox.take_prefix(mut self: Vec__ExprPtrBox, shallow n: USize) returns Vec__ExprPtrBox
#+end_src

* Vec__ExprPtrBox.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.truncate
:group: Compiler frontend
:END:

Drop the elements from index `n` to the end, freeing each via
elem_delete, and set count to `n` (the survivors [0, n) are left
untouched). Panics if `n` exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__ExprPtrBox.truncate(mut self: Vec__ExprPtrBox, shallow n: USize)
#+end_src

* Vec__ExprPtrBox.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.unsafe_set
:group: Compiler frontend
:END:

Overwrite the element at index `i` without a bounds check, dropping
the old value. Caller guarantees `i < self.count`; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__ExprPtrBox.unsafe_set(mut self: Vec__ExprPtrBox, shallow i: USize, own val: ExprPtrBox)
#+end_src

* Vec__ExprPtrBox.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.set
:group: Compiler frontend
:END:

Overwrite the element at index `i`. `val` is owned. Throws
OutOfBounds if `i` is past the end; call as `vec[i] = val`
(or `vec.set(i, own val)!`) to panic on a bad index.

#+begin_src til
func Vec__ExprPtrBox.set(mut self: Vec__ExprPtrBox, shallow i: USize, own val: ExprPtrBox, mut _err_kind: I64)
#+end_src

* Vec__ExprPtrBox.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.push_take
:group: Compiler frontend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__ExprPtrBox.push_take(mut self: Vec__ExprPtrBox, mut src: Vec__ExprPtrBox, shallow i: USize)
#+end_src

* Vec__ExprPtrBox.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.delete
:group: Compiler frontend
:END:

Drop each element via elem_delete, free storage, then free self
unless `call_free` is false.

#+begin_src til
func Vec__ExprPtrBox.delete(own self: Vec__ExprPtrBox, shallow call_free: Bool)
#+end_src

* Vec__ExprPtrBox.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.Vec__ExprPtrBox.clone
:group: Compiler frontend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__ExprPtrBox.clone(self: Vec__ExprPtrBox) returns Vec__ExprPtrBox
#+end_src

* adopt__BorrowRoot
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__BorrowRoot
:group: Compiler frontend
:END:

#+begin_src til
func adopt__BorrowRoot(dest: Dynamic, own src: BorrowRoot)
#+end_src

* adopt__TypeBinding
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__TypeBinding
:group: Compiler frontend
:END:

#+begin_src til
func adopt__TypeBinding(dest: Dynamic, own src: TypeBinding)
#+end_src

* adopt__U32
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__U32
:group: Compiler frontend
:END:

#+begin_src til
func adopt__U32(dest: Dynamic, own src: U32)
#+end_src

* adopt__Mode
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__Mode
:group: Compiler frontend
:END:

#+begin_src til
func adopt__Mode(dest: Dynamic, own src: Mode)
#+end_src

* adopt__FuncType
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__FuncType
:group: Compiler frontend
:END:

#+begin_src til
func adopt__FuncType(dest: Dynamic, own src: FuncType)
#+end_src

* adopt__ImportUnit
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__ImportUnit
:group: Compiler frontend
:END:

#+begin_src til
func adopt__ImportUnit(dest: Dynamic, own src: ImportUnit)
#+end_src

* adopt__StructLayout
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__StructLayout
:group: Compiler frontend
:END:

#+begin_src til
func adopt__StructLayout(dest: Dynamic, own src: StructLayout)
#+end_src

* adopt__Vec__Str
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__Vec__Str
:group: Compiler frontend
:END:

#+begin_src til
func adopt__Vec__Str(dest: Dynamic, own src: Vec__Str)
#+end_src

* adopt__FFIEntry
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__FFIEntry
:group: Compiler frontend
:END:

#+begin_src til
func adopt__FFIEntry(dest: Dynamic, own src: FFIEntry)
#+end_src

* adopt__ExprPtrBox
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__ExprPtrBox
:group: Compiler frontend
:END:

#+begin_src til
func adopt__ExprPtrBox(dest: Dynamic, own src: ExprPtrBox)
#+end_src

* adopt__FFITypePtrBox
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__FFITypePtrBox
:group: Compiler frontend
:END:

#+begin_src til
func adopt__FFITypePtrBox(dest: Dynamic, own src: FFITypePtrBox)
#+end_src

* adopt__I64
:PROPERTIES:
:CUSTOM_ID: til.src.self.context.adopt__I64
:group: Compiler frontend
:END:

#+begin_src til
func adopt__I64(dest: Dynamic, own src: I64)
#+end_src
