#+TITLE: src/self/scavenger.til
#+SUBTITLE: til 0.0.1-80788b616
#+OPTIONS: toc:t

* vec_push_str
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.vec_push_str
:group: Compiler backend
:END:

#+begin_src til
func vec_push_str(mut v: Vec__Str, s: Str)
#+end_src

* push_qn
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.push_qn
:group: Compiler backend
:END:

#+begin_src til
func push_qn(mut v: Vec__Str, type_name: Str, method: Str)
#+end_src

* collect_refs
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.collect_refs
:group: Compiler backend
:END:

#+begin_src til
func collect_refs(e: Expr, mut refs: Vec__Str, symbols: SymbolPool)
#+end_src

* scav_value_decl_sweepable
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.scav_value_decl_sweepable
:group: Compiler backend
:END:

#+begin_src til
func scav_value_decl_sweepable(stmt: Expr, ctx: Context) returns shallow Bool
#+end_src

* scavenge_filter
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.scavenge_filter
:group: Compiler backend
:END:

#+begin_src til
func scavenge_filter(mut program: Expr, visited: Set__Str, shallow sweep_values: Bool, mut swept_names: Vec__Str, ctx: Context)
#+end_src

* dce_program_bodies
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.dce_program_bodies
:group: Compiler backend
:END:

#+begin_src til
proc dce_program_bodies(mut program: Expr, symbols: SymbolPool)
#+end_src

* scavenge_imported
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.scavenge_imported
:group: Compiler backend
:END:

#+begin_src til
proc scavenge_imported(mut lp: LoadedProgram)
#+end_src

* Map__Str_DeclRef
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef
:group: Compiler backend
:END:

#+begin_src til
struct Map__Str_DeclRef {
    mut keys : Vec__Str
    mut values : Vec__DeclRef
namespace:
    func new() returns Map__Str_DeclRef
    func with_capacity(shallow n: USize) returns Map__Str_DeclRef
    func len(self: Map__Str_DeclRef) returns shallow USize
    func clear(mut self: Map__Str_DeclRef)
    func has(self: Map__Str_DeclRef, key: Str) returns shallow Bool
    func get(self: Map__Str_DeclRef, key: Str, mut _err_kind: I64) returns ref priv___src_self_scavenger_til__DeclRef
    func set(mut self: Map__Str_DeclRef, key: Str, own val: priv___src_self_scavenger_til__DeclRef)
    func remove(mut self: Map__Str_DeclRef, key: Str) returns shallow Bool
    func delete(own self: Map__Str_DeclRef, shallow call_free: Bool)
    func clone(self: Map__Str_DeclRef) returns Map__Str_DeclRef
    func hash(self: Map__Str_DeclRef, hasher: HashFn) returns shallow U64
    func size() returns shallow USize
}
#+end_src

* Map__Str_DeclRef.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.new
:group: Compiler backend
:END:

Create an empty Map of the types provided for the key and values respectively.
The key type must implement ~cmp~.

#+begin_src til
func Map__Str_DeclRef.new() returns Map__Str_DeclRef
#+end_src

* Map__Str_DeclRef.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.with_capacity
:group: Compiler backend
:END:

Create an empty Map pre-sized for ~n~ entries, so inserting up to
~n~ reallocs neither backing Vec (Vec.with_capacity's
counterpart). n=0 behaves like new(): lazy-empty, no allocation.

#+begin_src til
func Map__Str_DeclRef.with_capacity(shallow n: USize) returns Map__Str_DeclRef
#+end_src

* Map__Str_DeclRef.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.clear
:group: Compiler backend
:END:

Drop all live keys and values but keep the allocated storage so the Map can be reused.

#+begin_src til
func Map__Str_DeclRef.clear(mut self: Map__Str_DeclRef)
#+end_src

* Map__Str_DeclRef.has
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.has
:group: Compiler backend
:END:

True if a value exists for the given key.

#+begin_src til
func Map__Str_DeclRef.has(self: Map__Str_DeclRef, key: Str) returns shallow Bool
#+end_src

* Map__Str_DeclRef.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.get
:group: Compiler backend
:END:

Return a ref to the value for ~key~. Throws KeyNotFound if
~key~ is absent; call as ~map.get(k)!~ (or ~map[k]~) to panic
on a missing key.

#+begin_src til
func Map__Str_DeclRef.get(self: Map__Str_DeclRef, key: Str, mut _err_kind: I64) returns ref priv___src_self_scavenger_til__DeclRef
#+end_src

* Map__Str_DeclRef.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.set
:group: Compiler backend
:END:

Insert or overwrite the value for ~key~. The value is owned.

#+begin_src til
func Map__Str_DeclRef.set(mut self: Map__Str_DeclRef, key: Str, own val: priv___src_self_scavenger_til__DeclRef)
#+end_src

* Map__Str_DeclRef.remove
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Map__Str_DeclRef.remove
:group: Compiler backend
:END:

Remove ~key~, dropping its key and value. Returns false if absent.

#+begin_src til
func Map__Str_DeclRef.remove(mut self: Map__Str_DeclRef, key: Str) returns shallow Bool
#+end_src

* Vec__DeclRef
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef
:group: Compiler backend
:END:

#+begin_src til
struct Vec__DeclRef {
    own mut data : U8
    mut count : USize
    mut cap : USize
namespace:
    func new() returns Vec__DeclRef
    func with_capacity(shallow n: USize) returns Vec__DeclRef
    func len(self: Vec__DeclRef) returns shallow USize
    func clear(mut self: Vec__DeclRef)
    func delete_marked(mut self: Vec__DeclRef, marks: Dynamic)
    func push(mut self: Vec__DeclRef, own val: priv___src_self_scavenger_til__DeclRef)
    func append(mut self: Vec__DeclRef, own other: Vec__DeclRef)
    func take(mut self: Vec__DeclRef) returns Vec__DeclRef
    func move_to(own self: Vec__DeclRef, mut dest: Vec__DeclRef)
    func move_from(mut self: Vec__DeclRef, own other: Vec__DeclRef)
    func unsafe_get(self: Vec__DeclRef, i: USize) returns ref priv___src_self_scavenger_til__DeclRef
    func get(self: Vec__DeclRef, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_scavenger_til__DeclRef
    func pop(mut self: Vec__DeclRef) returns priv___src_self_scavenger_til__DeclRef
    func remove_at(mut self: Vec__DeclRef, shallow i: USize, mut _err_kind: I64)
    func take_prefix(mut self: Vec__DeclRef, shallow n: USize) returns Vec__DeclRef
    func truncate(mut self: Vec__DeclRef, shallow n: USize)
    func unsafe_set(mut self: Vec__DeclRef, shallow i: USize, own val: priv___src_self_scavenger_til__DeclRef)
    func set(mut self: Vec__DeclRef, shallow i: USize, own val: priv___src_self_scavenger_til__DeclRef, mut _err_kind: I64)
    func push_take(mut self: Vec__DeclRef, mut src: Vec__DeclRef, shallow i: USize)
    func delete(own self: Vec__DeclRef, shallow call_free: Bool)
    func clone(self: Vec__DeclRef) returns Vec__DeclRef
    func size() returns shallow USize
}
#+end_src

* Vec__DeclRef.new
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.new
:group: Compiler backend
:END:

Create a new empty Vec for elements of type T. Lazy: no allocation
until the first push -- an empty Vec is (data=NULL, count=0, cap=0),
and the grow sites below seed cap 0->1 on the first insert. This keeps
the many containers that stay empty (e.g. the unused maps in a fresh
Scope) allocation-free, which is the dominant per-call interpreter cost.

#+begin_src til
func Vec__DeclRef.new() returns Vec__DeclRef
#+end_src

* Vec__DeclRef.with_capacity
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.with_capacity
:group: Compiler backend
:END:

Create an empty Vec pre-sized for ~n~ elements, so pushing up to ~n~
items reallocs zero times. Use at call sites where the final length is
known up front (cloning a Vec of known length, building a fixed arg
list, ...) to avoid the 1->2->4->...->n grow-and-realloc churn that
~new()~ + repeated ~push~ pays. n=0 behaves like new(): lazy-empty,
no allocation (the grow sites seed cap 0->1 on the first insert).

#+begin_src til
func Vec__DeclRef.with_capacity(shallow n: USize) returns Vec__DeclRef
#+end_src

* Vec__DeclRef.clear
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.clear
:group: Compiler backend
:END:

Drop all live elements via elem_delete but keep the allocated
storage and element-type metadata so the Vec can be reused.

#+begin_src til
func Vec__DeclRef.clear(mut self: Vec__DeclRef)
#+end_src

* Vec__DeclRef.delete_marked
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.delete_marked
:group: Compiler backend
:END:

Drop the elements flagged in ~marks~ (a byte-per-element buffer,
nonzero = drop, at least self.count entries) via elem_delete, then
compact the survivors forward and update count. This is the
container home for the scavenger's mark/sweep/compact, which used
to reach into data/count directly.

#+begin_src til
func Vec__DeclRef.delete_marked(mut self: Vec__DeclRef, marks: Dynamic)
#+end_src

* Vec__DeclRef.push
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.push
:group: Compiler backend
:END:

Append an element to the end. ~val~ is owned: after this call the
caller must not use the value passed -- clone it first if needed.

#+begin_src til
func Vec__DeclRef.push(mut self: Vec__DeclRef, own val: priv___src_self_scavenger_til__DeclRef)
#+end_src

* Vec__DeclRef.append
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.append
:group: Compiler backend
:END:

Consume the contents of ~other~ by appending them to ~self~.
~other~ is taken by ownership and will be empty after the call.

#+begin_src til
func Vec__DeclRef.append(mut self: Vec__DeclRef, own other: Vec__DeclRef)
#+end_src

* Vec__DeclRef.take
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.take
:group: Compiler backend
:END:

Take the contents out: the returned Vec owns self's buffer and
self is left lazy-empty. No element is copied or cloned.

This is how a struct FIELD hands its contents over
(~mut old := body.children.take()~): a field cannot be an ~own~
argument or receiver, because the callee would then free the
ADDRESS OF THE FIELD -- the compiler dies with "free(): invalid
pointer" compiling itself. Everything the caller can give away
uses the two move forms below instead.

#+begin_src til
func Vec__DeclRef.take(mut self: Vec__DeclRef) returns Vec__DeclRef
#+end_src

* Vec__DeclRef.move_to
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.move_to
:group: Compiler backend
:END:

Give self's contents to ~dest~, which drops whatever it held.
SELF is consumed, so the receiver is the owned one and the call
spells the type out: ~Vec(T).move_to(own src, dest)~.

The three fields move as BYTES on purpose:
~dest.data = self.data~ drops dest's old pointer (right) and then
~self.data = 0~ drops the pointer dest just adopted -- a double
free, which is what the emitted C does with field assignment.
Blanking the source leaves a genuinely valid lazy-empty Vec, so
a later push on it allocates afresh instead of reallocating a
buffer that now belongs to someone else.

#+begin_src til
func Vec__DeclRef.move_to(own self: Vec__DeclRef, mut dest: Vec__DeclRef)
#+end_src

* Vec__DeclRef.move_from
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.move_from
:group: Compiler backend
:END:

Take ~other~'s contents, dropping whatever self held. OTHER is
consumed -- it is the value being emptied, so it is the owned
argument: ~dest.move_from(own src)~.

#+begin_src til
func Vec__DeclRef.move_from(mut self: Vec__DeclRef, own other: Vec__DeclRef)
#+end_src

* Vec__DeclRef.unsafe_get
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.unsafe_get
:group: Compiler backend
:END:

Unchecked element access: return a reference to the element at
index ~i~ with NO bounds check. The caller must prove ~i < len()~.
This is the unchecked core that ~get~ wraps; for-in uses it
directly because its ~_fi < len()~ termination test already
proves the index in bounds (issue #284).

#+begin_src til
func Vec__DeclRef.unsafe_get(self: Vec__DeclRef, i: USize) returns ref priv___src_self_scavenger_til__DeclRef
#+end_src

* Vec__DeclRef.get
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.get
:group: Compiler backend
:END:

Return a reference to the element at index ~i~. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i]~
to panic on a bad index. The reference is valid until the
next mutation.

#+begin_src til
func Vec__DeclRef.get(self: Vec__DeclRef, shallow i: USize, mut _err_kind: I64) returns ref priv___src_self_scavenger_til__DeclRef
#+end_src

* Vec__DeclRef.pop
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.pop
:group: Compiler backend
:END:

Remove and return the last element. Panics if empty. The caller
takes ownership of the returned value.

#+begin_src til
func Vec__DeclRef.pop(mut self: Vec__DeclRef) returns priv___src_self_scavenger_til__DeclRef
#+end_src

* Vec__DeclRef.remove_at
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.remove_at
:group: Compiler backend
:END:

Remove and drop the element at ~i~, shifting later elements left.

#+begin_src til
func Vec__DeclRef.remove_at(mut self: Vec__DeclRef, shallow i: USize, mut _err_kind: I64)
#+end_src

* Vec__DeclRef.take_prefix
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.take_prefix
:group: Compiler backend
:END:

Split off the first ~n~ elements into a new Vec. Panics if ~n~
exceeds self.len(). The remaining elements shift to the front.

#+begin_src til
func Vec__DeclRef.take_prefix(mut self: Vec__DeclRef, shallow n: USize) returns Vec__DeclRef
#+end_src

* Vec__DeclRef.truncate
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.truncate
:group: Compiler backend
:END:

Drop the elements from index ~n~ to the end, freeing each via
elem_delete, and set count to ~n~ (the survivors [0, n) are left
untouched). Panics if ~n~ exceeds len. This is the container home
for a pop-and-free-the-tail loop shrinking a Vec back to a mark.

#+begin_src til
func Vec__DeclRef.truncate(mut self: Vec__DeclRef, shallow n: USize)
#+end_src

* Vec__DeclRef.unsafe_set
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.unsafe_set
:group: Compiler backend
:END:

Overwrite the element at index ~i~ without a bounds check, dropping
the old value. Caller guarantees ~i < self.count~; the write-side
mirror of unsafe_get (issue #284).

#+begin_src til
func Vec__DeclRef.unsafe_set(mut self: Vec__DeclRef, shallow i: USize, own val: priv___src_self_scavenger_til__DeclRef)
#+end_src

* Vec__DeclRef.set
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.set
:group: Compiler backend
:END:

Overwrite the element at index ~i~. ~val~ is owned. Throws
OutOfBounds if ~i~ is past the end; call as ~vec[i] = val~
(or ~vec.set(i, own val)!~) to panic on a bad index.

#+begin_src til
func Vec__DeclRef.set(mut self: Vec__DeclRef, shallow i: USize, own val: priv___src_self_scavenger_til__DeclRef, mut _err_kind: I64)
#+end_src

* Vec__DeclRef.push_take
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.push_take
:group: Compiler backend
:END:

Move bytes of src[i] into self (push). Source slot is zeroed so that
src's eventual elem_delete on it is a no-op (relies on the same
count==0/data==NULL convention used by take_children and Vec.delete).

#+begin_src til
func Vec__DeclRef.push_take(mut self: Vec__DeclRef, mut src: Vec__DeclRef, shallow i: USize)
#+end_src

* Vec__DeclRef.delete
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.delete
:group: Compiler backend
:END:

Drop each element via elem_delete, free storage, then free self
unless ~call_free~ is false.

#+begin_src til
func Vec__DeclRef.delete(own self: Vec__DeclRef, shallow call_free: Bool)
#+end_src

* Vec__DeclRef.clone
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.Vec__DeclRef.clone
:group: Compiler backend
:END:

Deep-copy the Vec and all its elements via elem_clone.

#+begin_src til
func Vec__DeclRef.clone(self: Vec__DeclRef) returns Vec__DeclRef
#+end_src

* adopt__priv___src_self_scavenger_til__DeclRef
:PROPERTIES:
:CUSTOM_ID: til.src.self.scavenger.adopt__priv___src_self_scavenger_til__DeclRef
:group: Compiler backend
:END:

#+begin_src til
func adopt__priv___src_self_scavenger_til__DeclRef(dest: Dynamic, own src: priv___src_self_scavenger_til__DeclRef)
#+end_src
