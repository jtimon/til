#+TITLE: src/self/dispatch.til
#+OPTIONS: toc:t

* priv___src_self_dispatch_til__ExtStr
#+begin_src til
struct priv___src_self_dispatch_til__ExtStr {
    ref mut data : U8
    mut count : U32
    mut cap : U32
namespace:
    func clone(self: priv___src_self_dispatch_til__ExtStr) returns priv___src_self_dispatch_til__ExtStr
    func delete(own self: priv___src_self_dispatch_til__ExtStr, shallow call_free: Bool)
    func size() returns shallow U32
}
#+end_src

* FFIEntry
#+begin_src til
struct FFIEntry {
    ref mut fn : U8
    ref mut return_type : Str
    mut nparam : I32
    ref mut param_shallows : U8
    mut return_is_shallow : Bool
    ref mut cif : ffi_cif
    ref mut arg_types : Dynamic
namespace:
    func clone(self: FFIEntry) returns FFIEntry
    func delete(own self: FFIEntry, shallow call_free: Bool)
    func size() returns shallow U32
}
#+end_src

* ExprPtrBox
#+begin_src til
struct ExprPtrBox {
    ref mut ptr : Expr
namespace:
    func clone(self: ExprPtrBox) returns ExprPtrBox
    func delete(own self: ExprPtrBox, shallow call_free: Bool)
    func size() returns shallow U32
}
#+end_src

* FFITypePtrBox
#+begin_src til
struct FFITypePtrBox {
    ref mut ptr : ffi_type
namespace:
    func clone(self: FFITypePtrBox) returns FFITypePtrBox
    func delete(own self: FFITypePtrBox, shallow call_free: Bool)
    func size() returns shallow U32
}
#+end_src

* ffi_reset
#+begin_src til
proc ffi_reset(ctx: Context)
#+end_src

* priv___src_self_dispatch_til__ffi_type_ref
#+begin_src til
func priv___src_self_dispatch_til__ffi_type_ref(name: Str) returns ref ffi_type
#+end_src

* ffi_alloc_cif
#+begin_src til
func ffi_alloc_cif() returns Dynamic
#+end_src

* ffi_cif_rtype_is_struct
#+begin_src til
func ffi_cif_rtype_is_struct(cif: ffi_cif) returns shallow Bool
#+end_src

* ffi_load_global_lib
#+begin_src til
ext_func ffi_load_global_lib(soname: Str) returns shallow Bool
#+end_src

* ffi_open_user_so
#+begin_src til
ext_func ffi_open_user_so(path: Str) returns shallow Bool
#+end_src

* ffi_close_user_so
#+begin_src til
ext_proc ffi_close_user_so()
#+end_src

* ffi_user_symbol
#+begin_src til
ext_func ffi_user_symbol(name: Str) returns ref U8
#+end_src

* ffi_global_symbol
#+begin_src til
ext_func ffi_global_symbol(name: Str) returns ref U8
#+end_src

* ffi_last_error
#+begin_src til
ext_func ffi_last_error() returns Str
#+end_src

* stderr_print
#+begin_src til
ext_func stderr_print(msg: Str) returns shallow I32
#+end_src

* system_cmd
#+begin_src til
ext_func system_cmd(cmd: Str) returns shallow I32
#+end_src

* unlink_path
#+begin_src til
ext_proc unlink_path(path: Str)
#+end_src

* process_id
#+begin_src til
ext_func process_id() returns shallow I32
#+end_src

* ffi_dlsym
#+begin_src til
func ffi_dlsym(name: Str) returns ref U8
#+end_src

* ffi_init_link_libs
#+begin_src til
func ffi_init_link_libs(link_flags: Str)
#+end_src

* ffi_init_user_so
#+begin_src til
func ffi_init_user_so(fwd_path: Str, user_c_path: Str, ext_c_path: Str, link_flags: Str, mut so_path_out: Str) returns shallow I32
#+end_src

* ffi_init_struct_defs
#+begin_src til
func ffi_init_struct_defs(program: Expr, ctx: Context)
#+end_src

* ffi_init_struct_defs_append
#+begin_src til
func ffi_init_struct_defs_append(program: Expr, ctx: Context)
#+end_src

* value_to_u64
#+begin_src til
func value_to_u64(v: Value) returns shallow U64
#+end_src

* uptr_ffi_type
#+begin_src til
func uptr_ffi_type(ctx: Context) returns ref ffi_type
#+end_src

* usize_ffi_type
#+begin_src til
func usize_ffi_type(ctx: Context) returns ref ffi_type
#+end_src

* priv___src_self_dispatch_til__value_to_f32
#+begin_src til
func priv___src_self_dispatch_til__value_to_f32(v: Value) returns shallow F32
#+end_src

* priv___src_self_dispatch_til__value_variant_name
#+begin_src til
func priv___src_self_dispatch_til__value_variant_name(v: Value) returns Str
#+end_src

* priv___src_self_dispatch_til__narrow_bool_arg
#+begin_src til
proc priv___src_self_dispatch_til__narrow_bool_arg(s: Scope, e: Expr, ctx: Context) returns shallow Bool
#+end_src

* h_Bool_and
#+begin_src til
proc h_Bool_and(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_Bool_or
#+begin_src til
proc h_Bool_or(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_Bool_not
#+begin_src til
proc h_Bool_not(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_U8_from_i64
#+begin_src til
proc h_U8_from_i64(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_I16_from_i64
#+begin_src til
proc h_I16_from_i64(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_U16_from_i64
#+begin_src til
proc h_U16_from_i64(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_I32_from_i64
#+begin_src til
proc h_I32_from_i64(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_F32_from_i64
#+begin_src til
proc h_F32_from_i64(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_U32_from_i64
#+begin_src til
proc h_U32_from_i64(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_cast
#+begin_src til
proc h_cast(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* priv___src_self_dispatch_til__h_print_single
#+begin_src til
proc priv___src_self_dispatch_til__h_print_single(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_print_flush
#+begin_src til
proc h_print_flush(_s: Scope, _e: Expr, mut r: Value, _ctx: Context) returns shallow Bool
#+end_src

* h_exit
#+begin_src til
proc h_exit(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_readfile
#+begin_src til
proc h_readfile(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_writefile
#+begin_src til
proc h_writefile(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_spawn_cmd
#+begin_src til
proc h_spawn_cmd(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* val_to_ptr
#+begin_src til
func val_to_ptr(v: Value) returns ref Dynamic
#+end_src

* h_dyn_has_method
#+begin_src til
proc h_dyn_has_method(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_dyn_fn
#+begin_src til
proc h_dyn_fn(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_dyn_size_of
#+begin_src til
proc h_dyn_size_of(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* priv___src_self_dispatch_til__str_to_owned_value
#+begin_src til
func priv___src_self_dispatch_til__str_to_owned_value(src: Str, ctx: Context) returns shallow Value
#+end_src

* interp_lookup_struct
#+begin_src til
proc interp_lookup_struct(s: Scope, e: Expr, type_name: Str, who: Str, ctx: Context) returns ref Expr
#+end_src

* h_struct_field_count
#+begin_src til
proc h_struct_field_count(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_struct_field_name
#+begin_src til
proc h_struct_field_name(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_struct_field_is_mut
#+begin_src til
proc h_struct_field_is_mut(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_struct_field_type
#+begin_src til
proc h_struct_field_type(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* interp_lookup_enum
#+begin_src til
proc interp_lookup_enum(s: Scope, e: Expr, type_name: Str, who: Str, ctx: Context) returns ref Expr
#+end_src

* h_enum_variant_count
#+begin_src til
proc h_enum_variant_count(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_enum_variant_name
#+begin_src til
proc h_enum_variant_name(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_enum_variant_has_payload
#+begin_src til
proc h_enum_variant_has_payload(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_enum_variant_payload_type
#+begin_src til
proc h_enum_variant_payload_type(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* interp_lookup_funcsig
#+begin_src til
proc interp_lookup_funcsig(s: Scope, e: Expr, type_name: Str, who: Str, ctx: Context) returns ref Expr
#+end_src

* h_func_sig_param_count
#+begin_src til
proc h_func_sig_param_count(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_func_sig_param_type
#+begin_src til
proc h_func_sig_param_type(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_func_sig_return_count
#+begin_src til
proc h_func_sig_return_count(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_func_sig_return_type
#+begin_src til
proc h_func_sig_return_type(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_func_sig_throw_count
#+begin_src til
proc h_func_sig_throw_count(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_func_sig_throw_type
#+begin_src til
proc h_func_sig_throw_type(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_dyn_type_to_str
#+begin_src til
proc h_dyn_type_to_str(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_check_cmd_status
#+begin_src til
proc h_check_cmd_status(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_sleep
#+begin_src til
proc h_sleep(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_file_mtime
#+begin_src til
proc h_file_mtime(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_str_parse_f32
#+begin_src til
proc h_str_parse_f32(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_clock_ms
#+begin_src til
func h_clock_ms(_s: Scope, _e: Expr, mut r: Value) returns shallow Bool
#+end_src

* h_get_thread_count
#+begin_src til
func h_get_thread_count(_s: Scope, _e: Expr, mut r: Value) returns shallow Bool
#+end_src

* h_cfile_open
#+begin_src til
proc h_cfile_open(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* priv___src_self_dispatch_til__known_ffi_type
#+begin_src til
func priv___src_self_dispatch_til__known_ffi_type(type_name: Str, ctx: Context) returns ref ffi_type
#+end_src

* priv___src_self_dispatch_til__shallow_ffi_type
#+begin_src til
func priv___src_self_dispatch_til__shallow_ffi_type(type_name: Str, ctx: Context) returns ref ffi_type
#+end_src

* field_ffi_type
#+begin_src til
func field_ffi_type(dd: Declaration, ctx: Context) returns ref ffi_type
#+end_src

* build_struct_ffi_type
#+begin_src til
func build_struct_ffi_type(struct_def: Expr, ctx: Context) returns ref ffi_type
#+end_src

* h_free
#+begin_src til
proc h_free(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_cfile_close
#+begin_src til
proc h_cfile_close(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_cfile_write_str
#+begin_src til
proc h_cfile_write_str(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_ptr_add
#+begin_src til
proc h_ptr_add(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* h_cfile_read_all
#+begin_src til
proc h_cfile_read_all(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* DispatchFn
#+begin_src til
proc DispatchFn(: Scope, : Expr, mut : Value, : Context) returns shallow Bool
#+end_src

* reg_dispatch
#+begin_src til
func reg_dispatch(name: Str, handler: DispatchFn, ctx: Context)
#+end_src

* dispatch_init
#+begin_src til
func dispatch_init(ctx: Context)
#+end_src

* ext_dispatch_builtin
#+begin_src til
func ext_dispatch_builtin(name: Str, scope: Scope, e: Expr, mut result: Value, ctx: Context) returns shallow Bool
#+end_src

* h_dyn_call
#+begin_src til
proc h_dyn_call(s: Scope, e: Expr, mut r: Value, ctx: Context) returns shallow Bool
#+end_src

* get_elem_size
#+begin_src til
proc get_elem_size(s: Scope, type_name: Str, src: Expr, ctx: Context) returns shallow I32
#+end_src

* enum_method_dispatch
#+begin_src til
proc enum_method_dispatch(method: Str, scope: Scope, enum_def: Expr, enum_name: Str, e: Expr, mut result: Value, ctx: Context) returns shallow Bool
#+end_src

* ffi_decode_scalar
#+begin_src til
func ffi_decode_scalar(rtype: Str, p: Dynamic, mut result: Value, ctx: Context) returns shallow Bool
#+end_src

* ffi_call_ret_struct_def
#+begin_src til
func ffi_call_ret_struct_def(fe: FFIEntry, ctx: Context) returns ref Expr
#+end_src

* priv___src_self_dispatch_til__ffi_shallow_type_info
#+begin_src til
func priv___src_self_dispatch_til__ffi_shallow_type_info(atype: Dynamic, mut type_name: Str, mut size: I32) returns shallow Bool
#+end_src

* priv___src_self_dispatch_til__ffi_write_shallow_arg
#+begin_src til
func priv___src_self_dispatch_til__ffi_write_shallow_arg(scope: Scope, v: Value, atype: Dynamic, dst: Dynamic, ctx: Context) returns shallow Bool
#+end_src

* ext_dispatch_ffi
#+begin_src til
proc ext_dispatch_ffi(name: Str, scope: Scope, e: Expr, mut result: Value, ctx: Context) returns shallow Bool
#+end_src

* ext_function_dispatch
#+begin_src til
proc ext_function_dispatch(name: Str, scope: Scope, e: Expr, mut result: Value, ctx: Context) returns shallow Bool
#+end_src

* ffi_register
#+begin_src til
proc ffi_register(name: Str, fn: Dynamic, fdef: Expr, ctx: Context)
#+end_src

* ffi_init_scan_program
#+begin_src til
proc ffi_init_scan_program(program: Expr, ctx: Context)
#+end_src

* ffi_init
#+begin_src til
proc ffi_init(program: Expr, fwd_path: Str, user_c_path: Str, ext_c_path: Str, link_flags: Str, ctx: Context) returns shallow I32
#+end_src

* ffi_cleanup
#+begin_src til
proc ffi_cleanup(ctx: Context)
#+end_src

