mode liba

// This file is mode liba, which allows print/println in funcs
// When imported, the mode should still be respected

// This func calls println, which should be allowed in mode liba
print_debug : func(msg: Str) returns I64 = {
    println("DEBUG:", msg)
    return 42
}
