mode test

// #32: Keyword arguments (kwargs) capture
// TODO(#121): kwargs desugaring still relies on a raw Map value path
// with empty val_type. Re-enable these tests once kwargs storage has a
// real explicit container type.

test_kwargs_todo := test() {
    // TODO(#121)
}
