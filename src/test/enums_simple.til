mode test

// Simple enum without payloads
ExampleEnum := enum {
    A,
    B,
    C,
}

// Test enum value assignment
test_simple_enum := proc() {
    a := ExampleEnum.A
    b := ExampleEnum.B
    println("Simple enum test passed")
}

test_simple_enum()
