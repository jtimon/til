mode test

// TODO uncoment in src/tests.cil
test(eq(Bool.to_int(true), atoi(eval_to_str("true"))), "eval_to_str() can't turn literal bools strings into 'true'")
test(eq(Bool.to_int(false), atoi(eval_to_str("false"))), "eval_to_str() can't turn literal bools strings into 'false'")
test(eq(42, atoi(eval_to_str("42"))), "eval_to_str() can't turn literal numbers strings into ints")
test(str_eq("aaa", eval_to_str("aaa")), "eval_to_str() can't turn literal strings into identical strings")
