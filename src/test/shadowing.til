mode test

test_shadow : proc() = {
    mut x := 1
    if true {
        mut x := "inner"
        println(x)
    }
    // After if block, x is now Str (TIL overwrites, not true shadowing)
    println(x)
}
test_shadow()
