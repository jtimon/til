mode lib

import("loader")
import("targets")

system_cmd := ext_func(cmd: Str) returns shallow I32 {}

priv CollectionInfo := struct {
    ref mut type_name : Str = NULL
    mut is_vec : I32 = 0
}

priv DynCallInfo := struct {
    ref mut method : Str = NULL
    mut nargs : I32 = 1
    mut has_return := false
}

// --- Globals ---

// Points at the LoadedProgram passed to cmd_build/cmd_run for the duration of
// codegen, so helpers can iterate per-file ASTs without building merged views.
mut codegen_lp_slot : Dynamic = 0
mut in_func_def := false
mut in_main_func := false
mut ctor_seq : I32 = 0

codegen_lp := func() returns ref LoadedProgram {
    if is_null(codegen_lp_slot) {
        panic(__LOC__, "builder: codegen_lp_slot is NULL")
    }
    ref lp : LoadedProgram = deref(codegen_lp_slot)
    if is_null(lp) {
        panic(__LOC__, "builder: codegen_lp_slot points to NULL")
    }
    return lp
}

codegen_set_lp := func(lp: LoadedProgram) {
    // Store pointer to stack-owned lp; valid for duration of build.
    codegen_lp_slot = to_ptr(lp)
}

codegen_clear_lp := func() {
    codegen_lp_slot = NULL
}

// --- Leaf functions ---

priv is_dyn_call_name := func(name: Str, mut has_ret: Bool) returns shallow Bool {
    if name.eq("dyn_call") { has_ret = false; return true }
    if name.eq("dyn_call_ret") { has_ret = true; return true }
    return false
}

// Per-function-emission cache of FuncPtr local signatures, populated at
// emit_stmt_decl and read back by fcall_fn_sig when a FuncPtr local is
// called. Lives on Context (#178 first table); the builder swaps it at
// emit_func_def boundaries to scope it per emission.
priv resolve_local_fn_sig := func(name: Str) returns ref Expr {
    ref m : Map = codegen_lp().ctx.local_fn_sigs
    if m.has(name).not() { return NULL }
    return deref(m.get(name))
}

// Resolve the FuncDef Expr that backs a FuncPtr-typed Decl, by looking
// at the rhs (Ident or FCall) and falling back to the declared
// explicit_type alias. Returns NULL if no resolution succeeds.
resolve_decl_fn_sig := func(rhs: Expr, dd: Declaration, scope: TypeScope) returns ref Expr {
    switch rhs.node_type {
    case NodeType.Ident(rhs_name):
        ref fdef : Expr = find_callee_fdef(rhs_name)
        if is_null(fdef).not() { return fdef }
        sf := scope.find(rhs_name)
        switch sf {
        case ScopeFind.Found(b):
            if is_null(b.func_def).not() { return b.func_def }
        case:
        }
    case NodeType.FCall:
        ref fdef : Expr = resolve_fn_sig(rhs, scope)
        if is_null(fdef).not() { return fdef }
    case:
    }
    // Fall back to the declared FuncSig alias name.
    if dd.explicit_type.len().eq(0) { return NULL }
    sf := scope.find(dd.explicit_type)
    switch sf {
    case ScopeFind.Found(b):
        if is_null(b.func_def).not() {
            if b.func_def.children.len().eq(0) { return b.func_def }
        }
    case:
    }
    return NULL
}

fcall_fn_sig := func(fcall: Expr) returns ref Expr {
    if is_null(fcall) { return NULL }
    if fcall.node_type.is(NodeType.FCall).not() { return NULL }
    if fcall.children.len().eq(0) { return NULL }
    ref callee : Expr = fcall.child(0)
    // Only FuncPtr-typed callees go through the indirect-call emit path, so
    // this is the only shape that actually needs an answer. Other callees
    // (struct constructors, regular func calls) have null fn_sig today.
    if callee.til_type.is(Type.FuncPtr).not() { return NULL }
    ref s : TypeScope = codegen_lp().ctx.scope
    ref via_scope : Expr = resolve_fn_sig(fcall, s)
    if is_null(via_scope).not() { return via_scope }
    // resolve_fn_sig only sees globals + struct fields. The remaining FuncPtr
    // shapes are params and locals of the function currently being emitted;
    // their bindings lived in a transient func_scope that is gone by now.
    switch callee.node_type {
    case NodeType.Ident(name):
        // Fallback 1: FuncPtr param of the current function. The param's
        // explicit_type is the FuncSig alias name (always a global binding).
        ref cf1 : Expr = codegen_lp().ctx.current_fdef
        if is_null(cf1).not() {
            switch cf1.node_type {
            case NodeType.FuncDef(cur_fd):
                for p : Declaration in cur_fd.params {
                    if p.name.eq(name).not() { continue }
                    if p.explicit_type.len().eq(0) { return NULL }
                    sf := s.find(p.explicit_type)
                    switch sf {
                    case ScopeFind.Found(ftb):
                        if is_null(ftb.func_def) { return NULL }
                        if ftb.func_def.children.len().neq(0) { return NULL }
                        return ftb.func_def
                    case:
                    }
                    return NULL
                }
            case:
            }
        }
        // Fallback 2: local FuncPtr decl whose signature was captured at
        // emit_stmt_decl time.
        ref via_local : Expr = resolve_local_fn_sig(name)
        if is_null(via_local).not() { return via_local }
        // Fallback 3: the Ident itself is a known global function name used
        // as a FuncPtr value (e.g. an inlined ref to a top-level func).
        ref via_global : Expr = find_callee_fdef(name)
        if is_null(via_global).not() { return via_global }
        return NULL
    case:
        return NULL
    }
}

priv is_stack_local := func(name: Str) returns shallow Bool {
    return codegen_lp().ctx.stack_locals.has(name)
}

priv is_current_func_param := func(name: Str) returns shallow Bool {
    ref cf : Expr = codegen_lp().ctx.current_fdef
    if is_null(cf) { return false }
    switch cf.node_type {
    case NodeType.FuncDef(fd):
        for p : Declaration in fd.params {
            if p.name.eq(name) { return true }
        }
        return false
    case:
        return false
    }
}

priv is_value_global := func(name: Str) returns shallow Bool {
    if is_current_func_param(name) { return false }
    return codegen_lp().ctx.script_globals.has(name)
}

is_ref_global := func(name: Str) returns shallow Bool {
    if is_current_func_param(name) { return false }
    return codegen_lp().ctx.ref_globals.has(name)
}

is_ref_local := func(name: Str) returns shallow Bool {
    if codegen_lp().ctx.ref_locals.has(name) { return true }
    return is_ref_global(name)
}

use_dot_access := func(obj: Expr) returns shallow Bool {
    if obj.node_type.is(NodeType.FieldAccess) {
        if builder_fa_is_ptr(obj).not() { return true }
    }
    switch obj.node_type {
    case NodeType.Ident(ident):
        if is_stack_local(ident) { return true }
        if is_value_global(ident) { return true }
    case:
    }
    return false
}

is_primitive_type := func(name: Str) returns shallow Bool {
    if name.eq("I64") { return true }
    if name.eq("U8") { return true }
    if name.eq("I8") { return true }
    if name.eq("I16") { return true }
    if name.eq("I32") { return true }
    if name.eq("U32") { return true }
    if name.eq("U64") { return true }
    if name.eq("USize") { return true }
    if name.eq("UPtr") { return true }
    if name.eq("F32") { return true }
    if name.eq("Bool") { return true }
    return false
}

// A POD struct has only Shallow-owned fields that are themselves
// stack-cheap: primitives, or nested POD structs. No Str / Vec /
// Array / Own-marked fields that would need recursive heap cleanup.
// Ctor Decls for these structs can live on the stack as a C compound
// literal (no malloc / no recursive free), and the same emission
// works inline as an expression when a non-POD parent struct has a
// POD field set from a ctor FCall.  Callers must still pair this with
// a non-freeing delete at scope end (call_free=false) so the cleanup
// doesn't try to free a stack pointer.
is_pod_struct := func(struct_name: Str) returns shallow Bool {
    if struct_name.len().eq(0) { return false }
    if is_primitive_type(struct_name) { return false }
    ref sdef : Expr = find_struct_def(struct_name)
    if is_null(sdef) { return false }
    switch sdef.node_type {
    case NodeType.StructDef(sdd):
        if sdd.fields.len().eq(0) { return false }
        for fd : Declaration in sdd.fields {
            if fd.own_type.is(OwnType.Own) { return false }
            if fd.own_type.is(OwnType.Ref) { return false }
            if is_primitive_type(fd.explicit_type) { continue }
            if is_pod_struct(fd.explicit_type) { continue }
            return false
        }
        return true
    case:
    }
    return false
}

is_funcsig_type := func(name: Str) returns shallow Bool {
    return codegen_lp().ctx.funcsig_names.has(name)
}

is_ext_h_type := func(name: Str) returns shallow Bool {
    if name.eq("U8") { return true }
    if name.eq("I8") { return true }
    if name.eq("I16") { return true }
    if name.eq("I32") { return true }
    if name.eq("F32") { return true }
    if name.eq("U32") { return true }
    if name.eq("U64") { return true }
    if name.eq("I64") { return true }
    if name.eq("Bool") { return true }
    return false
}

// print_single / print_flush have hand-written prototypes emitted as
// part of the standard prelude (see emit_header_defs_and_funcs_lp);
// suppressing the ext_proc-side emission avoids a duplicate decl.
is_skip_ext_decl := func(name: Str) returns shallow Bool {
    if name.eq("print_single") { return true }
    if name.eq("print_flush") { return true }
    return false
}

is_scalar_method_type := func(name: Str) returns shallow Bool {
    if name.eq("U8") { return true }
    if name.eq("I8") { return true }
    if name.eq("I16") { return true }
    if name.eq("I32") { return true }
    if name.eq("F32") { return true }
    if name.eq("U32") { return true }
    if name.eq("U64") { return true }
    if name.eq("I64") { return true }
    if name.eq("Bool") { return true }
    return false
}

is_exported_top_level_global := func(stmt: Expr) returns shallow Bool {
    if stmt.node_type.is(NodeType.Decl).not() { return false }
    ref rhs : Expr = stmt.child(0)
    if rhs.node_type.is(NodeType.FuncDef) { return false }
    if rhs.node_type.is(NodeType.StructDef) { return false }
    if rhs.node_type.is(NodeType.EnumDef) { return false }
    if stmt.til_type.is(Type.None) {
        if rhs.node_type.is(NodeType.Ident) { return false }
    }
    switch stmt.node_type {
    case NodeType.Decl(decl):
        if decl.name.starts_with("hoisted__") { return false }
        return true
    case:
        UNREACHABLE(__LOC__)
        return false
    }
}

priv find_struct_def := func(name: Str) returns ref Expr {
    ref m : Map = codegen_lp().ctx.struct_def_exprs
    if m.has(name).not() { return NULL }
    return deref(m.get(name))
}

priv find_callee_fdef := func(name: Str) returns ref Expr {
    ref m : Map = codegen_lp().ctx.func_defs
    if m.has(name).not() { return NULL }
    return deref(m.get(name))
}

fa_struct_name := func(e: Expr) returns Str {
    if e.children.len().eq(0) { return til_type_name(e.til_type) }
    ref obj : Expr = e.child(0)
    obj_sname := til_type_name(obj.til_type)
    if obj_sname.len().gt(0) { return obj_sname }
    switch obj.node_type {
    case NodeType.Ident(ident):
        return ident.clone()
    case:
    }
    return obj_sname
}

priv builder_fa_is_ptr_with_fname := func(sname: Str, fname: Str) returns shallow Bool {
    ref fap_def : Expr = find_struct_def(sname)
    if is_null(fap_def) { return false }
    switch fap_def.node_type {
    case NodeType.StructDef(fap_sdd):
        for dd : Declaration in fap_sdd.fields {
            if dd.name.eq(fname) {
                return dd.own_type.is(OwnType.Shallow).not()
            }
        }
        return false
    case:
        return false
    }
}

// Check if a FieldAccess/FieldAssign accesses an own/ref (pointer) field.
builder_fa_is_ptr := func(e: Expr) returns shallow Bool {
    sname := fa_struct_name(e)
    switch e.node_type {
    case NodeType.FieldAccess(fname):
        return builder_fa_is_ptr_with_fname(sname, fname)
    case NodeType.FieldAssign(fname):
        return builder_fa_is_ptr_with_fname(sname, fname)
    case:
        return false
    }
}

priv builder_fa_is_ns_with_fname := func(e: Expr, sname: Str, fname: Str) returns shallow Bool {
    ref bfn_def : Expr = find_struct_def(sname)
    if is_null(bfn_def) { return false }
    mut has_ns := false
    mut has_inst := false
    ref bfn_ns : Vec = def_ns_decls(bfn_def)
    for bfn_d : Declaration in bfn_ns {
        if bfn_d.name.eq(fname) { has_ns = true }
    }
    switch bfn_def.node_type {
    case NodeType.StructDef(bfn_sdd):
        for dd : Declaration in bfn_sdd.fields {
            if dd.name.eq(fname) { has_inst = true }
        }
    case:
    }
    if has_ns.and(has_inst.not()) { return true }
    if has_ns.not() { return false }
    ref obj : Expr = e.child(0)
    switch obj.node_type {
    case NodeType.Ident(ident):
        if ident.eq(sname) { return true }
        if ident.len().gt(0) {
            if ident.get(0).gte('A').and(ident.get(0).lte('Z')) {
                return true
            }
        }
    case:
    }
    return false
}

builder_fa_is_ns := func(e: Expr) returns shallow Bool {
    sname := fa_struct_name(e)
    switch e.node_type {
    case NodeType.FieldAccess(fname):
        return builder_fa_is_ns_with_fname(e, sname, fname)
    case NodeType.FieldAssign(fname):
        return builder_fa_is_ns_with_fname(e, sname, fname)
    case:
        return false
    }
}

is_shallow_param := func(name: Str) returns shallow Bool {
    ref cf : Expr = codegen_lp().ctx.current_fdef
    if is_null(cf) { return false }
    switch cf.node_type {
    case NodeType.FuncDef(fd):
        for p : Declaration in fd.params {
            if p.own_type.is(OwnType.Shallow) {
                if p.name.eq(name) { return true }
            }
        }
        return false
    case:
        UNREACHABLE(__LOC__)
        return false
    }
}

callee_returns_shallow := func(callee_name: Str) returns shallow Bool {
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return false }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        return fd.return_own_type.is(OwnType.Shallow)
    case:
        return false
    }
}

callee_returns_dynamic := func(callee_name: Str) returns shallow Bool {
    if callee_name.eq("malloc") { return true }
    if callee_name.eq("calloc") { return true }
    if callee_name.eq("realloc") { return true }
    if callee_name.eq("dyn_call_ret") { return true }
    if callee_name.eq("dyn_fn") { return true }
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return false }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        return fd.return_type.eq("Dynamic")
    case:
        return false
    }
}

callee_param_is_shallow := func(callee_name: Str, shallow arg_index: U32) returns shallow Bool {
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return false }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if arg_index.gte(fd.params.len()) { return false }
        if fd.params.len().eq(0) { return false }
        ref p : Declaration = fd.params.get(arg_index)
        return p.own_type.is(OwnType.Shallow)
    case:
        return false
    }
}

callee_param_is_usize := func(callee_name: Str, shallow arg_index: U32) returns shallow Bool {
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return false }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if arg_index.gte(fd.params.len()) { return false }
        ref p : Declaration = fd.params.get(arg_index)
        return p.explicit_type.eq("USize")
    case:
        return false
    }
}

callee_param_is_uptr := func(callee_name: Str, shallow arg_index: U32) returns shallow Bool {
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return false }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if arg_index.gte(fd.params.len()) { return false }
        ref p : Declaration = fd.params.get(arg_index)
        return p.explicit_type.eq("UPtr")
    case:
        return false
    }
}

callee_param_is_own := func(callee_name: Str, shallow arg_index: U32) returns shallow Bool {
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return false }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if arg_index.gte(fd.params.len()) { return false }
        ref p : Declaration = fd.params.get(arg_index)
        return p.own_type.is(OwnType.Own)
    case:
        return false
    }
}

priv fcall_is_shallow_return := func(fcall: Expr) returns shallow Bool {
    if fcall.node_type.is(NodeType.FCall).not() { return false }
    ref callee : Expr = fcall.child(0)
    switch callee.node_type {
    case NodeType.Ident(ident):
        if callee_returns_shallow(ident) { return true }
    case NodeType.FieldAccess(mname):
        sname := til_type_name(callee.child(0).til_type)
        if sname.len().eq(0) { return false }
        flat := sname.format("_", mname)
        if callee_returns_shallow(flat) { return true }
    case:
    }
    ref sig : Expr = fcall_fn_sig(fcall)
    if is_null(sig).not() {
        switch sig.node_type {
        case NodeType.FuncDef(sfd):
            return sfd.return_own_type.is(OwnType.Shallow)
        case:
        }
    }
    return false
}

fcall_returns_dynamic := func(fcall: Expr) returns shallow Bool {
    if fcall.node_type.is(NodeType.FCall).not() { return false }
    ref callee_node : Expr = fcall.child(0)
    switch callee_node.node_type {
    case NodeType.FieldAccess(mname):
        sname := til_type_name(callee_node.child(0).til_type)
        if sname.len().eq(0) { return false }
        flat := sname.format("_", mname)
        return callee_returns_dynamic(flat)
    case NodeType.Ident(ident):
        return callee_returns_dynamic(ident)
    case:
        return false
    }
}

priv til_type_to_c := func(shallow t: Type) returns Str {
    switch t {
    case Type.I64:     return "I64"
    case Type.U8:      return "U8"
    case Type.I8:      return "I8"
    case Type.Primitive(Primitive.I16):     return "I16"
    case Type.I32:     return "I32"
    case Type.U32:     return "U32"
    case Type.U64:     return "U64"
    case Type.F32:     return "F32"
    case Type.Bool:    return "Bool"
    case Type.None:    return "void"
    case Type.Dynamic: return "void *"
    case Type.FuncPtr: return "void *"
    case:                 return "I64"
    }
}

// Resolve the C type for an `extern` global decl. Replaces the
// `mut ctype := ""; if dyn { ... } else { ... }` reassign-leak
// pattern in emit_header_global_decls / emit_funcsig_typedefs.
extern_decl_ctype := func(shallow t: Type, shallow rhs_t: Type) returns Str {
    if t.is(Type.Dynamic) { return til_type_to_c(t) }
    return c_type_name(t, til_type_name(rhs_t))
}

// Resolve the C type name for a Decl whose explicit_type may be empty.
// Skips the heap-allocated empty-Str dance from til_type_name when t isn't
// Struct/Enum/FuncPtr (where the name actually matters) -- saves the Str_lit("")
// leak per call.
resolve_decl_ctype := func(shallow t: Type, shallow rhs_t: Type, explicit: Str) returns Str {
    if t.is(Type.Struct).or(t.is(Type.Enum)).or(t.is(Type.FuncPtr)) {
        sn := til_type_name(t)
        if sn.len().gt(0) { return c_type_name(t, sn) }
        sn2 := til_type_name(rhs_t)
        if sn2.len().gt(0) { return c_type_name(t, sn2) }
        return c_type_name(t, explicit)
    }
    return c_type_name(t, "")
}

c_type_name := func(shallow t: Type, struct_name: Str) returns Str {
    if t.is(Type.Struct).or(t.is(Type.Enum)) {
        if is_null(struct_name).not() {
            if struct_name.len().gt(0) {
                return struct_name.clone()
            }
        }
    }
    if t.is(Type.FuncPtr) {
        if is_null(struct_name).not() {
            if struct_name.len().gt(0) {
                if is_funcsig_type(struct_name) { return struct_name.clone() }
            }
        }
    }
    if t.is(Type.Dynamic) { return "void" }
    return til_type_to_c(t)
}

func_to_c := func(name: Str) returns Str {
    if name.eq("double") { return "double_" }
    return name.clone()
}

type_name_to_c := func(name: Str) returns Str {
    if name.eq("I64") { return "I64 *" }
    if name.eq("U8") { return "U8 *" }
    if name.eq("I8") { return "I8 *" }
    if name.eq("I16") { return "I16 *" }
    if name.eq("I32") { return "I32 *" }
    if name.eq("U32") { return "U32 *" }
    if name.eq("U64") { return "U64 *" }
    if name.eq("USize") { return "USize *" }
    if name.eq("UPtr") { return "UPtr *" }
    if name.eq("F32") { return "F32 *" }
    if name.eq("Bool") { return "Bool *" }
    if name.eq("Dynamic") { return "void *" }
    if name.eq("Fn") { return "void *" }
    if codegen_lp().ctx.funcsig_names.has(name) { return name.clone() }
    return name.format(" *")
}

type_name_to_c_value := func(name: Str) returns Str {
    if name.eq("I64") { return "I64" }
    if name.eq("U8") { return "U8" }
    if name.eq("I8") { return "I8" }
    if name.eq("I16") { return "I16" }
    if name.eq("I32") { return "I32" }
    if name.eq("U32") { return "U32" }
    if name.eq("U64") { return "U64" }
    if name.eq("USize") { return "USize" }
    if name.eq("UPtr") { return "UPtr" }
    if name.eq("F32") { return "F32" }
    if name.eq("Bool") { return "Bool" }
    if name.eq("Dynamic") { return "void *" }
    if name.eq("Fn") { return "void *" }
    if codegen_lp().ctx.funcsig_names.has(name) { return name.clone() }
    return name.clone()
}

emit_u64 := proc(f: File, shallow v: U64) {
    f.write(v.to_str())
}

emit_i32 := proc(f: File, shallow v: I32) {
    f.write(v.to_str())
}

priv emit_indent := proc(f: File, shallow depth: I32) {
    for _ : I32 in 0..depth {
        f.write("    ")
    }
}

// Post-escape byte count for a source-form string literal: each `\n`,
// `\t`, `\xNN`, etc. collapses to one byte. The emitted `.count` must
// be this, not the source-form length.
priv c_lit_byte_count := func(s: Str) returns USize {
    n := s.len()
    mut count : USize = 0
    mut i : USize = 0
    while i.lt(n) {
        ref b : I8 = s.byte_at(i)
        if b.eq('\\').and(i.add(1).lt(n)) {
            ref nx : I8 = s.byte_at(i.add(1))
            if nx.eq('x').and(i.add(3).lt(n)) {
                i = i.add(4)
                count = count.add(1)
                continue
            }
            i = i.add(2)
            count = count.add(1)
            continue
        }
        i = i.add(1)
        count = count.add(1)
    }
    return count
}

// Collect every Ident reference under `e` into `refs`. Used by the
// function-body entry pass to decide which params actually need the
// `(void)<name>;` unused-arg suppressor.
priv collect_ident_refs := proc(e: Expr, mut refs: Set) {
    if is_null(e) { return }
    switch e.node_type {
    case NodeType.Ident(name): refs.add(own name.clone())
    case NodeType.Assign(ad):  refs.add(own ad.name.clone())
    case:
    }
    for c : Expr in e.children {
        collect_ident_refs(c, refs)
    }
}

emit_til_default := proc(f: File, shallow t: Type) {
    if t.is(Type.I64).or(t.is(Type.I8)).or(t.is(Type.Primitive(Primitive.I16))).or(t.is(Type.I32))
    .or(t.is(Type.U32)).or(t.is(Type.U64)).or(t.is(Type.F32))
    .or(t.is(Type.U8)) {
        f.write("0")
    } else if t.is(Type.Bool) {
        f.write("false")
    } else if t.is(Type.Struct).or(t.is(Type.Enum)) {
        name := til_type_name(t)
        if name.eq("Str") {
            f.write("\"\"")
        } else if name.len().gt(0) {
            f.write(name)
            f.write("()")
        } else {
            f.write("0")
        }
    } else {
        f.write("0")
    }
}

callee_return_ctype := ext_func(callee_name: Str) returns Str {}

// Resolve the C parameter type for fd.params[i]. Avoids the
// `mut ptype := ""` pre-init pattern in emit_param_list, whose empty
// Str leaked once per parameter on each call.
param_ctype := func(fd: FunctionDef, shallow i: USize) returns Str {
    ii := i.to_i32()
    if ii.eq(fd.variadic_index) { return "Array *" }
    ref p : Declaration = fd.params.get(i)
    if ii.eq(fd.kwargs_index) { return type_name_to_c(p.explicit_type) }
    if p.own_type.is(OwnType.Shallow) { return type_name_to_c_value(p.explicit_type) }
    return type_name_to_c(p.explicit_type)
}

emit_param_list := proc(f: File, fdef: Expr, shallow with_names: Bool, shallow for_ext_decl: Bool) {
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if fd.params.len().eq(0) {
            f.write("void")
        } else {
            for i in 0..fd.params.len() {
                if i.gt(0) { f.write(", ") }
                ii := i.to_i32()
                // Issue #176 bullet 1: emit `const` for non-mut Ref pointer
                // params at the FFI boundary. Variadic / kwargs are excluded
                // (their C type is fixed by codegen, not the user's qualifier);
                // shallow / own params are excluded (const adds nothing or is
                // wrong). `mut` stays as the escape hatch.
                mut const_prefix := false
                if for_ext_decl.and(ii.neq(fd.variadic_index)).and(ii.neq(fd.kwargs_index)) {
                    ref pp : Declaration = fd.params.get(i)
                    if pp.own_type.is(OwnType.Ref).and(pp.is_mut.not()) {
                        const_prefix = true
                    }
                }
                ptype := param_ctype(fd, i)
                if with_names {
                    ref p : Declaration = fd.params.get(i)
                    if const_prefix { f.write("const ") }
                    f.write(ptype)
                    f.write(" ")
                    f.write(p.name)
                } else {
                    if const_prefix { f.write("const ") }
                    f.write(ptype)
                }
            }
        }
    case:
        UNREACHABLE(__LOC__)
    }
}

priv collect_dyn_methods := proc(e: Expr, mut methods: Vec) {
    if is_null(e) { return }
    if e.node_type.is(NodeType.FCall) {
        if e.children.len().gte(3) {
            ref c0 : Expr = e.child(0)
            ref c2 : Expr = e.child(2)
            switch c0.node_type {
            case NodeType.Ident(callee_name):
                switch c2.node_type {
                case NodeType.LiteralStr(method):
                    mut has_ret := false
                    if is_dyn_call_name(callee_name, has_ret) {
                        mut nargs := 1.to_i32()
                        if e.children.len().gte(4) {
                            ref c3 : Expr = e.child(3)
                            switch c3.node_type {
                            case NodeType.LiteralNum(num_str):
                                nargs = num_str.to_i32()
                            case:
                            }
                        }
                        mut found := false
                        for existing : DynCallInfo in methods {
                            if existing.method.eq(method) { found = true }
                        }
                        if found.not() {
                            info := DynCallInfo(method = method, nargs = nargs, has_return = has_ret)
                            methods.push(own info)
                        }
                    }
                case:
                }
            case:
            }
        }
    }
    if e.node_type.is(NodeType.StructDef).or(e.node_type.is(NodeType.EnumDef)) {
        ref cdm_ns : Vec = def_ns_decls(e)
        for cdm_d : Declaration in cdm_ns {
            if is_null(cdm_d.default_value).not() {
                collect_dyn_methods(cdm_d.default_value, methods)
            }
        }
    }
    for e_ci : Expr in e.children {
        collect_dyn_methods(e_ci, methods)
    }
}

collect_collection_builtins := proc(e: Expr, mut infos: Vec) {
    if is_null(e) { return }
    if e.node_type.is(NodeType.FCall) {
        if e.children.len().gte(2) {
            ref c0 : Expr = e.child(0)
            ref c1 : Expr = e.child(1)
            switch c0.node_type {
            case NodeType.Ident(name):
                switch c1.node_type {
                case NodeType.LiteralStr(type_name):
                    mut is_vec := -1.to_i32()
                    if name.eq("array") { is_vec = 0.to_i32() }
                    else if name.eq("vec") { is_vec = 1.to_i32() }
                    if is_vec.gte(0.to_i32()) {
                        mut found := false
                        for existing : CollectionInfo in infos {
                            if existing.type_name.eq(type_name) {
                                if existing.is_vec.eq(is_vec) { found = true }
                            }
                        }
                        if found.not() {
                            info := CollectionInfo(type_name = type_name, is_vec = is_vec)
                            infos.push(own info)
                        }
                    }
                case:
                }
            case:
            }
        }
    }
    if e.node_type.is(NodeType.StructDef).or(e.node_type.is(NodeType.EnumDef)) {
        ref ccb_ns : Vec = def_ns_decls(e)
        for ccb_d : Declaration in ccb_ns {
            if is_null(ccb_d.default_value).not() {
                collect_collection_builtins(ccb_d.default_value, infos)
            }
        }
    }
    for e_ci : Expr in e.children {
        collect_collection_builtins(e_ci, infos)
    }
}

emit_field := proc(f: File, var: Str, field: Str) {
    f.write(var)
    if is_stack_local(var).or(is_value_global(var)) {
        f.write(".")
    } else {
        f.write("->")
    }
    f.write(field)
}

get_stack_local_ctype := func(name: Str) returns ref Str {
    ref m : Map = codegen_lp().ctx.stack_local_types
    if m.has(name) {
        ref p : Str = m.get(name)
        return p
    }
    return NULL
}

// Resolve an Ident's C type name to an owned Str: the recorded
// stack-local type if any, otherwise derived from the Expr's til_type.
// Replaces the previous `ref mut ctype : Str = NULL; if is_null { ctype
// = c_type_name(...) }` shape that leaked an owned Str into a ref slot.
resolve_ident_ctype := func(ident: Str, ttype: Type, sname: Str) returns Str {
    ref existing : Str = get_stack_local_ctype(ident)
    if is_null(existing).not() { return existing.clone() }
    return c_type_name(ttype, sname)
}

resolve_callee_name := ext_func(fcall: Expr) returns Str {}

resolve_callee_name := func(fcall: Expr) returns Str {
    ref callee_node : Expr = fcall.child(0)
    switch callee_node.node_type {
    case NodeType.FieldAccess(mname):
        sname := til_type_name(callee_node.child(0).til_type)
        if sname.len().eq(0) { return "" }
        return sname.format("_", mname)
    case NodeType.Ident(ident):
        return ident.clone()
    case:
        return ""
    }
}

callee_return_ctype := func(callee_name: Str) returns Str {
    ref fdef : Expr = find_callee_fdef(callee_name)
    if is_null(fdef) { return "" }
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if fd.return_type.len().eq(0) { return "" }
        return type_name_to_c_value(fd.return_type)
    case:
        return ""
    }
}

fcall_return_ctype := func(fcall: Expr) returns Str {
    if fcall.node_type.is(NodeType.FCall).not() { return "" }
    callee := resolve_callee_name(fcall)
    if callee.len().eq(0) { return "" }
    return callee_return_ctype(callee)
}

check_fcall_mut_args := proc(e: Expr) {
    if is_null(e) { return }
    ref mut lp : LoadedProgram = codegen_lp()
    if e.node_type.is(NodeType.FCall) {
        ref mut fdef : Expr = fcall_fn_sig(e)
        if is_null(fdef) {
            callee := resolve_callee_name(e)
            if callee.len().gt(0) {
                fdef = find_callee_fdef(callee)
            }
        }
        if is_null(fdef).not() {
            switch fdef.node_type {
            case NodeType.FuncDef(fd):
                if fd.params.len().gt(0) {
                    for a in 1..e.children.len() {
                        pi := a.sub(1)
                        if pi.lt(fd.params.len()) {
                            ref p : Declaration = fd.params.get(pi)
                            if p.is_mut {
                                ref arg : Expr = e.child(a)
                                switch arg.node_type {
                                case NodeType.Ident(arg_name):
                                    lp.ctx.unsafe_to_hoist.add(own arg_name.clone())
                                case:
                                }
                            }
                        }
                    }
                }
            case:
            }
        }
    }
    for e_ci : Expr in e.children {
        check_fcall_mut_args(e_ci)
    }
}

collect_unsafe_to_hoist := proc(body: Expr) {
    ref mut lp : LoadedProgram = codegen_lp()
    for stmt : Expr in body.children {
        check_fcall_mut_args(stmt)
        if stmt.node_type.is(NodeType.Decl) {
            switch stmt.node_type {
            case NodeType.Decl(dd):
                if dd.own_type.is(OwnType.Ref) {
                    ref rhs : Expr = stmt.child(0)
                    switch rhs.node_type {
                    case NodeType.Ident(rhs_name):
                        lp.ctx.unsafe_to_hoist.add(own rhs_name.clone())
                    case NodeType.FCall:
                        callee := resolve_callee_name(rhs)
                        if callee.len().gt(0) {
                            ref fdef : Expr = find_callee_fdef(callee)
                            if is_null(fdef).not() {
                                switch fdef.node_type {
                                case NodeType.FuncDef(fd):
                                    if fd.return_own_type.is(OwnType.Ref) {
                                        for a in 1..rhs.children.len() {
                                            ref arg : Expr = rhs.child(a)
                                            switch arg.node_type {
                                            case NodeType.Ident(arg_name):
                                                lp.ctx.unsafe_to_hoist.add(own arg_name.clone())
                                            case:
                                            }
                                        }
                                    }
                                case:
                                }
                            }
                        }
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
        if stmt.node_type.is(NodeType.If) {
            for c in 1..stmt.children.len() {
                collect_unsafe_to_hoist(stmt.child(c))
            }
        }
        if stmt.node_type.is(NodeType.While) {
            if stmt.children.len().gt(1) {
                collect_unsafe_to_hoist(stmt.child(1))
            }
        }
        if stmt.node_type.is(NodeType.Body) {
            collect_unsafe_to_hoist(stmt)
        }
    }
}

collect_dyn_has_methods := proc(e: Expr, mut methods: Vec) {
    if is_null(e) { return }
    if e.node_type.is(NodeType.FCall) {
        if e.children.len().gte(3) {
            ref c0 : Expr = e.child(0)
            ref c2 : Expr = e.child(2)
            switch c0.node_type {
            case NodeType.Ident(cname):
                if cname.eq("dyn_has_method") {
                    switch c2.node_type {
                    case NodeType.LiteralStr(method):
                        mut found := false
                        for existing : Str in methods {
                            if existing.eq(method) { found = true }
                        }
                        if found.not() {
                            methods.push(own method.clone())
                        }
                    case:
                    }
                }
            case:
            }
        }
    }
    if e.node_type.is(NodeType.StructDef).or(e.node_type.is(NodeType.EnumDef)) {
        ref cdh_ns : Vec = def_ns_decls(e)
        for cdh_d : Declaration in cdh_ns {
            if is_null(cdh_d.default_value).not() {
                collect_dyn_has_methods(cdh_d.default_value, methods)
            }
        }
    }
    for e_ci : Expr in e.children {
        collect_dyn_has_methods(e_ci, methods)
    }
}

// True when `e` is a struct constructor FCall (callee is the
// struct-name Ident), not a method call like Point.clone(...) whose
// callee is a FieldAccess. The POD compound-literal path uses this
// to decide whether the FCall's children are positional field
// initializers (ctor) vs an arbitrary call (which would type-clash
// inside a `.field = ...` initializer).
priv fcall_is_struct_ctor := func(e: Expr) returns shallow Bool {
    if e.node_type.is(NodeType.FCall).not() { return false }
    if e.children.len().eq(0) { return false }
    sname := til_type_name(e.til_type)
    if sname.len().eq(0) { return false }
    switch e.child(0).node_type {
    case NodeType.Ident(ci): return ci.eq(sname)
    case:
    }
    return false
}

// True when every FCall arg of `ctor` is either non-FCall or itself a
// POD-struct ctor (recursively safe). The POD compound-literal path
// requires this -- a non-ctor FCall arg (e.g. Point.clone(...)) would
// either type-clash with the positional field assumption or return a
// pointer that can't be assigned into a value field.
priv pod_ctor_args_are_safe := func(ctor: Expr) returns shallow Bool {
    for i in 1..ctor.children.len() {
        ref arg : Expr = ctor.child(i)
        if arg.node_type.is(NodeType.FCall).not() { continue }
        if fcall_is_struct_ctor(arg).not() { return false }
        arg_sname := til_type_name(arg.til_type)
        if is_pod_struct(arg_sname).not() { return false }
        if pod_ctor_args_are_safe(arg).not() { return false }
    }
    return true
}

// Emit a C compound literal initializer for a POD-struct ctor FCall:
// `(Type){.f1 = v1, .f2 = v2, ...}`.  Nested POD ctor args recurse so
// `(Line){.start = (Point){.x = ..., .y = ...}, .end = ...}` works.
// Caller must have verified is_pod_struct(ctor type) AND
// pod_ctor_args_are_safe(ctor). Caller is responsible for trailing
// punctuation -- this emits the `(...){...}` expression with no
// semicolon.
emit_pod_compound_literal := proc(f: File, ctor: Expr, shallow depth: I32) {
    ctor_sname := til_type_name(ctor.til_type)
    ctype := c_type_name(ctor.til_type, ctor_sname)
    f.write("("); f.write(ctype); f.write("){")
    ref pod_def : Expr = find_struct_def(ctor_sname)
    if is_null(pod_def).not() {
        switch pod_def.node_type {
        case NodeType.StructDef(pod_sdd):
            mut pod_fi : U32 = 0
            for i in 1..ctor.children.len() {
                if i.gt(1) { f.write(", ") }
                mut pod_fname := ""
                if pod_fi.lt(pod_sdd.fields.len()) {
                    ref pod_fd : Declaration = pod_sdd.fields.get(pod_fi)
                    pod_fname = pod_fd.name
                    pod_fi = pod_fi.add(1)
                }
                f.write("."); f.write(pod_fname); f.write(" = ")
                ref arg : Expr = ctor.child(i)
                if fcall_is_struct_ctor(arg) {
                    emit_pod_compound_literal(f, arg, depth)
                } else {
                    emit_deref(f, arg, depth)
                }
            }
        case:
        }
    }
    f.write("}")
}

// Emit layer
emit_stmt_decl := proc(f: File, e: Expr, shallow depth: I32) {
    if e.children.len().eq(0) { return }
    switch e.node_type {
    case NodeType.Decl(dd):
    ref rhs : Expr = e.child(0)
    ref mut lp : LoadedProgram = codegen_lp()
    // For any FuncPtr-typed decl (own, ref, or value), capture the local's
    // signature so fcall_fn_sig can recover it at the eventual call site.
    // The original typer scope where `dd` lived is gone by emit time, so we
    // pin the FuncDef pointer here while we still have access to the rhs.
    if e.til_type.is(Type.FuncPtr) {
        ref decl_sig : Expr = resolve_decl_fn_sig(rhs, dd, codegen_lp().ctx.scope)
        if is_null(decl_sig).not() {
            sig_buf : Dynamic = malloc(I64.size())
            write_ptr(sig_buf, decl_sig)
            ref mut m : Map = codegen_lp().ctx.local_fn_sigs
            m.set(own dd.name.clone(), own sig_buf)
        }
    }
    if e.til_type.is(Type.FuncPtr).and(dd.own_type.is(OwnType.Ref).not()).and(rhs.node_type.is(NodeType.FuncDef).not()) {
        mut ct := "void *"
        mut is_typedef := false
        if dd.explicit_type.len().gt(0) {
            if is_funcsig_type(dd.explicit_type) {
                ct = dd.explicit_type.clone()
                is_typedef = true
            }
        }
        if is_typedef { f.write(ct); f.write(" "); f.write(dd.name); f.write(" = ") }
        else { f.write(ct); f.write(dd.name); f.write(" = ") }
        emit_expr(f, rhs, depth); f.write(";\n")
    } else if rhs.node_type.is(NodeType.FuncDef) {
        f.write("/* TODO: nested func "); f.write(dd.name); f.write(" */\n")
    } else if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
        f.write("/* ")
        if rhs.node_type.is(NodeType.EnumDef) { f.write("enum") } else { f.write("struct") }
        f.write(" "); f.write(dd.name); f.write(" defined above */\n")
    } else if e.til_type.is(Type.None).and(rhs.node_type.is(NodeType.Ident)) {
        // REM Type alias -- no-op
    } else {
        if dd.own_type.is(OwnType.Ref) {
            is_rg := is_ref_global(dd.name).and(in_func_def.not())
            if is_rg.not() {
                lp.ctx.ref_locals.add(own dd.name.clone())
                // #185: track ref-Dynamic locals so emit_deref can typed-
                // cast at use sites where narrow_dynamic has retyped the
                // Ident to a concrete primitive.
                if dd.explicit_type.eq("Dynamic") {
                    lp.ctx.ref_dyn_locals.add(own dd.name.clone())
                }
            }
            ctype := resolve_decl_ctype(e.til_type, rhs.til_type, dd.explicit_type)
            if is_rg { f.write(dd.name); f.write(" = ") }
            else { f.write(ctype); f.write(" *"); f.write(dd.name); f.write(" = ") }
            emit_as_ptr(f, rhs, depth, false); f.write(";\n")
            // `_x` Ref decls (e.g. switch-case payload binds named `_s`)
            // are til's deliberately-unused convention; the typer skips
            // them in check_unused_locals, so we need a (void) guard to
            // keep C's -Wunused-variable quiet.
            if dd.name.starts_with("_") {
                emit_indent(f, depth); f.write("(void)"); f.write(dd.name); f.write(";\n")
            }
        } else {
            ctype := resolve_decl_ctype(e.til_type, rhs.til_type, dd.explicit_type)
            is_global := is_value_global(dd.name).and(in_func_def.not())
            if e.til_type.is(Type.Dynamic) {
                if is_global { f.write(dd.name); f.write(" = ") }
                else { tc := til_type_to_c(e.til_type); f.write(tc); f.write(" "); f.write(dd.name); f.write(" = ") }
                emit_deref(f, rhs, depth); f.write(";\n")
            } else {
                can_hoist := is_global.not().and(dd.own_type.is(OwnType.Own).not())
                .and(e.til_type.is(Type.FuncPtr).not()).and(e.til_type.is(Type.Dynamic).not())
                .and(rhs.node_type.is(NodeType.FCall).and(fcall_returns_dynamic(rhs)).not())
                // Struct constructor
                rhs_sname := til_type_name(rhs.til_type)
                mut is_ctor := rhs.node_type.is(NodeType.FCall).and(rhs_sname.len().gt(0))
                if is_ctor {
                    if rhs.children.len().gt(0) {
                        ref rc0 : Expr = rhs.child(0)
                        switch rc0.node_type {
                        case NodeType.Ident(ci):
                            is_ctor = ci.eq(rhs_sname)
                        case:
                            is_ctor = false
                        }
                    } else { is_ctor = false }
                }
                if is_ctor {
                    if is_global {
                        f.write("memset(&"); f.write(dd.name); f.write(", 0, sizeof("); f.write(ctype); f.write("));\n")
                        emit_ctor_fields(f, dd.name, rhs, depth)
                    } else if is_pod_struct(rhs_sname).and(pod_ctor_args_are_safe(rhs)) {
                        // POD struct ctor: emit as a stack value
                        // initialized with a C compound literal,
                        // mirroring the LiteralStr Decl path. Matching
                        // delete is forced to call_free=false in the
                        // shim at emit_stmt (NodeType.FCall case) so
                        // we never call free() on the stack pointer.
                        f.write(ctype); f.write(" "); f.write(dd.name); f.write(" = ")
                        emit_pod_compound_literal(f, rhs, depth)
                        f.write(";\n")
                        lp.ctx.stack_locals.add(own dd.name.clone())
                        lp.ctx.stack_local_types.set(own dd.name.clone(), own ctype.clone())
                    } else {
                        f.write(ctype); f.write(" *"); f.write(dd.name); f.write(" = malloc(sizeof("); f.write(ctype); f.write("));\n")
                        emit_ctor_fields(f, dd.name, rhs, depth)
                    }
                } else if rhs.node_type.is(NodeType.LiteralStr).and(is_global.not()) {
                    // Str literal: emit as a stack-allocated compound-literal
                    // Str value -- no malloc, no Str_lit. Pairs with the
                    // typer's hoist/promote pass keeping LiteralStr decls
                    // Shallow so make_delete_call emits call_free=false and
                    // the existing FCall-arg emission writes `&name` for
                    // non-own slots. For own-taking callees (Array_set,
                    // Vec_push, etc.) emit_as_ptr's stack_local + is_own=true
                    // branch does the usual malloc round-trip so `free(own
                    // val)` still operates on a heap wrapper.
                    f.write(ctype); f.write(" "); f.write(dd.name); f.write(" = (Str){.c_str = (void *)\"")
                    switch rhs.node_type {
                    case NodeType.LiteralStr(s):
                        f.write(s)
                        f.write("\", .count = ")
                        emit_u64(f, c_lit_byte_count(s).to_u64())
                    case:
                    }
                    f.write("ULL, .cap = TIL_CAP_LIT};\n")
                    lp.ctx.stack_locals.add(own dd.name.clone())
                    lp.ctx.stack_local_types.set(own dd.name.clone(), own ctype.clone())
                } else if rhs.node_type.is(NodeType.FCall).or(rhs.node_type.is(NodeType.LiteralStr))
                .or(rhs.node_type.is(NodeType.FieldAccess).and(builder_fa_is_ns(rhs)).and(rhs.til_type.is(Type.Enum))) {
                    if is_global {
                        if rhs.node_type.is(NodeType.FCall).and(fcall_is_shallow_return(rhs)) {
                            f.write(dd.name); f.write(" = "); emit_expr(f, rhs, depth); f.write(";\n")
                        } else if rhs.node_type.is(NodeType.FCall) {
                            mut htype := fcall_return_ctype(rhs)
                            if htype.len().eq(0) { htype = ctype.clone() }
                            f.write("{ "); f.write(htype); f.write(" *_hp = ("); f.write(htype); f.write(" *)")
                            emit_expr(f, rhs, depth)
                            f.write("; "); f.write(dd.name); f.write(" = *_hp; free(_hp); }\n")
                        } else {
                            f.write(dd.name); f.write(" = "); emit_deref(f, rhs, depth); f.write(";\n")
                        }
                    } else if rhs.node_type.is(NodeType.FCall).and(fcall_is_shallow_return(rhs)) {
                        // Shallow returns are by-value -- always stack-alloc.
                        // The malloc'd-and-deref'd path leaks the heap box (the
                        // typer doesn't track these for delete).
                        mut htype := fcall_return_ctype(rhs)
                        if htype.len().eq(0) { htype = ctype.clone() }
                        f.write(htype); f.write(" "); f.write(dd.name); f.write(" = ")
                        emit_expr(f, rhs, depth); f.write(";\n")
                        lp.ctx.stack_locals.add(own dd.name.clone())
                        lp.ctx.stack_local_types.set(own dd.name.clone(), own htype.clone())
                    } else if can_hoist.and(rhs.node_type.is(NodeType.FCall)) {
                        mut htype := fcall_return_ctype(rhs)
                        if htype.len().eq(0) { htype = ctype.clone() }
                        f.write(htype); f.write(" "); f.write(dd.name); f.write("; { "); f.write(htype); f.write(" *_hp = ("); f.write(htype); f.write(" *)")
                        emit_expr(f, rhs, depth)
                        f.write("; "); f.write(dd.name); f.write(" = *_hp; free(_hp); }\n")
                        lp.ctx.stack_locals.add(own dd.name.clone())
                        lp.ctx.stack_local_types.set(own dd.name.clone(), own htype.clone())
                    } else if can_hoist {
                        f.write(ctype); f.write(" "); f.write(dd.name); f.write("; { "); f.write(ctype); f.write(" *_hp = ("); f.write(ctype); f.write(" *)")
                        emit_expr(f, rhs, depth)
                        f.write("; "); f.write(dd.name); f.write(" = *_hp; free(_hp); }\n")
                        lp.ctx.stack_locals.add(own dd.name.clone())
                        lp.ctx.stack_local_types.set(own dd.name.clone(), own ctype.clone())
                    } else {
                        if is_global { f.write(dd.name); f.write(" = ") }
                        else { f.write(ctype); f.write(" *"); f.write(dd.name); f.write(" = ") }
                        emit_expr(f, rhs, depth); f.write(";\n")
                    }
                } else {
                    if is_global {
                        f.write(dd.name); f.write(" = "); emit_deref(f, rhs, depth); f.write(";\n")
                    } else if can_hoist {
                        f.write(ctype); f.write(" "); f.write(dd.name); f.write(" = ")
                        emit_deref(f, rhs, depth); f.write(";\n")
                        lp.ctx.stack_locals.add(own dd.name.clone())
                        lp.ctx.stack_local_types.set(own dd.name.clone(), own ctype.clone())
                    } else {
                        f.write(ctype); f.write(" *"); f.write(dd.name); f.write(" = malloc(sizeof("); f.write(ctype); f.write("));\n")
                        emit_indent(f, depth)
                        f.write("*"); f.write(dd.name); f.write(" = "); emit_deref(f, rhs, depth); f.write(";\n")
                    }
                }
            }
        }
        // `_x` locals are til's deliberately-unused convention; the
        // typer skips them in check_unused_locals, so we need a (void)
        // guard for C. Any non-_-prefixed local that reaches here is
        // either used or a typer bug -- gcc surfacing it is fine.
        if dd.name.starts_with("_") {
            emit_indent(f, depth); f.write("(void)"); f.write(dd.name); f.write(";\n")
        }
    }
    case:
        UNREACHABLE(__LOC__)
    }
}
emit_return_ctype := func(rv: Expr) returns Str {
    ref cf : Expr = codegen_lp().ctx.current_fdef
    if is_null(cf).not() {
        switch cf.node_type {
        case NodeType.FuncDef(fd):
            if fd.return_type.len().gt(0) { return type_name_to_c_value(fd.return_type) }
        case:
        }
    }
    return c_type_name(rv.til_type, til_type_name(rv.til_type))
}

emit_return_box := proc(f: File, rv: Expr, shallow depth: I32) {
    ctype := emit_return_ctype(rv)
    f.write("{ "); f.write(ctype); f.write(" *_r = malloc(sizeof("); f.write(ctype); f.write(")); *_r = ")
    emit_expr(f, rv, depth)
    f.write("; return _r; }\n")
}

emit_stmt_return := proc(f: File, e: Expr, shallow depth: I32) {
    if e.children.len().eq(0) {
        if in_main_func { f.write("return 0;\n") }
        else { f.write("return;\n") }
    } else {
        ref rv : Expr = e.child(0)
        // Struct constructor return
        rv_sname := til_type_name(rv.til_type)
        if rv.node_type.is(NodeType.FCall).and(rv_sname.len().gt(0)).and(rv.children.len().gt(0)) {
            ref rvc0 : Expr = rv.child(0)
            switch rvc0.node_type {
            case NodeType.Ident(ci):
                if ci.eq(rv_sname) {
                    ctype := c_type_name(rv.til_type, rv_sname)
                    f.write("{ "); f.write(ctype); f.write(" *_r = malloc(sizeof("); f.write(ctype); f.write("));\n")
                    emit_ctor_fields(f, "_r", rv, depth)
                    emit_indent(f, depth); f.write("return _r; }\n")
                    return
                }
            case:
            }
        }
        // Ref return
        ref cf2 : Expr = codegen_lp().ctx.current_fdef
        if is_null(cf2).not() {
            switch cf2.node_type {
            case NodeType.FuncDef(fd):
                if fd.return_own_type.is(OwnType.Ref) {
                    f.write("return "); emit_as_ptr(f, rv, depth, false); f.write(";\n"); return
                }
                if fd.return_own_type.is(OwnType.Shallow) {
                    if rv.node_type.is(NodeType.FCall).and(fcall_is_shallow_return(rv)) {
                        f.write("return "); emit_expr(f, rv, depth); f.write(";\n"); return
                    }
                    // Asap fix: shallow return of a hoisted compiler temp
                    // (hoist_to_temp always heap-promotes Own for FCall /
                    // Literal*) -- the typer's insert_exit_deletes_into_stmt
                    // skips locals used in the return expr, so the wrapper
                    // would otherwise leak. Capture by value, raw-free the
                    // wrapper, return the captured value.
                    if rv.node_type.is(NodeType.Ident) {
                        switch rv.node_type {
                        case NodeType.Ident(rv_name):
                            if rv_name.starts_with("hoisted__").and(
                                is_stack_local(rv_name).not()).and(
                                is_value_global(rv_name).not()).and(
                                rv.til_type.is(Type.Dynamic).not()) {
                                ctype := emit_return_ctype(rv)
                                f.write("{ "); f.write(ctype); f.write(" _ret = DEREF("); f.write(rv_name); f.write("); free("); f.write(rv_name); f.write("); return _ret; }\n")
                                return
                            }
                        case:
                        }
                    }
                    f.write("return "); emit_deref(f, rv, depth); f.write(";\n"); return
                }
            case:
            }
        }
        // Inline field value: box for return
        if rv.node_type.is(NodeType.FieldAccess).and(builder_fa_is_ptr(rv).not())
        .and(builder_fa_is_ns(rv).not()).and(rv.til_type.is(Type.Dynamic).not()) {
            emit_return_box(f, rv, depth)
            // Shallow fcall: box for return
        } else if rv.node_type.is(NodeType.FCall).and(fcall_is_shallow_return(rv)) {
            emit_return_box(f, rv, depth)
            // Hoisted local: box for return
        } else if rv.node_type.is(NodeType.Ident) {
            switch rv.node_type {
            case NodeType.Ident(rv_name):
                mut is_shallow_ret := false
                ref cf3 : Expr = codegen_lp().ctx.current_fdef
                if is_null(cf3).not() {
                    switch cf3.node_type {
                    case NodeType.FuncDef(fd):
                        is_shallow_ret = fd.return_own_type.is(OwnType.Shallow)
                    case:
                    }
                }
                if is_stack_local(rv_name).or(is_value_global(rv_name)) {
                    if is_shallow_ret.not() {
                        ctype := emit_return_ctype(rv)
                        f.write("{ "); f.write(ctype); f.write(" *_r = malloc(sizeof("); f.write(ctype); f.write(")); *_r = "); f.write(rv_name); f.write("; return _r; }\n")
                    } else {
                        f.write("return "); emit_expr(f, rv, depth); f.write(";\n")
                    }
                } else {
                    f.write("return "); emit_expr(f, rv, depth); f.write(";\n")
                }
            case:
                UNREACHABLE(__LOC__)
            }
            // Bare enum variant ref: heap-allocate
        } else if rv.node_type.is(NodeType.FieldAccess).and(builder_fa_is_ns(rv)).and(rv.til_type.is(Type.Enum)) {
            sn := til_type_name(rv.child(0).til_type)
            switch rv.node_type {
            case NodeType.FieldAccess(vname):
                f.write("{ "); f.write(sn); f.write(" *_r = malloc(sizeof("); f.write(sn); f.write(")); _r->tag = "); f.write(sn); f.write("_TAG_"); f.write(vname); f.write("; return _r; }\n")
            case:
                UNREACHABLE(__LOC__)
            }
        } else {
            f.write("return "); emit_expr(f, rv, depth); f.write(";\n")
        }
    }
}

// Trivial no-op delete: `Type.delete(stack_local_primitive, ...)` -- the
// typer emits these for hoisted stack temps whose type has no destructor
// work to do. The builder previously suppressed the call body but still
// wrote indent+";", littering generated C with lonely semicolons. Detect
// the shape here so emit_stmt can skip the indent too.
priv is_noop_delete_stmt := func(e: Expr) returns shallow Bool {
    if e.node_type.is(NodeType.FCall).not() { return false }
    if e.children.len().lt(2.to_u32()) { return false }
    ref c0 : Expr = e.child(0)
    switch c0.node_type {
    case NodeType.FieldAccess(del_name):
        if del_name.eq("delete").not() { return false }
    case:
        return false
    }
    ref c1 : Expr = e.child(1)
    switch c1.node_type {
    case NodeType.Ident(del_arg):
        if is_stack_local(del_arg).not() { return false }
        if c1.til_type.is(Type.Struct) { return false }
        if c1.til_type.is(Type.Enum) { return false }
        return true
    case:
        return false
    }
}

priv emit_stmt := proc(f: File, e: Expr, shallow depth: I32) {
    if is_noop_delete_stmt(e) { return }
    emit_indent(f, depth)
    switch e.node_type {
    case NodeType.Decl:
        emit_stmt_decl(f, e, depth)
    case NodeType.Assign(ad):
        aname := ad.name
        ref rhs : Expr = e.child(0)
        if is_ref_local(aname) {
            mut do_rebind := false
            switch rhs.node_type {
            case NodeType.FieldAccess: do_rebind = true
            case NodeType.Ident(rhs_ident):
                if is_stack_local(rhs_ident).not() { do_rebind = true }
            case:
            }
            if do_rebind {
                f.write(aname); f.write(" = ")
                emit_as_ptr(f, rhs, depth, false)
                f.write(";\n"); return
            }
        }
        if ad.save_old_delete {
            ctype := c_type_name(e.til_type, til_type_name(e.til_type))
            if is_stack_local(aname).or(is_value_global(aname)) {
                f.write("{ "); f.write(ctype); f.write(" *_new = ("); f.write(ctype); f.write(" *)")
                emit_expr(f, rhs, depth)
                f.write("; "); f.write(ctype); f.write("_delete(&"); f.write(aname); f.write(", &(Bool){0}); "); f.write(aname); f.write(" = *_new; free(_new); }\n")
            } else {
                f.write("{ "); f.write(ctype); f.write(" *_old = "); f.write(aname); f.write("; "); f.write(aname); f.write(" = ")
                emit_expr(f, rhs, depth)
                f.write("; "); f.write(ctype); f.write("_delete(_old, &(Bool){1}); }\n")
            }
            return
        }
        is_hoisted := is_stack_local(aname).or(is_value_global(aname))
        if e.til_type.is(Type.Dynamic) {
            f.write(aname); f.write(" = "); emit_deref(f, rhs, depth); f.write(";\n"); return
        }
        if is_hoisted {
            if rhs.node_type.is(NodeType.FCall).and(fcall_is_shallow_return(rhs)) {
                f.write(aname); f.write(" = "); emit_expr(f, rhs, depth); f.write(";\n")
            } else if rhs.node_type.is(NodeType.FCall) {
                ctype := c_type_name(e.til_type, til_type_name(e.til_type))
                f.write("{ "); f.write(ctype); f.write(" *_hp = ("); f.write(ctype); f.write(" *)")
                emit_expr(f, rhs, depth)
                f.write("; "); f.write(aname); f.write(" = *_hp; free(_hp); }\n")
            } else {
                f.write(aname); f.write(" = "); emit_deref(f, rhs, depth); f.write(";\n")
            }
        } else {
            if rhs.node_type.is(NodeType.FCall).or(rhs.node_type.is(NodeType.LiteralStr))
            .or(rhs.node_type.is(NodeType.FieldAccess).and(builder_fa_is_ns(rhs)).and(rhs.til_type.is(Type.Enum))) {
                if rhs.node_type.is(NodeType.FCall).and(fcall_is_shallow_return(rhs)) {
                    f.write("*"); f.write(aname); f.write(" = ")
                } else {
                    f.write(aname); f.write(" = ")
                }
                emit_expr(f, rhs, depth)
            } else if e.til_type.is(Type.FuncPtr) {
                f.write(aname); f.write(" = "); emit_expr(f, rhs, depth)
            } else {
                f.write("*"); f.write(aname); f.write(" = "); emit_deref(f, rhs, depth)
            }
            f.write(";\n")
        }
    case NodeType.FieldAssign(fname):
        ref obj : Expr = e.child(0)
        ref rhs_fa : Expr = e.child(1)
        if rhs_fa.node_type.is(NodeType.FCall).and(builder_fa_is_ptr(e).not()).and(builder_fa_is_ns(e).not()) {
            if fcall_is_shallow_return(rhs_fa) {
                emit_expr(f, obj, depth)
                if use_dot_access(obj) { f.write(".") } else { f.write("->") }
                f.write(fname); f.write(" = "); emit_expr(f, rhs_fa, depth); f.write(";\n")
            } else {
                ftype := c_type_name(rhs_fa.til_type, til_type_name(rhs_fa.til_type))
                f.write("{ "); f.write(ftype); f.write(" *_fa = "); emit_expr(f, rhs_fa, depth); f.write("; ")
                emit_expr(f, obj, depth)
                if use_dot_access(obj) { f.write(".") } else { f.write("->") }
                f.write(fname); f.write(" = *_fa; free(_fa); }\n")
            }
        } else {
            if builder_fa_is_ns(e) {
                f.write(til_type_name(obj.til_type)); f.write("_"); f.write(fname); f.write(" = ")
            } else {
                emit_expr(f, obj, depth)
                if use_dot_access(obj) { f.write(".") } else { f.write("->") }
                f.write(fname); f.write(" = ")
            }
            if builder_fa_is_ptr(e) {
                emit_as_ptr(f, rhs_fa, depth, true)
            } else {
                emit_deref(f, rhs_fa, depth)
            }
            f.write(";\n")
        }
    case NodeType.FCall:
        if e.children.len().gt(0) {
            ref c0 : Expr = e.child(0)
            switch c0.node_type {
            case NodeType.FieldAccess(del_name):
                if del_name.eq("delete").and(e.children.len().gte(2)) {
                    ref c1 : Expr = e.child(1)
                    switch c1.node_type {
                    case NodeType.Ident(del_arg):
                        // Stack-local primitive deletes are filtered out by
                        // is_noop_delete_stmt above before any indent is
                        // written, so we never reach here for them.
                        // Stack-local Str (compound-literal init). The typer
                        // may have generated this delete with arg_is_own=true
                        // and call_free=true because the binding's own_type
                        // was Own pre-precomp. Force the safe shape:
                        // `Str_delete(&name, &(Bool){0});` (no malloc, no
                        // free on a stack pointer). cap=CAP_LIT means the
                        // c_str data isn't freed either -- delete becomes
                        // a no-op at runtime. Also `(void)` the hoisted
                        // call_free temp if present, so -Wunused-variable
                        // doesn't trip.
                        if is_stack_local(del_arg).and(c1.til_type.is(Type.Struct)) {
                            ref _del_ct : Str = get_stack_local_ctype(del_arg)
                            if is_null(_del_ct).not().and(_del_ct.eq("Str").or(is_pod_struct(_del_ct))) {
                                if e.children.len().gte(3) {
                                    ref _cf_arg : Expr = e.child(2)
                                    switch _cf_arg.node_type {
                                    case NodeType.Ident(_cf_name):
                                        f.write("(void)"); f.write(_cf_name); f.write("; ")
                                    case:
                                    }
                                }
                                f.write(_del_ct); f.write("_delete(&"); f.write(del_arg); f.write(", &(Bool){0});\n"); return
                            }
                        }
                    case:
                    }
                }
            case:
            }
        }
        e_sname := til_type_name(e.til_type)
        if e_sname.len().gt(0).and(e.children.len().gt(0)) {
            ref c0b : Expr = e.child(0)
            switch c0b.node_type {
            case NodeType.Ident(cname):
                if cname.eq(e_sname) { f.write("/* discarded struct constructor */;\n"); return }
            case:
            }
        }
        // Stmt-level FCalls whose return is Own arrive here in one of
        // two shapes:
        //  - typer's hoist_stmt_fcall left a non-Own bare call -- those
        //    have nothing on the heap, plain `<fcall>;` is correct.
        //  - body DCE replaced an `hoisted__T_X := <fcall>` Decl with
        //    the bare FCall (the temp was unused). The return is an
        //    Own heap pointer; without cleaning it up, we leak. `free`
        //    would also leak for compound types (Str's inner c_str,
        //    Vec's inner buffer, ...). Hand off to `T.delete(self,
        //    call_free=true)` -- the same ASAP path the typer would
        //    have taken if the temp had survived.
        if e.til_type.is(Type.None).not() {
            ref mut clp : LoadedProgram = codegen_lp()
            if fcall_returns_own(e, clp.ctx.scope) {
                tname := type_to_name(e.til_type)
                if tname.len().gt(0) {
                    f.write(tname); f.write("_delete(")
                    emit_expr(f, e, depth)
                    f.write(", &(Bool){1});\n")
                    return
                }
            }
        }
        emit_expr(f, e, depth); f.write(";\n")
    case NodeType.Return:
        emit_stmt_return(f, e, depth)
    case NodeType.Body:
        f.write("{\n"); emit_body_scoped(f, e, depth.add(1.to_i32())); emit_indent(f, depth); f.write("}\n")
    case NodeType.If:
        f.write("if ("); emit_deref(f, e.child(0), depth); f.write(") {\n")
        emit_body_scoped(f, e.child(1), depth.add(1.to_i32())); emit_indent(f, depth)
        if e.children.len().gt(2) { f.write("} else {\n"); emit_body_scoped(f, e.child(2), depth.add(1.to_i32())); emit_indent(f, depth) }
        f.write("}\n")
    case NodeType.While:
        f.write("while ("); emit_deref(f, e.child(0), depth); f.write(") {\n")
        emit_body_scoped(f, e.child(1), depth.add(1.to_i32())); emit_indent(f, depth); f.write("}\n")
    case NodeType.Break: f.write("break;\n")
    case NodeType.Continue: f.write("continue;\n")
    case: f.write("/* TODO: unknown stmt type */\n")
    }
}

emit_fcall_funcptr_cast := proc(f: File, e: Expr, sig: Expr) {
    switch sig.node_type {
    case NodeType.FuncDef(sfd):
        ret_shallow := sfd.return_own_type.is(OwnType.Shallow)
        mut ret_c := "void *"
        if sfd.return_type.len().eq(0) {
            ret_c = "void"
        } else if e.til_type.is(Type.None).not().and(e.til_type.is(Type.Unknown).not())
        .and(e.til_type.is(Type.Dynamic).not()) {
            ret_c = til_type_to_c(e.til_type)
            if e.til_type.is(Type.Struct).or(e.til_type.is(Type.Enum)) {
                e_sname := til_type_name(e.til_type)
                if e_sname.len().gt(0) { ret_c = e_sname }
            }
            if ret_shallow.not() { ret_c = ret_c.concat(" *") }
        }
        f.write("((")
        f.write(ret_c)
        f.write(" (*)(")
        for i in 1..e.children.len() {
            if i.gt(1) { f.write(", ") }
            ref arg : Expr = e.child(i)
            mut arg_shallow := false
            if i.sub(1).lt(sfd.params.len()) {
                ref p : Declaration = sfd.params.get(i.sub(1))
                arg_shallow = p.own_type.is(OwnType.Shallow)
            }
            mut arg_c := "void *"
            if arg.til_type.is(Type.Unknown).not().and(arg.til_type.is(Type.Dynamic).not()) {
                arg_c = til_type_to_c(arg.til_type)
                if arg.til_type.is(Type.Struct).or(arg.til_type.is(Type.Enum)) {
                    arg_sname := til_type_name(arg.til_type)
                    if arg_sname.len().gt(0) { arg_c = arg_sname }
                }
                if arg_shallow.not() { arg_c = arg_c.concat(" *") }
            }
            f.write(arg_c)
        }
        if e.children.len().eq(1) { f.write("void") }
    case:
        UNREACHABLE(__LOC__)
    }
}

emit_fcall_funcptr_args := proc(f: File, e: Expr, sig: Expr, shallow depth: I32) {
    switch sig.node_type {
    case NodeType.FuncDef(sfd):
        for i in 1..e.children.len() {
            if i.gt(1) { f.write(", ") }
            mut arg_shallow := false
            if i.sub(1).lt(sfd.params.len()) {
                ref p : Declaration = sfd.params.get(i.sub(1))
                arg_shallow = p.own_type.is(OwnType.Shallow)
            }
            if arg_shallow { emit_deref(f, e.child(i), depth) }
            else { emit_as_ptr(f, e.child(i), depth, get_own_arg(e, i)) }
        }
    case:
        UNREACHABLE(__LOC__)
    }
}

// Emit a Str literal as an inline expression that mallocs the wrapper
// and initialises it from a compound literal. Replaces the old
// Str_lit("...", N) runtime helper -- same heap-wrapper-with-static-c_str
// semantics, just inlined so the codegen has no Str_lit function to call.
// The `own` contract for callers like Array_set / Vec_push that
// `free(own val)` the wrapper continues to hold.
emit_str_lit_expr := proc(f: File, s: Str) {
    f.write("({ Str *_lit = malloc(sizeof(Str)); *_lit = (Str){.c_str = (void *)\"")
    f.write(s)
    f.write("\", .count = ")
    emit_u64(f, c_lit_byte_count(s).to_u64())
    f.write("ULL, .cap = TIL_CAP_LIT}; _lit; })")
}

emit_expr := proc(f: File, e: Expr, shallow depth: I32) {
    switch e.node_type {
    case NodeType.LiteralStr(s):
        emit_str_lit_expr(f, s)
    case NodeType.LiteralNum(num):
        f.write(num)
    case NodeType.LiteralBool(b):
        if b { emit_i32(f, 1.to_i32()) } else { emit_i32(f, 0.to_i32()) }
    case NodeType.LiteralNull:
        f.write("NULL")
    case NodeType.Ident(ident):
        if e.til_type.is(Type.FuncPtr) {
            f.write("(void *)")
            fc := func_to_c(ident)
            f.write(fc)
        } else {
            f.write(ident)
        }
    case NodeType.FCall:
        ref callee : Expr = e.child(0)
        // Indirect call through FuncSig-typed struct field
        if callee.node_type.is(NodeType.FieldAccess) {
            if callee.til_type.is(Type.FuncPtr) {
                ref sig : Expr = fcall_fn_sig(e)
                if is_null(sig).not() {
                    emit_fcall_funcptr_cast(f, e, sig)
                    f.write("))(void *)(")
                    emit_expr(f, callee, depth)
                    f.write("))(")
                    emit_fcall_funcptr_args(f, e, sig, depth)
                    f.write(")")
                    return
                }
            }
            // Namespace method call
            sname := til_type_name(callee.child(0).til_type)
            switch callee.node_type {
            case NodeType.FieldAccess(mname):
                flat := sname.format("_", mname)
                f.write(sname)
                f.write("_")
                f.write(mname)
                f.write("(")
                for i in 1..e.children.len() {
                    if i.gt(1) { f.write(", ") }
                    if callee_param_is_shallow(flat, i.sub(1)) {
                        emit_deref(f, e.child(i), depth)
                    } else if callee_param_is_usize(flat, i.sub(1)).and(callee_param_is_own(flat, i.sub(1)).not()) {
                        emit_usize_ref(f, e.child(i), depth)
                    } else if callee_param_is_uptr(flat, i.sub(1)).and(callee_param_is_own(flat, i.sub(1)).not()) {
                        emit_uptr_ref(f, e.child(i), depth)
                    } else {
                        emit_as_ptr(f, e.child(i), depth, get_own_arg(e, i))
                    }
                }
                f.write(")")
            case:
                UNREACHABLE(__LOC__)
            }
        } else {
        switch callee.node_type {
        case NodeType.Ident(name):
            if name.eq("dyn_call").or(name.eq("dyn_call_ret")) {
                ref c2 : Expr = e.child(2)
                ref c3 : Expr = e.child(3)
                switch c2.node_type {
                case NodeType.LiteralStr(method):
                    switch c3.node_type {
                    case NodeType.LiteralNum(nargs_str):
                        nargs := nargs_str.to_i32()
                        f.write("dyn_call_")
                        f.write(method)
                        f.write("(")
                        emit_as_ptr(f, e.child(1), depth, false)
                        for ai : I32 in 0..nargs {
                            f.write(", ")
                            emit_as_ptr(f, e.child(ai.add(4).to_u32()), depth, false)
                        }
                        f.write(")")
                    case:
                        UNREACHABLE(__LOC__)
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            } else if name.eq("dyn_fn") {
                f.write("dyn_fn(")
                emit_as_ptr(f, e.child(1), depth, false)
                f.write(", ")
                ref method_arg : Expr = e.child(2)
                switch method_arg.node_type {
                case NodeType.LiteralStr(ms):
                    f.write("&(Str){.c_str=(void*)\"")
                    f.write(ms)
                    f.write("\", .count=")
                    emit_u64(f, ms.len().to_u64())
                    f.write("ULL, .cap=TIL_CAP_LIT}")
                case:
                    emit_as_ptr(f, method_arg, depth, false)
                }
                f.write(")")
            } else if name.eq("dyn_size_of") {
                f.write("dyn_size_of(")
                emit_as_ptr(f, e.child(1), depth, false)
                f.write(")")
            } else if name.eq("dyn_type_to_str") {
                f.write("dyn_type_to_str(")
                emit_as_ptr(f, e.child(1), depth, false)
                f.write(")")
            } else if name.eq("dyn_has_method") {
                ref c2b : Expr = e.child(2)
                switch c2b.node_type {
                case NodeType.LiteralStr(method):
                    f.write("dyn_has_")
                    f.write(method)
                    f.write("(")
                    emit_as_ptr(f, e.child(1), depth, false)
                    f.write(")")
                case:
                    UNREACHABLE(__LOC__)
                }
            } else if til_type_name(e.til_type).len().gt(0).and(name.eq(til_type_name(e.til_type))) {
                // Struct constructor
                ctype := c_type_name(e.til_type, til_type_name(e.til_type))
                ctor_seq = ctor_seq.add(1.to_i32())
                id := ctor_seq
                f.write("({ ")
                f.write(ctype)
                f.write(" *_sc")
                emit_i32(f, id)
                f.write(" = malloc(sizeof(")
                f.write(ctype)
                f.write(")); ")
                tmp := "_sc".concat(id.to_str())
                emit_ctor_fields(f, tmp, e, depth)
                f.write(" _sc")
                emit_i32(f, id)
                f.write("; })")
            } else if callee.til_type.is(Type.FuncPtr) {
                // Indirect call through function pointer variable
                ref sig : Expr = fcall_fn_sig(e)
                emit_fcall_funcptr_cast(f, e, sig)
                if is_ref_local(name) {
                    f.write("))(void *)(*")
                    f.write(name)
                    f.write("))(")
                } else {
                    f.write("))(void *)")
                    f.write(name)
                    f.write(")(")
                }
                emit_fcall_funcptr_args(f, e, sig, depth)
                f.write(")")
            } else {
                // User-defined function call
                fc := func_to_c(name)
                f.write(fc)
                f.write("(")
                for i in 1..e.children.len() {
                    if i.gt(1) { f.write(", ") }
                    if callee_param_is_shallow(name, i.sub(1)) {
                        emit_deref(f, e.child(i), depth)
                    } else if callee_param_is_usize(name, i.sub(1)).and(callee_param_is_own(name, i.sub(1)).not()) {
                        emit_usize_ref(f, e.child(i), depth)
                    } else if callee_param_is_uptr(name, i.sub(1)).and(callee_param_is_own(name, i.sub(1)).not()) {
                        emit_uptr_ref(f, e.child(i), depth)
                    } else {
                        emit_as_ptr(f, e.child(i), depth, get_own_arg(e, i))
                    }
                }
                f.write(")")
            }
        case:
            UNREACHABLE(__LOC__)
        }
        }
    case NodeType.FieldAccess(fname):
        ref obj : Expr = e.child(0)
        if builder_fa_is_ns(e) {
            obj_sname := til_type_name(obj.til_type)
            if e.til_type.is(Type.Enum) {
                f.write("&(")
                f.write(obj_sname)
                f.write("){.tag = ")
                f.write(obj_sname)
                f.write("_TAG_")
                f.write(fname)
                f.write("}")
            } else {
                f.write(obj_sname)
                f.write("_")
                f.write(fname)
            }
        } else {
            // Issue #36: a field access whose receiver is the typer-internal
            // ~get_payload~ FCall (used by struct-pattern desugar inside enum
            // variants) needs an explicit C cast -- get_payload's C signature
            // returns ~void *~, but the typer preserves the variant payload's
            // Struct/Enum til_type on the FCall. Without the cast the
            // generated ~get_payload(&s)->x~ fails C compilation.
            mut obj_is_get_payload := false
            if obj.node_type.is(NodeType.FCall) {
                if obj.children.len().gt(0) {
                    if obj.child(0).node_type.is(NodeType.Ident("get_payload")) {
                        obj_is_get_payload = true
                    }
                }
            }
            if obj_is_get_payload {
                obj_sname_cast := til_type_name(obj.til_type)
                if obj_sname_cast.len().gt(0) {
                    f.write("((")
                    f.write(obj_sname_cast)
                    f.write(" *)")
                    emit_expr(f, obj, depth)
                    f.write(")->")
                    f.write(fname)
                } else {
                    emit_expr(f, obj, depth)
                    if use_dot_access(obj) { f.write(".") } else { f.write("->") }
                    f.write(fname)
                }
            } else {
                emit_expr(f, obj, depth)
                if use_dot_access(obj) { f.write(".") } else { f.write("->") }
                f.write(fname)
            }
        }
    case:
        f.write("/* TODO: unknown expr type */")
    }
}

priv emit_body := proc(f: File, body: Expr, shallow depth: I32) {
    for e_i : Expr in body.children {
        emit_stmt(f, e_i, depth)
    }
}
emit_ctor_fields := proc(f: File, var: Str, ctor: Expr, shallow depth: I32) {
    ref ecf_def : Expr = find_struct_def(til_type_name(ctor.til_type))
    mut fi : U32 = 0
    for i in 1..ctor.children.len() {
        mut fld_own := OwnType.Shallow
        mut field_type := Type.Unknown
        mut fname := ""
        if is_null(ecf_def).not() {
            switch ecf_def.node_type {
            case NodeType.StructDef(ecf_sdd):
                if fi.lt(ecf_sdd.fields.len()) {
                    ref dd : Declaration = ecf_sdd.fields.get(fi)
                    fld_own = dd.own_type
                    field_type = dd.til_type
                    fname = dd.name
                    fi = fi.add(1)
                }
            case:
            }
        }
        ref arg : Expr = ctor.child(i)
        emit_indent(f, depth)
        if fld_own.is(OwnType.Ref) {
            emit_field(f, var, fname)
            f.write(" = ")
            emit_expr(f, arg, depth)
            f.write(";\n")
        } else if fld_own.is(OwnType.Own) {
            if arg.node_type.is(NodeType.FCall) {
                arg_sname := til_type_name(arg.til_type)
                if arg_sname.len().gt(0) {
                    ref ac0 : Expr = arg.child(0)
                    mut handled := false
                    switch ac0.node_type {
                    case NodeType.Ident(callee_ident):
                        if callee_ident.eq(arg_sname) {
                            ct := c_type_name(arg.til_type, arg_sname)
                            ctor_seq = ctor_seq.add(1.to_i32())
                            id := ctor_seq
                            tmp := "_cs".concat(id.to_str())
                            f.write(ct)
                            f.write(" *")
                            f.write(tmp)
                            f.write(" = malloc(sizeof(")
                            f.write(ct)
                            f.write("));\n")
                            emit_ctor_fields(f, tmp, arg, depth)
                            emit_indent(f, depth)
                            emit_field(f, var, fname)
                            f.write(" = ")
                            f.write(tmp)
                            f.write(";\n")
                            handled = true
                        }
                    case:
                    }
                    if handled.not() {
                        emit_field(f, var, fname)
                        f.write(" = ")
                        emit_as_ptr(f, arg, depth, true)
                        f.write(";\n")
                    }
                } else {
                    emit_field(f, var, fname)
                    f.write(" = ")
                    emit_as_ptr(f, arg, depth, true)
                    f.write(";\n")
                }
            } else {
                emit_field(f, var, fname)
                f.write(" = ")
                emit_as_ptr(f, arg, depth, true)
                f.write(";\n")
            }
        } else if arg.node_type.is(NodeType.FCall) {
            arg_sname2 := til_type_name(arg.til_type)
            if arg_sname2.len().gt(0) {
                ref ac0 : Expr = arg.child(0)
                mut is_ctor_call := false
                switch ac0.node_type {
                case NodeType.Ident(callee_ident):
                    if callee_ident.eq(arg_sname2) {
                        // POD nested ctor: emit the field assign with
                        // a direct compound literal, skipping the
                        // malloc-deref-free dance below.
                        if is_pod_struct(arg_sname2).and(pod_ctor_args_are_safe(arg)) {
                            emit_field(f, var, fname)
                            f.write(" = ")
                            emit_pod_compound_literal(f, arg, depth)
                            f.write(";\n")
                            is_ctor_call = true
                        } else {
                        ct := c_type_name(arg.til_type, arg_sname2)
                        ctor_seq = ctor_seq.add(1.to_i32())
                        id := ctor_seq
                        tmp := "_cs".concat(id.to_str())
                        f.write(ct)
                        f.write(" *")
                        f.write(tmp)
                        f.write(" = malloc(sizeof(")
                        f.write(ct)
                        f.write("));\n")
                        emit_ctor_fields(f, tmp, arg, depth)
                        emit_indent(f, depth)
                        emit_field(f, var, fname)
                        f.write(" = *")
                        f.write(tmp)
                        f.write("; free(")
                        f.write(tmp)
                        f.write(");\n")
                        is_ctor_call = true
                        }
                    }
                case:
                }
                if is_ctor_call.not() {
                    if field_type.is(Type.FuncPtr).or(field_type.is(Type.Dynamic))
                    .or(fcall_is_shallow_return(arg)).or(fcall_returns_dynamic(arg)) {
                        emit_field(f, var, fname)
                        f.write(" = ")
                        emit_expr(f, arg, depth)
                        f.write(";\n")
                    } else {
                        ftype := c_type_name(arg.til_type, arg_sname2)
                        f.write("{ ")
                        f.write(ftype)
                        f.write(" *_ca = ")
                        emit_expr(f, arg, depth)
                        f.write("; ")
                        emit_field(f, var, fname)
                        f.write(" = *_ca; free(_ca); }\n")
                    }
                }
            } else {
                if field_type.is(Type.FuncPtr).or(field_type.is(Type.Dynamic))
                .or(fcall_is_shallow_return(arg)).or(fcall_returns_dynamic(arg)) {
                    emit_field(f, var, fname)
                    f.write(" = ")
                    emit_expr(f, arg, depth)
                    f.write(";\n")
                } else {
                    ftype := c_type_name(arg.til_type, arg_sname2)
                    f.write("{ ")
                    f.write(ftype)
                    f.write(" *_ca = ")
                    emit_expr(f, arg, depth)
                    f.write("; ")
                    emit_field(f, var, fname)
                    f.write(" = *_ca; free(_ca); }\n")
                }
            }
        } else {
            emit_field(f, var, fname)
            f.write(" = ")
            emit_deref(f, arg, depth)
            f.write(";\n")
        }
    }
}

emit_ns_inits := proc(f: File, shallow depth: I32) {
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if is_struct_or_enum(stmt) {
                    switch stmt.node_type {
                    case NodeType.Decl(sdecl):
                        ref edef : Expr = stmt.child(0)
                        if edef.node_type.is(NodeType.EnumDef) { continue }
                        ref ni_ns : Vec = def_ns_decls(edef)
                        for dd : Declaration in ni_ns {
                            if is_null(dd.default_value) { continue }
                            if dd.default_value.node_type.is(NodeType.FuncDef) { continue }
                            emit_indent(f, depth)
                            f.write(sdecl.name)
                            f.write("_")
                            f.write(dd.name)
                            f.write(" = ")
                            emit_deref(f, dd.default_value, depth)
                            f.write(";\n")
                        }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if is_struct_or_enum(stmt) {
                switch stmt.node_type {
                case NodeType.Decl(sdecl):
                    ref edef : Expr = stmt.child(0)
                    if edef.node_type.is(NodeType.EnumDef) { continue }
                    ref ni_ns : Vec = def_ns_decls(edef)
                    for dd : Declaration in ni_ns {
                        if is_null(dd.default_value) { continue }
                        if dd.default_value.node_type.is(NodeType.FuncDef) { continue }
                        emit_indent(f, depth)
                        f.write(sdecl.name)
                        f.write("_")
                        f.write(dd.name)
                        f.write(" = ")
                        emit_deref(f, dd.default_value, depth)
                        f.write(";\n")
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
}

emit_body_scoped := proc(f: File, body: Expr, shallow depth: I32) {
    ref mut lp : LoadedProgram = codegen_lp()
    saved_sl := lp.ctx.stack_locals.clone()
    saved_rl := lp.ctx.ref_locals.clone()
    saved_rdl := lp.ctx.ref_dyn_locals.clone()
    emit_body(f, body, depth)
    lp.ctx.stack_locals = saved_sl
    lp.ctx.ref_locals = saved_rl
    lp.ctx.ref_dyn_locals = saved_rdl
}

priv emit_deref := proc(f: File, e: Expr, shallow depth: I32) {
    if e.til_type.is(Type.Dynamic) {
        emit_expr(f, e, depth)
    } else if e.til_type.is(Type.FuncPtr) {
        switch e.node_type {
        case NodeType.Ident(ident):
            if is_ref_local(ident) {
                f.write("(void *)DEREF(")
                emit_expr(f, e, depth)
                f.write(")")
            } else {
                f.write("(void *)")
                emit_expr(f, e, depth)
            }
        case:
            f.write("(void *)")
            emit_expr(f, e, depth)
        }
    } else {
        switch e.node_type {
        case NodeType.Ident(ident):
            if is_shallow_param(ident).or(is_stack_local(ident)).or(is_value_global(ident)) {
                emit_expr(f, e, depth)
            } else if codegen_lp().ctx.ref_dyn_locals.has(ident) {
                // #185: ident is a ref-Dynamic local but the typer's
                // narrow_dynamic has retyped this use to a concrete
                // primitive. Emit a typed cast so DEREF doesn't try to
                // dereference a `void *` (which is invalid C).
                ctype := til_type_to_c(e.til_type)
                f.write("DEREF((")
                f.write(ctype)
                f.write(" *)")
                emit_expr(f, e, depth)
                f.write(")")
            } else {
                f.write("DEREF(")
                emit_expr(f, e, depth)
                f.write(")")
            }
        case NodeType.LiteralStr(s):
            f.write("(Str){.c_str=(void*)\"")
            f.write(s)
            f.write("\", .count=")
            emit_u64(f, c_lit_byte_count(s).to_u64())
            f.write("ULL, .cap=TIL_CAP_LIT}")
        case NodeType.FieldAccess:
            if builder_fa_is_ns(e).and(e.til_type.is(Type.Enum)) {
                f.write("(*")
                emit_expr(f, e, depth)
                f.write(")")
            } else {
                emit_expr(f, e, depth)
            }
        case:
            emit_expr(f, e, depth)
        }
    }
}

emit_as_ptr := proc(f: File, e: Expr, shallow depth: I32, shallow is_own: Bool) {
    switch e.node_type {
    case NodeType.Ident(ident):
        if is_shallow_param(ident).or(is_stack_local(ident)).or(is_value_global(ident)) {
            if is_own {
                ctype := resolve_ident_ctype(ident, e.til_type, til_type_name(e.til_type))
                f.write("({ ")
                f.write(ctype)
                f.write(" *_oa = malloc(sizeof(")
                f.write(ctype)
                f.write(")); *_oa = ")
                emit_expr(f, e, depth)
                f.write("; _oa; })")
            } else {
                f.write("&")
                f.write(ident)
            }
        } else {
            emit_expr(f, e, depth)
        }
    case NodeType.FCall:
        e_sname := til_type_name(e.til_type)
        if e_sname.len().gt(0).and(e.children.len().gt(0)) {
            ref c0 : Expr = e.child(0)
            switch c0.node_type {
            case NodeType.Ident(callee_ident):
                if callee_ident.eq(e_sname) {
                    ctype := c_type_name(e.til_type, e_sname)
                    ctor_seq = ctor_seq.add(1.to_i32())
                    id := ctor_seq
                    f.write("({ ")
                    f.write(ctype)
                    f.write(" *_sc")
                    emit_i32(f, id)
                    f.write(" = malloc(sizeof(")
                    f.write(ctype)
                    f.write(")); ")
                    tmp := "_sc".concat(id.to_str())
                    emit_ctor_fields(f, tmp, e, depth)
                    f.write(" _sc")
                    emit_i32(f, id)
                    f.write("; })")
                    return
                }
            case:
            }
        }
        emit_expr(f, e, depth)
    case NodeType.LiteralStr:
        emit_expr(f, e, depth)
    case NodeType.FieldAccess(fname):
        if builder_fa_is_ptr(e) {
            emit_expr(f, e, depth)
        } else if builder_fa_is_ns(e).and(e.til_type.is(Type.Enum)) {
            if is_own {
                ref obj : Expr = e.child(0)
                obj_sname := til_type_name(obj.til_type)
                f.write("({ ")
                f.write(obj_sname)
                f.write(" *_oa = malloc(sizeof(")
                f.write(obj_sname)
                f.write(")); *_oa = (")
                f.write(obj_sname)
                f.write("){.tag = ")
                f.write(obj_sname)
                f.write("_TAG_")
                f.write(fname)
                f.write("}; _oa; })")
            } else {
                emit_expr(f, e, depth)
            }
        } else if e.til_type.is(Type.Dynamic) {
            emit_expr(f, e, depth)
        } else {
            f.write("&")
            emit_expr(f, e, depth)
        }
    case NodeType.LiteralNull:
        f.write("NULL")
    case:
        ctype := c_type_name(e.til_type, til_type_name(e.til_type))
        f.write("&(")
        f.write(ctype)
        f.write("){")
        emit_expr(f, e, depth)
        f.write("}")
    }
}

emit_usize_ref := proc(f: File, e: Expr, shallow depth: I32) {
    f.write("USIZE_REF(")
    emit_deref(f, e, depth)
    f.write(")")
}

emit_uptr_ref := proc(f: File, e: Expr, shallow depth: I32) {
    f.write("UPTR_REF(")
    emit_deref(f, e, depth)
    f.write(")")
}

emit_cli_parse_arg := proc(f: File, pname: Str, ptype: Str, shallow argi: I32) {
    if ptype.eq("Str") {
        f.write("    Str *"); f.write(pname); f.write(" = Str_clone(&(Str){.c_str = (void *)argv["); emit_i32(f, argi); f.write("], .count = strlen(argv["); emit_i32(f, argi); f.write("]), .cap = TIL_CAP_LIT});\n")
    } else if ptype.eq("I64") {
        f.write("    I64 *"); f.write(pname); f.write(" = cli_parse_i64(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("U8") {
        f.write("    U8 *"); f.write(pname); f.write(" = cli_parse_u8(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("I8") {
        f.write("    I8 *"); f.write(pname); f.write(" = cli_parse_i8(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("I16") {
        f.write("    I16 *"); f.write(pname); f.write(" = cli_parse_i16(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("I32") {
        f.write("    I32 *"); f.write(pname); f.write(" = cli_parse_i32(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("U32") {
        f.write("    U32 *"); f.write(pname); f.write(" = cli_parse_u32(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("U64") {
        f.write("    U64 *"); f.write(pname); f.write(" = cli_parse_u64(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("USize") {
        f.write("    USize *"); f.write(pname); f.write(" = (USize *)cli_parse_u64(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("UPtr") {
        f.write("    UPtr *"); f.write(pname); f.write(" = cli_parse_uptr(argv["); emit_i32(f, argi); f.write("]);\n")
    } else if ptype.eq("Bool") {
        f.write("    Bool *"); f.write(pname); f.write(" = cli_parse_bool(argv["); emit_i32(f, argi); f.write("]);\n")
    } else {
        f.write("    // unsupported CLI arg type: "); f.write(ptype); f.write("\n")
        f.write("    fprintf(stderr, \"error: unsupported CLI argument type '"); f.write(ptype); f.write("'\\n\"); return 1;\n")
    }
}

emit_func_def := proc(f: File, name: Str, func_def: Expr, mode: Mode, shallow is_static: Bool) {
    switch func_def.node_type {
    case NodeType.FuncDef(fd):
    ref body : Expr = func_def.child(0)
    mut is_main := false
    if is_null(mode).not() {
        if mode.needs_main { is_main = name.eq("main") }
    }

    if is_main {
        nparam := fd.params.len()
        vi := fd.variadic_index
        if nparam.eq(0) {
            f.write("int main(int argc, char **argv) {\n")
            f.write("    (void)argv;\n")
            f.write("    if (argc > 1) { fprintf(stderr, \"error: main expects no arguments, got %d\\n\", argc - 1); return 1; }\n")
        } else {
            f.write("int main(int argc, char **argv) {\n")
            mut fixed := nparam.to_i32()
            if vi.gte(0.to_i32()) { fixed = fixed.sub(1.to_i32()) }
            if vi.gte(0.to_i32()) {
                f.write("    if (argc - 1 < "); emit_i32(f, fixed); f.write(") { fprintf(stderr, \"error: main expects at least "); emit_i32(f, fixed); f.write(" argument(s), got %d\\n\", argc - 1); return 1; }\n")
            } else {
                f.write("    if (argc - 1 != "); emit_i32(f, nparam.to_i32()); f.write(") { fprintf(stderr, \"error: main expects "); emit_i32(f, nparam.to_i32()); f.write(" argument(s), got %d\\n\", argc - 1); return 1; }\n")
            }
        }
        emit_ns_inits(f, 1.to_i32())
        if codegen_lp().ctx.script_globals.len().gt(0) {
            ref lp : LoadedProgram = codegen_lp()
            if lp.skip_core.not() {
                for u : ProgramUnit in lp.core_units {
                    ref iu : ImportUnit = lp.ctx.imported.get(u.path)
                    for gi in 0..iu.ast.children.len() {
                        ref gs : Expr = iu.ast.child(gi)
                        if gs.node_type.is(NodeType.Decl) {
                            ref rhs : Expr = gs.child(0)
                            if rhs.node_type.is(NodeType.FuncDef).or(rhs.node_type.is(NodeType.StructDef)).or(rhs.node_type.is(NodeType.EnumDef)) { continue }
                            if gs.til_type.is(Type.None).and(rhs.node_type.is(NodeType.Ident)) { continue }
                        } else if gs.node_type.is(NodeType.FCall).not() { continue }
                        emit_stmt(f, gs, 1.to_i32())
                    }
                }
            }
            for u : ProgramUnit in lp.units {
                ref iu : ImportUnit = lp.ctx.imported.get(u.path)
                for gi in 0..iu.ast.children.len() {
                    ref gs : Expr = iu.ast.child(gi)
                    if gs.node_type.is(NodeType.Decl) {
                        ref rhs : Expr = gs.child(0)
                        if rhs.node_type.is(NodeType.FuncDef).or(rhs.node_type.is(NodeType.StructDef)).or(rhs.node_type.is(NodeType.EnumDef)) { continue }
                        if gs.til_type.is(Type.None).and(rhs.node_type.is(NodeType.Ident)) { continue }
                    } else if gs.node_type.is(NodeType.FCall).not() { continue }
                    emit_stmt(f, gs, 1.to_i32())
                }
            }
        }
        if nparam.gt(0) {
            mut argi := 1.to_i32()
            for i in 0..nparam {
                ref p : Declaration = fd.params.get(i)
                ii := i.to_i32()
                if ii.eq(vi) {
                    // Variadic array
                    f.write("    int _va_argc = argc - "); emit_i32(f, argi); f.write(";\n")
                    f.write("    USize _va_cap = _va_argc;\n")
                    f.write("    Array *"); f.write(p.name); f.write(" = Array_new_type_name(&(Str){.c_str = (void *)\""); f.write(p.explicit_type); f.write("\", .count = "); emit_u64(f, p.explicit_type.len().to_u64()); f.write("ULL, .cap = TIL_CAP_LIT}, &(USize){_va_cap});\n")
                    f.write("    for (int _i = 0; _i < _va_argc; _i++) {\n")
                    f.write("        USize _idx = _i;\n")
                    if p.explicit_type.eq("Str") {
                        f.write("        Str *_val = Str_clone(&(Str){.c_str = (void *)argv["); emit_i32(f, argi); f.write(" + _i], .count = strlen(argv["); emit_i32(f, argi); f.write(" + _i]), .cap = TIL_CAP_LIT});\n")
                    } else if p.explicit_type.eq("I64") {
                        f.write("        I64 *_val = cli_parse_i64(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("U8") {
                        f.write("        U8 *_val = cli_parse_u8(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("I8") {
                        f.write("        I8 *_val = cli_parse_i8(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("I16") {
                        f.write("        I16 *_val = cli_parse_i16(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("I32") {
                        f.write("        I32 *_val = cli_parse_i32(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("U32") {
                        f.write("        U32 *_val = cli_parse_u32(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("U64") {
                        f.write("        U64 *_val = cli_parse_u64(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("USize") {
                        f.write("        USize *_val = (USize *)cli_parse_u64(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("UPtr") {
                        f.write("        UPtr *_val = cli_parse_uptr(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    } else if p.explicit_type.eq("Bool") {
                        f.write("        Bool *_val = cli_parse_bool(argv["); emit_i32(f, argi); f.write(" + _i]);\n")
                    }
                    f.write("        Array_set("); f.write(p.name); f.write(", &(USize){_idx}, _val);\n")
                    f.write("    }\n")
                } else {
                    emit_cli_parse_arg(f, p.name, p.explicit_type, argi)
                    argi = argi.add(1.to_i32())
                }
            }
        }
        ref mut lp : LoadedProgram = codegen_lp()
        lp.ctx.stack_locals = Set.new(Str)
        lp.ctx.stack_local_types = Map.new(Str, Str)
        lp.ctx.unsafe_to_hoist = Set.new(Str)
        lp.ctx.ref_locals = Set.new(Str)
        lp.ctx.ref_dyn_locals = Set.new(Str)
        lp.ctx.local_fn_sigs = Map.new(Str, Dynamic)
        collect_unsafe_to_hoist(body)
        in_main_func = true
        emit_body(f, body, 1.to_i32())
        in_main_func = false
        f.write("    return 0;\n")
        f.write("}\n")
    } else {
        // Return type
        ret := func_return_ctype(fd)
        // Signature
        if is_static { f.write("static __attribute__((unused)) ") }
        f.write(ret); f.write(" "); fc := func_to_c(name); f.write(fc); f.write("(")
        emit_param_list(f, func_def, true, false)
        f.write(") {\n")
        // Suppress -Wunused-parameter for params that aren't referenced
        // in the body. At this point any non-_ param is body-referenced
        // (otherwise check_unused_params would have errored), so this
        // narrows to `_x`-prefixed deliberately-unused params.
        body_refs := Set.new(Str)
        collect_ident_refs(body, body_refs)
        for p : Declaration in fd.params {
            if body_refs.has(p.name).not() {
                f.write("    (void)"); f.write(p.name); f.write(";\n")
            }
        }
        Set.delete(own body_refs, false)
        in_func_def = true
        ref mut lp : LoadedProgram = codegen_lp()
        lp.ctx.current_fdef = func_def
        lp.ctx.stack_locals = Set.new(Str)
        lp.ctx.stack_local_types = Map.new(Str, Str)
        lp.ctx.unsafe_to_hoist = Set.new(Str)
        lp.ctx.ref_locals = Set.new(Str)
        lp.ctx.ref_dyn_locals = Set.new(Str)
        lp.ctx.local_fn_sigs = Map.new(Str, Dynamic)
        collect_unsafe_to_hoist(body)
        emit_body(f, body, 1.to_i32())
        in_func_def = false
        f.write("}\n")
    }
    case:
        UNREACHABLE(__LOC__)
    }
}

emit_struct_typedef := proc(f: File, name: Str, struct_def: Expr) {
    if is_ext_h_type(name) { return }
    switch struct_def.node_type {
    case NodeType.StructDef(sdd):
        if sdd.c_tag.eq("@anon") {
            f.write("typedef struct {\n")
        } else if sdd.c_tag.len().gt(0) {
            f.write("typedef struct "); f.write(sdd.c_tag); f.write(" {\n")
        } else {
            f.write("typedef struct "); f.write(name); f.write(" {\n")
        }
        if sdd.fields.len().eq(0) { f.write("    char _;\n") }
        for dd : Declaration in sdd.fields {
            if dd.own_type.is(OwnType.Shallow).not() {
                if dd.til_type.is(Type.Struct).or(dd.til_type.is(Type.Enum)) {
                    sname := til_type_name(dd.til_type)
                    if sname.len().gt(0) {
                        f.write("    "); f.write(sname); f.write(" *"); f.write(dd.name); f.write(";\n")
                    } else {
                        tc := til_type_to_c(dd.til_type); f.write("    "); f.write(tc); f.write(" *"); f.write(dd.name); f.write(";\n")
                    }
                } else {
                    tc := til_type_to_c(dd.til_type); f.write("    "); f.write(tc); f.write(" *"); f.write(dd.name); f.write(";\n")
                }
            } else if dd.til_type.is(Type.Struct).or(dd.til_type.is(Type.Enum)) {
                sname := til_type_name(dd.til_type)
                if sname.len().gt(0) {
                    f.write("    "); f.write(sname); f.write(" "); f.write(dd.name); f.write(";\n")
                } else {
                    tc := til_type_to_c(dd.til_type); f.write("    "); f.write(tc); f.write(" "); f.write(dd.name); f.write(";\n")
                }
            } else {
                tc := til_type_to_c(dd.til_type); f.write("    "); f.write(tc); f.write(" "); f.write(dd.name); f.write(";\n")
            }
        }
        f.write("} "); f.write(name); f.write(";\n\n")
    case:
        UNREACHABLE(__LOC__)
    }
    // Namespace non-function fields as globals
    ref ng_ns : Vec = def_ns_decls(struct_def)
    for dd : Declaration in ng_ns {
        if is_null(dd.default_value) { continue }
        if dd.default_value.node_type.is(NodeType.FuncDef) { continue }
        if dd.til_type.is(Type.Struct).or(dd.til_type.is(Type.Enum)) {
            ng_sname := til_type_name(dd.default_value.til_type)
            if ng_sname.len().gt(0) {
                f.write(ng_sname); f.write(" "); f.write(name); f.write("_"); f.write(dd.name); f.write(";\n")
            } else {
                tc := til_type_to_c(dd.til_type); f.write(tc); f.write(" "); f.write(name); f.write("_"); f.write(dd.name); f.write(";\n")
            }
        } else {
            tc := til_type_to_c(dd.til_type); f.write(tc); f.write(" "); f.write(name); f.write("_"); f.write(dd.name); f.write(";\n")
        }
    }
}

emit_ns_method := proc(f: File, name: Str, dd_name: Str, fdef: Expr) {
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { return }
    case:
        UNREACHABLE(__LOC__)
        return
    }
    full_name := name.format("_", dd_name)
    emit_func_def(f, full_name, fdef, NULL, false)
    f.write("\n")
}

emit_struct_funcs := proc(f: File, name: Str, struct_def: Expr) {
    // User-defined from ns_decls
    ref ns : Vec = def_ns_decls(struct_def)
    for dd : Declaration in ns {
        if is_null(dd.default_value) { continue }
        if dd.default_value.node_type.is(NodeType.NoDefaultArg) { continue }
        if dd.default_value.node_type.is(NodeType.FuncDef) {
            emit_ns_method(f, name, dd.name, dd.default_value)
        }
    }
}

emit_enum_def := proc(f: File, name: Str, enum_def: Expr) {
    switch enum_def.node_type {
    case NodeType.EnumDef(edd):
        hp := enum_has_payloads(enum_def)
        if hp.not() {
            // Simple enum
            for vn : Str in edd.variants {
                f.write(name); f.write(" *"); f.write(name); f.write("_"); f.write(vn); f.write("() {\n")
                f.write("    "); f.write(name); f.write(" *r = malloc(sizeof("); f.write(name); f.write("));\n")
                f.write("    *r = ("); f.write(name); f.write("){ .tag = "); f.write(name); f.write("_TAG_"); f.write(vn); f.write(" };\n")
                f.write("    return r;\n"); f.write("}\n")
            }
        } else {
            // Payload enum
            for i in 0..edd.variants.len() {
                ref vn : Str = edd.variants.get(i)
                vt := enum_variant_type(enum_def, i.to_i32())
                if vt.len().eq(0) {
                    f.write(name); f.write(" *"); f.write(name); f.write("_"); f.write(vn); f.write("() {\n")
                    f.write("    "); f.write(name); f.write(" *r = malloc(sizeof("); f.write(name); f.write("));\n")
                    f.write("    r->tag = "); f.write(name); f.write("_TAG_"); f.write(vn); f.write(";\n")
                    f.write("    return r;\n"); f.write("}\n")
                    continue
                }
                ptype := type_name_to_c(vt)
                f.write(name); f.write(" *"); f.write(name); f.write("_"); f.write(vn); f.write("("); f.write(ptype); f.write(" val) {\n")
                f.write("    "); f.write(name); f.write(" *r = malloc(sizeof("); f.write(name); f.write("));\n")
                f.write("    r->tag = "); f.write(name); f.write("_TAG_"); f.write(vn); f.write(";\n")
                if is_primitive_type(vt) {
                    f.write("    r->data."); f.write(vn); f.write(" = *val;\n")
                } else if is_funcsig_type(vt).or(vt.eq("Dynamic")) {
                    f.write("    r->data."); f.write(vn); f.write(" = val;\n")
                } else {
                    tnc := type_name_to_c(vt)
                    f.write("    { "); f.write(tnc); f.write(" _tmp = "); f.write(vt); f.write("_clone(val); r->data."); f.write(vn); f.write(" = *_tmp; free(_tmp); }\n")
                }
                f.write("    return r;\n"); f.write("}\n")
            }
        }
    case:
        UNREACHABLE(__LOC__)
    }
    // Namespace methods from ns_decls
    ref _ens : Vec = def_ns_decls(enum_def)
    for dd : Declaration in _ens {
        if is_null(dd.default_value) { continue }
        if dd.default_value.node_type.is(NodeType.FuncDef).not() { continue }
        emit_ns_method(f, name, dd.name, dd.default_value)
    }
}

emit_enum_struct_body := proc(f: File, ename: Str, enum_def: Expr) {
    f.write("struct "); f.write(ename); f.write(" {\n")
    f.write("    "); f.write(ename); f.write("_tag tag;\n")
    if enum_has_payloads(enum_def) {
        f.write("    union {\n")
        switch enum_def.node_type {
        case NodeType.EnumDef(edd):
            for j in 0..edd.variants.len() {
                ref vn : Str = edd.variants.get(j)
                vt := enum_variant_type(enum_def, j.to_i32())
                if vt.len().gt(0) {
                    tnv := type_name_to_c_value(vt)
                    f.write("        "); f.write(tnv); f.write(" "); f.write(vn); f.write(";\n")
                }
            }
        case:
            UNREACHABLE(__LOC__)
        }
        f.write("    } data;\n")
    }
    f.write("};\n\n")
}

topo_emit_struct_enum_defs := proc(f: File, prog: Expr, mut emitted: Set) {
    mut indices := Vec.new(U32)
    for i in 0..prog.children.len() {
        ref stmt : Expr = prog.child(i)
        if stmt.node_type.is(NodeType.Decl) {
            if stmt.children.len().gt(0) {
                if stmt.child(0).node_type.is(NodeType.StructDef).or(stmt.child(0).node_type.is(NodeType.EnumDef)) {
                    mut idx := i
                    indices.push(own idx)
                }
            }
        }
    }
    mut remaining := indices.len()
    mut done := Array.new(Bool, indices.len())
    for di in 0..indices.len() {
        mut f_val := false
        done.set(di, own f_val)
    }
    while remaining.gt(0) {
        mut progress := false
        for ei in 0..indices.len() {
            ref d : Bool = done.get(ei)
            if d { continue }
            ref idx : U32 = indices.get(ei)
            ref stmt : Expr = prog.child(idx)
            switch stmt.node_type {
            case NodeType.Decl(sdecl):
                ref def : Expr = stmt.child(0)
                mut deps_ok := true
                switch def.node_type {
                case NodeType.StructDef(dep_sdd):
                    for dd : Declaration in dep_sdd.fields {
                        if dd.own_type.is(OwnType.Shallow) {
                            if dd.til_type.is(Type.Struct).or(dd.til_type.is(Type.Enum)) {
                                dep_sname := til_type_name(dd.til_type)
                                if dep_sname.len().gt(0) {
                                    if emitted.has(dep_sname).not() { deps_ok = false }
                                }
                            }
                        }
                    }
                case NodeType.EnumDef(dep_edd):
                    for fi in 0..dep_edd.variants.len() {
                        dep_vt := enum_variant_type(def, fi.to_i32())
                        if dep_vt.len().gt(0) {
                            if is_primitive_type(dep_vt).not().and(is_funcsig_type(dep_vt).not()) {
                                if emitted.has(dep_vt).not() { deps_ok = false }
                            }
                        }
                    }
                case:
                }
                if deps_ok.not() { continue }
                if def.node_type.is(NodeType.StructDef) {
                    emit_struct_typedef(f, sdecl.name, def)
                    f.write("\n")
                } else {
                    emit_enum_struct_body(f, sdecl.name, def)
                }
                emitted.add(own sdecl.name.clone())
                mut t_val := true
                done.set(ei, own t_val)
                remaining = remaining.sub(1)
                progress = true
            case:
                UNREACHABLE(__LOC__)
            }
        }
        if progress.not() {
            for ei in 0..indices.len() {
                ref d : Bool = done.get(ei)
                if d { continue }
                ref idx : U32 = indices.get(ei)
                ref stmt : Expr = prog.child(idx)
                switch stmt.node_type {
                case NodeType.Decl(sdecl):
                    ref def : Expr = stmt.child(0)
                    if def.node_type.is(NodeType.StructDef) {
                        emit_struct_typedef(f, sdecl.name, def)
                        f.write("\n")
                    } else {
                        emit_enum_struct_body(f, sdecl.name, def)
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
            break
        }
    }
    own done.delete()
    own indices.delete()
}

// Topologically emit struct/enum typedefs across all per-file units in lp.
topo_emit_struct_enum_defs_lp := proc(f: File, mut emitted: Set) {
    ref lp : LoadedProgram = codegen_lp()
    // Vec<Dynamic> stores raw pointers (8-byte values). Each push takes an
    // allocated 8-byte temp, copies the pointer bytes, then frees the temp.
    mut items := Vec.new(Dynamic)
    // Collect pointers to all struct/enum decl statements in stable order.
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).and(stmt.children.len().gt(0)) {
                    if stmt.child(0).node_type.is(NodeType.StructDef).or(stmt.child(0).node_type.is(NodeType.EnumDef)) {
                        tmp : Dynamic = malloc(I64.size())
                        write_ptr(tmp, stmt)
                        items.push(own tmp)
                    }
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).and(stmt.children.len().gt(0)) {
                if stmt.child(0).node_type.is(NodeType.StructDef).or(stmt.child(0).node_type.is(NodeType.EnumDef)) {
                    tmp2 : Dynamic = malloc(I64.size())
                    write_ptr(tmp2, stmt)
                    items.push(own tmp2)
                }
            }
        }
    }

    mut remaining := items.len()
    mut done := Array.new(Bool, items.len())
    for di in 0..items.len() {
        mut f_val := false
        done.set(di, own f_val)
    }

    while remaining.gt(0) {
        mut progress := false
        for ei in 0..items.len() {
            ref d : Bool = done.get(ei)
            if d { continue }
            ref ptr : Dynamic = items.get(ei)
            ref stmt : Expr = deref(ptr)
            switch stmt.node_type {
            case NodeType.Decl(sdecl):
                ref def : Expr = stmt.child(0)
                mut deps_ok := true
                switch def.node_type {
                case NodeType.StructDef(dep_sdd):
                    for dd : Declaration in dep_sdd.fields {
                        if dd.own_type.is(OwnType.Shallow) {
                            if dd.til_type.is(Type.Struct).or(dd.til_type.is(Type.Enum)) {
                                dep_sname := til_type_name(dd.til_type)
                                if dep_sname.len().gt(0) {
                                    if emitted.has(dep_sname).not() { deps_ok = false }
                                }
                            }
                        }
                    }
                case NodeType.EnumDef(dep_edd):
                    for fi in 0..dep_edd.variants.len() {
                        dep_vt := enum_variant_type(def, fi.to_i32())
                        if dep_vt.len().gt(0) {
                            if is_primitive_type(dep_vt).not().and(is_funcsig_type(dep_vt).not()) {
                                if emitted.has(dep_vt).not() { deps_ok = false }
                            }
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
                if deps_ok.not() { continue }
                if def.node_type.is(NodeType.StructDef) {
                    emit_struct_typedef(f, sdecl.name, def)
                    f.write("\n")
                } else {
                    emit_enum_struct_body(f, sdecl.name, def)
                }
                emitted.add(own sdecl.name.clone())
                mut t_val := true
                done.set(ei, own t_val)
                remaining = remaining.sub(1)
                progress = true
            case:
                UNREACHABLE(__LOC__)
            }
        }
        if progress.not() {
            // Fallback: emit remaining in scan order.
            for ei in 0..items.len() {
                ref d : Bool = done.get(ei)
                if d { continue }
                ref ptr2 : Dynamic = items.get(ei)
                ref stmt : Expr = deref(ptr2)
                switch stmt.node_type {
                case NodeType.Decl(sdecl):
                    ref def : Expr = stmt.child(0)
                    if def.node_type.is(NodeType.StructDef) {
                        emit_struct_typedef(f, sdecl.name, def)
                        f.write("\n")
                    } else {
                        emit_enum_struct_body(f, sdecl.name, def)
                    }
                    emitted.add(own sdecl.name.clone())
                case:
                    UNREACHABLE(__LOC__)
                }
            }
            break
        }
    }

    own items.delete()
    own done.delete()
}

// Resolve the C return type for a FunctionDef; "void" when the proc
// has no return type. Avoids the `mut ret := "void"` reassign pattern
// in emit_func_forward_decl / emit_funcsig_typedef whose default
// "void" Str_lit leaked when reassigned.
func_return_ctype := func(fd: FunctionDef) returns Str {
    if fd.return_type.len().eq(0) { return "void" }
    if fd.return_own_type.is(OwnType.Shallow) { return type_name_to_c_value(fd.return_type) }
    return type_name_to_c(fd.return_type)
}

priv emit_func_forward_decl := proc(f: File, name: Str, fdef: Expr, shallow top_level: Bool) {
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        // Issue #176: top-level ext_func / ext_proc prototypes are emitted by
        // emit_ext_func_declarations (with the FFI const convention). Skip
        // them here so til_forward.h / monolithic headers don't contain a
        // second non-const prototype that clashes with the const definition
        // in ext.c / ext.h. Namespace ext_func methods still need their
        // forward decl from this path (top_level=false) -- nothing else
        // covers them.
        if top_level.and(fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc))) { return }
        ret := func_return_ctype(fd)
        f.write(ret); f.write(" "); f.write(name); f.write("(")
        emit_param_list(f, fdef, true, false)
        f.write(");\n")
    case:
        UNREACHABLE(__LOC__)
    }
}

emit_funcsig_typedef := proc(f: File, name: Str, fdef: Expr) {
    switch fdef.node_type {
    case NodeType.FuncDef(fd):
        ret := func_return_ctype(fd)
        f.write("typedef "); f.write(ret); f.write(" (*"); f.write(name); f.write(")(")
        emit_param_list(f, fdef, false, false)
        f.write(");\n")
    case:
        UNREACHABLE(__LOC__)
    }
}

emit_funcsig_typedefs_prog := proc(f: File, prog: Expr) {
    for stmt : Expr in prog.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        if stmt.children.len().eq(0) { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)
            if rhs.node_type.is(NodeType.FuncDef) {
                if rhs.children.len().eq(0) {
                    emit_funcsig_typedef(f, dd.name, rhs)
                }
            } else {
                switch rhs.node_type {
                case NodeType.Ident(rhs_name):
                    if stmt.til_type.is(Type.None).and(is_funcsig_type(rhs_name)) {
                        f.write("typedef "); f.write(rhs_name); f.write(" "); f.write(dd.name); f.write(";\n")
                    }
                case:
                }
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

emit_funcsig_typedefs := proc(f: File, core_program: Expr, program: Expr) {
    if is_null(core_program).not() { emit_funcsig_typedefs_prog(f, core_program) }
    if is_null(program).not()      { emit_funcsig_typedefs_prog(f, program) }
    f.write("\n")
}

emit_funcsig_typedefs_lp := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                if stmt.children.len().eq(0) { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref rhs : Expr = stmt.child(0)
                    if rhs.node_type.is(NodeType.FuncDef) {
                        if rhs.children.len().eq(0) {
                            emit_funcsig_typedef(f, dd.name, rhs)
                        }
                    } else {
                        switch rhs.node_type {
                        case NodeType.Ident(rhs_name):
                            if stmt.til_type.is(Type.None).and(is_funcsig_type(rhs_name)) {
                                f.write("typedef "); f.write(rhs_name); f.write(" "); f.write(dd.name); f.write(";\n")
                            }
                        case:
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.FuncDef) {
                    if rhs.children.len().eq(0) {
                        emit_funcsig_typedef(f, dd.name, rhs)
                    }
                } else {
                    switch rhs.node_type {
                    case NodeType.Ident(rhs_name):
                        if stmt.til_type.is(Type.None).and(is_funcsig_type(rhs_name)) {
                            f.write("typedef "); f.write(rhs_name); f.write(" "); f.write(dd.name); f.write(";\n")
                        }
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")
}

emit_header_forward_decls_prog := proc(f: File, prog: Expr) {
    for stmt : Expr in prog.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        if stmt.children.len().eq(0) { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)
            switch rhs.node_type {
            case NodeType.StructDef(fwd_sdd):
                if is_ext_h_type(dd.name) { continue }
                if fwd_sdd.c_tag.eq("@anon") {
                    // anonymous ext_struct: no forward decl possible
                } else if fwd_sdd.c_tag.len().gt(0) {
                    f.write("typedef struct "); f.write(fwd_sdd.c_tag); f.write(" "); f.write(dd.name); f.write(";\n")
                } else {
                    f.write("typedef struct "); f.write(dd.name); f.write(" "); f.write(dd.name); f.write(";\n")
                }
            case NodeType.EnumDef(fwd_edd):
                f.write("typedef enum {\n")
                for j in 0..fwd_edd.variants.len() {
                    ref fwd_vn : Str = fwd_edd.variants.get(j)
                    if j.gt(0) { f.write(",\n") }
                    f.write("    "); f.write(dd.name); f.write("_TAG_"); f.write(fwd_vn)
                }
                f.write("\n} "); f.write(dd.name); f.write("_tag;\n")
                f.write("typedef struct "); f.write(dd.name); f.write(" "); f.write(dd.name); f.write(";\n")
            case:
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

emit_header_forward_decls := proc(f: File, core_program: Expr, program: Expr) {
    if is_null(core_program).not() { emit_header_forward_decls_prog(f, core_program) }
    if is_null(program).not()      { emit_header_forward_decls_prog(f, program) }
    f.write("\n")
}

emit_header_forward_decls_lp := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                if stmt.children.len().eq(0) { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref rhs : Expr = stmt.child(0)
                    switch rhs.node_type {
                    case NodeType.StructDef(fwd_sdd):
                        if is_ext_h_type(dd.name) { continue }
                        if fwd_sdd.c_tag.eq("@anon") {
                            // anonymous ext_struct: no forward decl possible
                        } else if fwd_sdd.c_tag.len().gt(0) {
                            f.write("typedef struct "); f.write(fwd_sdd.c_tag); f.write(" "); f.write(dd.name); f.write(";\n")
                        } else {
                            f.write("typedef struct "); f.write(dd.name); f.write(" "); f.write(dd.name); f.write(";\n")
                        }
                    case NodeType.EnumDef(fwd_edd):
                        f.write("typedef enum {\n")
                        for j in 0..fwd_edd.variants.len() {
                            ref fwd_vn : Str = fwd_edd.variants.get(j)
                            if j.gt(0) { f.write(",\n") }
                            f.write("    "); f.write(dd.name); f.write("_TAG_"); f.write(fwd_vn)
                        }
                        f.write("\n} "); f.write(dd.name); f.write("_tag;\n")
                        f.write("typedef struct "); f.write(dd.name); f.write(" "); f.write(dd.name); f.write(";\n")
                    case:
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                switch rhs.node_type {
                case NodeType.StructDef(fwd_sdd):
                    if is_ext_h_type(dd.name) { continue }
                    if fwd_sdd.c_tag.eq("@anon") {
                        // anonymous ext_struct: no forward decl possible
                    } else if fwd_sdd.c_tag.len().gt(0) {
                        f.write("typedef struct "); f.write(fwd_sdd.c_tag); f.write(" "); f.write(dd.name); f.write(";\n")
                    } else {
                        f.write("typedef struct "); f.write(dd.name); f.write(" "); f.write(dd.name); f.write(";\n")
                    }
                case NodeType.EnumDef(fwd_edd):
                    f.write("typedef enum {\n")
                    for j in 0..fwd_edd.variants.len() {
                        ref fwd_vn : Str = fwd_edd.variants.get(j)
                        if j.gt(0) { f.write(",\n") }
                        f.write("    "); f.write(dd.name); f.write("_TAG_"); f.write(fwd_vn)
                    }
                    f.write("\n} "); f.write(dd.name); f.write("_tag;\n")
                    f.write("typedef struct "); f.write(dd.name); f.write(" "); f.write(dd.name); f.write(";\n")
                case:
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")
}

emit_header_func_fwds_prog := proc(f: File, prog: Expr, shallow is_core_pass: Bool) {
    for stmt : Expr in prog.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        if stmt.children.len().eq(0) { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)
            if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                // Forward-declare from ns_decls (user-defined)
                ref fwd_ns : Vec = def_ns_decls(rhs)
                for fdd : Declaration in fwd_ns {
                    if is_null(fdd.default_value) { continue }
                    switch fdd.default_value.node_type {
                    case NodeType.FuncDef(fd):
                        if is_core_pass.and(fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc))) { continue }
                        full_name := dd.name.format("_", fdd.name)
                        emit_func_forward_decl(f, full_name, fdd.default_value, false)
                    case:
                    }
                }
            } else {
                switch rhs.node_type {
                case NodeType.FuncDef(fd):
                    if rhs.children.len().eq(0) { continue }
                    // Issue #176: top-level ext_func / ext_proc are
                    // emitted by emit_ext_func_declarations (with the FFI
                    // const convention). Skip them here regardless of
                    // is_core_pass so til_forward.h / monolithic headers
                    // don't contain a second prototype that disagrees on
                    // const-ness with the canonical one. Namespace
                    // ext_func / ext_proc methods (the branch above) keep
                    // emitting -- no other emit path covers them.
                    if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                    fc := func_to_c(dd.name)
                    emit_func_forward_decl(f, fc, rhs, true)
                case:
                }
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

emit_header_defs_and_funcs := proc(f: File, core_program: Expr, program: Expr) {
    mut emitted_h := Set.new(Str)
    emitted_h.add(own "U8".clone()); emitted_h.add(own "I8".clone()); emitted_h.add(own "I16".clone()); emitted_h.add(own "I32".clone())
    emitted_h.add(own "U32".clone()); emitted_h.add(own "U64".clone()); emitted_h.add(own "I64".clone())
    emitted_h.add(own "F32".clone()); emitted_h.add(own "Bool".clone())
    if is_null(core_program).not() { topo_emit_struct_enum_defs(f, core_program, emitted_h) }
    topo_emit_struct_enum_defs(f, program, emitted_h)
    own emitted_h.delete()

    // Function forward declarations
    if is_null(core_program).not() { emit_header_func_fwds_prog(f, core_program, true) }
    if is_null(program).not()      { emit_header_func_fwds_prog(f, program, false) }

    // Enum auto-helper forward declarations
    for stmt : Expr in program.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        if stmt.children.len().eq(0) { continue }
        if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            // Find eq fdef from ns_decls
            mut eq_fdef := Expr.new(NodeType.Body, stmt.line, stmt.col)
            mut has_eq_fdef := false
            ref eq_ns : Vec = def_ns_decls(stmt.child(0))
            for nsd : Declaration in eq_ns {
                if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                    if nsd.default_value.node_type.is(NodeType.FuncDef) {
                        eq_fdef = nsd.default_value.clone()
                        has_eq_fdef = true
                        break
                    }
                }
            }
            mut eq_ret := "Bool *"
            if has_eq_fdef {
                switch eq_fdef.node_type {
                case NodeType.FuncDef(eqfd):
                    if eqfd.return_own_type.is(OwnType.Shallow) { eq_ret = "Bool" }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
            f.write(eq_ret); f.write(" "); f.write(dd.name); f.write("_eq("); f.write(dd.name); f.write(" *, "); f.write(dd.name); f.write(" *);\n")
            switch stmt.child(0).node_type {
            case NodeType.EnumDef(hdr_edd):
                mut hdr_vi : USize = 0
                for hdr_vn : Str in hdr_edd.variants {
                    hdr_vt := enum_variant_type(stmt.child(0), hdr_vi.to_i32())
                    if hdr_vt.len().gt(0) {
                        tnc := type_name_to_c(hdr_vt)
                        f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("("); f.write(tnc); f.write(");\n")
                    } else {
                        f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("();\n")
                    }
                    hdr_vi = hdr_vi.add(1)
                }
            case:
                UNREACHABLE(__LOC__)
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
    f.write("\n")
}

emit_header_defs_and_funcs_lp := proc(f: File) {
    mut emitted_h := Set.new(Str)
    emitted_h.add(own "U8".clone()); emitted_h.add(own "I8".clone()); emitted_h.add(own "I16".clone()); emitted_h.add(own "I32".clone())
    emitted_h.add(own "U32".clone()); emitted_h.add(own "U64".clone()); emitted_h.add(own "I64".clone())
    emitted_h.add(own "F32".clone()); emitted_h.add(own "Bool".clone())
    topo_emit_struct_enum_defs_lp(f, emitted_h)
    own emitted_h.delete()

    ref lp : LoadedProgram = codegen_lp()

    // Function forward declarations
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                if stmt.children.len().eq(0) { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref rhs : Expr = stmt.child(0)
                    if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                        ref fwd_ns : Vec = def_ns_decls(rhs)
                        for fdd : Declaration in fwd_ns {
                            if is_null(fdd.default_value) { continue }
                            switch fdd.default_value.node_type {
                            case NodeType.FuncDef(fd):
                                if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                                full_name := dd.name.format("_", fdd.name)
                                emit_func_forward_decl(f, full_name, fdd.default_value, false)
                            case:
                            }
                        }
                    } else {
                        switch rhs.node_type {
                        case NodeType.FuncDef(fd):
                            if rhs.children.len().eq(0) { continue }
                            if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                            fc := func_to_c(dd.name)
                            emit_func_forward_decl(f, fc, rhs, true)
                        case:
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                    ref fwd_ns : Vec = def_ns_decls(rhs)
                    for fdd : Declaration in fwd_ns {
                        if is_null(fdd.default_value) { continue }
                        if fdd.default_value.node_type.is(NodeType.FuncDef).not() { continue }
                        full_name := dd.name.format("_", fdd.name)
                        emit_func_forward_decl(f, full_name, fdd.default_value, false)
                    }
                } else if rhs.node_type.is(NodeType.FuncDef) {
                    if rhs.children.len().eq(0) { continue }
                    fc := func_to_c(dd.name)
                    emit_func_forward_decl(f, fc, rhs, true)
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }

    // Enum auto-helper forward declarations (user units only)
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                // Find eq fdef from ns_decls
                mut eq_fdef := Expr.new(NodeType.Body, stmt.line, stmt.col)
                mut has_eq_fdef := false
                ref eq_ns : Vec = def_ns_decls(stmt.child(0))
                for nsd : Declaration in eq_ns {
                    if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                        if nsd.default_value.node_type.is(NodeType.FuncDef) {
                            eq_fdef = nsd.default_value.clone()
                            has_eq_fdef = true
                            break
                        }
                    }
                }
                mut eq_ret := "Bool *"
                if has_eq_fdef {
                    switch eq_fdef.node_type {
                    case NodeType.FuncDef(eqfd):
                        if eqfd.return_own_type.is(OwnType.Shallow) { eq_ret = "Bool" }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                }
                f.write(eq_ret); f.write(" "); f.write(dd.name); f.write("_eq("); f.write(dd.name); f.write(" *, "); f.write(dd.name); f.write(" *);\n")
                switch stmt.child(0).node_type {
                case NodeType.EnumDef(hdr_edd):
                    mut hdr_vi : USize = 0
                    for hdr_vn : Str in hdr_edd.variants {
                        hdr_vt := enum_variant_type(stmt.child(0), hdr_vi.to_i32())
                        if hdr_vt.len().gt(0) {
                            tnc := type_name_to_c(hdr_vt)
                            f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("("); f.write(tnc); f.write(");\n")
                        } else {
                            f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("();\n")
                        }
                        hdr_vi = hdr_vi.add(1)
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")
}

emit_header_global_decls_prog := proc(f: File, prog: Expr) {
    for stmt : Expr in prog.children {
        if is_exported_top_level_global(stmt).not() { continue }
        ref rhs : Expr = stmt.child(0)
        switch stmt.node_type {
        case NodeType.Decl(dd):
            ctype := extern_decl_ctype(stmt.til_type, rhs.til_type)
            if dd.own_type.is(OwnType.Ref) {
                f.write("extern "); f.write(ctype); f.write(" *"); f.write(dd.name); f.write(";\n")
            } else {
                f.write("extern "); f.write(ctype); f.write(" "); f.write(dd.name); f.write(";\n")
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

emit_header_global_decls := proc(f: File, core_program: Expr, program: Expr) {
    if is_null(core_program).not() { emit_header_global_decls_prog(f, core_program) }
    if is_null(program).not()      { emit_header_global_decls_prog(f, program) }
    f.write("\n")
}

emit_header_global_decls_lp := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if is_exported_top_level_global(stmt).not() { continue }
                ref rhs : Expr = stmt.child(0)
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ctype := extern_decl_ctype(stmt.til_type, rhs.til_type)
                    if dd.own_type.is(OwnType.Ref) {
                        f.write("extern "); f.write(ctype); f.write(" *"); f.write(dd.name); f.write(";\n")
                    } else {
                        f.write("extern "); f.write(ctype); f.write(" "); f.write(dd.name); f.write(";\n")
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if is_exported_top_level_global(stmt).not() { continue }
            ref rhs : Expr = stmt.child(0)
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ctype := extern_decl_ctype(stmt.til_type, rhs.til_type)
                if dd.own_type.is(OwnType.Ref) {
                    f.write("extern "); f.write(ctype); f.write(" *"); f.write(dd.name); f.write(";\n")
                } else {
                    f.write("extern "); f.write(ctype); f.write(" "); f.write(dd.name); f.write(";\n")
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")
}

build_forward_header := proc(core_program: Expr, program: Expr, fwd_path: Str) returns shallow I32 {
    build_register_funcsig_names(core_program, program)
    f := File.new(fwd_path, true)
    f.write("#pragma once\n#include \"ext.h\"\n\n")
    emit_header_forward_decls(f, core_program, program)
    emit_funcsig_typedefs(f, core_program, program)
    emit_header_defs_and_funcs(f, core_program, program)
    emit_header_global_decls(f, core_program, program)
    f.close()
    return 0.to_i32()
}

build_forward_header_lp := proc(fwd_path: Str) returns shallow I32 {
    build_register_funcsig_names_lp()
    f := File.new(fwd_path, true)
    f.write("#pragma once\n#include \"ext.h\"\n\n")
    emit_header_forward_decls_lp(f)
    emit_funcsig_typedefs_lp(f)
    emit_header_defs_and_funcs_lp(f)
    emit_header_global_decls_lp(f)
    f.close()
    return 0.to_i32()
}

build_header := proc(core_program: Expr, program: Expr, h_path: Str) returns shallow I32 {
    build_register_funcsig_names(core_program, program)
    f := File.new(h_path, true)
    f.write("#pragma once\n#include \"ext.h\"\n\n")
    emit_header_forward_decls(f, core_program, program)
    emit_funcsig_typedefs(f, core_program, program)
    emit_header_defs_and_funcs(f, core_program, program)
    emit_header_global_decls(f, core_program, program)
    f.write("#include \"ext.h\"\n\n")
    f.close()
    return 0.to_i32()
}

build_header_lp := proc(h_path: Str) returns shallow I32 {
    build_register_funcsig_names_lp()
    f := File.new(h_path, true)
    f.write("#pragma once\n#include \"ext.h\"\n\n")
    emit_header_forward_decls_lp(f)
    emit_funcsig_typedefs_lp(f)
    emit_header_defs_and_funcs_lp(f)
    emit_header_global_decls_lp(f)
    f.write("#include \"ext.h\"\n\n")
    f.close()
    return 0.to_i32()
}

emit_binding_ns_method := proc(f: File, fdd_name: Str, fd: FunctionDef, mut has_ns: Bool) returns shallow Bool {
    if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { return has_ns }
    if has_ns.not() { f.write("    namespace:\n"); has_ns = true }
    mut kw := "ext_func"
    if fd.func_type.is(FuncType.Proc).or(fd.func_type.is(FuncType.Test)) { kw = "ext_proc" }
    f.write("    "); f.write(fdd_name); f.write(" := "); f.write(kw); f.write("(")
    for p in 0..fd.params.len() {
        if p.gt(0) { f.write(", ") }
        ref param : Declaration = fd.params.get(p)
        if param.own_type.is(OwnType.Own) { f.write("own ") }
        f.write(param.name); f.write(": "); f.write(param.explicit_type)
    }
    f.write(")")
    if fd.return_type.len().gt(0) {
        f.write(" returns ")
        if fd.return_own_type.is(OwnType.Ref) { f.write("ref ") }
        if fd.return_own_type.is(OwnType.Shallow) { f.write("shallow ") }
        f.write(fd.return_type)
    }
    f.write(" {}\n")
    return has_ns
}

emit_binding_ns_methods := proc(f: File, def: Expr) {
    mut has_ns := false
    ref ebn_ns : Vec = def_ns_decls(def)
    for nsd : Declaration in ebn_ns {
        if is_null(nsd.default_value) { continue }
        switch nsd.default_value.node_type {
        case NodeType.FuncDef(fd):
            has_ns = emit_binding_ns_method(f, nsd.name, fd, has_ns)
        case:
        }
    }
}

build_til_binding := proc(program: Expr, til_path: Str, lib_name: Str) returns shallow I32 {
    // program is kept for signature compatibility; emit from lp units.
    if is_null(program) { }
    ref lp : LoadedProgram = codegen_lp()
    f := File.new(til_path, true)
    f.write("// Auto-generated FFI binding for "); f.write(lib_name); f.write("\n")
    f.write("link(\""); f.write(lib_name); f.write("\")\n\n")
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)
            switch rhs.node_type {
            case NodeType.StructDef(btb_sdd):
                f.write(dd.name); f.write(" := struct {\n")
                for fdd : Declaration in btb_sdd.fields {
                    f.write("    ")
                    if fdd.is_mut { f.write("mut ") }
                    if fdd.own_type.is(OwnType.Own) { f.write("own ") }
                    f.write(fdd.name)
                    if fdd.explicit_type.len().gt(0) { f.write(" : "); f.write(fdd.explicit_type) }
                    f.write(" := ")
                    if is_null(fdd.default_value).not() { emit_til_default(f, fdd.til_type) }
                    else { f.write("0") }
                    f.write("\n")
                }
                emit_binding_ns_methods(f, rhs)
                f.write("}\n\n")
            case NodeType.EnumDef(bind_edd):
                f.write(dd.name); f.write(" := enum {\n")
                mut bind_vi : USize = 0
                for bind_vn : Str in bind_edd.variants {
                    bind_vt := enum_variant_type(rhs, bind_vi.to_i32())
                    f.write("    "); f.write(bind_vn)
                    if bind_vt.len().gt(0) { f.write(": "); f.write(bind_vt) }
                    f.write(",\n")
                    bind_vi = bind_vi.add(1)
                }
                emit_binding_ns_methods(f, rhs)
                f.write("}\n\n")
            case NodeType.FuncDef(fd):
                if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                if fd.func_type.is(FuncType.Test) { continue }
                mut kw := "ext_func"
                if fd.func_type.is(FuncType.Proc) { kw = "ext_proc" }
                f.write(dd.name); f.write(" := "); f.write(kw); f.write("(")
                for p in 0..fd.params.len() {
                    if p.gt(0) { f.write(", ") }
                    ref param : Declaration = fd.params.get(p)
                    if param.own_type.is(OwnType.Own) { f.write("own ") }
                    pi := p.to_i32()
                    if pi.eq(fd.variadic_index) { f.write("..") }
                    if pi.eq(fd.kwargs_index) {
                        f.write(param.name); f.write(": ...")
                    } else {
                        f.write(param.name); f.write(": "); f.write(param.explicit_type)
                    }
                }
                f.write(")")
                if fd.return_type.len().gt(0) {
                    f.write(" returns ")
                    if fd.return_own_type.is(OwnType.Ref) { f.write("ref ") }
                    if fd.return_own_type.is(OwnType.Shallow) { f.write("shallow ") }
                    f.write(fd.return_type)
                }
                f.write(" {}\n\n")
            case:
            }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.close()
    return 0.to_i32()
}

type_name_to_ctypes := func(name: Str) returns Str {
    if name.eq("I64") { return "ctypes.c_longlong" }
    if name.eq("U8") { return "ctypes.c_ubyte" }
    if name.eq("I8") { return "ctypes.c_byte" }
    if name.eq("I16") { return "ctypes.c_short" }
    if name.eq("I32") { return "ctypes.c_int" }
    if name.eq("U32") { return "ctypes.c_uint" }
    if name.eq("U64") { return "ctypes.c_ulonglong" }
    if name.eq("F32") { return "ctypes.c_float" }
    if name.eq("Bool") { return "ctypes.c_bool" }
    if name.eq("Str") { return "Str" }
    if name.eq("Dynamic") { return "ctypes.c_void_p" }
    return name.clone()
}

type_name_to_ctypes_param := func(name: Str, shallow is_shallow: Bool) returns Str {
    base := type_name_to_ctypes(name)
    if is_shallow { return base }
    return format("ctypes.POINTER(", base, ")")
}

type_name_to_ctypes_return := func(name: Str, shallow is_shallow: Bool) returns Str {
    base := type_name_to_ctypes(name)
    if is_shallow { return base }
    return format("ctypes.POINTER(", base, ")")
}

build_python_binding := proc(program: Expr, py_path: Str, lib_name: Str) returns shallow I32 {
    // program is kept for signature compatibility; emit from lp units.
    if is_null(program) { }
    ref lp : LoadedProgram = codegen_lp()
    f := File.new(py_path, true)
    f.write("# Auto-generated Python binding for "); f.write(lib_name); f.write("\n")
    f.write("import ctypes\n")
    f.write("import os\n\n")
    f.write("_dir = os.path.dirname(os.path.abspath(__file__))\n")
    f.write("_candidates = [\n")
    f.write("    os.path.abspath(os.path.join(_dir, '..', 'lib', 'lib")
    f.write(lib_name); f.write(".so')),\n")
    f.write("    os.path.abspath(os.path.join(_dir, '..', '..', '..', 'lib")
    f.write(lib_name); f.write(".so')),\n")
    f.write("]\n")
    f.write("_lib = None\n")
    f.write("for _path in _candidates:\n")
    f.write("    if os.path.exists(_path):\n")
    f.write("        _lib = ctypes.CDLL(_path)\n")
    f.write("        break\n")
    f.write("if _lib is None:\n")
    f.write("    raise OSError('could not find lib")
    f.write(lib_name); f.write(".so')\n\n")

    f.write("\nclass Str(ctypes.Structure):\n")
    f.write("    _fields_ = [\n")
    f.write("        ('c_str', ctypes.POINTER(ctypes.c_ubyte)),\n")
    f.write("        ('count', ctypes.c_uint),\n")
    f.write("        ('cap', ctypes.c_uint),\n")
    f.write("    ]\n\n")
    f.write("\nclass Array(ctypes.Structure):\n")
    f.write("    _fields_ = [\n")
    f.write("        ('data', ctypes.POINTER(ctypes.c_ubyte)),\n")
    f.write("        ('cap', ctypes.c_uint),\n")
    f.write("        ('elem_size', ctypes.c_uint),\n")
    f.write("        ('elem_type', Str),\n")
    f.write("        ('elem_clone', ctypes.c_void_p),\n")
    f.write("        ('elem_delete', ctypes.c_void_p),\n")
    f.write("    ]\n\n")

    // Forward-declare all struct/enum classes
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                    f.write("\nclass "); f.write(dd.name); f.write("(ctypes.Structure):\n")
                    f.write("    pass\n")
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")

    // Emit struct field definitions and enum definitions
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)

            switch rhs.node_type {
            case NodeType.StructDef(bpb_sdd):
                f.write("\n"); f.write(dd.name); f.write("._fields_ = [\n")
                for fdd : Declaration in bpb_sdd.fields {
                    mut ct := ""
                    if fdd.explicit_type.len().gt(0) {
                        ct = type_name_to_ctypes_param(fdd.explicit_type, false)
                    } else {
                        switch fdd.til_type {
                        case Type.I64:  ct = "ctypes.POINTER(ctypes.c_longlong)"
                        case Type.U8:   ct = "ctypes.POINTER(ctypes.c_ubyte)"
                        case Type.I8:   ct = "ctypes.POINTER(ctypes.c_byte)"
                        case Type.Primitive(Primitive.I16):  ct = "ctypes.POINTER(ctypes.c_short)"
                        case Type.I32:  ct = "ctypes.POINTER(ctypes.c_int)"
                        case Type.U32:  ct = "ctypes.POINTER(ctypes.c_uint)"
                        case Type.U64:  ct = "ctypes.POINTER(ctypes.c_ulonglong)"
                        case Type.F32:  ct = "ctypes.POINTER(ctypes.c_float)"
                        case Type.Bool: ct = "ctypes.POINTER(ctypes.c_bool)"
                        case:              ct = "ctypes.c_void_p"
                        }
                    }
                    f.write("    ('"); f.write(fdd.name); f.write("', ")
                    f.write(ct); f.write("),\n")
                }
                f.write("]\n")

            case NodeType.EnumDef(ct_edd):
                hp := enum_has_payloads(rhs)

                // Emit tag constants
                mut tag := 0
                for ct_vn : Str in ct_edd.variants {
                    f.write(dd.name); f.write("_TAG_"); f.write(ct_vn)
                    f.write(" = "); f.write(tag.to_str()); f.write("\n")
                    tag = tag.add(1)
                }

                // Emit union if has payloads
                if hp {
                    f.write("\nclass "); f.write(dd.name); f.write("_data(ctypes.Union):\n")
                    f.write("    _fields_ = [\n")
                    mut ct_vi : USize = 0
                    for ct_uvn : Str in ct_edd.variants {
                        ct_vt := enum_variant_type(rhs, ct_vi.to_i32())
                        if ct_vt.len().gt(0) {
                            f.write("        ('"); f.write(ct_uvn); f.write("', ")
                            f.write(type_name_to_ctypes_param(ct_vt, false))
                            f.write("),\n")
                        }
                        ct_vi = ct_vi.add(1)
                    }
                    f.write("    ]\n")
                }

                // Emit enum struct _fields_
                f.write("\n"); f.write(dd.name); f.write("._fields_ = [\n")
                f.write("    ('tag', ctypes.c_int),\n")
                if hp {
                    f.write("    ('data', "); f.write(dd.name); f.write("_data),\n")
                }
                f.write("]\n")
            case:
            }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")

    // Emit function bindings
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)

            if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                // Emit namespace method bindings from ns_decls
                ref pyb_ns : Vec = def_ns_decls(rhs)
                for nsd : Declaration in pyb_ns {
                    if is_null(nsd.default_value) { continue }
                    switch nsd.default_value.node_type {
                    case NodeType.FuncDef(fd):
                    if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                    f.write("_lib."); f.write(dd.name); f.write("_"); f.write(nsd.name)
                    f.write(".argtypes = [")
                    for p in 0..fd.params.len() {
                        if p.gt(0) { f.write(", ") }
                        ref param : Declaration = fd.params.get(p)
                        ps := param.own_type.is(OwnType.Shallow)
                        f.write(type_name_to_ctypes_param(param.explicit_type, ps))
                    }
                    f.write("]\n")
                    if fd.return_type.len().gt(0) {
                        rs := fd.return_own_type.is(OwnType.Shallow)
                        f.write("_lib."); f.write(dd.name); f.write("_"); f.write(nsd.name)
                        f.write(".restype = ")
                        f.write(type_name_to_ctypes_return(fd.return_type, rs)); f.write("\n")
                    } else {
                        f.write("_lib."); f.write(dd.name); f.write("_"); f.write(nsd.name)
                        f.write(".restype = None\n")
                    }
                    f.write("\n")
                    case:
                    }
                }

            } else {
                switch rhs.node_type {
                case NodeType.FuncDef(fd):
                    if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                    if fd.func_type.is(FuncType.Test) { continue }

                    c_name := func_to_c(dd.name)

                    // argtypes
                    f.write("_lib."); f.write(c_name); f.write(".argtypes = [")
                    for p in 0..fd.params.len() {
                        if p.gt(0) { f.write(", ") }
                        ref param : Declaration = fd.params.get(p)
                        pi := p.to_i32()
                        if pi.eq(fd.variadic_index) {
                            f.write("ctypes.POINTER(Array)")
                        } else {
                            ps := param.own_type.is(OwnType.Shallow)
                            f.write(type_name_to_ctypes_param(param.explicit_type, ps))
                        }
                    }
                    f.write("]\n")

                    // restype
                    if fd.return_type.len().gt(0) {
                        rs := fd.return_own_type.is(OwnType.Shallow)
                        f.write("_lib."); f.write(c_name); f.write(".restype = ")
                        f.write(type_name_to_ctypes_return(fd.return_type, rs)); f.write("\n")
                    } else {
                        f.write("_lib."); f.write(c_name); f.write(".restype = None\n")
                    }
                    f.write("\n")
                case:
                }
            }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }

    // Emit enum auto-helper bindings (eq, constructors, is_Variant, get_Variant)
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.EnumDef).not() { continue }
                sname := dd.name
                hp := enum_has_payloads(rhs)

                // Find eq fdef to check return shallow
                mut eq_shallow := false
                ref py_eq_ns : Vec = def_ns_decls(rhs)
                for nsd : Declaration in py_eq_ns {
                    if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                        switch nsd.default_value.node_type {
                        case NodeType.FuncDef(eqfd):
                            eq_shallow = eqfd.return_own_type.is(OwnType.Shallow)
                        case:
                        }
                        break
                    }
                }
                mut eq_ret := "ctypes.POINTER(ctypes.c_bool)"
                if eq_shallow { eq_ret = "ctypes.c_bool" }

                // eq
                f.write("_lib."); f.write(sname); f.write("_eq.argtypes = [ctypes.POINTER(")
                f.write(sname); f.write("), ctypes.POINTER("); f.write(sname); f.write(")]\n")
                f.write("_lib."); f.write(sname); f.write("_eq.restype = ")
                f.write(eq_ret); f.write("\n\n")

                // Per-variant helpers
                switch rhs.node_type {
                case NodeType.EnumDef(py_edd):
                    mut py_vi : USize = 0
                    for py_vn : Str in py_edd.variants {
                        py_vt := enum_variant_type(rhs, py_vi.to_i32())

                        if hp {
                            // is_Variant
                            f.write("_lib."); f.write(sname); f.write("_is_"); f.write(py_vn)
                            f.write(".argtypes = [ctypes.POINTER("); f.write(sname); f.write(")]\n")
                            f.write("_lib."); f.write(sname); f.write("_is_"); f.write(py_vn)
                            f.write(".restype = "); f.write(eq_ret); f.write("\n\n")
                        }

                        if py_vt.len().gt(0) {
                            // Constructor: takes payload, returns POINTER(Enum)
                            f.write("_lib."); f.write(sname); f.write("_"); f.write(py_vn)
                            f.write(".argtypes = [")
                            f.write(type_name_to_ctypes_param(py_vt, false)); f.write("]\n")
                            f.write("_lib."); f.write(sname); f.write("_"); f.write(py_vn)
                            f.write(".restype = ctypes.POINTER("); f.write(sname); f.write(")\n\n")
                            // get_Variant
                            f.write("_lib."); f.write(sname); f.write("_get_"); f.write(py_vn)
                            f.write(".argtypes = [ctypes.POINTER("); f.write(sname); f.write(")]\n")
                            f.write("_lib."); f.write(sname); f.write("_get_"); f.write(py_vn)
                            f.write(".restype = ")
                            f.write(type_name_to_ctypes_param(py_vt, false)); f.write("\n\n")
                        } else {
                            // Constructor: no args, returns POINTER(Enum)
                            f.write("_lib."); f.write(sname); f.write("_"); f.write(py_vn)
                            f.write(".argtypes = []\n")
                            f.write("_lib."); f.write(sname); f.write("_"); f.write(py_vn)
                            f.write(".restype = ctypes.POINTER("); f.write(sname); f.write(")\n\n")
                        }
                        py_vi = py_vi.add(1)
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }

    f.close()
    return 0.to_i32()
}

compile_lib := proc(c_path: Str, lib_name: Str, ext_c_path: Str, user_c_path: Str, link_flags: Str, include_flags: Str, target: Target, cc_override: Str, asan: Bool) returns shallow I32 {
    mut uc := user_c_path
    mut lf := link_flags
    mut ifl := include_flags
    if uc.len().eq(0) { uc = "" }
    if lf.len().eq(0) { lf = "" }
    if ifl.len().eq(0) { ifl = "" }

    mut compiler := cc_override
    if compiler.len().eq(0) { compiler = toolchain_command(target, Lang.C) }
    extra := toolchain_extra_args(target, compiler)

    // ASAN: swap -Werror for -Wno-all and add the sanitizer flags. The
    // generated C is meant to compile clean under -Werror, but the asan
    // pass occasionally surfaces additional warnings; matching the
    // historical Makefile recipe avoids spurious failures.
    mut werr := " -Werror"
    mut asan_flags := ""
    if asan {
        werr = " -Wno-all"
        asan_flags = " -fsanitize=address -fno-omit-frame-pointer"
    }

    // Extract directory from ext_c_path for -I flag
    mut ext_dir := "."
    slash := ext_c_path.rfind("/")
    if slash.gte(0) { ext_dir = ext_c_path.substr(0, slash.to_usize()).clone() }

    // Compile library .c to object
    obj_path := format("gen/lib/", lib_name, ".o")
    mut cmd := compiler.format(extra, werr, asan_flags, " -fPIC -I", ext_dir,
        " -c ", c_path, " -o ", obj_path)
    mut result := system_cmd(cmd)
    if result.neq(0) {
        println("error: library compilation failed")
        return 1.to_i32()
    }

    // Compile ext.c to object
    ext_obj := "gen/lib/ext.o"
    cmd = compiler.format(extra, werr, asan_flags, " -fPIC -I", ext_dir,
        " -c ", ext_c_path, " -o ", ext_obj)
    result = system_cmd(cmd)
    if result.neq(0) {
        println("error: ext.c compilation failed")
        return 1.to_i32()
    }

    // Compile user .c to object if present
    mut user_obj := ""
    if uc.len().gt(0) {
        user_obj = "gen/lib/user.o"
        cmd = compiler.format(extra, werr, asan_flags, " -fPIC -I", ext_dir, ifl,
            " -c ", uc, " -o ", user_obj)
        result = system_cmd(cmd)
        if result.neq(0) {
            println("error: user .c compilation failed")
            return 1.to_i32()
        }
    }

    // Create shared library
    so_path := format("gen/lib/lib", lib_name, ".so")
    cmd = compiler.format(asan_flags, " -shared -o ", so_path,
        " ", obj_path, " ", ext_obj,
        " ", user_obj, lf)
    result = system_cmd(cmd)
    if result.neq(0) {
        println("error: shared library creation failed")
        return 1.to_i32()
    }

    // Create static library
    a_path := format("gen/lib/lib", lib_name, ".a")
    cmd = format("ar rcs ", a_path,
        " ", obj_path, " ", ext_obj,
        " ", user_obj)
    result = system_cmd(cmd)
    if result.neq(0) {
        println("error: static library creation failed")
        return 1.to_i32()
    }

    return 0.to_i32()
}

compile_c := proc(c_path: Str, bin_path: Str, ext_c_path: Str, user_c_path: Str, link_flags: Str, include_flags: Str, target: Target, cc_override: Str, mode_auto_import: Str, asan: Bool) returns shallow I32 {
    mut uc := user_c_path
    mut lf := link_flags
    mut ifl := include_flags
    if uc.len().eq(0) { uc = "" }
    if lf.len().eq(0) { lf = "" }
    if ifl.len().eq(0) { ifl = "" }

    mut compiler := cc_override
    if compiler.len().eq(0) { compiler = toolchain_command(target, Lang.C) }
    extra := toolchain_extra_args(target, compiler)

    // ASAN: swap -Werror for -Wno-all and add the sanitizer flags. Single
    // cc invocation here, so the flags work for compile and link in one go.
    mut werr := " -Werror"
    mut asan_flags := ""
    if asan {
        werr = " -Wno-all"
        asan_flags = " -fsanitize=address -fno-omit-frame-pointer"
    }

    // Extract directory from ext_c_path for -I flag
    mut ext_dir := "."
    slash := ext_c_path.rfind("/")
    if slash.gte(0) { ext_dir = ext_c_path.substr(0, slash.to_usize()).clone() }

    // If link_c files present, compile each to .o separately with -include forward.h
    mut user_part := ""
    if uc.len().gt(0) {
        c_slash := c_path.rfind("/")
        mut c_dir := "."
        if c_slash.gte(0) { c_dir = c_path.substr(0, c_slash.to_usize()).clone() }
        c_dot := c_path.rfind(".")
        mut c_base := c_path
        if c_dot.gt(c_slash) { c_base = c_path.substr(0, c_dot.to_usize()) }
        fwd_path := c_base.format("_forward.h")

        // Split space-separated link_c files and compile each individually
        files := uc.split(" ")
        mut all_objs := ""
        for fi in 0..files.len() {
            ref file : Str = files.get(fi)
            if file.len().eq(0) { continue }
            user_obj := c_dir.format("/user_", fi.to_str(), ".o")

            // Skip -include forward.h for src/c/ files
            mut fwd_flag := format(" -include ", fwd_path)
            if file.starts_with("src/c/") { fwd_flag = " -Isrc/c -Iboot" }
            obj_cmd := compiler.format(extra, werr, asan_flags, " -I", ext_dir,
                fwd_flag, ifl, " -c ", file,
                " -o ", user_obj)

            obj_result := system_cmd(obj_cmd)
            if obj_result.neq(0) {
                println(format("error: link_c compilation failed for ", file))
                return 1.to_i32()
            }
            all_objs = all_objs.format(" ", user_obj)
        }
        user_part = all_objs
    }

    // -rdynamic is a Linux/ELF gcc driver flag for exporting symbols to dlsym.
    // mingw silently ignores it but clangs error out, so only add for non-Windows.
    mut dyn_flag := " -rdynamic"
    if target.is(Target.WindowsX64) { dyn_flag = "" }

    // For Windows cross-compile, rewrite link_flags inherited from
    // src/lib/{raylib,libffi,tinyfd}.til. Their link() directives carry
    // hardcoded Linux paths and POSIX-only -l flags; mingw doesn't ship
    // -ldl/-lrt and can't load Linux ELF .a files. The Windows variants
    // are added via target_gui_libs / target_ffi_lib / target_system_libs
    // below, so it's safe to strip the Linux ones here.
    if target.is(Target.WindowsX64) {
        lf = lf.replace(" -ldl", "")
        lf = lf.replace(" -lrt", "")
        lf = lf.replace(" vendor/raylib/src/libraylib.a", "")
        lf = lf.replace(" vendor/tinyfiledialogs/libtinyfd.a", "")
        lf = lf.replace(" vendor/libffi/x86_64-pc-linux-gnu/.libs/libffi.a", "")
    }

    // Per-target system + ffi + (gui) link flags. Tables in
    // src/self/targets.til. mode_auto_import is "gui" iff the program
    // declared mode gui.
    sys_libs := target_system_libs(target)
    ffi_lib  := target_ffi_lib(target)
    mut gui_part := ""
    if mode_auto_import.eq("gui") { gui_part = format(" ", target_gui_libs(target)) }

    cmd := compiler.format(extra, werr, asan_flags, " -g", dyn_flag, " -I", ext_dir,
        " -o ", bin_path, " ", c_path,
        " ", ext_c_path, user_part, lf,
        " ", ffi_lib, gui_part, " ", sys_libs)

    result := system_cmd(cmd)
    if result.neq(0) {
        println("error: C compilation failed")
        return 1.to_i32()
    }
    return 0.to_i32()
}

register_funcsig_prog := func(prog: Expr) {
    ref mut lp : LoadedProgram = codegen_lp()
    for stmt : Expr in prog.children {
        if stmt.node_type.is(NodeType.Decl) {
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.FuncDef) {
                    if rhs.children.len().eq(0) {
                        lp.ctx.funcsig_names.add(own dd.name.clone())
                    }
                }
                if stmt.til_type.is(Type.None) {
                    switch rhs.node_type {
                    case NodeType.Ident(rhs_name):
                        if lp.ctx.funcsig_names.has(rhs_name) {
                            lp.ctx.funcsig_names.add(own dd.name.clone())
                        }
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
}

emit_global_inits_prog := proc(f: File, prog: Expr) {
    for gs : Expr in prog.children {
        if gs.node_type.is(NodeType.Decl) {
            ref rhs : Expr = gs.child(0)
            if rhs.node_type.is(NodeType.FuncDef).or(rhs.node_type.is(NodeType.StructDef))
            .or(rhs.node_type.is(NodeType.EnumDef)) { continue }
            if gs.til_type.is(Type.None) {
                if rhs.node_type.is(NodeType.Ident) { continue }
            }
        } else {
            if gs.node_type.is(NodeType.FCall).not() { continue }
        }
        emit_stmt(f, gs, 1)
    }
}

emit_global_inits := proc(f: File) {
    if codegen_lp().ctx.script_globals.len().eq(0) { return }
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            emit_global_inits_prog(f, iu.ast)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        emit_global_inits_prog(f, iu.ast)
    }
}

build_register_funcsig_names := func(core_program: Expr, program: Expr) {
    ref mut lp : LoadedProgram = codegen_lp()
    lp.ctx.funcsig_names = Set.new(Str)
    if is_null(core_program).not() { register_funcsig_prog(core_program) }
    if is_null(program).not()      { register_funcsig_prog(program) }
}

build_register_funcsig_names_lp := func() {
    ref mut lp : LoadedProgram = codegen_lp()
    lp.ctx.funcsig_names = Set.new(Str)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            register_funcsig_prog(iu.ast)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        register_funcsig_prog(iu.ast)
    }
}

register_lookups_prog := func(prog: Expr) {
    ref mut lp : LoadedProgram = codegen_lp()
    for stmt : Expr in prog.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)
            if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                _sdv : Dynamic = malloc(I64.size())
                write_ptr(_sdv, rhs)
                lp.ctx.struct_def_exprs.set(own dd.name.clone(), own _sdv)
                // Register ns_decls methods into func_defs
                ref rl_ns : Vec = def_ns_decls(rhs)
                for d_j : Declaration in rl_ns {
                    ref nsd : Declaration = d_j
                    if is_null(nsd.default_value) { continue }
                    if nsd.default_value.node_type.is(NodeType.FuncDef).not() { continue }
                    key := dd.name.format("_", nsd.name)
                    if lp.ctx.func_defs.has(key) { continue }
                    fv : Dynamic = malloc(I64.size())
                    write_ptr(fv, nsd.default_value)
                    lp.ctx.func_defs.set(own key, own fv)
                    if nsd.default_value.children.len().gt(0) {
                        ref fbody : Expr = nsd.default_value.child(0)
                        for nested : Expr in fbody.children {
                            if is_func_decl(nested).not() { continue }
                            ref nfdef : Expr = nested.child(0)
                            switch nested.node_type {
                            case NodeType.Decl(nd):
                                nv : Dynamic = malloc(I64.size())
                                write_ptr(nv, nfdef)
                                lp.ctx.func_defs.set(own nd.name.clone(), own nv)
                            case:
                                UNREACHABLE(__LOC__)
                            }
                        }
                    }
                }
            }
            if rhs.node_type.is(NodeType.FuncDef) {
                fv2 : Dynamic = malloc(I64.size())
                write_ptr(fv2, rhs)
                lp.ctx.func_defs.set(own dd.name.clone(), own fv2)
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

// #135: can't use for prog_idx in 0..2 with ref mut prog here because
// the clone bug makes stored pointers point to cloned nodes
build_register_lookups := func(core_program: Expr, program: Expr) {
    ref mut lp : LoadedProgram = codegen_lp()
    lp.ctx.struct_def_exprs = Map.new(Str, Dynamic)
    lp.ctx.func_defs = Map.new(Str, Dynamic)
    lp.ctx.stack_locals = Set.new(Str)
    lp.ctx.stack_local_types = Map.new(Str, Str)
    lp.ctx.unsafe_to_hoist = Set.new(Str)
    lp.ctx.ref_locals = Set.new(Str)
    lp.ctx.ref_dyn_locals = Set.new(Str)
    if is_null(core_program).not() { register_lookups_prog(core_program) }
    register_lookups_prog(program)
}

build_register_lookups_lp := func() {
    ref mut lp : LoadedProgram = codegen_lp()
    lp.ctx.struct_def_exprs = Map.new(Str, Dynamic)
    lp.ctx.func_defs = Map.new(Str, Dynamic)
    lp.ctx.stack_locals = Set.new(Str)
    lp.ctx.stack_local_types = Map.new(Str, Str)
    lp.ctx.unsafe_to_hoist = Set.new(Str)
    lp.ctx.ref_locals = Set.new(Str)
    lp.ctx.ref_dyn_locals = Set.new(Str)

    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            register_lookups_prog(iu.ast)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        register_lookups_prog(iu.ast)
    }
}

emit_monolithic_header_func_decls_prog := proc(f: File, prog: Expr, mode: Mode) {
    for stmt : Expr in prog.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        if stmt.children.len().eq(0) { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            ref rhs : Expr = stmt.child(0)
            if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                ref fwd_ns2 : Vec = def_ns_decls(rhs)
                for j in 0..fwd_ns2.len() {
                    ref fdd : Declaration = fwd_ns2.get(j)
                    if is_null(fdd.default_value) { continue }
                    switch fdd.default_value.node_type {
                    case NodeType.FuncDef(fd):
                        if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                        full_name := dd.name.format("_", fdd.name)
                        emit_func_forward_decl(f, full_name, fdd.default_value, false)
                    case:
                    }
                }
            } else {
                switch rhs.node_type {
                case NodeType.FuncDef(fd):
                    if rhs.children.len().eq(0) { continue }
                    if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                    if is_null(mode).not() {
                        if mode.needs_main {
                            if dd.name.eq("main") { continue }
                        }
                    }
                    fc := func_to_c(dd.name)
                    emit_func_forward_decl(f, fc, rhs, true)
                case:
                }
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

emit_monolithic_header := proc(f: File, core_program: Expr, program: Expr, mode: Mode) {
    f.write("#include \"ext.h\"\n\n")
    emit_header_forward_decls(f, core_program, program)
    emit_funcsig_typedefs(f, core_program, program)

    // Topo-sort struct/enum defs
    mut emitted_mh := Set.new(Str)
    emitted_mh.add(own "U8".clone()); emitted_mh.add(own "I8".clone()); emitted_mh.add(own "I16".clone()); emitted_mh.add(own "I32".clone())
    emitted_mh.add(own "U32".clone()); emitted_mh.add(own "U64".clone()); emitted_mh.add(own "I64".clone())
    emitted_mh.add(own "F32".clone()); emitted_mh.add(own "Bool".clone())
    if is_null(core_program).not() { topo_emit_struct_enum_defs(f, core_program, emitted_mh) }
    topo_emit_struct_enum_defs(f, program, emitted_mh)
    own emitted_mh.delete()

    // Emit function declarations
    if is_null(core_program).not() { emit_monolithic_header_func_decls_prog(f, core_program, mode) }
    if is_null(program).not()      { emit_monolithic_header_func_decls_prog(f, program, mode) }
    // Enum auto-helper declarations (user program only)
    for stmt : Expr in program.children {
        if stmt.node_type.is(NodeType.Decl).not() { continue }
        if stmt.children.len().eq(0) { continue }
        if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
        switch stmt.node_type {
        case NodeType.Decl(dd):
            mut eq_fdef := Expr.new(NodeType.Body, stmt.line, stmt.col)
            mut has_eq_fdef := false
            ref sh3_eq_ns : Vec = def_ns_decls(stmt.child(0))
            for d_j : Declaration in sh3_eq_ns {
                ref nsd : Declaration = d_j
                if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                    if nsd.default_value.node_type.is(NodeType.FuncDef) {
                        eq_fdef = nsd.default_value.clone()
                        has_eq_fdef = true
                        break
                    }
                }
            }
            mut eq_ret := "Bool *"
            if has_eq_fdef {
                switch eq_fdef.node_type {
                case NodeType.FuncDef(eqfd):
                    if eqfd.return_own_type.is(OwnType.Shallow) { eq_ret = "Bool" }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
            f.write(eq_ret); f.write(" "); f.write(dd.name); f.write("_eq("); f.write(dd.name); f.write(" *, "); f.write(dd.name); f.write(" *);\n")
            switch stmt.child(0).node_type {
            case NodeType.EnumDef(hdr_edd):
                mut hdr_vi : USize = 0
                for hdr_vn : Str in hdr_edd.variants {
                    hdr_vt := enum_variant_type(stmt.child(0), hdr_vi.to_i32())
                    if hdr_vt.len().gt(0) {
                        tnc := type_name_to_c(hdr_vt)
                        f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("("); f.write(tnc); f.write(");\n")
                    } else {
                        f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("();\n")
                    }
                    hdr_vi = hdr_vi.add(1)
                }
            case:
                UNREACHABLE(__LOC__)
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
}

emit_monolithic_header_lp := proc(f: File, mode: Mode) {
    f.write("#include \"ext.h\"\n\n")
    emit_header_forward_decls_lp(f)
    emit_funcsig_typedefs_lp(f)

    // Topo-sort struct/enum defs across all units
    mut emitted_mh := Set.new(Str)
    emitted_mh.add(own "U8".clone()); emitted_mh.add(own "I8".clone()); emitted_mh.add(own "I16".clone()); emitted_mh.add(own "I32".clone())
    emitted_mh.add(own "U32".clone()); emitted_mh.add(own "U64".clone()); emitted_mh.add(own "I64".clone())
    emitted_mh.add(own "F32".clone()); emitted_mh.add(own "Bool".clone())
    topo_emit_struct_enum_defs_lp(f, emitted_mh)
    own emitted_mh.delete()

    ref lp : LoadedProgram = codegen_lp()

    // Emit function declarations
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                if stmt.children.len().eq(0) { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref rhs : Expr = stmt.child(0)
                    if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                        ref fwd_ns2 : Vec = def_ns_decls(rhs)
                        for fdd : Declaration in fwd_ns2 {
                            if is_null(fdd.default_value) { continue }
                            switch fdd.default_value.node_type {
                            case NodeType.FuncDef(fd):
                                if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                                full_name := dd.name.format("_", fdd.name)
                                emit_func_forward_decl(f, full_name, fdd.default_value, false)
                            case:
                            }
                        }
                    } else {
                        switch rhs.node_type {
                        case NodeType.FuncDef(fd):
                            if rhs.children.len().eq(0) { continue }
                            if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                            if is_null(mode).not() {
                                if mode.needs_main {
                                    if dd.name.eq("main") { continue }
                                }
                            }
                            fc := func_to_c(dd.name)
                            emit_func_forward_decl(f, fc, rhs, true)
                        case:
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                    ref fwd_ns2 : Vec = def_ns_decls(rhs)
                    for fdd : Declaration in fwd_ns2 {
                        if is_null(fdd.default_value) { continue }
                        switch fdd.default_value.node_type {
                        case NodeType.FuncDef(fd):
                            if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                            full_name := dd.name.format("_", fdd.name)
                            emit_func_forward_decl(f, full_name, fdd.default_value, false)
                        case:
                        }
                    }
                } else {
                    switch rhs.node_type {
                    case NodeType.FuncDef(fd):
                        if rhs.children.len().eq(0) { continue }
                        if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                        if is_null(mode).not() {
                            if mode.needs_main {
                                if dd.name.eq("main") { continue }
                            }
                        }
                        fc := func_to_c(dd.name)
                        emit_func_forward_decl(f, fc, rhs, true)
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }

    // Enum auto-helper declarations (user units only)
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                mut eq_fdef := Expr.new(NodeType.Body, stmt.line, stmt.col)
                mut has_eq_fdef := false
                ref sh3_eq_ns : Vec = def_ns_decls(stmt.child(0))
                for nsd : Declaration in sh3_eq_ns {
                    if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                        if nsd.default_value.node_type.is(NodeType.FuncDef) {
                            eq_fdef = nsd.default_value.clone()
                            has_eq_fdef = true
                            break
                        }
                    }
                }
                mut eq_ret := "Bool *"
                if has_eq_fdef {
                    switch eq_fdef.node_type {
                    case NodeType.FuncDef(eqfd):
                        if eqfd.return_own_type.is(OwnType.Shallow) { eq_ret = "Bool" }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                }
                f.write(eq_ret); f.write(" "); f.write(dd.name); f.write("_eq("); f.write(dd.name); f.write(" *, "); f.write(dd.name); f.write(" *);\n")
                switch stmt.child(0).node_type {
                case NodeType.EnumDef(hdr_edd):
                    mut hdr_vi : USize = 0
                    for hdr_vn : Str in hdr_edd.variants {
                        hdr_vt := enum_variant_type(stmt.child(0), hdr_vi.to_i32())
                        if hdr_vt.len().gt(0) {
                            tnc := type_name_to_c(hdr_vt)
                            f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("("); f.write(tnc); f.write(");\n")
                        } else {
                            f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(hdr_vn); f.write("();\n")
                        }
                        hdr_vi = hdr_vi.add(1)
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
}

emit_all_forward_declarations := proc(f: File, mode: Mode) {
    ref lp : LoadedProgram = codegen_lp()
    // Forward-declare struct/enum namespace methods and top-level functions
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                if stmt.children.len().eq(0) { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref rhs : Expr = stmt.child(0)
                    if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                        ref afd_ns : Vec = def_ns_decls(rhs)
                        for fdd : Declaration in afd_ns {
                            if is_null(fdd.default_value) { continue }
                            switch fdd.default_value.node_type {
                            case NodeType.FuncDef(fd):
                                if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                                full_name := dd.name.format("_", fdd.name)
                                emit_func_forward_decl(f, full_name, fdd.default_value, false)
                            case:
                            }
                        }
                    } else {
                        switch rhs.node_type {
                        case NodeType.FuncDef(fd):
                            if rhs.children.len().eq(0) { continue }
                            if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                            if is_null(mode).not() {
                                if mode.needs_main {
                                    if dd.name.eq("main") { continue }
                                }
                            }
                            fc := func_to_c(dd.name)
                            emit_func_forward_decl(f, fc, rhs, true)
                        case:
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                    ref afd_ns : Vec = def_ns_decls(rhs)
                    for fdd : Declaration in afd_ns {
                        if is_null(fdd.default_value) { continue }
                        if fdd.default_value.node_type.is(NodeType.FuncDef).not() { continue }
                        full_name := dd.name.format("_", fdd.name)
                        emit_func_forward_decl(f, full_name, fdd.default_value, false)
                    }
                } else {
                    switch rhs.node_type {
                    case NodeType.FuncDef(fd):
                        if rhs.children.len().eq(0) { continue }
                        if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                        if is_null(mode).not() {
                            if mode.needs_main {
                                if dd.name.eq("main") { continue }
                            }
                        }
                        fc := func_to_c(dd.name)
                        emit_func_forward_decl(f, fc, rhs, true)
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }

    // Forward-declare enum ext methods (eq, constructors)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for i in 0..iu.ast.children.len() {
                ref stmt : Expr = iu.ast.child(i)
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                if stmt.children.len().eq(0) { continue }
                if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    mut eq_fdef := Expr.new(NodeType.Body, stmt.line, stmt.col)
                    mut has_eq_fdef := false
                    ref sh_eq_ns : Vec = def_ns_decls(stmt.child(0))
                    for nsd : Declaration in sh_eq_ns {
                        if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                            if nsd.default_value.node_type.is(NodeType.FuncDef) {
                                eq_fdef = nsd.default_value.clone()
                                has_eq_fdef = true
                                break
                            }
                        }
                    }
                    mut eq_ret := "Bool *"
                    if has_eq_fdef {
                        switch eq_fdef.node_type {
                        case NodeType.FuncDef(eqfd):
                            if eqfd.return_own_type.is(OwnType.Shallow) { eq_ret = "Bool" }
                        case:
                            UNREACHABLE(__LOC__)
                        }
                    }
                    f.write(eq_ret); f.write(" "); f.write(dd.name); f.write("_eq("); f.write(dd.name); f.write(" *, "); f.write(dd.name); f.write(" *);\n")
                    switch stmt.child(0).node_type {
                    case NodeType.EnumDef(shdr_edd):
                        mut shdr_vi : USize = 0
                        for shdr_vn : Str in shdr_edd.variants {
                            shdr_vt := enum_variant_type(stmt.child(0), shdr_vi.to_i32())
                            if shdr_vt.len().gt(0) {
                                tnc := type_name_to_c(shdr_vt)
                                f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(shdr_vn); f.write("("); f.write(tnc); f.write(");\n")
                            } else {
                                f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(shdr_vn); f.write("();\n")
                            }
                            shdr_vi = shdr_vi.add(1)
                        }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for i in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(i)
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
            mut eq_fdef := Expr.new(NodeType.Body, stmt.line, stmt.col)
            mut has_eq_fdef := false
            ref sh_eq_ns : Vec = def_ns_decls(stmt.child(0))
            for nsd : Declaration in sh_eq_ns {
                if nsd.name.eq("eq").and(is_null(nsd.default_value).not()) {
                    if nsd.default_value.node_type.is(NodeType.FuncDef) {
                        eq_fdef = nsd.default_value.clone()
                        has_eq_fdef = true
                        break
                    }
                }
            }
            mut eq_ret := "Bool *"
            if has_eq_fdef {
                switch eq_fdef.node_type {
                case NodeType.FuncDef(eqfd):
                    if eqfd.return_own_type.is(OwnType.Shallow) { eq_ret = "Bool" }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
            f.write(eq_ret); f.write(" "); f.write(dd.name); f.write("_eq("); f.write(dd.name); f.write(" *, "); f.write(dd.name); f.write(" *);\n")
            switch stmt.child(0).node_type {
            case NodeType.EnumDef(shdr_edd):
                mut shdr_vi : USize = 0
                for shdr_vn : Str in shdr_edd.variants {
                    shdr_vt := enum_variant_type(stmt.child(0), shdr_vi.to_i32())
                    if shdr_vt.len().gt(0) {
                        tnc := type_name_to_c(shdr_vt)
                        f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(shdr_vn); f.write("("); f.write(tnc); f.write(");\n")
                    } else {
                        f.write(dd.name); f.write(" *"); f.write(dd.name); f.write("_"); f.write(shdr_vn); f.write("();\n")
                    }
                    shdr_vi = shdr_vi.add(1)
                }
            case:
                UNREACHABLE(__LOC__)
            }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")

    // Forward declarations for dyn_call dispatch functions
    mut dyn_methods := Vec.new(DynCallInfo)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            collect_dyn_methods(iu.ast, dyn_methods)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_dyn_methods(iu.ast, dyn_methods)
    }
    for info : DynCallInfo in dyn_methods {
        if info.has_return {
            if info.nargs.eq(1) {
                f.write("void *dyn_call_"); f.write(info.method); f.write("(Str *type_name, void *val);\n")
            } else {
                f.write("void *dyn_call_"); f.write(info.method); f.write("(Str *type_name, void *val, void *arg2);\n")
            }
        } else {
            if info.nargs.eq(1) {
                f.write("void dyn_call_"); f.write(info.method); f.write("(Str *type_name, void *val);\n")
            } else {
                f.write("void dyn_call_"); f.write(info.method); f.write("(Str *type_name, void *val, void *arg2);\n")
            }
        }
    }
    mut has_dyn_type_to_str := false
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            if has_dyn_type_to_str { break }
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_func_decl(stmt).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(sdd):
                    if sdd.name.eq("dyn_type_to_str") {
                        has_dyn_type_to_str = true
                        break
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        if has_dyn_type_to_str { break }
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_func_decl(stmt).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(sdd):
                if sdd.name.eq("dyn_type_to_str") {
                    has_dyn_type_to_str = true
                    break
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("void *dyn_fn(Str *type_name, Str *method);\n")
    f.write("U32 dyn_size_of(Str *type_name);\n")
    if has_dyn_type_to_str {
        f.write("Str *dyn_type_to_str(Type *type);\n")
    }
    f.write("\n")
    own dyn_methods.delete()

    // Forward declarations for dyn_has_method dispatch functions
    mut dyn_has_shallow := false
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            if dyn_has_shallow { break }
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_func_decl(stmt).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(sdd):
                    if sdd.name.eq("dyn_has_method") {
                        switch stmt.child(0).node_type {
                        case NodeType.FuncDef(fd):
                            dyn_has_shallow = fd.return_own_type.is(OwnType.Shallow)
                        case:
                            UNREACHABLE(__LOC__)
                        }
                        break
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        if dyn_has_shallow { break }
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_func_decl(stmt).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(sdd):
                if sdd.name.eq("dyn_has_method") {
                    switch stmt.child(0).node_type {
                    case NodeType.FuncDef(fd):
                        dyn_has_shallow = fd.return_own_type.is(OwnType.Shallow)
                    case:
                        UNREACHABLE(__LOC__)
                    }
                    break
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    mut dyn_has_ret := "Bool *"
    if dyn_has_shallow { dyn_has_ret = "Bool" }
    mut has_methods := Vec.new(Str)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            collect_dyn_has_methods(iu.ast, has_methods)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_dyn_has_methods(iu.ast, has_methods)
    }
    for method : Str in has_methods {
        f.write(dyn_has_ret); f.write(" dyn_has_"); f.write(method); f.write("(Str *type_name);\n")
    }
    if has_methods.len().gt(0) { f.write("\n") }
    own has_methods.delete()

    // Forward declarations for array/vec builtin helpers
    mut coll_infos := Vec.new(CollectionInfo)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            collect_collection_builtins(iu.ast, coll_infos)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_collection_builtins(iu.ast, coll_infos)
    }
    for ci : CollectionInfo in coll_infos {
        mut prefix := "array"
        mut ret := "Array"
        if ci.is_vec.neq(0) { prefix = "vec"; ret = "Vec" }
        f.write(ret); f.write(" *"); f.write(prefix); f.write("_of_"); f.write(ci.type_name); f.write("(int count, ...);\n")
    }
    if coll_infos.len().gt(0) { f.write("\n") }
    own coll_infos.delete()
}

emit_dyn_fn_wrapper := proc(f: File, type_name: Str, method_name: Str, fd: FunctionDef) {
    np := fd.params.len()
    ret_shallow := fd.return_own_type.is(OwnType.Shallow)
    has_return := fd.return_type.len().gt(0)
    mut any_shallow := ret_shallow
    for p in 0..np {
        ref pp : Declaration = fd.params.get(p)
        if pp.own_type.is(OwnType.Shallow) { any_shallow = true }
    }
    if any_shallow.not() { return }
    if has_return {
        f.write("void *"); f.write(type_name); f.write("_"); f.write(method_name); f.write("_dyn(")
    } else {
        f.write("void "); f.write(type_name); f.write("_"); f.write(method_name); f.write("_dyn(")
    }
    for p in 0..np {
        if p.gt(0) { f.write(", ") }
        f.write("void *_a"); emit_i32(f, p.to_i32())
    }
    if np.eq(0) { f.write("void") }
    f.write(") {\n")
    if has_return {
        if ret_shallow {
            rc := type_name_to_c_value(fd.return_type)
            f.write("    "); f.write(rc); f.write(" *_r = malloc(sizeof("); f.write(rc); f.write(")); *_r = "); f.write(type_name); f.write("_"); f.write(method_name); f.write("(")
        } else {
            f.write("    return (void *)"); f.write(type_name); f.write("_"); f.write(method_name); f.write("(")
        }
    } else {
        f.write("    "); f.write(type_name); f.write("_"); f.write(method_name); f.write("(")
    }
    for p in 0..np {
        if p.gt(0) { f.write(", ") }
        ref pp : Declaration = fd.params.get(p)
        if pp.own_type.is(OwnType.Shallow) {
            pc := type_name_to_c_value(pp.explicit_type)
            f.write("*("); f.write(pc); f.write(" *)_a"); emit_i32(f, p.to_i32())
        } else {
            f.write("_a"); emit_i32(f, p.to_i32())
        }
    }
    f.write(");")
    if has_return {
        if ret_shallow { f.write(" return _r;") }
    }
    f.write("\n}\n")
}

emit_dyn_fn_wrappers := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    // Emit wrapper functions that normalize calling convention
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_struct_or_enum(stmt).not() { continue }
                ref def : Expr = stmt.child(0)
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref dfw_ns : Vec = def_ns_decls(def)
                    for d_j : Declaration in dfw_ns {
                        ref nsd : Declaration = d_j
                        if is_null(nsd.default_value) { continue }
                        switch nsd.default_value.node_type {
                        case NodeType.FuncDef(fd):
                            emit_dyn_fn_wrapper(f, dd.name, nsd.name, fd)
                        case:
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_struct_or_enum(stmt).not() { continue }
            ref def : Expr = stmt.child(0)
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref dfw_ns : Vec = def_ns_decls(def)
                for d_j : Declaration in dfw_ns {
                    ref nsd : Declaration = d_j
                    if is_null(nsd.default_value) { continue }
                    switch nsd.default_value.node_type {
                    case NodeType.FuncDef(fd):
                        emit_dyn_fn_wrapper(f, dd.name, nsd.name, fd)
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    // Emit dyn_fn dispatch
    f.write("void *dyn_fn(Str *type_name, Str *method) {\n    (void)type_name; (void)method;\n")
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_struct_or_enum(stmt).not() { continue }
                ref def : Expr = stmt.child(0)
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref dfn_ns : Vec = def_ns_decls(def)
                    for d_j : Declaration in dfn_ns {
                        ref nsd : Declaration = d_j
                        if is_null(nsd.default_value) { continue }
                        switch nsd.default_value.node_type {
                        case NodeType.FuncDef(fd):
                            mut any_shallow := fd.return_own_type.is(OwnType.Shallow)
                            for pp : Declaration in fd.params {
                                if pp.own_type.is(OwnType.Shallow) { any_shallow = true }
                            }
                            mut suffix := ""
                            if any_shallow { suffix = "_dyn" }
                            tlen := dd.name.len().to_u64()
                            mlen := nsd.name.len().to_u64()
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0 && method->count == "); emit_u64(f, mlen); f.write("ULL && memcmp(method->c_str, \""); f.write(nsd.name); f.write("\", "); emit_u64(f, mlen); f.write("ULL) == 0) return (void*)"); f.write(dd.name); f.write("_"); f.write(nsd.name); f.write(suffix); f.write(";\n")
                        case:
                        }
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_struct_or_enum(stmt).not() { continue }
            ref def : Expr = stmt.child(0)
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref dfn_ns : Vec = def_ns_decls(def)
                for d_j : Declaration in dfn_ns {
                    ref nsd : Declaration = d_j
                    if is_null(nsd.default_value) { continue }
                    switch nsd.default_value.node_type {
                    case NodeType.FuncDef(fd):
                        mut any_shallow := fd.return_own_type.is(OwnType.Shallow)
                        for pp : Declaration in fd.params {
                            if pp.own_type.is(OwnType.Shallow) { any_shallow = true }
                        }
                        mut suffix := ""
                        if any_shallow { suffix = "_dyn" }
                        tlen := dd.name.len().to_u64()
                        mlen := nsd.name.len().to_u64()
                        f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0 && method->count == "); emit_u64(f, mlen); f.write("ULL && memcmp(method->c_str, \""); f.write(nsd.name); f.write("\", "); emit_u64(f, mlen); f.write("ULL) == 0) return (void*)"); f.write(dd.name); f.write("_"); f.write(nsd.name); f.write(suffix); f.write(";\n")
                    case:
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("    fprintf(stderr, \"dyn_fn: unknown %s.%s\\n\", (char*)type_name->c_str, (char*)method->c_str);\n")
    f.write("    exit(1);\n")
    f.write("}\n\n")
}

emit_dyn_size_of_body := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    f.write("U32 dyn_size_of(Str *type_name) {\n    (void)type_name;\n")
    f.write("    if (type_name->count == 7ULL && memcmp(type_name->c_str, \"Dynamic\", 7ULL) == 0) return 8;\n")
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_struct_or_enum(stmt).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    tlen := dd.name.len().to_u64()
                    f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) return sizeof("); f.write(dd.name); f.write(");\n")
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_struct_or_enum(stmt).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                tlen := dd.name.len().to_u64()
                f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) return sizeof("); f.write(dd.name); f.write(");\n")
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("    fprintf(stderr, \"dyn_size_of: unknown type %.*s\\n\", (int)type_name->count, (char*)type_name->c_str);\n")
    f.write("    exit(1);\n")
    f.write("}\n\n")
}

emit_tag_case := proc(f: File, tag: Str, name: Str) {
    f.write("    case ")
    f.write(tag)
    f.write(": return ")
    emit_str_lit_expr(f, name)
    f.write(";\n")
}

// Display strings for variants that don't follow the
// "case Type_TAG_<vn>: return <vn>" default (legacy idiosyncrasies
// preserved so dyn_type_to_str output stays stable).
priv dyn_type_str_display := func(vn: Str) returns Str {
    if vn.eq("Unknown") { return "" }
    if vn.eq("None")    { return "" }
    if vn.eq("FuncDef") { return "FunctionDef" }
    if vn.eq("FuncPtr") { return "Fn" }
    return vn.clone()
}

// Locate the variants of an enum definition by name in the loaded
// program. Returns true iff found. The first hit wins; out-vecs are
// only written on a hit.
priv find_loaded_enum_variants := func(ename: Str, mut variant_names: Vec, mut variant_types: Vec) returns shallow Bool {
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_struct_or_enum(stmt).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    if dd.name.eq(ename).not() { continue }
                    if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
                    mut hp := false
                    collect_enum_variants(stmt.child(0), variant_names, variant_types, hp)
                    return true
                case:
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_struct_or_enum(stmt).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                if dd.name.eq(ename).not() { continue }
                if stmt.child(0).node_type.is(NodeType.EnumDef).not() { continue }
                mut hp := false
                collect_enum_variants(stmt.child(0), variant_names, variant_types, hp)
                return true
            case:
            }
        }
    }
    return false
}

// Data-driven: walk the user's ~Type~ enum and emit one case per
// variant. Tag-only -> literal string return. Str payload ->
// Str_clone(&type->data.<v>). Enum payload (e.g. Primitive) ->
// inner switch over the payload enum's variants. Issue #203: this
// replaces the previous hardcoded list of emit_tag_case calls so
// migrating a primitive in/out of Type doesn't require editing
// this function in lockstep.
emit_dyn_type_to_str_body := proc(f: File) {
    f.write("Str *dyn_type_to_str(Type *type) {\n")
    f.write("    switch (type->tag) {\n")
    mut variant_names := Vec.new(Str)
    mut variant_types := Vec.new(Str)
    if find_loaded_enum_variants("Type", variant_names, variant_types).not() {
        panic(__LOC__, "emit_dyn_type_to_str_body: cannot find Type enum in loaded program")
    }
    for j in 0..variant_names.len() {
        ref vn : Str = variant_names.get(j)
        ref vt : Str = variant_types.get(j)
        if vt.len().eq(0) {
            display := dyn_type_str_display(vn)
            f.write("    case Type_TAG_")
            f.write(vn)
            f.write(": return ")
            emit_str_lit_expr(f, display)
            f.write(";\n")
        } else if vt.eq("Str") {
            f.write("    case Type_TAG_")
            f.write(vn)
            f.write(": return Str_clone(&type->data.")
            f.write(vn)
            f.write(");\n")
        } else {
            mut inner_names := Vec.new(Str)
            mut inner_types := Vec.new(Str)
            if find_loaded_enum_variants(vt, inner_names, inner_types).not() {
                panic(__LOC__, "emit_dyn_type_to_str_body: cannot find payload enum '", vt, "'")
            }
            f.write("    case Type_TAG_")
            f.write(vn)
            f.write(":\n")
            f.write("        switch (type->data.")
            f.write(vn)
            f.write(".tag) {\n")
            for k in 0..inner_names.len() {
                ref ivn : Str = inner_names.get(k)
                f.write("        case ")
                f.write(vt)
                f.write("_TAG_")
                f.write(ivn)
                f.write(": return ")
                emit_str_lit_expr(f, ivn)
                f.write(";\n")
            }
            f.write("        }\n")
            f.write("        break;\n")
            Vec.delete(own inner_names, false)
            Vec.delete(own inner_types, false)
        }
    }
    // Fallback: reachable only for runtime tag values that don't
    // match any variant we emitted (corrupted Type, future shape
    // mismatch). The ~?~ string mirrors til_type_name_c's default.
    f.write("    default: break;\n")
    f.write("    }\n")
    f.write("    return ")
    emit_str_lit_expr(f, "?")
    f.write(";\n")
    f.write("}\n\n")
    Vec.delete(own variant_names, false)
    Vec.delete(own variant_types, false)
}

emit_dyn_call_bodies := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    mut dyn_methods := Vec.new(DynCallInfo)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            collect_dyn_methods(iu.ast, dyn_methods)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_dyn_methods(iu.ast, dyn_methods)
    }
    for info : DynCallInfo in dyn_methods {
        ref method : Str = info.method
        mut ret_type := "void "
        if info.has_return { ret_type = "void *" }
        if info.nargs.eq(1) {
            f.write(ret_type); f.write("dyn_call_"); f.write(method); f.write("(Str *type_name, void *val) {\n")
        } else {
            f.write(ret_type); f.write("dyn_call_"); f.write(method); f.write("(Str *type_name, void *val, void *arg2) {\n")
        }
        if lp.skip_core.not() {
            for u : ProgramUnit in lp.core_units {
                ref iu : ImportUnit = lp.ctx.imported.get(u.path)
                for e_i : Expr in iu.ast.children {
                    ref stmt : Expr = e_i
                    if is_struct_or_enum(stmt).not() { continue }
                    ref def : Expr = stmt.child(0)
                    ref method_fdef : Expr = find_namespace_func(def, method)
                    if is_null(method_fdef) { continue }
                    if method_fdef.node_type.is(NodeType.FuncDef).not() { continue }
                    switch stmt.node_type {
                    case NodeType.Decl(dd):
                    switch method_fdef.node_type {
                    case NodeType.FuncDef(mfd):
                    mut arg1 := "val"
                    if mfd.params.len().gt(0) {
                        ref p0 : Declaration = mfd.params.get(0)
                        if p0.own_type.is(OwnType.Shallow) { arg1 = format("*(", dd.name, " *)val") }
                    }
                    mut arg2_s := "arg2"
                    if info.nargs.eq(2) {
                        if mfd.params.len().gt(1) {
                            if mfd.params.len().gt(1) {
                                ref p1 : Declaration = mfd.params.get(1)
                                if p1.own_type.is(OwnType.Shallow) { arg2_s = format("*(", dd.name, " *)arg2") }
                            }
                        }
                    }
                    ret_sh := mfd.return_own_type.is(OwnType.Shallow)
                    mut ret_ct := ""
                    if info.has_return {
                        if ret_sh {
                            if mfd.return_type.len().gt(0) { ret_ct = type_name_to_c_value(mfd.return_type) }
                        }
                    }
                    tlen := dd.name.len().to_u64()
                    if info.nargs.eq(2) {
                        if info.has_return {
                            if ret_sh {
                                f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(ret_ct); f.write(" *_r = malloc(sizeof("); f.write(ret_ct); f.write(")); *_r = "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(", "); f.write(arg2_s); f.write("); return _r; }\n")
                            } else {
                                f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) return (void *)"); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(", "); f.write(arg2_s); f.write(");\n")
                            }
                        } else {
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(", "); f.write(arg2_s); f.write("); return; }\n")
                        }
                    } else {
                        if info.has_return {
                            if ret_sh {
                                f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(ret_ct); f.write(" *_r = malloc(sizeof("); f.write(ret_ct); f.write(")); *_r = "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write("); return _r; }\n")
                            } else {
                                f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) return (void *)"); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(");\n")
                            }
                        } else {
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write("); return; }\n")
                        }
                    }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                }
            }
        }
        for u : ProgramUnit in lp.units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for e_i : Expr in iu.ast.children {
                ref stmt : Expr = e_i
                if is_struct_or_enum(stmt).not() { continue }
                ref def : Expr = stmt.child(0)
                ref method_fdef : Expr = find_namespace_func(def, method)
                if is_null(method_fdef) { continue }
                if method_fdef.node_type.is(NodeType.FuncDef).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                switch method_fdef.node_type {
                case NodeType.FuncDef(mfd):
                mut arg1 := "val"
                if mfd.params.len().gt(0) {
                    ref p0 : Declaration = mfd.params.get(0)
                    if p0.own_type.is(OwnType.Shallow) { arg1 = format("*(", dd.name, " *)val") }
                }
                mut arg2_s := "arg2"
                if info.nargs.eq(2) {
                    if mfd.params.len().gt(1) {
                        if mfd.params.len().gt(1) {
                            ref p1 : Declaration = mfd.params.get(1)
                            if p1.own_type.is(OwnType.Shallow) { arg2_s = format("*(", dd.name, " *)arg2") }
                        }
                    }
                }
                ret_sh := mfd.return_own_type.is(OwnType.Shallow)
                mut ret_ct := ""
                if info.has_return {
                    if ret_sh {
                        if mfd.return_type.len().gt(0) { ret_ct = type_name_to_c_value(mfd.return_type) }
                    }
                }
                tlen := dd.name.len().to_u64()
                if info.nargs.eq(2) {
                    if info.has_return {
                        if ret_sh {
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(ret_ct); f.write(" *_r = malloc(sizeof("); f.write(ret_ct); f.write(")); *_r = "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(", "); f.write(arg2_s); f.write("); return _r; }\n")
                        } else {
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) return (void *)"); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(", "); f.write(arg2_s); f.write(");\n")
                        }
                    } else {
                        f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(", "); f.write(arg2_s); f.write("); return; }\n")
                    }
                } else {
                    if info.has_return {
                        if ret_sh {
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(ret_ct); f.write(" *_r = malloc(sizeof("); f.write(ret_ct); f.write(")); *_r = "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write("); return _r; }\n")
                        } else {
                            f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) return (void *)"); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write(");\n")
                        }
                    } else {
                        f.write("    if (type_name->count == "); emit_u64(f, tlen); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, tlen); f.write("ULL) == 0) { "); f.write(dd.name); f.write("_"); f.write(method); f.write("("); f.write(arg1); f.write("); return; }\n")
                    }
                }
                case:
                    UNREACHABLE(__LOC__)
                }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
        f.write("    fprintf(stderr, \"dyn_call: unknown type for "); f.write(method); f.write("\\n\");\n")
        f.write("    exit(1);\n")
        f.write("}\n\n")
    }
    own dyn_methods.delete()
}

emit_dyn_has_bodies := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()

    // Find dyn_has_method to check return_is_shallow
    mut dyn_has_shallow := false
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            if dyn_has_shallow { break }
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if stmt.node_type.is(NodeType.Decl).and(stmt.children.len().gt(0)).and(stmt.child(0).node_type.is(NodeType.FuncDef)) {
                    switch stmt.node_type {
                    case NodeType.Decl(dd):
                        if dd.name.eq("dyn_has_method") {
                            switch stmt.child(0).node_type {
                            case NodeType.FuncDef(fd):
                                dyn_has_shallow = fd.return_own_type.is(OwnType.Shallow)
                            case:
                                UNREACHABLE(__LOC__)
                            }
                            break
                        }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        if dyn_has_shallow { break }
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).and(stmt.children.len().gt(0)).and(stmt.child(0).node_type.is(NodeType.FuncDef)) {
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    if dd.name.eq("dyn_has_method") {
                        switch stmt.child(0).node_type {
                        case NodeType.FuncDef(fd):
                            dyn_has_shallow = fd.return_own_type.is(OwnType.Shallow)
                        case:
                            UNREACHABLE(__LOC__)
                        }
                        break
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    mut dyn_has_ret := "Bool *"
    if dyn_has_shallow { dyn_has_ret = "Bool" }
    mut has_methods := Vec.new(Str)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            collect_dyn_has_methods(iu.ast, has_methods)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_dyn_has_methods(iu.ast, has_methods)
    }
    for method : Str in has_methods {
        f.write(dyn_has_ret); f.write(" dyn_has_"); f.write(method); f.write("(Str *type_name) {\n    (void)type_name;\n")
        if lp.skip_core.not() {
            for u : ProgramUnit in lp.core_units {
                ref iu : ImportUnit = lp.ctx.imported.get(u.path)
                for e_i : Expr in iu.ast.children {
                    ref stmt : Expr = e_i
                    if is_struct_or_enum(stmt).not() { continue }
                    ref rhs : Expr = stmt.child(0)
                    switch stmt.node_type {
                    case NodeType.Decl(dd):
                        mut found := false
                        ref dhb_ns : Vec = def_ns_decls(rhs)
                        for nsd : Declaration in dhb_ns {
                            if nsd.name.eq(method) { found = true; break }
                        }
                        if found.not() { continue }
                        if dyn_has_shallow {
                            f.write("    if (type_name->count == "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL) == 0) return 1;\n")
                        } else {
                            f.write("    if (type_name->count == "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL) == 0) { Bool *r = malloc(sizeof(Bool)); *r = 1; return r; }\n")
                        }
                    case:
                        UNREACHABLE(__LOC__)
                    }
                }
            }
        }
        for u : ProgramUnit in lp.units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for e_i : Expr in iu.ast.children {
                ref stmt : Expr = e_i
                if is_struct_or_enum(stmt).not() { continue }
                ref rhs : Expr = stmt.child(0)
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    mut found := false
                    ref dhb_ns : Vec = def_ns_decls(rhs)
                    for nsd : Declaration in dhb_ns {
                        if nsd.name.eq(method) { found = true; break }
                    }
                    if found.not() { continue }
                    if dyn_has_shallow {
                        f.write("    if (type_name->count == "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL) == 0) return 1;\n")
                    } else {
                        f.write("    if (type_name->count == "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL && memcmp(type_name->c_str, \""); f.write(dd.name); f.write("\", "); emit_u64(f, dd.name.len().to_u64()); f.write("ULL) == 0) { Bool *r = malloc(sizeof(Bool)); *r = 1; return r; }\n")
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
        if dyn_has_shallow { f.write("    return 0;\n") }
        else { f.write("    Bool *r = malloc(sizeof(Bool)); *r = 0; return r;\n") }
        f.write("}\n\n")
    }
    own has_methods.delete()
}

emit_collection_helpers := proc(f: File) {
    ref lp : LoadedProgram = codegen_lp()
    mut coll_infos := Vec.new(CollectionInfo)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            collect_collection_builtins(iu.ast, coll_infos)
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_collection_builtins(iu.ast, coll_infos)
    }
    for ci : CollectionInfo in coll_infos {
        et := ci.type_name
        et_len := et.len().to_u64()
        if ci.is_vec.neq(0) {
            f.write("Vec *vec_of_"); f.write(et); f.write("(int count, ...) {\n")
            f.write("    USize _esz = sizeof("); f.write(et); f.write(");\n")
            f.write("    Vec *_v = Vec_new(&(Str){.c_str = (void *)\""); f.write(et); f.write("\", .count = "); emit_u64(f, et_len); f.write("ULL, .cap = TIL_CAP_LIT}, &(USize){_esz});\n")
            f.write("    va_list ap; va_start(ap, count);\n")
            f.write("    for (int _i = 0; _i < count; _i++) {\n")
            cn := et.format("_clone")
            is_shallow := callee_returns_shallow(cn)
            if is_shallow {
                f.write("        "); f.write(et); f.write(" *_val = malloc(sizeof("); f.write(et); f.write(")); *_val = "); f.write(et); f.write("_clone(va_arg(ap, "); f.write(et); f.write(" *));\n")
            } else {
                f.write("        "); f.write(et); f.write(" *_val = "); f.write(et); f.write("_clone(va_arg(ap, "); f.write(et); f.write(" *));\n")
            }
            f.write("        Vec_push(_v, _val);\n")
            f.write("    }\n")
            f.write("    va_end(ap);\n")
            f.write("    return _v;\n")
            f.write("}\n\n")
        } else {
            f.write("Array *array_of_"); f.write(et); f.write("(int count, ...) {\n")
            f.write("    USize _cap = count;\n")
            f.write("    Array *_a = Array_new_type_name(&(Str){.c_str = (void *)\""); f.write(et); f.write("\", .count = "); emit_u64(f, et_len); f.write("ULL, .cap = TIL_CAP_LIT}, &(USize){_cap});\n")
            f.write("    va_list ap; va_start(ap, count);\n")
            f.write("    for (int _i = 0; _i < count; _i++) {\n")
            f.write("        USize _idx = _i;\n")
            cn := et.format("_clone")
            is_shallow := callee_returns_shallow(cn)
            if is_shallow {
                f.write("        "); f.write(et); f.write(" *_val = malloc(sizeof("); f.write(et); f.write(")); *_val = "); f.write(et); f.write("_clone(va_arg(ap, "); f.write(et); f.write(" *));\n")
            } else {
                f.write("        "); f.write(et); f.write(" *_val = "); f.write(et); f.write("_clone(va_arg(ap, "); f.write(et); f.write(" *));\n")
            }
            f.write("        Array_set(_a, &(USize){_idx}, _val);\n")
            f.write("    }\n")
            f.write("    va_end(ap);\n")
            f.write("    return _a;\n")
            f.write("}\n\n")
        }
    }
    own coll_infos.delete()
}

emit_global_declarations := proc(f: File) {
    ref mut lp : LoadedProgram = codegen_lp()
    lp.ctx.script_globals = Set.new(Str)
    lp.ctx.ref_globals = Set.new(Str)
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_exported_top_level_global(stmt).not() { continue }
                ref rhs : Expr = stmt.child(0)
                ctype := extern_decl_ctype(stmt.til_type, rhs.til_type)
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    if dd.own_type.is(OwnType.Ref) {
                        f.write(ctype); f.write(" *"); f.write(dd.name); f.write(";\n")
                        lp.ctx.ref_globals.add(own dd.name.clone())
                    } else {
                        f.write(ctype); f.write(" "); f.write(dd.name); f.write(";\n")
                        lp.ctx.script_globals.add(own dd.name.clone())
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_exported_top_level_global(stmt).not() { continue }
            ref rhs : Expr = stmt.child(0)
            ctype := extern_decl_ctype(stmt.til_type, rhs.til_type)
            switch stmt.node_type {
            case NodeType.Decl(dd):
                if dd.own_type.is(OwnType.Ref) {
                    f.write(ctype); f.write(" *"); f.write(dd.name); f.write(";\n")
                    lp.ctx.ref_globals.add(own dd.name.clone())
                } else {
                    f.write(ctype); f.write(" "); f.write(dd.name); f.write(";\n")
                    lp.ctx.script_globals.add(own dd.name.clone())
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    f.write("\n")
}

emit_test_main := proc(f: File, lp: LoadedProgram) {
    f.write("int main(void) {\n")
    emit_ns_inits(f, 1)
    emit_global_inits(f)
    mut test_count : I32 = 0
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            if stmt.children.len().eq(0) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.FuncDef) {
                    switch rhs.node_type {
                    case NodeType.FuncDef(fd):
                        if fd.func_type.is(FuncType.Test).not() { continue }
                        f.write("    "); f.write(dd.name); f.write("();\n")
                        f.write("    fprintf(stderr, \"  pass: %s\\n\", \""); f.write(dd.name); f.write("\");\n")
                        test_count = test_count.add(1.to_i32())
                    case:
                        UNREACHABLE(__LOC__)
                    }
                    continue
                }
                if rhs.node_type.is(NodeType.StructDef).or(rhs.node_type.is(NodeType.EnumDef)) {
                    ref ns : Vec = def_ns_decls(rhs)
                    for nsd : Declaration in ns {
                        ref nsfdef : Expr = nsd.default_value
                        if is_null(nsfdef) { continue }
                        switch nsfdef.node_type {
                        case NodeType.FuncDef(nsfd):
                            if nsfd.func_type.is(FuncType.Test).not() { continue }
                            full_name := dd.name.format("_", nsd.name)
                            qual_name := dd.name.format(".", nsd.name)
                            f.write("    "); f.write(full_name); f.write("();\n")
                            f.write("    fprintf(stderr, \"  pass: %s\\n\", \""); f.write(qual_name); f.write("\");\n")
                            test_count = test_count.add(1.to_i32())
                        case:
                        }
                    }
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    if test_count.eq(0.to_i32()) {
        f.write("    fprintf(stderr, \"no tests found\\n\");\n")
    } else {
        f.write("    fprintf(stderr, \""); emit_i32(f, test_count); f.write("/"); emit_i32(f, test_count); f.write(" tests passed\\n\");\n")
    }
    f.write("    return 0;\n")
    f.write("}\n")
}

emit_script_main := proc(f: File) {
    f.write("int main(void) {\n")
    emit_ns_inits(f, 1)
    ref mut lp : LoadedProgram = codegen_lp()
    lp.ctx.unsafe_to_hoist = Set.new(Str)
    // Collect hoist restrictions across all user units.
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        collect_unsafe_to_hoist(iu.ast)
    }
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for gi in 0..iu.ast.children.len() {
                ref gs : Expr = iu.ast.child(gi)
                if gs.node_type.is(NodeType.Decl) {
                    ref rhs : Expr = gs.child(0)
                    if rhs.node_type.is(NodeType.FuncDef).or(rhs.node_type.is(NodeType.StructDef)).or(rhs.node_type.is(NodeType.EnumDef)) { continue }
                    if gs.til_type.is(Type.None).and(rhs.node_type.is(NodeType.Ident)) { continue }
                } else if gs.node_type.is(NodeType.FCall).not() { continue }
                emit_stmt(f, gs, 1)
            }
        }
    }
    // Emit top-level non-def statements across all user units, in order.
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for gi in 0..iu.ast.children.len() {
            ref stmt : Expr = iu.ast.child(gi)
            if stmt.node_type.is(NodeType.Decl) {
                ref rhs : Expr = stmt.child(0)
                if rhs.node_type.is(NodeType.FuncDef).or(rhs.node_type.is(NodeType.StructDef)).or(rhs.node_type.is(NodeType.EnumDef)) { continue }
            }
            emit_stmt(f, stmt, 1)
        }
    }
    f.write("    return 0;\n")
    f.write("}\n")
    ref mut lp_after : LoadedProgram = codegen_lp()
    lp_after.ctx.script_globals = Set.new(Str)
}

emit_function_bodies := proc(f: File, mode: Mode) {
    ref lp : LoadedProgram = codegen_lp()
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units {
            ref iu : ImportUnit = lp.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if stmt.node_type.is(NodeType.Decl).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(dd):
                    ref rhs : Expr = stmt.child(0)
                    switch rhs.node_type {
                    case NodeType.StructDef:
                        emit_struct_funcs(f, dd.name, rhs)
                    case NodeType.EnumDef:
                        emit_enum_def(f, dd.name, rhs)
                        f.write("\n")
                    case NodeType.FuncDef(fd):
                        if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                        if rhs.children.len().eq(0) { continue }
                        emit_func_def(f, dd.name, rhs, mode, false)
                        f.write("\n")
                    case:
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if stmt.node_type.is(NodeType.Decl).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                ref rhs : Expr = stmt.child(0)
                switch rhs.node_type {
                case NodeType.StructDef:
                    emit_struct_funcs(f, dd.name, rhs)
                case NodeType.EnumDef:
                    emit_enum_def(f, dd.name, rhs)
                    f.write("\n")
                case NodeType.FuncDef(fd):
                    if fd.func_type.is(FuncType.ExtFunc).or(fd.func_type.is(FuncType.ExtProc)) { continue }
                    if rhs.children.len().eq(0) { continue }
                    emit_func_def(f, dd.name, rhs, mode, false)
                    f.write("\n")
                case:
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
}

emit_ext_func_declarations := proc(f: File, program: Expr) {
    for stmt : Expr in program.children {
        if is_func_decl(stmt).not() { continue }
        ref fdef : Expr = stmt.child(0)
        switch fdef.node_type {
        case NodeType.FuncDef(fd):
            if fd.func_type.is(FuncType.ExtFunc).not().and(fd.func_type.is(FuncType.ExtProc).not()) { continue }
            switch stmt.node_type {
            case NodeType.Decl(dd):
                if is_skip_ext_decl(dd.name) { continue }
                // Issue #176: builder.til (and similar units) use the
                // ext_func / ext_proc declaration as a forward-decl
                // shim so a regular func / proc body defined later can be
                // referenced earlier. When a real func / proc with the
                // same name exists, the canonical prototype + body comes
                // from that definition's own forward-decl emit path; the
                // ext_decl here would be a redundant prototype that
                // disagrees with the body on const-ness, so skip it.
                ref shadow_fdef : Expr = find_callee_fdef(dd.name)
                mut shadowed_by_func := false
                if is_null(shadow_fdef).not() {
                    switch shadow_fdef.node_type {
                    case NodeType.FuncDef(sfd):
                        if sfd.func_type.is(FuncType.ExtFunc).not().and(sfd.func_type.is(FuncType.ExtProc).not()) {
                            shadowed_by_func = true
                        }
                    case:
                    }
                }
                if shadowed_by_func { continue }
                if fd.return_type.len().gt(0) {
                    mut rt := ""
                    if fd.return_own_type.is(OwnType.Shallow) { rt = type_name_to_c_value(fd.return_type) }
                    else { rt = type_name_to_c(fd.return_type) }
                    f.write(rt); f.write(" "); fc := func_to_c(dd.name); f.write(fc); f.write("(")
                } else {
                    f.write("void "); fc := func_to_c(dd.name); f.write(fc); f.write("(")
                }
                emit_param_list(f, fdef, false, true)
                f.write(");\n")
            case:
                UNREACHABLE(__LOC__)
            }
        case:
            UNREACHABLE(__LOC__)
        }
    }
    f.write("\n")
}

emit_lib_init := proc(f: File) {
    f.write("__attribute__((constructor))\nstatic void _til_lib_init(void) {\n")
    emit_ns_inits(f, 1)
    emit_global_inits(f)
    f.write("}\n\n")
}

build := proc(mode: Mode, shallow run_tests: Bool, c_output_path: Str) returns shallow I32 {
    mut is_lib := false
    if is_null(mode).not() { is_lib = mode.is_library }

    build_register_lookups_lp()
    build_register_funcsig_names_lp()
    f := File.new(c_output_path, true)
    if is_null(f) { return 1.to_i32() }

    f.write("#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <stdarg.h>\n#include <limits.h>\n\n")

    mut is_script := true
    if is_null(mode).not() {
        if mode.decls_only { is_script = false }
    }

    emit_monolithic_header_lp(f, mode)
    f.write("#include \"ext.h\"\n\n")

    // Ext decls from user units only.
    ref lp : LoadedProgram = codegen_lp()
    for u : ProgramUnit in lp.units {
        ref iu : ImportUnit = lp.ctx.imported.get(u.path)
        emit_ext_func_declarations(f, iu.ast)
    }

    f.write("void print_single(Str *s);\n")
    f.write("void print_flush();\n\n")

    emit_all_forward_declarations(f, mode)

    f.write("#define DEREF(p) (*(p ? p : (fprintf(stderr, \"panic: null deref\\n\"), exit(1), p)))\n")

    f.write("#define TIL_CAP_LIT ((USize)-1)\n")
    f.write("void print_single(Str *s) {\n")
    f.write("    fwrite(s->c_str, 1, (size_t)s->count, stdout);\n")
    f.write("}\n")
    f.write("void print_flush() {\n")
    f.write("    putchar('\\n');\n")
    f.write("}\n\n")

    emit_global_declarations(f)
    emit_function_bodies(f, mode)

    emit_dyn_call_bodies(f)
    emit_dyn_fn_wrappers(f)
    emit_dyn_has_bodies(f)
    emit_dyn_size_of_body(f)
    ref lp3 : LoadedProgram = codegen_lp()
    mut has_dyn_type_to_str2 := false
    if lp3.skip_core.not() {
        for u : ProgramUnit in lp3.core_units {
            if has_dyn_type_to_str2 { break }
            ref iu : ImportUnit = lp3.ctx.imported.get(u.path)
            for stmt : Expr in iu.ast.children {
                if is_func_decl(stmt).not() { continue }
                switch stmt.node_type {
                case NodeType.Decl(sdd):
                    if sdd.name.eq("dyn_type_to_str") {
                        has_dyn_type_to_str2 = true
                        break
                    }
                case:
                    UNREACHABLE(__LOC__)
                }
            }
        }
    }
    for u : ProgramUnit in lp3.units {
        if has_dyn_type_to_str2 { break }
        ref iu : ImportUnit = lp3.ctx.imported.get(u.path)
        for stmt : Expr in iu.ast.children {
            if is_func_decl(stmt).not() { continue }
            switch stmt.node_type {
            case NodeType.Decl(sdd):
                if sdd.name.eq("dyn_type_to_str") {
                    has_dyn_type_to_str2 = true
                    break
                }
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    if has_dyn_type_to_str2 {
        emit_dyn_type_to_str_body(f)
    }
    emit_collection_helpers(f)

    if run_tests {
        // Discover tests across every loaded user unit (matches the
        // interpret pass), so `til test foo.til` runs tests inline in
        // foo.til's imports, recursively (issue #51).
        emit_test_main(f, codegen_lp())
    }
    if run_tests.not() {
        if is_script.not() {
            if is_lib {
                if codegen_lp().ctx.script_globals.len().gt(0) { emit_lib_init(f) }
            }
        }
    }
    if run_tests.not() {
        if is_script {
            mut needs_main := false
            if is_null(mode).not() { needs_main = mode.needs_main }
            if needs_main.not() {
                emit_script_main(f)
            }
        }
    }

    f.close()
    ref mut lp_end : LoadedProgram = codegen_lp()
    lp_end.ctx.func_defs = Map.new(Str, Dynamic)
    lp_end.ctx.stack_locals = Set.new(Str)
    lp_end.ctx.stack_local_types = Map.new(Str, Str)
    lp_end.ctx.unsafe_to_hoist = Set.new(Str)
    return 0.to_i32()
}

priv BuildPaths := struct {
    mut name := ""
    mut c_path := ""
    mut bin_path := ""
    mut fwd_path := ""
    mut h_path := ""
    mut til_path := ""
    mut py_path := ""
    mut do_lib := false
}

derive_build_paths := func(lp: LoadedProgram, custom_bin: Str, custom_c: Str, target: Target) returns BuildPaths {
    mut paths := BuildPaths()

    last_slash := lp.path.rfind("/")
    mut basename := lp.path
    if last_slash.gte(0) {
        last_slash_u : USize = last_slash.to_usize()
        basename = lp.path.substr(last_slash_u.add(1), lp.path.len().sub(last_slash_u).sub(1))
    }
    mut name_len := basename.len()
    if basename.ends_with(".til") {
        name_len = name_len.sub(4)
    }
    name := basename.substr(0, name_len)
    paths.name = name.clone()

    mut c_path := format("gen/til/", name, ".c")
    if custom_c.len().gt(0) { c_path = custom_c.clone() }
    mut bin_path := format("bin/", name, executable_extension(target))
    if custom_bin.len().gt(0) { bin_path = custom_bin.clone() }

    paths.c_path = c_path
    paths.bin_path = bin_path
    paths.fwd_path = forward_header_path(c_path)
    paths.h_path = format("gen/til/", name, ".h")
    paths.til_path = format("gen/til/", name, ".til")
    paths.py_path = format("gen/til/", name, ".py")
    paths.do_lib = lp.cur_mode.is_library
    return paths
}

make_build_dirs := func(paths: BuildPaths) {
    if paths.do_lib {
        if mkdir_p("gen/til").neq(0) { panic(__LOC__, "failed to create gen/til") }
        if mkdir_p("gen/lib").neq(0) { panic(__LOC__, "failed to create gen/lib") }
    } else {
        cp_slash := paths.c_path.rfind("/")
        bp_slash := paths.bin_path.rfind("/")
        mut cp_dir := "gen/til"
        if cp_slash.gt(0) {
            cp_slash_u : USize = cp_slash.to_usize()
            cp_dir = paths.c_path.substr(0, cp_slash_u)
        }
        mut bp_dir := "bin"
        if bp_slash.gt(0) {
            bp_slash_u : USize = bp_slash.to_usize()
            bp_dir = paths.bin_path.substr(0, bp_slash_u)
        }
        if mkdir_p(cp_dir).neq(0) { panic(__LOC__, "failed to create build output dir") }
        if mkdir_p(bp_dir).neq(0) { panic(__LOC__, "failed to create binary output dir") }
    }
}

priv translate_ast := proc(lp: LoadedProgram, paths: BuildPaths) returns shallow I32 {
    codegen_set_lp(lp)
    mut result := build_forward_header_lp(paths.fwd_path)
    if result.eq(0) {
        result = build(lp.cur_mode, lp.run_tests, paths.c_path)
    }
    if result.eq(0).and(paths.do_lib) {
        result = build_header_lp(paths.h_path)
        if result.eq(0) {
            result = build_til_binding(NULL, paths.til_path, paths.name)
        }
        if result.eq(0) {
            result = build_python_binding(NULL, paths.py_path, paths.name)
        }
    }
    codegen_clear_lp()
    return result
}

print_translate_success := proc(paths: BuildPaths) {
    println(format("Generated: ", paths.c_path))
    println(format("Generated: ", paths.fwd_path))
    if paths.do_lib {
        println(format("Generated: ", paths.h_path))
        println(format("Generated: ", paths.til_path))
        println(format("Generated: ", paths.py_path))
    }
}

compile_ast := proc(lp: LoadedProgram, paths: BuildPaths, target: Target, cc_override: Str, asan: Bool) returns shallow I32 {
    if paths.do_lib {
        return compile_lib(paths.c_path, paths.name, lp.ext_c_path, lp.link_c_paths, lp.link_flags, lp.include_flags, target, cc_override, asan)
    }
    return compile_c(paths.c_path, paths.bin_path, lp.ext_c_path, lp.link_c_paths, lp.link_flags, lp.include_flags, target, cc_override, lp.cur_mode.auto_import, asan)
}

cmd_translate := proc(lp: LoadedProgram, custom_c: Str, target: Target) returns shallow I32 {
    paths := derive_build_paths(lp, "", custom_c, target)
    make_build_dirs(paths)
    result := translate_ast(lp, paths)
    if result.eq(0) { print_translate_success(paths) }
    return result
}

cmd_build := proc(lp: LoadedProgram, custom_bin: Str, custom_c: Str, target: Target, cc_override: Str, asan: Bool) returns shallow I32 {
    paths := derive_build_paths(lp, custom_bin, custom_c, target)
    make_build_dirs(paths)
    mut result := translate_ast(lp, paths)
    if result.eq(0) { result = compile_ast(lp, paths, target, cc_override, asan) }
    if result.eq(0).and(paths.do_lib) {
        print_translate_success(paths)
        println(format("Generated: gen/lib/lib", paths.name, ".so"))
        println(format("Generated: gen/lib/lib", paths.name, ".a"))
    }
    return result
}

default_install_prefix := func() returns Str {
    if host_os().eq("windows") {
        return "C:/Program Files/til"
    }
    return "/usr/local"
}

check_install_rc := proc(rc: I32, what: Str) returns shallow I32 {
    if rc.neq(0) {
        println(format("error: failed to ", what))
        return 1
    }
    return 0
}

builder_compiler_root := func(lp: LoadedProgram) returns Str {
    return compiler_root(lp.ext_c_path)
}

support_root := func(prefix: Str) returns Str {
    return prefix.concat("/lib/til")
}

install_file := proc(src: Str, dst: Str, what: Str) returns shallow I32 {
    return check_install_rc(copy_file(src, dst), what)
}

install_tree := proc(src: Str, dst: Str, what: Str) returns shallow I32 {
    return check_install_rc(copy_tree(src, dst), what)
}

is_compiler_install := func(lp: LoadedProgram, paths: BuildPaths) returns shallow Bool {
    if paths.name.eq("til").not() { return false }
    if lp.path.eq("src/til.til").or(lp.path.eq("./src/til.til")) { return true }
    return lp.path.ends_with("/src/til.til")
}

install_compiler_support := proc(lp: LoadedProgram, prefix: Str) returns shallow I32 {
    root := builder_compiler_root(lp)
    dst_root := support_root(prefix)
    mut rc := install_tree(root.concat("/src/core"), dst_root.concat("/src/core"), "install compiler core sources")
    if rc.neq(0) { return rc }
    rc = install_tree(root.concat("/src/lib"), dst_root.concat("/src/lib"), "install compiler library sources")
    if rc.neq(0) { return rc }
    rc = install_tree(root.concat("/src/modes"), dst_root.concat("/src/modes"), "install compiler mode sources")
    if rc.neq(0) { return rc }
    rc = install_tree(root.concat("/src/c"), dst_root.concat("/src/c"), "install compiler C sources")
    if rc.neq(0) { return rc }
    rc = install_tree(root.concat("/boot"), dst_root.concat("/boot"), "install compiler boot files")
    if rc.neq(0) { return rc }
    rc = install_tree(root.concat("/lib"), dst_root.concat("/lib"), "install bundled dependency tree")
    if rc.neq(0) { return rc }
    return 0
}

print_install_success := proc(path: Str) {
    println(format("Installed: ", path))
}

cmd_install := proc(lp: LoadedProgram, install_prefix_in: Str, target: Target, cc_override: Str, asan: Bool) returns shallow I32 {
    mut install_prefix := install_prefix_in
    if install_prefix.len().eq(0) {
        install_prefix = default_install_prefix()
    }

    paths := derive_build_paths(lp, "", "", target)
    make_build_dirs(paths)
    mut result := translate_ast(lp, paths)
    if result.eq(0) { result = compile_ast(lp, paths, target, cc_override, asan) }
    if result.neq(0) { return result }

    if paths.do_lib {
        so_dst := format(install_prefix, "/lib/lib", paths.name, ".so")
        a_dst := format(install_prefix, "/lib/lib", paths.name, ".a")
        h_dst := format(install_prefix, "/include/", paths.name, ".h")
        til_dst := format(support_root(install_prefix), "/src/lib/", paths.name, ".til")
        py_dst := format(support_root(install_prefix), "/src/lib/", paths.name, ".py")

        result = install_file(format("gen/lib/lib", paths.name, ".so"), so_dst, "install shared library")
        if result.eq(0) { result = install_file(format("gen/lib/lib", paths.name, ".a"), a_dst, "install static library") }
        if result.eq(0) { result = install_file(paths.h_path, h_dst, "install C header") }
        if result.eq(0) { result = install_file(paths.til_path, til_dst, "install til binding") }
        if result.eq(0) { result = install_file(paths.py_path, py_dst, "install Python binding") }
        if result.eq(0) {
            print_install_success(so_dst)
            print_install_success(a_dst)
            print_install_success(h_dst)
            print_install_success(til_dst)
            print_install_success(py_dst)
        }
        return result
    }

    bin_dst := format(install_prefix, "/bin/", paths.name, executable_extension(target))
    result = install_file(paths.bin_path, bin_dst, "install executable")
    if result.neq(0) { return result }
    print_install_success(bin_dst)

    if is_compiler_install(lp, paths) {
        result = install_compiler_support(lp, install_prefix)
        if result.neq(0) { return result }
        print_install_success(support_root(install_prefix))
    }
    return 0
}

cmd_run := proc(lp: LoadedProgram, custom_bin: Str, custom_c: Str, user_argv: Vec, target: Target, cc_override: Str, asan: Bool) returns shallow I32 {
    if lp.cur_mode.is_library {
        panic(__LOC__, "cannot run a library -- use translate or build")
    }
    paths := derive_build_paths(lp, custom_bin, custom_c, target)
    make_build_dirs(paths)
    mut result := translate_ast(lp, paths)
    if result.eq(0) { result = compile_ast(lp, paths, target, cc_override, asan) }
    if result.eq(0) {
        mut cmd := paths.bin_path
        for uarg : Str in user_argv {
            cmd = cmd.format(" '", uarg, "'")
        }
        result = system_cmd(cmd)
    }
    return result
}

// Compile and run a test binary. Forces do_lib=false so library-mode
// files still produce an executable -- emit_test_main is gated on
// lp.run_tests, so the C output has a real main().
cmd_test_build_run := proc(lp: LoadedProgram, custom_bin: Str, custom_c: Str, target: Target, cc_override: Str, asan: Bool) returns shallow I32 {
    mut paths := derive_build_paths(lp, custom_bin, custom_c, target)
    paths.do_lib = false
    make_build_dirs(paths)
    mut result := translate_ast(lp, paths)
    if result.eq(0) { result = compile_ast(lp, paths, target, cc_override, asan) }
    if result.eq(0) { result = system_cmd(paths.bin_path) }
    return result
}

// --- til doc <path> -- documentation generator ---

// Walk a single source file's typed AST and render it as an org-mode page.
// One headline per top-level Decl, with the /// doc text and a kind/signature
// info block (reusing format_decl_info for the rich rendering).
format_unit_doc_org := func(unit_path: Str, ast: Expr, scope: TypeScope) returns Str {
    mut title := unit_path.clone()
    if title.starts_with("./") {
        title = title.substr(2.to_usize(), title.len().sub(2)).clone()
    }
    // First pass: render each Decl's chunk separately. format_decl_info
    // already sizes its returned Str to fit, so we don't need to pre-size
    // the page buffer here -- chunks are accumulated and concatenated once.
    chunks := Vec.new(Str)
    mut total : USize = title.len().add(64.to_usize())
    for stmt : Expr in ast.children {
        if stmt.node_type.is(NodeType.Decl) {
            switch stmt.node_type {
            case NodeType.Decl(dd):
                info := format_decl_info(stmt, scope)
                chunk_cap : USize = dd.name.len()
                    .add(dd.doc.len())
                    .add(info.len())
                    .add(64.to_usize())
                mut chunk := Str.with_capacity(chunk_cap)
                chunk.push_str("* ")
                chunk.push_str(dd.name)
                chunk.push_str("\n")
                if dd.doc.is_empty().not() {
                    chunk.push_str(dd.doc)
                    if dd.doc.ends_with("\n").not() { chunk.push_str("\n") }
                    chunk.push_str("\n")
                }
                if info.is_empty().not() {
                    chunk.push_str("#+begin_src til\n")
                    chunk.push_str(info)
                    if info.ends_with("\n").not() { chunk.push_str("\n") }
                    chunk.push_str("#+end_src\n\n")
                }
                total = total.add(chunk.len())
                chunks.push(own chunk.clone())
            case:
                UNREACHABLE(__LOC__)
            }
        }
    }
    mut buf := Str.with_capacity(total)
    buf.push_str("#+TITLE: ")
    buf.push_str(title)
    buf.push_str("\n#+OPTIONS: toc:t\n\n")
    for c : Str in chunks { buf.push_str(c) }
    return buf
}

// "src/core/vec.til" -> "doc/gen/src/core/vec.org"
til_doc_out_path := func(unit_path: Str) returns Str {
    mut name := unit_path.clone()
    if name.starts_with("./") {
        name = name.substr(2.to_usize(), name.len().sub(2)).clone()
    }
    if name.ends_with(".til") {
        name = name.substr(0, name.len().sub(4)).clone()
    }
    return format("doc/gen/", name, ".org")
}

// Ensure the parent directory of `path` exists (mkdir -p).
ensure_parent_dir := func(path: Str) {
    last_slash := path.rfind("/")
    if last_slash.lt(0) { return }
    last_slash_u : USize = last_slash.to_usize()
    dir := path.substr(0, last_slash_u).clone()
    if mkdir_p(dir).neq(0) { panic(__LOC__, "failed to create parent dir for ", path) }
}

emit_unit_doc := proc(u: ProgramUnit, lp: LoadedProgram) {
    ref iu : ImportUnit = lp.ctx.imported.get(u.path)
    out := til_doc_out_path(u.path)
    ensure_parent_dir(out)
    page := format_unit_doc_org(u.path, iu.ast, lp.ctx.scope)
    File.writefile(out, page)
    println(format("Generated: ", out))
}

cmd_doc := proc(lp: LoadedProgram) returns shallow I32 {
    if lp.skip_core.not() {
        for u : ProgramUnit in lp.core_units { emit_unit_doc(u, lp) }
    }
    for u : ProgramUnit in lp.units { emit_unit_doc(u, lp) }
    return 0.to_i32()
}
