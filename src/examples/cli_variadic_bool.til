mode cli

main := proc(flags: ..Bool) {
    mut all := true
    mut i : U64 = 0
    while i.lt(flags.len()) {
        ref f: Bool = flags.get(i)
        all = all.and(f)
        i = i.add(U64.from_i64(1))
    }
    println(all.to_str())
}
