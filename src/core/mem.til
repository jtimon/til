mode lib

import("bool")
import("dyn")
import("numbers")

free := ext_func(own elem: Dynamic) {}
til_closure_delete := ext_func(closure: Dynamic) {}
til_closure_slot_delete := ext_func(slot: Dynamic) {}
malloc := ext_func(shallow count: UPtr) returns Dynamic {}
calloc := ext_func(shallow nmemb: UPtr, shallow size: UPtr) returns Dynamic {}
realloc := ext_func(own buf: Dynamic, shallow count: UPtr) returns Dynamic {}
ptr_add := core_func(buf: Dynamic, shallow offset: UPtr) returns ref Dynamic {}
deref := ext_func(slot: Dynamic) returns ref Dynamic {}
write_ptr := ext_func(dest: Dynamic, val: Dynamic) {}
// #211 follow-up: typed primitive writers used by the AST interpreter
// to mirror writes through payload-aliased bindings into the enum's
// payload memory. The compiled backend doesn't need them (writes go
// straight through the pointer); they exist for the interp path.
write_i64 := ext_func(dest: Dynamic, shallow val: I64) {}
write_u8  := ext_func(dest: Dynamic, shallow val: U8)  {}
write_i8  := ext_func(dest: Dynamic, shallow val: I8)  {}
write_i16 := ext_func(dest: Dynamic, shallow val: I16) {}
write_u16 := ext_func(dest: Dynamic, shallow val: U16) {}
write_i32 := ext_func(dest: Dynamic, shallow val: I32) {}
write_u32 := ext_func(dest: Dynamic, shallow val: U32) {}
write_u64 := ext_func(dest: Dynamic, shallow val: U64) {}
write_f32 := ext_func(dest: Dynamic, shallow val: F32) {}
write_bool := ext_func(dest: Dynamic, shallow val: Bool) {}
memcpy := ext_func(dest: Dynamic, src: Dynamic, shallow len: UPtr) {}
memmove := ext_func(dest: Dynamic, src: Dynamic, shallow len: UPtr) {}
memset := ext_func(dest: Dynamic, shallow val: I32, shallow len: UPtr) {}
// libc memcmp: compares `len` raw bytes as unsigned char, returns <0/0/>0.
// Used by Str.cmp to compare a whole prefix in one call instead of a
// per-byte I8.cmp loop. Dispatches at comptime via the same FFI path as
// memcpy/memset (not a hardcoded builtin), so no interp change is needed.
memcmp := ext_func(a: Dynamic, b: Dynamic, shallow len: UPtr) returns shallow I32 {}
ptr_eq := ext_func(a: Dynamic, b: Dynamic) returns shallow Bool {}

// is_null is a core_func: the C backend lowers `p.is_null()` to a native
// null test `(UPtr)p == 0` inline (no call, no symbol), and the AST
// interpreter evaluates it via the registered h_is_null handler (a core_func
// has no body to run and is not FFI-registered).  Reached via UFCS as
// `aaa.is_null()` on any Dynamic-typed value.
is_null := core_func(p: Dynamic) returns shallow Bool {}

to_ptr := ext_func(a: Dynamic) returns ref Dynamic {}

/// Compiler-internal raw byte swap: the desugarer lowers `param = rhs`
/// for mut aggregate params to `swap_bytes(param, temp, T.size())`
/// (hoist_param_swap_assign), which runs after generic instantiation and
/// so cannot call the typed swap below. Not for user code -- it has no
/// ownership semantics by itself (the lowering pairs it with the temp's
/// scheduled delete). Dies with #302 stage 4's first-class move marker.
swap_bytes := func(a: Dynamic, b: Dynamic, size: UPtr) {
    tmp : U8 = malloc(size)
    memcpy(tmp, a, size)
    memcpy(a, b, size)
    memcpy(b, tmp, size)
}

/// Exchange the values of two owned bindings of the same type -- what
/// std::swap / mem::swap mean (#302): the contents change owners WITH
/// the bytes, so nothing is cloned, nothing is deleted, nothing leaks
/// and nothing double-frees. Ownership of a's old contents continues in
/// b and vice versa; both bindings' scheduled deletes stay correct.
swap := func(T: Type, mut a: T, mut b: T) {
    swap_bytes(a, b, T.size())
}

/// Move src into dest -- what a move means everywhere else (#302):
/// dest's OLD value is destroyed, src's bytes become dest's, and src is
/// consumed. Exactly one owner before, exactly one after. The body is
/// just a byte swap: dest ends up owning src's contents, src's box ends
/// up holding dest's OLD contents -- and src is an `own` param, so the
/// garbager's scheduled deep delete of src reclaims exactly that old
/// value plus the box. No explicit delete or free anywhere.
move := func(T: Type, mut dest: T, own src: T) {
    swap_bytes(dest, src, T.size())
}

/// Adopt src into UNINITIALIZED storage (#302 stage 4): dest is a raw
/// slot with no value in it -- a container's `data[count]` -- so there
/// is nothing to destroy and move() cannot express it (its swap would
/// hand dest's garbage back to src, whose deep delete would then free
/// it). The caller guarantees dest is uninitialized; that is the unsafe
/// part, and why this belongs to container internals like swap_bytes
/// above.
///
/// Ownership is exact: src's contents become dest's, and free(own src)
/// releases the BOX only -- it is also the transfer that stands the
/// garbager's scheduled deep delete down, which is what the
/// hand-written `memset(src, 0, T.size())` used to do at every call
/// site. Freeing beats blanking: no zero-fill, and no destructor call
/// walking fields that were just zeroed.
///
/// TYPED on purpose. A Dynamic version compiles and then aborts on a
/// literal Str: the value points into the static _til_str_lits table,
/// and the guard that saves it -- `if (v->cap != TIL_CAP_LIT) free(v);`
/// -- is emitted only because the call site knows the type.
adopt := func(T: Type, dest: Dynamic, own src: T) {
    memcpy(dest, src, T.size())
    free(own src)
}
