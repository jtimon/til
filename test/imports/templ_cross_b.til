mode lib

import("templ_cross_base")

cross_b_box := func() returns CrossBox(I64) {
    return CrossBox(I64).new(own 21)
}

cross_b_value := func() returns I64 {
    b := CrossBox(I64).new(own 22)
    ref v : I64 = b.get()
    return v.clone()
}
