mode test

# Issue #244: every match arm must yield the same type.

Color := enum { Red, Green }

test_arms_differ := test() {
    x := match Color.Red {
    case Color.Red:   1
    case Color.Green: "two"
    }
    assert_eq(__LOC__, x, 1)
}
